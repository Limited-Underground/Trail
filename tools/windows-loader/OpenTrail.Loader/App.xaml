<Application x:Class="OpenTrail.Loader.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="BackgroundBrush" Color="#C0C0C0" />
        <SolidColorBrush x:Key="PanelBrush" Color="#C0C0C0" />
        <SolidColorBrush x:Key="PanelRaisedBrush" Color="#DFDFDF" />
        <SolidColorBrush x:Key="BorderBrush" Color="#808080" />
        <SolidColorBrush x:Key="TextBrush" Color="#000000" />
        <SolidColorBrush x:Key="MutedTextBrush" Color="#404040" />
        <SolidColorBrush x:Key="AccentBrush" Color="#000080" />
        <SolidColorBrush x:Key="InfoBrush" Color="#000080" />

        <Style x:Key="AccessibleFocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" Stroke="#000000"
                                   StrokeThickness="1" StrokeDashArray="1,1"
                                   SnapsToDevicePixels="True" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Microsoft Sans Serif" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Microsoft Sans Serif" />
            <Setter Property="FontWeight" Value="Normal" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Padding" Value="14,7" />
            <Setter Property="Background" Value="#C0C0C0" />
            <Setter Property="Foreground" Value="#000000" />
            <Setter Property="BorderBrush" Value="#000000" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="FocusVisualStyle" Value="{StaticResource AccessibleFocusVisual}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonHighlight" BorderBrush="#FFFFFF"
                                BorderThickness="2,2,0,0" SnapsToDevicePixels="True">
                            <Border x:Name="ButtonShadow" BorderBrush="#000000"
                                    BorderThickness="0,0,2,2">
                                <Border x:Name="ButtonBorder"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="#808080"
                                        BorderThickness="1"
                                        SnapsToDevicePixels="True">
                                    <ContentPresenter Margin="{TemplateBinding Padding}"
                                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                      RecognizesAccessKey="True"
                                                      TextElement.Foreground="{TemplateBinding Foreground}" />
                                </Border>
                            </Border>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#D4D4D4" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonHighlight" Property="BorderBrush" Value="#000000" />
                                <Setter TargetName="ButtonShadow" Property="BorderBrush" Value="#FFFFFF" />
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#B0B0B0" />
                                <Setter TargetName="ButtonBorder" Property="Padding" Value="1,1,0,0" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#C0C0C0" />
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#808080" />
                                <Setter Property="Foreground" Value="#5A5A5A" />
                                <Setter Property="Cursor" Value="Arrow" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
