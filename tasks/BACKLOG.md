# OpenTrail Engineering Backlog

Statuses: `done` means the documented acceptance criteria are evidenced; `partial` means bounded evidence exists but acceptance is incomplete; `planned` means no implementation claim.

## Foundation

| ID | Status | Task | Acceptance evidence |
| --- | --- | --- | --- |
| OT-001 | done | Project bootstrap and repository structure | Self-contained directories, agent guide, README, architecture, status, and backlog exist |
| OT-001A | done | OpenTrail GitHub host validation | GitHub Actions builds six C++ verifier/planning/operator CLIs and runs all 91 C++ test executables plus the Python MeshCore lease, privacy-safe field/pilot, crypto-benchmark, and seven-group offline-map manifest suites on `main` pushes and pull requests using bounded, least-privilege, commit-pinned Windows 2025/Python 3.13/UCRT64 automation. The sixth compiled CLI is one strict offline front door for canonical `OTPD0` and `OTRD0` records; local smoke checks cover both record families, malformed supported input, and unsupported prefixes without echoing rejected content. The current matrix includes the ten-group opt-in breadcrumb archive session, six-group ACK-session key/value composition, five-group outbound-counter key/value composition, eleven-group domain-aware map-selector runtime-transition coordinator, fourteen-group restart-safe domain-aware map-selector trial-boot coordinator, thirteen-group domain-aware map-selector candidate coordinator, thirteen-group read-only active map-selector trust-domain boot coordinator, fourteen-group recoverable stable map-selector trust-domain activation coordinator, thirteen-group authorized map-selector trust-domain provisioner, five-group map trust-domain key/value storage adapter, ten-group recoverable trust-domain store, ten-group map-selector trust-domain lifecycle record, ten-group map-selector protected-domain authorization boundary, ten-group map-selector reset/replacement policy, twelve-group map-selector protected trusted-reseed coordinator, eleven-group map-selector protected trusted-baseline coordinator, eleven-group map-selector protected trusted-candidate coordinator, eleven-group map-selector protected trusted-transition coordinator, ten-group map-selector protected trusted-boot coordinator, ten-group map-selector protected trusted-generation boundary, ten-group map-selector reseed authorization boundary, ten-group map-selector key/value storage adapter, twelve-group map-selector service-reseed coordinator, ten-group map-selector first-baseline coordinator, eleven-group map-selector candidate coordinator, thirteen-group map-selector transition coordinator, ten-group map-selector boot coordinator, fourteen-group map-selector store, ten-group map-selector checkpoint, and ten-group map-activation guard, ten-group update-recovery diagnostic operator decoder, ten-group position-sharing UI diagnostic operator decoder, ten-group position-sharing UI diagnostics, ten-group position-state observation, ten-group position-sharing UI coordinator, ten-group checked-time outbound position commands, ten-group fail-visible outbound position safety overlay, ten-group checked-time outbound coordinator, ten-group priority-to-delivery handoff, ten-group experimental position-packet admission, ten-group position-sharing control, ten-group position-broadcast scheduler, nine-group semantic update-recovery presentation, eight-group bounded production RAM ring, eight-group versioned update-recovery diagnostic adapter, eight-group redacted update-recovery status boundary, ten-group update-recovery transition coordinator, ten-group update-recovery save coordinator, fifteen-group update-recovery boot coordinator, nine-group update checkpoint key/value storage adapter, twenty-group update checkpoint store, ten-group protected-reassembly, eight-group traffic-key-context, seven-group AEAD-nonce, eight-group portable-client composition, twelve-group local-interface, eleven-group power-state, eight-group secure-randomness, and eight-group monotonic-clock boundaries in addition to the ten-group outbound-counter, nine-group pilot-result/template, eight-group crypto-benchmark, ten-group protected-packet-budget, nine-group immutable-repeater, and nine-group reboot-safe repeater-replay suites. This is host/build evidence, not physical radio, target firmware/bindings, cryptography, rendered UI, or measured-radio evidence |
| OT-001B | done | Cross-project public host validation | OpenGauge GitHub Actions separately validates the shared Windows host matrix on `main` pushes and pull requests using bounded, least-privilege, commit-pinned automation. Its current-main warning-free run passes all 41 executables with zero annotations. This is cross-project host evidence, not a physical radio or OpenTrail target build |
| OT-025 | partial | Funding readiness packet | A brand-neutral public packet contains the factual project core, reusable application answers, vendor-neutral hardware template, preliminary milestone budget, qualification/submission checklist, and opportunity register. Every cash, hardware, discount, loan, sponsorship, and service-credit path is on owner-directed hold: no opportunity research for outreach, contact, submission, account activity, acceptance, shipment, or announcement is authorized. Legal applicant/payee, exact hardware freeze, dated quotes, eligibility/terms, private-document handling, and explicit owner clearance remain later gates |
| OT-026 | done | First-release capacity policy | The planned v0 support ceiling is eight active clients in one group plus at most one optional authorized repeater. Evidence must progress from four standalone clients to four-plus-repeater and then eight-plus-repeater on frozen hardware/firmware. Base clients may not depend on repeater/server/internet/phone/laptop/vehicle operation. No phase has passed, so this is a release target rather than a current capacity/support claim |
| OT-027 | done | Base-versus-optional product boundary | One public map defines the self-contained base client and optional repeater, opt-in archive, OpenGauge vehicle, offline-map/large-display, and post-session management roles. Each boundary lists allowed data and fail-independent behavior; no add-on may become a base-operation dependency. Hosting operations, private location/participant data, credentials, and provider specifics remain outside the public repository |
| OT-028 | done | Opt-in breadcrumb archive session boundary | A fixed-memory host session wraps the explicit position scheduler without controlling base radio operation. Exact 56-byte `OTBA/v0` records contain only an opaque same-boot session ID, accepted-record sequence, boot-local capture time, canonical current-position payload, reserves, and CRC-32. Session IDs increase within one object lifetime; sequence advances only on local transport acceptance and retryable pressure retains it. Ten groups plus 100/100 repeats pass. No server, remote ACK/durability, stable identity, authorization, cryptography, retention/export/deletion, target binding, real-coordinate trial, or physical result exists |
| OT-002 | done | Initial architecture documentation | Layers, boundaries, failure modes, roles, and architecture gates documented |
| OT-003 | partial | Hardware abstraction contracts | Radio, GPS, logging, persistent-storage, secure-randomness, checked monotonic-clock, power-state, and local display/input contracts have deterministic fakes/tests. Portable-client structural review is host-tested under OT-003E, and OT-003F proves the first checked-time outbound service composition. Concrete ESP-IDF/board bindings, full boot/inbound/UI/persistence runtime, target build, and on-device evidence remain |
| OT-003A | partial | Hardware and regulatory inventory | Both companions are runtime-confirmed as Heltec V4 OLED with MeshCore USB Companion `v1.16.0-07a3ca9`; the repeater is runtime-confirmed as Seeed SenseCap Solar. Official-source reconciliation identifies likely Heltec WiFi LoRa 32 V4 and SenseCAP Solar Node family characteristics without promoting them to exact-unit facts. The dated fail-closed US field gate requires exact labels/FCC grants, firmware mode, antennas/gain/cable loss, and configuration review. `OT-DEV-001` has ROM-level MCU/memory evidence; `OT-DEV-002` does not. Exact SKUs/revisions, RF variants, installed antennas, pinouts/power details, and grant coverage remain unresolved |
| OT-003B | done | Checked monotonic-clock boundary | A production-facing guard accepts one boot-local 64-bit millisecond source, keeps UTC separate, permits equal ticks and temporary not-ready recovery, and latches rollback or source failure without consuming later samples. Fixed saturating status counters and a bounded 16-sample FIFO fake cover zero/equal/forward/full-range time, continuity, rollback, source failure, new-boot composition, and capacity/order across eight groups plus 100 repeats. The complete 30-executable matrix passes locally. ESP-IDF timer/task binding, native-wrap handling, deep-sleep/brownout policy, resolution/latency/drift, long-run continuity, and physical failure injection remain target gates |
| OT-003C | done | Fail-visible power-state boundary | One atomic adapter observation keeps source readiness, external power, battery presence, orthogonal charge state, optional normalized percentage/voltage, and monotonic age separate. Composition must inject distinct low/critical percentage and nonzero freshness policy; the common component never estimates percentage from voltage. Eleven groups plus 100 repeats cover exact bands, charging, external-only, missing, fault, invalid, future/stale, and bounded FIFO behavior in the complete 31-executable local matrix. Board adapter, charger/shutdown control, hardware thresholds, endurance, rendered UX, and physical power evidence remain target gates |
| OT-003D | done | Revision-bound local display/input boundary | A fixed semantic frame carries screen role, attention/notice, radio/position/power indicators, optional peer count, unread count, and up to four unique action slots without pixels, coordinates, GPIOs, free text, or identity-bearing content. Only an atomically presented increasing revision becomes active; normalized input must match it exactly. Canonical critical confirmation requires confirm/cancel order plus hold. Twelve base groups plus 100 repeats cover capabilities, frame validation, display failure/retry, revisions, stale/disabled/invalid input, critical/system-fault rules, and bounded fakes. OT-019A adds separately tested coarse recovery notices without execution authority; OT-012B adds local position-sharing privacy actions; OT-012F owns their cooperative revision/poll/action/refresh sequence. Target renderers/input adapters, localization/accessibility/readability, resource/power performance, distracted-driving policy, and physical evidence remain |
| OT-003E | done | Portable-client composition preflight and whole-contract review | Ten non-owning target endpoints cover radio, GPS, log sink, 64-byte multi-domain storage, separate 704-byte replay storage, entropy, monotonic clock, power, display, and input. Pure shared power/display validators plus explicit MTU/action/hold policy aggregate every structural issue while querying only radio MTU and performing no mutable adapter I/O. The whole-contract audit found and corrected the separate replay-storage obligation. Eight groups pass in the complete 33-executable matrix. Concrete adapters, ESP-IDF target/task/build, protected storage, and physical evidence remain OT-003 gates |
| OT-003F | done | Checked-time outbound service coordinator | One fixed-memory cooperative cycle reads `CheckedMonotonicClock` once, skips GPS while sharing is stopped, and orders active-sharing location, position scheduling, priority handoff, delivery, and opaque-radio service with the same successful time. Not-ready invokes no downstream work; rollback/source failure stops sharing and latches closed; position/handoff failure does not block existing messaging. Ten groups plus 100 repeats include same-cycle exact packet delivery and pressure recovery in the 58-executable matrix. OT-012D adds fail-visible position presentation, OT-012E owns checked action-time commands, and OT-012F owns cooperative UI sequencing. ESP-IDF tasks/synchronization, inbound/boot/persistence/power/log composition, concrete adapters, authentication, and physical behavior remain |
| OT-020 | planned | Wio Tracker L1 Pro compatibility | A non-destructive arrival procedure and read-only Windows preflight tool cover exact label/SKU/revision, shipping firmware preservation, USB/UF2 recovery enumeration, redacted BLE settings, GNSS current/stale/loss behavior, bounded Heltec interoperability, cleanup, and recovery. No unit has arrived or been tested |
| OT-015 | done | Diagnostics/logging foundation | Fixed-capacity ERROR/WARN/INFO/DEBUG/TRACE logger demonstrates compile/runtime filtering, monotonic timestamps, component tags, redaction, truncation/sanitization, test sink, and counted backpressure in seven host scenarios. A production-facing RAM ring retains the newest 32 canonical records, assigns boot-local sequences, snapshots oldest-first without partial output, preserves order across clear, and counts rollover/rejection across eight groups plus 100 repeats. The `OTRD0/v0` recovery adapter adds one versioned 32-bit, generation-free redacted event with exact fixed text, coherence validation, severity mapping, filtering, sink-failure evidence, and real ring capture across eight groups plus 100 repeats. OT-019B and OT-012I add strict offline host decoding; one unified CLI dispatches their exact canonical prefixes and passes four smoke paths without log retrieval or execution authority. Target task binding/concurrency, persistent audit/export, packaging, and physical capture remain |

## Transport and protocol

| ID | Status | Task | Acceptance evidence |
| --- | --- | --- | --- |
| OT-004 | done | LoRa transport abstraction | Fixed-capacity opaque-frame contract covers MTU, errors, metadata validity, cooperative state, queues/counters, and a deterministic two-node fake without protocol coupling; eight host scenarios pass |
| OT-005 | partial | Node identity and group model | Identity/name/alias/membership boundaries and threat model are documented; eight host lifecycle/collision scenarios pass. A dated candidate review fixes libsodium-first target benchmarking, mbedTLS/PSA and Monocypher comparisons, Noise-C reference-only status, separate Ed25519/X25519 purposes, and conditional Noise XK invitation direction without making a selection. A rollback-safe counter lease, key/value target composition, exact fail-closed 96-bit nonce packing, and canonical purpose-separated traffic-key context are host-tested under OT-005A/G/D/E. Exact target benchmark and KDF, entropy adapter, handshake/alias implementation, administrator recovery, protected rollback-resistant persistence, packet-v1 AEAD/AAD, and physical join/revoke/reset evidence remain |
| OT-005A | done | Rollback-safe outbound counter leasing | A fixed 64-byte `OTCN` record and separate two-slot persistence domain commit a nonoverlapping 64-bit range before any counter is returned. The fixed-memory allocator reserves before first/renewed use, wastes unused ranges on restart, binds state to a 128-bit domain plus group epoch, omits ordinary reset, and fails closed on mismatch, corruption, conflict, future version, exhaustion, I/O, and five mutation boundaries. Ten host groups plus the OT-005G key/value composition pass locally. Domain derivation, protected target storage/rollback, AEAD composition, and physical power-cut/endurance remain OT-005/target gates |
| OT-005G | done | Key/value-backed outbound counter composition | The real `OTCN` lease store now runs through the multi-domain key/value adapter using only exact `ot_state` / `ot_counter` / `slot_a|b` bindings. Fresh adapters prove restart rotation and non-overlap; failed-unapplied prepared commits permit retry only after confirmed absence, while applied-then-failed marker commits return no range and force the next boot to skip it. Wrong-sized values fail before mutation. Five groups, 100/100 focused repeats, and the complete 91-executable host matrix pass. ESP-IDF binding, protected integrity/rollback, rekey/reset authority, locking, physical interruption/endurance, packet-v1, and on-device evidence remain |
| OT-005D | done | Lease-bound AEAD nonce composition | A fixed 12-byte result requires matching nonzero 128-bit lease/key domains and a nonzero rollback-safe counter, then packs the crypto-adapter-supplied 32-bit prefix plus the 64-bit counter in network byte order. Mismatch and invalid input return typed failure with zero output across seven host groups. This performs no KDF, key derivation, counter allocation, AEAD, packet-v1, or library selection; exact cryptographic domain/key/prefix derivation and target evidence remain OT-005 gates |
| OT-005E | done | Canonical traffic-key derivation context | A fixed 52-byte `OTKD/v1` public context binds a nonzero group ID, group epoch, full 32-byte authoritative sender fingerprint, and separate group-AEAD-key/nonce-prefix/counter-domain purposes in network byte order. Eight host groups cover exact bytes, purpose separation, context changes, and zero/unknown refusal. It performs no KDF or secret handling; exact target library, salt/input key material, output lengths, vectors, wiping, and epoch-secret rotation remain OT-005 gates |
| OT-005F | done | Protected-header requirements reconciliation | Review against the accepted immutable one-repeater policy found that the former 36-byte budget could not carry both its required authenticated 64-bit destination alias and fragment metadata. The corrected 44-byte profile explicitly charges destination, has no mutable TTL, raises base overhead to 60 bytes, and leaves 39 signed-group plaintext bytes at the 163-byte example MTU. Ten updated budget groups pass. Final offsets/types/flags, fragment/reassembly policy, destination privacy, signature coverage, AEAD, and packet-v1 remain open |
| OT-005B | done | Reproducible cryptographic benchmark evidence boundary | Strict `OTCB0/v0` plans require the fixed three-candidate order, exact ready target/toolchain/sdkconfig/dependency/radio fields, 100 cold and warm repetitions, and eight canonical security/lifecycle gates. Results bind to a canonical plan hash and record eight operation timing sets, build warnings, resources, watchdogs, radio cost, hashes, raw-evidence retention, and every gate. Eight groups reject false readiness, overwrite, mismatch, private text, incomplete measurements, and measured failures; they pass locally and on public `main`. The public target plan remains `draft_blocked`; no on-device benchmark or library selection exists |
| OT-005C | done | Fail-closed secure-randomness source boundary | An algorithm-neutral interface exposes typed not-ready/ready/failed entropy state, bounds requests to 1-64 bytes, and requires full output or no buffer change. A deterministic 512-byte scripted fake is isolated under test support and covers sequential single-use bytes, invalid/oversized requests, exhaustion, injected failure/retry, readiness transitions, persistent failure, and the maximum boundary across eight groups plus 100 repeats. The complete 29-executable host matrix passes locally. No ESP-IDF entropy/DRBG adapter, target readiness proof, production key generation, physical cold-start/brownout/RF-concurrency evidence, or cryptographic selection is claimed |
| OT-006 | done | Experimental packet envelope v0 | A 22-byte v0 envelope documents MTU-derived budget, version/type/flags/length/ephemeral IDs, CRC-16, rejection behavior, a standard CRC vector, and six passing codec scenario groups; it is explicitly non-production and unauthenticated |
| OT-006A | done | Protected packet candidate budget | A fixed-memory model charges authenticated header, AEAD tag, source authentication, optional forwarding wrapper, and theoretical LoRa airtime to every fragment. After destination-alias reconciliation, the base 44+16 profile leaves 103 plaintext bytes at a 163-byte example MTU; adding a 64-byte Ed25519 signed-group candidate leaves 39 and makes a 16-byte position 140 bytes/461,312 us at the bench PHY. Ten groups cover base/signed/wrapper profiles, the 64-byte alert case, 163/255 MTUs, single/three/empty payloads, exact boundary, limits, and invalid PHY locally. This is sizing evidence, not packet-v1 offsets, crypto, fragmentation policy, target MTU, or physical airtime |
| OT-006B | done | Critical-alert protected-radio feasibility | The 64-byte `OGA0` and `OGK0` semantic frames each require two 39-byte signed-group candidate fragments at the 163-byte example MTU: 312 transmitted bytes and 1,025,024 us theoretical source airtime before retries/repeater copies. Alert plus ACK is four source fragments; one repeater doubles aggregate transmission airtime. The documented gate keeps fragmentation blocked pending source-signature scope, nonce/replay/reassembly/ACK/retry policy, bounded resource behavior, target measurement, and regulatory review |
| OT-006C | done | Verified protected-fragment reassembly policy | A fixed-memory host boundary accepts only future crypto-adapter-produced verified fragments, binds group/epoch/sender/message/count, supports four concurrent sessions and up to 16x103 plaintext bytes, reorders canonically, treats exact duplicates idempotently, clears byte/count conflicts, rejects full capacity without eviction, and expires at the exact timeout. Ten groups include the 39+25-byte alert shape. Cryptographic verification, unforgeable target composition, final packet/signature/replay/retry policy, ESP32 resources, power loss, and field behavior remain open |
| OT-007A | done | Two-node transport characterization | USB preflight and raw-RX authentication checks passed. A temporary private-channel sample delivered 5/5 numbered messages each direction with 0 loss, 0 duplicates, 233.3-247.2 ms latency, 11.25-12.25 dB SNR, exact +5 TX/+5 RX counters per node, zero errors, and empty queues. The temporary channel was erased and verified empty. RSSI remained invalid and airtime remained whole-second resolution; these are recorded measurement limitations, not omitted evidence. |
| OT-007 | done | Two-node message proof of concept | Two host integration scenarios pass; C++-encoded v0 frames then delivered 3/3 each direction through a temporary private MeshCore adapter with no loss/duplicates/errors, exact counter deltas, verified decode/CRC, and verified channel cleanup |
| OT-008 | done | Acknowledgement and duplicate handling | Six message-class policies, fixed-capacity delivery state, confirmation/retry/expiry/error behavior, reboot-restored duplicate window, and lost-ACK integration are host-tested. Authenticated ACK wire encoding and hardware timing remain later protocol/field gates |
| OT-008A | done | Canonical duplicate-checkpoint serialization | A fixed 672-byte `OTD0` record serializes up to 32 replay keys and remaining lifetimes with explicit little-endian fields, canonical zero padding, strict version/capacity/key/lifetime checks, duplicate-key rejection, CRC-32, and atomic output. Seven codec groups, the full 23-executable host matrix, and 100 codec/window repeats pass. Atomic durable storage, wear/privacy lifecycle, authentication, secure rollback, and physical power-loss evidence remain later gates |
| OT-008B | done | Two-slot duplicate-checkpoint host store | A fixed 704-byte `ODS0` envelope composes `OTD0` with automatic 64-bit generations, two-slot rotation/repair, readback byte/decode verification, visible single-slot recovery, and fail-closed I/O, invalid-only, conflict, and exhaustion behavior. Nine store groups, the full 24-executable host matrix, and 100 store/codec/window repeats pass. ESP32/NVS binding, physical atomicity/endurance, privacy lifecycle, authenticated integrity, trusted rollback protection, and physical power-loss evidence remain later gates |
| OT-008C | done | Context-bound duplicate checkpoint store | Fixed-size `ODS0/v1` reuses the original 704-byte slots while embedding a nonzero 64-bit group-context ID and 32-bit group epoch. Every inner key must match the outer epoch; zero, wrong-context/epoch, and legacy unbound v0 media fail closed without restore or overwrite. The coordinator also validates its store binding and returns typed bound-media/legacy service outcomes. Ten store groups, nine coordinator groups, the full 28-executable matrix, and 100 focused repeats of both suites pass locally; the exact published matrix passes in run `31374678550`. Protected target namespace, authenticated integrity, anti-rollback, authorized reset/migration, physical power-cut, and endurance remain |
| OT-008D | done | NVS-ready duplicate checkpoint key/value adapter | A backend-neutral adapter binds the two exact 704-byte context-bound `ODS0/v1` slots to `ot_state` / `ot_replay` / `ods_dup_a|b`, rejects invalid arguments and wrong-sized values, requires explicit backend commit after write/erase, treats missing-key erase as idempotent, and keeps failed or applied-then-failed commits visible for restart reconciliation. Nine groups, 100/100 focused repeats, and the complete 86-executable host matrix pass. Protected ESP-IDF namespace access, target locking, authenticated integrity, rollback protection, authorized reset/migration, physical interruption, endurance, and on-device evidence remain |
| OT-009 | partial | Controlled repeater proof of concept | Eight three-node host scenarios prove bounded role/permission forwarding, exact TTL decrement, origin/reflection duplicate suppression, group isolation, queue/rate congestion limits, and broadcast/unicast behavior. A SenseCAP repeater retransmitted exactly two private flood samples. Explicit one-hop direct routes then delivered in both directions; disabling repeat caused the same route to fail with +1 direct RX/+0 direct TX and no destination message, proving the logical repeater path. A non-secret temporary-channel lease has four host recovery groups plus real stopped-session cleanup. A 300-minute alternating close-bench run delivered 300/300 with zero loss/duplicates/errors, 229.8-312.1 ms latency, exact +300 repeater flood RX/TX, repeat preserved, and verified channel/journal cleanup. A post-soak packet-v0 regression then delivered 6/6 with valid codec/CRC and verified cleanup. Authenticated routing fields, direct SX1262/OpenTrail binding, and field evidence remain |
| OT-009B | done | Immutable zero/one-repeater architecture and host-policy boundary | Decision 0004 limits the first release to direct clients plus at most one authorized repeater. A new algorithm-neutral policy requires source auth/authorization, verified context/epoch, immutable permission, and monotonic process time before replay observation, then applies duplicate, exact-byte queue, rate, and exact-expiry controls without rewriting a TTL. Nine groups and the full 27-executable matrix pass locally and on public `main`. Named-source broadcast still needs cryptography beyond common group AEAD access; protected replay persistence, target binding, reboot/power-loss, and field behavior remain |
| OT-009C | done | Reboot-safe single-repeater replay coordination | The host coordinator requires exact protected group/epoch namespace evidence, initializes an authorized empty store, restores and repairs a known-degraded two-slot `ODS0` checkpoint before operation, and persists every queued/congestion replay observation before allowing queue release. Failed/uncertain saves and unreadable media disable forwarding. Nine groups and the full 28-executable matrix pass locally and on public `main` in run `31372816356`. The ordering prevents reboot amplification but can lose a volatile queued frame after save and before transmit. Protected target binding, authenticated integrity/rollback, physical power-cut/wear, durable-outbox policy, and field evidence remain |
| OT-010 | partial | Priority/emergency messaging | Nine host scenarios plus delivery integration prove class-derived priority, reserved urgent capacity, strict preemption, rate windows, expiry, FIFO, and explicit failure/preemption events. OT-010A adds loss-aware priority-to-delivery transfer without changing the unauthenticated wire boundary. Authenticated wire priority, measured mixed traffic, physical evidence, and rendered failure UX remain |
| OT-010A | done | Loss-aware priority-to-delivery handoff | A fixed-memory single-owner coordinator peeks the strict-priority/FIFO head, asks `DeliveryController` to accept the exact frame, and commits the source entry only after acceptance. Delivery capacity, duplicate ID, and MTU rejection retain the source; remaining queue lifetime caps delivery expiry; impossible commit mismatch latches closed. Ten groups plus 100 repeats cover retention, ordering, exact expiry, unsafe time spans, and the complete scheduler-produced position-packet path through a fake-radio peer in the 58-executable matrix. OT-003F services it in one checked-time outbound cycle. Authentication, concrete target concurrency, direct-radio binding, physical delivery, and rendered status remain |
| OT-021 | done | Baseline group-load estimator | A fixed-memory host model combines exact LoRa airtime with member count, periodic position/status traffic, alert bursts, bounded source attempts, and explicit repeater copies. Six groups cover the four-client standalone, four-plus-repeater, and eight-plus-repeater phases, retry scaling, zero traffic, and fail-closed validation. The current one-hour profile reports 1.2727%, 2.5455%, and 5.0911% scheduled demand; collision, delivery, range, protocol-overhead, power, and regulatory evidence remain physical gates |
| OT-022 | done | Privacy-safe field-test evidence boundary | `OTFL0` converts ignored raw MeshCore soak output into atomic aggregate public JSON under neutral role labels, rejects transport ports/serials/addresses/keys/secrets/PINs/channel names/coordinates plus incoherent counts, and refuses implicit overwrite. Four host groups pass. A new two-client-plus-repeater burst delivered 30/30 at a two-second interval with zero loss/duplicates/new errors, exact +30 repeater flood RX/TX, repeat preserved, 2/2 cleanup, and a validated public summary. This is close-bench logging evidence, not four/eight-client field capacity |
| OT-023 | partial | Four-person standalone pilot v0 | `OTFP0/v0` fixes four identical clients, zero infrastructure dependency during the session, three one-hour environment classes, 300 message origins, 900 peer-delivery opportunities, privacy-safe evidence, and provisional fail gates. Two host groups reject incorrect math and false readiness. The plan remains `draft_blocked` until one exact client model/firmware is frozen across four units with battery, enclosure, GNSS, display, local input, and USB recovery; no live four-person session has run |
| OT-024 | partial | Deterministic four-person pilot verdict | Strict `OTPR0/v0` aggregate results produce pass/fail/ineligible/invalid verdicts against one ready `OTFP0/v0` plan. Fail-closed template generation refuses blocked plans, unlisted scenarios, and overwrite while preloading frozen public configuration and leaving evidence claims false; all nine groups pass publicly. Live four-person evidence remains |

## Location, groups, and persistence

| ID | Status | Task | Acceptance evidence |
| --- | --- | --- | --- |
| OT-011 | done | GPS abstraction | Fixed-unit provider contract, optional-field validity, no-fix, validation, exact stale boundary, no-UTC boot, refresh recovery, and monotonic-time rejection pass nine deterministic host scenarios |
| OT-012 | done | Position broadcast format | Fixed 16-byte current/stale/unknown payload, conservative age/accuracy, canonical rejection, 38-byte packet integration, and theoretical airtime/cadence budget pass eight codec, one transport-integration, and four airtime scenario groups. OT-012A supplies host scheduling mechanics, OT-012B semantic local privacy control, OT-012C experimental packet-v0/background-priority admission, OT-010A loss-aware delivery handoff, OT-003F one checked-time outbound fake-radio cycle, OT-012D fail-visible runtime-aware presentation, OT-012E checked action-time command authority, OT-012F cooperative UI/revision ownership, OT-012G external semantic refresh before input, OT-012H privacy-safe UI diagnostics, and OT-012I strict offline operator decoding. Authentication, physical/rendered privacy UX, authenticated packet/priority composition, selected cadence, direct-radio hardware airtime, contention, and regulatory evidence remain later gates |
| OT-012A | done | Start/stop position broadcast scheduler | A fixed-memory scheduler starts only by explicit command, stops immediately, submits only current validated 16-byte positions with the actual attempt time, coalesces delayed cadence from actual accepted work, and uses a separate retry delay for missing fixes or typed sink pressure/failure. Repeated start while active is idempotent; monotonic rollback and deadline exhaustion latch off. Ten groups plus 100 repeats cover policy, exact boundaries, suppression, pressure, failures, stop/restart, time faults, malformed input, and bounded fakes in the complete 58-executable matrix. OT-003F supplies one checked service timestamp and avoids GPS reads while stopped. Final cadence, authenticated packet/priority composition, direct radio/GPS bindings, physical behavior, and regulatory evidence remain |
| OT-012B | done | Local position-sharing privacy control | Fixed stopped/active/waiting/deferred/failed notices and start/stop actions pass through the checked revision-bound UI. Start only arms scheduling without sink access; stop is immediate; stale, unrelated, unknown, and repeated actions cannot cause unintended mutation. Recoverable conditions retain stop while terminal policy/time faults expose no execution action. Ten groups plus 100 repeats cover button/touch shapes, checked resolution, failures, stale revisions, idempotence, and typed rejection in the complete 58-executable matrix. OT-012D adds runtime-aware latched-fault precedence, OT-012E removes target-supplied Start time, and OT-012F owns cooperative revisions/actions. Exact renderer/text, target synchronization, direct radio/GPS composition, physical behavior, and reboot/default preference policy remain |
| OT-012C | done | Experimental position packet and priority admission | A fixed-memory sink revalidates only canonical current payloads, obtains injected ephemeral packet-v0 metadata, encodes the exact 38-byte frame, and admits it only as background position traffic using the scheduler's actual attempt time. Metadata/rate pressure maps to not-ready, reserve/full pressure maps to full, and malformed/noncurrent payloads, invalid/exhausted metadata, duplicate IDs, or invalid queue state fail closed. Ten groups plus 100 repeats cover exact policy, round-trip, scheduler composition, pressure, priority, expiry, and failures in the complete 58-executable matrix. OT-010A carries admitted traffic into delivery without source loss, and OT-003F services the complete fake-radio path with one checked time. Packet v0 remains unauthenticated and prohibited for real coordinates; identity/counter lifecycle, authenticated composition, direct-radio binding, field behavior, and regulation remain |
| OT-012D | done | Fail-visible outbound position safety | The target-facing presentation validates scheduler plus outbound-runtime status. Coherent rollback/source fault overrides stopped sharing with a critical no-action frame; unknown/incoherent copied state fails closed; stale healthy Start and direct Start route through live coordinator authority; Stop remains safe/idempotent. Ten groups plus 100 repeats use real coordinator faults and checked UI revisions in the complete 58-executable matrix. OT-012E closes action-time checked-clock composition and OT-012F owns cooperative revisions/actions; renderer, exact target synchronization, reboot behavior, and physical input remain |
| OT-012E | done | Checked action-time position commands | `OutboundServiceCoordinator` owns target-facing Start/Stop. Start reads one checked action-time sample internally, defers without scheduler access on not-ready, and stops/latches on source failure or rollback; a latched retry consumes no source sample. Stop is immediate and clock-independent. The semantic adapter accepts no caller timestamp and maps retry, clock fault, and scheduler rejection separately. Ten groups plus 100 repeats and 100 updated safety repeats pass in the complete 58-executable matrix. OT-012F supplies cooperative UI ownership; exact target synchronization, concrete clock binding, renderer/retry UX, reboot policy, diagnostics, and physical input remain |
| OT-012F | done | Single-owner position-sharing UI coordinator | One cooperative service owns initial/current revision publication, one checked input poll, live coordinator Start/Stop, and mandatory post-action refresh. Temporary clock deferral retains the truthful Start frame; stale/failed input cannot mutate position; initial display failure retries its unused revision; post-action display failure and revision exhaustion stop sharing and latch further input closed. Ten groups plus 100 repeats pass in the complete 58-executable matrix. This is not an ESP-IDF task/lock, renderer, physical input, or concurrency result; exact target serialization, retry UX, reboot policy, diagnostics, and hardware evidence remain |
| OT-012G | done | Observed position-state refresh before input | The UI coordinator retains its last committed semantic frame and compares current live outbound/scheduler presentation before polling input. GPS wait/recovery, sink deferral, and permanent clock fault publish a higher revision first; queued older input remains unread until refresh and is then stale. Internal timestamps/counters do not churn revisions. Failed observed publication stops sharing and latches input closed. Ten groups plus 100 repeats pass in the complete 58-executable matrix. Exact ESP-IDF serialization/atomicity, renderer behavior, physical input/display timing, diagnostics, and hardware evidence remain |
| OT-012H | done | Privacy-safe position UI diagnostic event | Canonical `OTPD0/v0` validates one coordinator result and encodes a fixed 32-bit event with coarse event/outcome, displayed position notice, normalized reason, and presentation/change/containment flags. The logger writes exactly `OTPD0=XXXXXXXX`; idle is suppressed; filtering and sink rejection remain distinct. Revisions, timestamps, counters, location/content/identity, addresses, credentials, and free text are structurally absent. Ten groups plus 100 repeats pass in the complete 58-executable matrix. OT-012I supplies strict host decoding. Target log binding, retention/export/clear/persistence policy, and physical service evidence remain |
| OT-012I | done | Strict offline position UI diagnostic decoder | A checked parser accepts only the exact uppercase `OTPD0=XXXXXXXX` record, preserves unsupported-version versus invalid-word/message errors, reruns all word/coherence validation, and exposes stable names for every v0 category. The CLI prints deterministic fixed-field output, never echoes rejected input, and performs no network, file, device, retention, or export work. Ten groups, canonical/invalid CLI smoke tests, and 100 repeats pass in the complete 58-executable matrix. Packaged tooling, target log retrieval, consent/retention/export policy, and physical service evidence remain |
| OT-013 | done | Group membership/joining | Algorithm-neutral, fixed-capacity lifecycle and operator UX specify administrator-gated single-use invitations, four authentication obligations, separate promotion, epoch-advancing revoke/rekey, revoked-identity exclusion, last-admin protection, and reset/recovery boundaries; twelve host scenario groups pass. Exact cryptography, persistence, rendered UX, and physical multi-device evidence remain OT-005/OT-014/field gates |
| OT-014 | done | Persistent configuration | Two fixed 64-byte slots provide version/schema checks, CRC-32, commit-last recovery, generation selection/conflict/exhaustion, v1-to-v2 migration, safe defaults, structural secret-domain separation, no-op/rate/alternating-slot wear controls, and five power-loss boundaries across twelve host scenario groups. A backend-neutral target adapter now maps the four domains to isolated `ot_config`, `ot_secret`, `ot_proto`, and `ot_counter` namespaces while preserving erase-before-program, bit-clearing, partial RAM writes, full-blob sync/commit, and uncertainty latching across twelve groups plus 100 repeats in the complete 91-executable matrix. The secret namespace is structural separation only. CRC is not authentication; ESP-IDF binding, protected secret storage, secure rollback, and physical endurance remain later gates |
| OT-014A | done | NVS-ready multi-domain persistent storage adapter | A backend-neutral adapter binds the four exact 64-byte two-slot domains to separate `ot_config`, `ot_secret`, `ot_proto`, and `ot_counter` namespaces. It maps missing keys to erased `0xFF`, requires erase before partial bit-clearing writes, holds unsynced changes in RAM, writes/commits one complete blob per sync, and latches failed or uncertain mutation until erase/restart. Real configuration rotation, applied-then-failed final-commit restart recovery, OT-005G counter composition, and OT-017AD ACK-session composition pass. Twelve adapter groups, five counter groups, six ACK-session groups, 100/100 focused repeats each, and the complete 91-executable host matrix pass. The secret, counter, and protocol namespaces are not protected stores; ESP-IDF binding, access control, authentication, rollback protection, locking, physical interruption, endurance, and on-device evidence remain |

## Maps and integration

| ID | Status | Task | Acceptance evidence |
| --- | --- | --- | --- |
| OT-016 | partial | Offline map architecture research | A dated source/attribution and target gate now prohibits public OSM tile-server prefetch, requires explicit offline/redistribution rights and visible attribution, compares MBTiles 1.3, PMTiles v3, and an indexed raster reference, and fixes off-device preparation plus immutable staged/read-only activation, prior-good recovery, and mapless degradation. OT-016A supplies a strict host manifest/exact-byte verifier, OT-016B the activation/fallback policy, OT-016C the restart checkpoint, OT-016D the abstract recoverable store, OT-016E persist-before-exposure boot ordering, OT-016F verified runtime transitions, OT-016G safe replacement-candidate ordering, OT-016H the clean first-baseline path, OT-016I authorized selector-only service reseed, OT-016J the NVS-ready key/value adapter boundary, OT-016K the single-use local-service authorization handoff, OT-016L the protected trusted-generation source boundary, OT-016M the protected-source boot composition, OT-016N the protected-source runtime transition composition, OT-016O the protected-source candidate replacement composition, OT-016P the protected-source first-baseline composition, OT-016Q the protected-source authorized service-reseed composition, OT-016R the reset/replacement lifecycle policy, OT-016S the protected-domain authorization handoff, OT-016T the canonical trust-domain lifecycle record, OT-016U the recoverable trust-domain record store, OT-016AB the non-erasable trust-domain key/value adapter, OT-016V the permit-consuming trust-domain provisioner, OT-016W the recoverable stable trust-domain activation coordinator, OT-016X the read-only active trust-domain boot coordinator, OT-016Y the domain-aware candidate-entry coordinator, OT-016Z the restart-safe domain-aware trial-boot coordinator, and OT-016AA the domain-aware runtime-transition coordinator. The exact-target matrix still must cover JPEG/RGB565 raster rendering, storage/RAM/frame/power timing, corruption, interruption, removal, and circular-screen usability. No provider/data package, selected container/renderer/storage/transfer path, signature policy, received display, ESP-IDF backend, or on-device result is claimed |
| OT-016A | done | Offline map package manifest and host verification | Strict `OTMP0/v0` binds canonical source/licence/offline-rights/attribution, experimental container/encoding/scheme, Web Mercator coverage/zooms, exact length/tile count/SHA-256, and reader/firmware/storage/scratch requirements. It permits only MBTiles/JPEG/TMS, PMTiles/JPEG/XYZ, and indexed-raster JPEG-or-RGB565/XYZ candidates; public OSM tile endpoints, unlicensed offline/redistributable use, extra fields, incompatible layouts, bad bounds, insufficient storage, mutation, and truncation fail closed. Seven host groups include CLI path redaction. This is read-only off-device preflight, not source authentication, package approval, target activation, rendering, or compatibility evidence |
| OT-016B | done | Fail-safe offline map activation policy | A bounded C++ guard starts mapless on missing/unreadable/ambiguous/invalid boot selection, admits only fully evidenced alternate-slot candidates, leaves the active package unchanged through staging, begins trial only after exact selector-commit evidence, retains prior-good until bounded complete reads pass, and otherwise requires exact verified fallback or visible mapless degradation. Candidate, active, prior, and fallback-media loss fail closed across ten groups and 100/100 repeats. The interface contains no paths, geographic content, identity, secrets, free text, or communications authority. Package authentication, durable selector storage, filesystem/renderer integration, target composition, and physical evidence remain |
| OT-016C | done | Restart-safe map selector checkpoint | Fixed 64-byte `OTM0/v0` encodes canonical stable/trial/fallback state, abstract active/prior slots and generations, health/deadline/boot bounds, record generation, zero reserved bytes, a commit-last marker, and CRC. Atomic guard restore revalidates exact package evidence, clears volatile health/time, increments bounded trial boots, preserves prior recovery, and requires fallback at the boot limit. Ten groups and 100/100 repeats reject corruption, uncommitted/future/coherence/policy/evidence mismatch, missing prior, invalid arguments, and unsafe export. OT-016D supplies the abstract two-slot store; CRC is not authentication/anti-rollback, and protected generation, physical storage, filesystem/renderer integration, target composition, and physical interruption evidence remain |
| OT-016D | done | Recoverable abstract map selector store | `OTM0` byte 59 is a commit-last marker covered by CRC. A two-slot store writes the complete uncommitted record, commits one byte, verifies exact readback, rotates away from prior-good state, selects only a unique newer generation, repairs known invalid/uncommitted peers, and rejects unreadable slots, equal-generation conflict, exhaustion, and below-floor restore. Read-only exact live-checkpoint verification plus exact post-erase empty verification raises coverage to fourteen groups; OT-016E/F/I own boot/runtime/service ordering. No physical storage/atomicity/endurance/power-loss evidence, protected trusted floor, package authentication, target integration, or renderer exists |
| OT-016E | done | Persist-before-exposure map selector boot | A typed coordinator restores only into a private guard, publishes stable/fallback state without rewriting, and requires resumed-trial increments plus boot-limit fallback transitions to pass commit-last save and exact readback before release. Empty state becomes explicitly mapless; failed writes, uncertain commits, bad readback, unreadable/conflicting/stale media, package mismatch, invalid policy, and dirty live ownership never expose a private candidate. Ten groups and 100/100 repeats pass. No physical backend, trusted-floor ownership, target task, package access/rendering, power interruption, or on-device result exists |
| OT-016F | done | Verified runtime map transition persistence | A coordinator requires exact persisted/live/policy/current-generation agreement, applies trial read/time, fallback completion, and prior cleanup privately, avoids writes for volatile-only/rejected outcomes, and publishes persistent changes only after commit-last save/readback. Invalid fallback evidence verified-clears only the two selector records; failed clearing and storage/generation uncertainty stay mapless and reconciliation-required. Thirteen groups plus 100/100 repeats pass. No physical backend/erase guarantee, protected floor, package deletion, target task, renderer, interruption, or on-device result exists |
| OT-016G | done | Safe replacement-map candidate ordering | From one stable active baseline, a typed coordinator verifies exact live/policy/generation/floor agreement, stages and marks the externally evidenced alternate-slot package only on a private guard, rechecks the exact preflight generation, then publishes trial state only after commit-last save and exact readback. Invalid evidence leaves the current map active; save/generation uncertainty fails mapless. Eleven groups and all four affected suites pass 100/100 repeats. OT-016H separately owns first use; exclusive target ownership, physical package/storage adapters, authentication, renderer, interruption, and on-device evidence remain open |
| OT-016H | done | Restart-safe first map baseline | A first-use coordinator accepts only a clean `no_selector` mapless guard, exact policy, fully evidenced package, two readable empty selector slots, and zero trusted history. It constructs stable state privately and uses `save_if_empty` to commit/read back canonical selector record generation 1 before exposure. Existing, dirty, unreadable, conflicted, previously trusted, or concurrently changed state is never overwritten and cannot be reset/reseeded through this API. Ten groups and all six affected suites pass 100/100 repeats. OT-016I separately owns service reseed; explicit provisioning authority, locking, physical package/storage adapters, authentication, renderer, interruption, and on-device evidence remain open |
| OT-016I | done | Authorized map selector service reseed | A service-only coordinator requires a fresh exact-operation permit from OT-016K, an already-mapless owner, exact policy, valid package evidence, and exclusive selector-store ownership. It advances beyond observable/trusted generation history, verified-clears only the two selector records, rechecks emptiness through `save_if_empty`, and publishes a stable baseline only after commit-last exact readback. Twelve coordinator groups and fourteen store groups cover permit absence/binding/replay, clean-first-use routing, active-owner refusal, dirty/conflicted state, partial/dishonest erase, exhaustion, persistence uncertainty, restart restore, and a post-clear selector race. Protected trust, physical package/storage behavior, concrete service UX/audit, target locking, interruption, renderer, and on-device evidence remain open |
| OT-016J | done | NVS-ready map selector key/value adapter boundary | A backend-neutral adapter fixes `ot_state` / `ot_maps` / `otm_sel_a|b`, exact 64-byte blobs, durable backend commit after prepared write or key erase, full-blob byte-59 marker rewrite, idempotent missing-key erase, and strict native error containment. Ten groups cover binding/arguments, exact/missing/wrong-sized reads, write/commit failure, malformed/already-committed refusal, erase, and real store save/rotation/verified-clear composition. With OT-016K, all ten map suites pass 100/100 repeats and the complete 68-executable matrix passes. The accompanying official-source ESP-IDF plan prohibits broad erase and radio reseed, defines local service authority handoff and physical interruption evidence, and makes no ESP-IDF backend, partition/security configuration, target lock/task, durability, or on-device claim |
| OT-016K | done | Single-use local-service map reseed authorization | An injected verifier must atomically consume a nonzero opaque handle and return an exact-bound, short-lived grant for selector reseed over local USB or independently authenticated local wireless. The common authorizer checks boot, scope, transport, complete policy/package/trusted-floor binding, all five service confirmations, a committed local-confirmation revision, and a hard five-minute lifetime ceiling before minting a non-copyable permit. The coordinator burns the permit before store access, rechecks boot and checked use time, and rejects mismatch, expiry, or replay. Ten authorization groups, twelve reseed groups, all ten map suites at 100/100 repeats, and the complete 68-executable matrix pass. No credential verifier, challenge protocol, protected backend replay store, audit, target UI, ESP-IDF composition, or physical authentication result exists |
| OT-016L | done | Protected map selector trusted-generation source boundary | A boot-local non-copyable enforcer reads an injected protected-generation backend, refuses rollback/conflict/nonincreasing requests before mutation, requires an atomic expected-value compare-and-advance, and accepts success only after exact readback. Every advance error or post-write ambiguity latches all later source I/O closed until fresh-boot reconciliation; zero remains the explicit never-trusted baseline and no ordinary reset API exists. Ten groups pass under strict C++17 warnings-as-errors. Lower coordinators retain scalar generations for isolated tests, while protected boot, transition, candidate, baseline, and reseed wrappers now derive them; no protected backend, target composition, reset/replacement authority, physical durability, hardware counter, or anti-rollback claim exists |
| OT-016M | done | Protected-generation map selector boot composition | A boot-only coordinator obtains the trusted floor from OT-016L before selector access, runs restore and boot-created persistence against a private guard, and rechecks or atomically advances trust with exact readback before publication. Nonzero trusted history with empty selector media, selector rollback, failed trust reads, uncertain advance, final conflict, and dirty live ownership cannot expose a map; valid store-ahead state may advance trust only after complete selector validation. Ten groups, 100/100 focused repeats, and the complete 70-executable host matrix pass. Concrete protected storage/counter, reset/replacement authority, target locking, physical durability, and on-device evidence remain open |
| OT-016N | done | Protected-generation map selector runtime transition composition | A runtime wrapper obtains both the current selector generation and rollback floor from OT-016L before selector access, runs trial, deadline, fallback, and prior-cleanup operations against a private guard, then requires an exact protected recheck or selector-save-before-trust-advance ordering before publication. Initial trust failure blocks storage; zero/stale/mismatched state and final trust change contain map exposure; uncertain advance requires fresh-boot reconciliation; invalid-fallback selector clear retains protected history for authorized service rather than becoming first use. Eleven groups, 100/100 focused repeats, all thirteen map suites, and the complete 71-executable host matrix pass. Concrete protected storage/counter, reset/replacement authority, target locking, physical durability, and on-device evidence remain open |
| OT-016O | done | Protected-generation map selector candidate replacement composition | A replacement wrapper obtains both selector generation values from OT-016L before selector access, runs candidate staging and exact-generation commit-last save against a private guard, advances and exactly reads back protected history only after selector persistence, and publishes trial state only after exact agreement. Rejected candidates recheck trust before retaining the active map; initial trust failure blocks storage; zero/stale/mismatched state, save failure, post-save conflict, and uncertain advance contain map exposure. Eleven groups, 100/100 focused repeats, all fourteen map suites, and the complete 72-executable host matrix pass. Concrete protected storage/counter, reset/replacement authority, target/package-slot locking, physical durability, and on-device evidence remain open |
| OT-016P | done | Protected-generation map selector first-baseline composition | A first-use wrapper requires an exact clean no-selector mapless owner, inspects OT-016L before selector access, permits only zero protected history, saves canonical selector generation 1 on a private guard, advances and reads back protected history from 0 to 1, and publishes the first active map only after exact agreement. Retryable initial trust failure preserves clean first use; nonzero history blocks storage; invalid package/policy, existing or dirty selector, save failure, post-save conflict, and uncertain advance cannot expose the baseline. Eleven groups, 100/100 focused repeats, all fifteen map suites, and the complete 73-executable host matrix pass. Concrete protected storage/counter, reset/replacement authority, provisioning/package-slot locking, physical durability, and on-device evidence remain open |
| OT-016Q | done | Protected-generation map selector service-reseed composition | An authorized recovery wrapper inspects OT-016L before selector access, derives the lower reseed floor internally, and requires the single-use OT-016K permit to match that exact protected value. Selector-only verified clear and replacement save remain private; protected history advances only after exact selector readback; the recovered map publishes only after exact protected readback. Initial source failure preserves storage and permit, every lower-layer failure prevents trust advance, and post-save conflict or uncertainty remains ambiguous-mapless for fresh-boot reconciliation. Twelve groups, 100/100 focused repeats, all sixteen map suites, and the complete 74-executable host matrix pass. Concrete credential and protected-generation backends, reset/replacement authority, target locking, physical durability, and on-device evidence remain open |
| OT-016R | done | Map selector reset and replacement lifecycle policy | A pure fixed-shape classifier keeps ordinary factory reset outside both map persistence domains, routes selector reseed only through OT-016K/Q while protected history is intact, blocks temporary source failure without selector access, and refuses to treat missing/replaced same-device history as first use. Same-device source replacement requires a future independent external recovery decision; independently established blank replacement hardware routes only to fresh-domain commissioning, while retained-selector import is rejected. Ten groups, all seventeen map suites, 100/100 focused repeats, and the complete 75-executable host matrix pass. No physical continuity detector, external recovery authority, new-domain provisioner, credential verifier, target lock/executor, ESP-IDF composition, or physical result exists |
| OT-016S | done | Protected map-domain replacement and commissioning authorization | An injected target backend must atomically consume one opaque handle and return an exact-bound, short-lived grant for either same-device domain replacement or blank new-device commissioning. Common code derives the scope from OT-016R, requires local USB, a nonzero proposed 128-bit domain, coherent empty or quarantined selector evidence, six confirmations, a committed local revision, exact boot/binding/time echo, and move-only permit ownership. Temporary source failure, retained state on new hardware, invalid routes/media, wireless/radio transport, mismatch, expiry, and replay mint no permit. Ten groups, all eighteen map suites, 100/100 focused repeats, and the complete 76-executable host matrix pass. The permit has no consumer: device continuity, entropy, concrete credentials/audit/replay, domain/retirement records, provision/reset execution, target locking, ESP-IDF composition, and physical evidence remain open |
| OT-016T | done | Canonical map trust-domain lifecycle record | Fixed 80-byte `OTMD/v0` preserves `OTM0/v0` unchanged while binding a nonzero current domain, explicit retired domain, quarantined selector floor, accepted selector generation, domain epoch, record generation, commit-last marker, and CRC. Fresh commissioning permits only epoch-1 pending-baseline/active state with no retired domain; same-device replacement requires distinct current/retired domains, pending-reseed or active state, epoch at least 2, and active selector generation strictly above the retired floor. OT-016S now requires the retired domain for replacement and forbids one at commissioning. Ten codec groups plus the tightened authorization suite, all nineteen map suites at 100/100, and the complete 77-executable host matrix pass. No recoverable domain-record store, authenticated/rollback-resistant backend, continuity/entropy proof, permit consumer, provisioner, target lock, physical durability, or on-device evidence exists |
| OT-016U | done | Recoverable map trust-domain lifecycle store | A separate two-slot store for exact 80-byte `OTMD/v0` records selects only the newest unique valid generation, accepts generation 1 only as fresh pending commissioning on readable empty media, and thereafter enforces exact next-generation maintenance, activation, monotonic selector-acceptance, or linked replacement transitions. It preserves the prior committed record across twelve interrupted prepared-write boundaries, commits byte 75 last, verifies exact readback, repairs known degraded peers, and refuses unreadable/invalid-only/conflicted/exhausted/backward/floor-lowering/immediate-domain-reuse state. Ten groups, all twenty-seven map suites at 100/100, and the complete 91-executable matrix pass. The store has no erase/reset or provisioning authority; the target storage adapter is OT-016AB; no ESP-IDF backend, rollback anchor, permit consumer, target lock, physical durability, or on-device evidence exists |
| OT-016V | done | Authorized map trust-domain provisioner | The sole common-code permit consumer burns exact binding/boot/time authority before any I/O, requires exclusive stopped/mapless ownership, and preflights the domain store, selector media, and protected source before mutation. It commits and verifies the exact pending `OTMD/v0` record before verified selector clear and before establishing an independently uninitialized protected source at generation zero. Matching pending state resumes only under a new exact permit; applied-then-failed source calls can be reconciled without reapply, while every post-pending failure remains mapless and reconciliation-required. Thirteen groups, all twenty-one map suites at 100/100, and the complete 79-executable host matrix pass. Success is prepared, not active: protected target adapters, concrete credentials/continuity/entropy, baseline/reseed activation composition, target locking, physical durability, and on-device evidence remain |
| OT-016W | done | Recoverable stable map trust-domain activation | The coordinator resumes exact pending or active stable-baseline state, persists selector generation 1 or exactly retired-floor plus 1, atomically advances and verifies the exact protected domain/generation, commits and verifies active `OTMD/v0`, performs final selector/source checks, and only then publishes the live map. Selector-save, protected-advance, applied-then-failed, uncertain domain-commit, and already-active restarts reconcile without lowering or rebinding durable history. Fourteen groups, all twenty-two map suites at 100/100, and the complete 80-executable host matrix pass. Active-domain boot, candidate/trial/fallback/cleanup synchronization, protected target adapters, target locking, physical durability, and on-device evidence remain |
| OT-016X | done | Read-only active map trust-domain boot | The boot coordinator accepts only a clean stopped owner and exact stable active state. It verifies the active `OTMD/v0` record and protected domain/generation before selector access, restores the exact accepted `OTM0/v0` privately, then rereads the domain, selector, and protected source before publication. It performs zero writes, commits, erases, repairs, or protected mutations; disagreement stays mapless. Thirteen groups, all twenty-three map suites at 100/100, and the complete 81-executable host matrix pass. Candidate/trial/fallback/cleanup synchronization, protected target adapters, physical restart, and on-device evidence remain |
| OT-016Y | done | Domain-aware map candidate entry | From an exact stable active baseline, the coordinator persists private trial selector generation `N+1`, rechecks the active domain, atomically advances/readbacks the exact protected domain, reverifies the selector, advances/readbacks `OTMD/v0` accepted generation, then rereads all three owners before trial publication. Rejected candidates preserve the active map only after exact final rechecks; post-selector failure stays mapless without rollback. Thirteen groups, all twenty-seven map suites at 100/100, and the complete 91-executable host matrix pass. Domain-aware trial boot/recovery is OT-016Z and runtime transitions are OT-016AA; protected target adapters, physical interruption, and on-device evidence remain |
| OT-016Z | done | Restart-safe domain-aware map trial boot | The coordinator accepts only exact active-domain relationships `D=S=G` or selector `G=D+1` with protected generation on either side, restores trial/fallback state privately, persists a resumed boot or boot-limit transition before advancing the exact protected generation and then the active `OTMD/v0` accepted generation, and publishes only after final three-owner agreement. Unrelated gaps and every uncertain post-selector result stay mapless for reconciliation; no rollback or rebind exists. Fourteen groups, all twenty-seven map suites at 100/100, and the complete 91-executable host matrix pass. Healthy promotion, fallback completion, and previous-package cleanup are now OT-016AA; protected target adapters, physical interruption, and on-device evidence remain |
| OT-016AA | done | Domain-aware map runtime transitions | Healthy trial reads and rejected operations require exact unchanged domain, selector, and protected-source rechecks. Promotion, deadline/failure fallback, valid fallback completion, and previous-package cleanup commit selector generation `N+1`, advance/read back the exact protected domain, then advance/read back `OTMD/v0` accepted history before final three-owner rechecks and publication. Interrupted promotion and uncertain domain commit recover through the expanded OT-016Z active-checkpoint boot path; invalid fallback evidence retains protected/domain history for service. Eleven groups, all twenty-seven map suites at 100/100, and the complete 91-executable host matrix pass. Protected target adapters, target locking, physical package operations, interruption/wear, rendering, and on-device evidence remain |
| OT-016AB | done | Non-erasable map trust-domain key/value adapter | A backend-neutral adapter binds the two exact 80-byte `OTMD/v0` records to `ot_state` / `ot_map_domain` / `otmd_a|b`, requires zero-marker prepared writes, commits each backend mutation, and implements byte-75 marker commit through exact read/verify/full-blob rewrite. It exposes no erase or reset API and leaves failed marker commit uncertain for restart inspection. Five groups, all twenty-seven map suites at 100/100, and the complete 91-executable host matrix pass. ESP-IDF binding, protected authentication/rollback, target locking, physical interruption, endurance, and on-device evidence remain |
| OT-017 | done | OpenGauge alert interface specification | Mirrored specifications and three normative fixtures define an explicit 64-byte frame for seven alert types, canonical units, assert/clear lifecycle IDs, optional UTC/value fields, and CRC-32. Independent OpenGauge exporter and OpenTrail ingress codecs round-trip the same bytes. Nine ingress scenario groups enforce semantic, trust/authorization, stale/future, duplicate/conflict, fixed-rate/emergency-reserve, monotonic-time, and fail-closed producer-capacity policy; four producer groups reject invalid exports. Physical transport authentication and field delivery remain later integration gates |
| OT-017A | done | Critical-alert acknowledgement interface | Independent OpenTrail and OpenGauge codecs encode/decode an explicit 64-byte `OGK0` accepted/rejected ACK containing consumer/producer/event/condition/lifecycle, boot session, sequence, observed age, canonical reserved bytes, and CRC. Three mirrored normative vectors and four scenario groups per repo pass full host matrices plus 100 repeats. Transport authentication/authorization, replay window/persistence, delivery-controller/outbox composition, and physical end-to-end ACK delivery remain |
| OT-017B | done | Final-ingress-to-ACK responder | A bounded responder converts only final alert-ingress decisions into `OGK0`: accepted and identical duplicate become accepted/none; authenticated unauthorized/stale/conflict/rate decisions map to canonical rejection reasons; malformed, unauthenticated, producer-mismatched, and clock-rollback input is suppressed. It adds monotonic elapsed age and advances boot-session sequence only after successful encoding. Eight host groups plus 100 repeats cover lifecycle, correlation/decode, duplicate recovery, negative mapping, suppression, inconsistent state, age/clock, and sequence wrap. OT-017C supplies boot-session allocation; authenticated response transport, per-session sequence persistence, full round-trip composition, and physical delivery remain |
| OT-017C | done | Durable ACK responder boot-session allocator | A fixed 64-byte `OTAS` record stores exact consumer ID, authorization epoch, nonzero boot session, generation, canonical reserved bytes, CRC, and a commit-last marker in a separate protocol-state domain. Two-slot allocation increments the session before returning it, alternates slots, verifies readback, and requires explicit reset/reseed for identity/epoch change. Ten host groups plus 100 repeats cover first use/responder composition, increment/rotation, explicit rebind, corruption, equal conflict/future version, exhaustion, reads/requests, all five power-loss mutation boundaries, uncommitted/malformed state, and reset failure; OT-017AD adds exact key/value composition. Per-session sequence, protected target storage, secure rollback, OpenGauge rebind, authenticated transport, and physical power-loss evidence remain |
| OT-017AD | done | Key/value-backed ACK boot-session composition | The real `OTAS` allocator now runs through the multi-domain key/value adapter using only exact `ot_state` / `ot_proto` / `slot_a|b` bindings. Fresh adapters prove session increment and slot rotation; failed-unapplied prepared commits retry only after confirmed absence, applied-then-failed marker commits return no session and force the next boot to skip it, explicit reset durably erases both keys before different-identity reseed, and wrong-sized values fail before mutation. Six groups, 100/100 focused repeats, and the complete 91-executable host matrix pass. ESP-IDF binding, authenticated integrity, trusted rollback, reset/reseed UI, target locking, physical interruption/endurance, OpenGauge rebind, and on-device evidence remain |
| OT-017D | done | Host-mediated physical alert/ACK round trip | Two role-reversed close-bench cycles carried 2/2 exact 64-byte `OGA0` alerts and 2/2 responder-produced correlated 64-byte `OGK0` ACKs over temporary MeshCore channel text. Every correlation field passed; loss, duplicates, and new errors were zero; total round trips were 1009.6-1014.0 ms; the SenseCAP recorded exact aggregate +4 flood RX/+4 flood TX and stayed repeat-on; 4/4 endpoint cleanup checks passed with no journal remaining. The host supplied authenticated/authorized context, so on-device authenticated transport, direct SX1262 binding, forced-route evidence for this payload, failure injection, outbox completion, and field behavior remain later gates |
| OT-017E | done | Cross-project physical ACK completion composition | Two strengthened role-reversed cycles delivered exact `OGA0`/`OGK0` frames with zero loss/duplicates/errors and 1005.3-1014.2 ms round trips. Each returned ACK passed the real OpenGauge peer authorization, session, replay, and exact correlation ingress and completed its reconstructed in-flight outbox; final queued/in-flight counts were zero. SenseCAP aggregate was exact +4 flood RX/TX and 4/4 cleanup passed. Persistent live target state, protected keys, direct transport, negative/failure/restart/revoke cases, and field evidence remain |
| OT-017F | done | Physical stale-rejection policy | Two role-reversed cycles returned exact correlated rejected/stale `OGK0` frames with zero loss/duplicates/errors and 1013.1-1014.7 ms round trips. OpenGauge processed both through authorization/session/replay/correlation but reported `outbox_completed=false`, zero delivery acknowledgements, no retry release, and explicit terminal failure. SenseCAP aggregate was exact +4 flood RX/TX and 4/4 cleanup passed. Retryable rejection and persistent failure/restart/revoke/field cases remain |
| OT-017G | done | Physical retryable rate-limit rejection | Two role-reversed cycles returned exact correlated rejected/rate-limited `OGK0` frames with zero loss/duplicates/errors and 1000.8-1008.3 ms round trips. OpenGauge processed both with zero acknowledgements/completions, released exactly one queued retry, left zero in flight, and did not record terminal failure. SenseCAP aggregate was exact +4 flood RX/TX and cleanup passed 4/4. Persistent backoff/retry completion and failure/restart/revoke/field cases remain |
| OT-017H | done | Physical rate-limit retry to accepted completion | Two role-reversed four-leg sequences returned rate-limit rejection, enforced not-ready at 25 ms and byte-identical retry readiness at 26 ms, retransmitted the same alert, and completed only after an accepted next-sequence ACK. Both finished with one acknowledgement, one recorded remote retry, zero queued/in-flight/terminal failures, zero application duplicates/new radio errors, exact aggregate +8 SenseCAP flood RX/TX, and 4/4 cleanup. State was reconstructed after both responses; durable live/restart/field cases remain |
| OT-017I | done | Live OpenGauge state across physical retry lifecycle | A single OpenGauge process started with one real in-flight entry before each first radio send and retained authorization/replay/outbox state across rate-limit rejection, exact backoff, same-frame physical retry, and final accepted ACK in both endpoint roles. Both finished with one acknowledgement/retry, zero queued/in-flight/terminal failures, zero application duplicates/new radio errors, exact aggregate +8 SenseCAP flood RX/TX, and 4/4 cleanup. Durable restart/power-loss and on-device/field cases remain |
| OT-017J | done | Cross-project outbox restart reconstruction | OpenGauge's canonical 640-byte `OOC0` checkpoint is now wired into boot-only atomic live-outbox import/export. Five integration groups reconstruct exact queued retry readiness, in-flight ACK timeout, maximum lifetime, frames/state/attempts against a new monotonic origin and fail closed on prepared, corrupt, configuration-mismatched, nonempty, or unrepresentable state. Its full 29-executable matrix and 100 focused repeats pass. Coordinated ACK/authorization generation, durable storage, power-loss, authenticated integrity, rollback, and physical/on-device restart remain |
| OT-017K | done | Canonical cross-project outbox policy identity | OpenGauge now derives the nonzero versioned `OCF0` compatibility fingerprint internally from all four outbox timers, maximum attempts, and emergency reserve; callers cannot label incompatible retry/lifetime policy as matching. A thirteenth checkpoint group, the full 29-executable matrix, and 100 focused repeats pass. Coordinated durable storage and physical/on-device restart remain |
| OT-017L | done | Coordinated cross-project recovery generation | OpenGauge's fixed 960-byte `OCR0` envelope binds the exact ACK replay/authorization checkpoint and exact outbox checkpoint to one nonzero generation with canonical padding, nested validation, outer CRC, and atomic decode. Four groups, the full 30-executable matrix, and 100 repeats pass. Live dual import, recoverable durable storage, power-loss, authentication, rollback, and physical/on-device restart remain |
| OT-017M | done | Preflighted live cross-project recovery | OpenGauge now exports both state owners into one `OCR0` generation and validates both exact boot imports on private component copies before committing either live state under exclusive ownership. Five groups, the full 31-executable matrix, and 100 repeats restore exact retry readiness and replay state while proving invalid generation, stopped/prepared state, policy/authorization mismatch, and corruption cause no partial live mutation. Two-slot storage and physical/on-device restart remain |
| OT-017N | done | Recoverable cross-project recovery store | OpenGauge's two-slot host store writes exact coordinated `OCR0` generations with full readback verification, restores the newest unique valid slot, preserves the prior good generation across partial/corrupt writes, exposes degraded I/O, and fails closed on conflicts or policy/authorization mismatch. Eight groups, the full 32-executable matrix, and 100 repeats pass. Target NVS, physical power-cut/wear, authenticated integrity, rollback, and on-device restart remain |
| OT-017O | done | Store-owned recovery generation allocation | OpenGauge's normal recovery save now allocates highest-valid plus one, starts at 1 on empty media, rotates slots monotonically, rejects unreadable/conflicted baselines, and reports 64-bit exhaustion before export/write. Ten store groups, the full 32-executable matrix, and 100 repeats pass. Factory-reset authority, target NVS, physical power-cut/wear, authenticated integrity, rollback, and on-device restart remain |
| OT-017P | done | Uncertain recovery commit reconciliation | OpenGauge marks every storage write error commit-uncertain with intended slot/generation. Sixteen interrupted-overwrite lengths spanning `OCR0` layout boundaries recover the prior good generation; a complete write followed by I/O failure reconciles as committed on boot. Twelve store groups, the full 32-executable matrix, and 100 repeats pass. Target-backend atomicity, physical power-cut/wear, authenticated integrity, rollback, and on-device restart remain |
| OT-017Q | done | Cross-project peer-authorization restart boundary | OpenGauge's fixed 256-byte `OPA0` record preserves up to eight active/revoked logical peers, role permissions, channel, opaque key handles, and authorization epochs. Export refuses pending approvals; boot-only import is canonical, policy-bound, and atomic. Eight groups, the full 33-executable matrix, and 100 repeats pass. Coordinated `OPA0`/`OCR0` restore, protected target storage, authenticated integrity, rollback, and physical power-loss evidence remain |
| OT-017R | done | Cross-project recoverable peer-authorization store | OpenGauge's fixed 288-byte `OPS0` envelope and two-slot host store wrap exact `OPA0`, allocate monotonic generations, verify exact readback, preserve the prior good generation across ten interrupted-write boundaries, reconcile a full write followed by I/O error at boot, expose degraded reads, and fail closed on conflict/exhaustion. Ten groups, the full 34-executable matrix, and 100 repeats pass. Coordinated authorization/replay/outbox restore, protected target storage, authenticated integrity, rollback, and physical power-loss evidence remain |
| OT-017S | done | Cross-project atomic alert-system recovery | OpenGauge's fixed 1280-byte `ORS0` record binds exact `OPA0` peer authorization and exact `OCR0` ACK/outbox state to one generation. Dependency-correct private registry/outbox candidates and a temporary ACK ingress preflight epoch, policy, pointer, replay, and retry dependencies before any live owner changes. Six groups, the full 35-executable matrix, and 100 repeats pass. Recoverable `ORS0` storage, target binding, authenticated integrity, rollback, physical power-loss, and on-device boot evidence remain |
| OT-017T | done | Cross-project recoverable alert-system store | OpenGauge's two-slot host store writes exact 1280-byte `ORS0` generations, owns normal monotonic allocation, preserves the newest good slot across eleven interrupted-write boundaries, requires exact readback/decode, reconciles a full write followed by I/O error at boot, exposes degraded reads, and refuses conflict/exhaustion. Eight groups, the full 36-executable matrix, and 100 repeats pass. ESP-IDF protected storage, authenticated integrity, rollback/reset authority, physical power-loss/wear, and on-device boot evidence remain |
| OT-017U | done | Cross-project trusted generation boundary | OpenGauge `restore_at_or_above` refuses a newest valid `ORS0` below a caller-supplied trusted minimum before any live owner import; `save_next_after` advances beyond both last-trusted and valid local generations, with pre-write exhaustion. Ten focused groups, the unchanged 36-executable matrix, and 100 repeats pass. A hardware-backed trusted source, authenticated integrity, reset/replacement authority, and physical evidence remain |
| OT-017V | partial | Cross-project target recovery adapter | OpenGauge's target plan defines exact two-slot `ORS0` semantics, protected opaque-handle resolution, a separate trusted-generation source, boot/save ordering, coordinated reset/replacement, and physical interruption/endurance evidence. OT-017AE now records its exact backend-neutral key/value byte binding plus real boot/save restart composition. No exact ESP-IDF target/backend, protected key store, monotonic trust source, physical interruption, or on-device boot result exists yet |
| OT-017W | done | Cross-project protected key-handle preflight | Direct and stored OpenGauge `ORS0` restore validate active peers through an injected opaque-handle boundary before outbox/ACK preflight or any live mutation; revoked peers are skipped. Unavailable, purpose-mismatch, and backend failures retain typed peer-specific evidence and leave all live owners unchanged. Eight system groups, eleven store groups, the unchanged 36-executable matrix, and 100 repeats each pass. A concrete protected-key backend and on-device evidence remain |
| OT-017X | done | Cross-project typed recovery boot | OpenGauge's host coordinator combines provisioning, trusted-generation state, non-mutating slot inspection, protected-key `ORS0` restore, and exact trusted-floor catch-up. It distinguishes first boot, restored, degraded, safe-mode, and service-required outcomes and keeps transport disabled until operational. Nine groups, the complete 37-executable matrix, and 100 repeats pass. Target boot composition, protected backends, physical interruption, and operator recovery remain |
| OT-017Y | done | Cross-project verified recovery save | OpenGauge normal persistence now requires exact local/trusted generation agreement, verifies the next `ORS0` before advancing trust, and verifies exact trust readback before reporting committed. Missing, rollback, conflict, local-ahead, uncertain commit, and trust-update failures remain typed. Eight groups, the complete 38-executable matrix, and 100 repeats pass. Target storage/trust, physical interruption, endurance, provisioning/reset, and operator recovery remain |
| OT-017Z | done | Cross-project unreadable-slot fail-close | OpenGauge distinguishes known empty/invalid peer media from unreadable media that may conceal a newer committed generation. Unreadable state returns service-required, does not advance trust, and cannot enable transport even when the older visible generation imports privately. Ten boot groups, the complete 38-executable matrix, and 100 repeats pass. Physical backend diagnosis and operator recovery remain |
| OT-017AA | done | Cross-project known-degraded repair | OpenGauge repairs only a current operational degraded result with matching active/trusted generation and one valid plus one known empty/invalid slot. It commits the next `ORS0`, advances/readbacks trust, and verifies both slots valid; healthy, unreadable, service, stale, and uncertain cases fail closed. Five groups, the complete 39-executable matrix, and 100 repeats pass. Physical durability, scheduling, audit, and unreadable-media service remain |
| OT-017AB | done | Cross-project redacted recovery status | OpenGauge maps boot, save, and repair results into one fixed-shape operator record with coarse state/reason/action, slot health, generations, protected-key failure class, and transport/repair flags. Peer IDs and key handles are structurally absent; unknown or inconsistent input fails closed. Seven groups, the complete 40-executable matrix, and 100 repeats pass locally. Target logging/rendering, audit retention, and physical service UX remain |
| OT-017AC | done | Cross-project versioned recovery diagnostic event | OpenGauge records one atomic 32-bit word containing only redacted recovery operation, state/reason/action, slot health, key-failure class, and transport/attention/repair/redaction flags. Magic/version/coherence validation fails closed; generations and identity-bearing fields are omitted. Eight groups, the complete 43-executable matrix, and 100 repeats pass publicly. Target logger binding, persistent retention/export, and physical service capture remain |
| OT-017AE | done | Cross-project `ORS0` key/value boot/save composition | OpenGauge binds the two exact 1280-byte recovery slots to `og_state` / `og_recovery` / `ors0_a|b`, requires commit after write/present-key erase, and composes real boot/save coordinators through restarted adapter/store instances. Normal one/two-slot restart, verified save, applied-uncertain trust catch-up, and unapplied-uncertain prior-generation recovery pass thirteen groups, 100/100 repeats, and the public 43-executable matrix. No OpenTrail ESP-IDF binding, protected key/trust source, physical interruption, or on-device result is claimed |
| OT-018 | planned | Display/UI feasibility spike | Candidate hardware renders representative map/peer/alert screen with measured RAM, frame time, boot time, and input behavior |
| OT-019 | partial | Update/recovery architecture | A v0 boundary fixes signed/versioned hardware-bound bundles, inactive-slot write/readback, persisted trial selection, bounded health confirmation, automatic rollback, trusted version floors, USB-first delivery, and layered physical/ROM recovery. A pure lifecycle guard passes eight host groups. The canonical 64-byte `OTU0/v0` checkpoint adds exact policy/hardware/version binding, corruption detection, atomic restore, and reboot-safe trial/rollback intent across eight groups plus 100 repeats while discarding boot-local evidence. Its abstract two-slot store owns generation allocation, preserves prior-good state across partial/corrupt writes, verifies readback, repairs known degradation, fails closed on unreadable/conflicted state, rejects a newest generation below a caller-supplied trusted minimum, allocates beyond local/trusted state, and supports non-mutating inspection across 20 groups plus 100 repeats. A typed boot coordinator privately restores and validates the observed image, durably records trial/rollback transitions, exactly advances trust, and exposes live state only after successful ordering across 15 groups plus 100 repeats. A verified normal-save coordinator requires exact local/trusted agreement, verifies checkpoint-before-trust ordering, and routes uncertain/local-ahead state to reboot reconciliation across 10 groups plus 100 repeats. A lifecycle-transition coordinator applies health, tick, confirmation, and rollback to a private guard copy, publishes durable state only after the verified save commits, and stops the original guard on persistence failure across 10 groups plus 100 repeats. A fixed redacted status boundary validates boot/save/transition coherence and emits only coarse operator state/reason/action, generation evidence, and recovery flags across eight groups plus 100 repeats; unknown or contradictory input blocks normal operation as service-required. `OTRD0/v0` then writes one canonical generation-free 32-bit outcome through the existing logger with independent encode/decode coherence, filtering, and sink-failure checks across eight groups plus 100 repeats. A bounded production-facing RAM ring retains and decodes real `OTRD0` events without adding persistence claims. OT-019B adds strict offline category decoding without file, device, network, log-retrieval, or recovery execution authority. A backend-neutral key/value adapter fixes exact `ot_state` / `ot_update` / `otu_chk_a|b` names, 64-byte values, explicit durable commit after write/erase, idempotent missing-key erase, and applied-then-failed restart discovery across nine groups plus 100 repeats. Target task binding/concurrency, retention/rendering, scheduling/reboot, exact ESP-IDF target and partition/security configuration, signer/key custody, authenticated target storage, hardware-backed trusted generation, cleanup/reset authority, updater adapters, and physical interruption/recovery evidence remain |
| OT-019A | done | Fail-visible semantic recovery presentation | Valid `OTRD0/v0` outcomes map to fixed status/system-fault UI frames. Trial, rejected-transition, and cleanup notices allow acknowledgement only; blocked rollback, safe-mode, service, and reconciliation states expose no execution action. Invalid words with a valid revision produce a generic critical service frame, while revision zero is unpresentable. Nine groups plus 100 repeats exercise generated/fallback frames through the real checked local-interface boundary in the complete 58-executable matrix. Exact renderer, target scheduling/revision ownership, physical recovery execution, and operator workflow remain |
| OT-019B | done | Strict offline update-recovery diagnostic decoder | A checked parser accepts only the exact uppercase `OTRD0=XXXXXXXX` record, preserves unsupported-version versus invalid-word/message errors, reruns all binary/coherence validation, and exposes stable names for every v0 operation, state, reason, and action. The CLI prints deterministic fixed-field output, never echoes rejected input, performs no file/device/network/log/export work, and has no recovery execution authority. Ten groups, canonical/invalid CLI smoke tests, and 100 repeats pass in the complete 58-executable matrix. Packaged tooling, target log retrieval, accessible rendering, authorization/retention/deletion policy, and physical service evidence remain |
| OT-019C | done | NVS-ready update checkpoint key/value adapter | A backend-neutral adapter binds the two exact 64-byte `OTU0/v0` slots to `ot_state` / `ot_update` / `otu_chk_a|b`, rejects invalid arguments and wrong-sized values before acceptance, requires explicit backend commit after write/erase, treats missing-key erase as idempotent, and keeps failed or applied-then-failed commits visible for restart reconciliation. Nine groups, 100/100 focused repeats, and the complete 85-executable host matrix pass. ESP-IDF binding, partition/security configuration, target locking, hardware-backed trust, physical interruption, endurance, and on-device evidence remain |

## Recommended sequence

Complete the remaining OT-003A physical/regulatory inventory and OT-005
cryptographic gates. Run OT-020 only after the Wio Tracker arrives, preserving
its shipping firmware before any write. OT-009's remaining hardware work is a
direct SX1262/OpenTrail binding and later field measurements; the
software-forced MeshCore path is proven. OT-017's physical serial/wireless
adapter, key lifecycle, replay protection, and field failure UX remain
integration gates rather than semantic-schema work. Security and regulatory
constraints must remain inputs before any public packet v1 is declared.
