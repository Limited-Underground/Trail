# OpenTrail Engineering Backlog

Statuses: `done` means the documented acceptance criteria are evidenced; `partial` means bounded evidence exists but acceptance is incomplete; `planned` means no implementation claim.

## Foundation

| ID | Status | Task | Acceptance evidence |
| --- | --- | --- | --- |
| OT-089 | done | Permanent V1/V1.5 scope and security boundary | Decision 0033 and OTVS0/v0 permanently define V1 as two supported Heltec-and-Android pairs with one authorized phone per node and coherent bidirectional BLE/direct-LoRa/BLE acceptance. Practical fresh-six-digit-PIN BLE Secure Connections authorization accepts the disclosed physical-reset/reflash rollback limit, so no secure element or independent monotonic floor is a V1 prerequisite. Secure LoRa remains a separate required gate. V1.5 is unmeasured and requires four supported nodes; mixed hardware is allowed/preferred, identical supported nodes remain eligible, four phones are unnecessary, and a relay claim requires three physical radios. Result is `OWNER-APPROVED-SCOPE-ADOPTED`; no implementation/physical authority or evidence was added. Android remains 60%, V1 exact 43.75%/displayed 44%. Next: freeze the exact physical pairing/replacement protocol, then host-test it before target/app work. See [Decision 0033](../docs/decisions/0033-permanent-v1-v1-5-scope-and-security-boundary.md), [canonical scope](../docs/testing/V1_V1_5_ACCEPTANCE_SCOPE_V0.md), and [OT-089 evidence](../tests/hardware/OT-089-2026-08-19.md). |
| OT-088 | done | Android private-pilot operational policy freeze | Decision 0032 accepts one policy revision for offline/transient privacy and data safety, first-release removal with no downgrade, and bounded best-effort/no-SLA support beginning only after a complete OTAR pass. The later run must still physically verify privacy surfaces, lifecycle, uninstall/data removal, rollback, support, and cleanup on one coherent candidate. Exactly five of eight prerequisites are satisfied; physical matrix, release identity, and signer/custody remain blocked. The plan remains `PLAN-ACCEPTED-EXECUTION-BLOCKED`, release gate `NOT-EVALUATED`, and execution authority false. No phone, install, key/certificate, signing, account, upload, or distribution operation occurred. Android remains 60%; V1 remains exact 43.75%/displayed 44%. See [Decision 0032](../docs/decisions/0032-android-private-pilot-operational-policy-freeze.md), the [policy](../docs/platform/ANDROID_PRIVATE_PILOT_OPERATIONAL_POLICY_V0.md), and [OT-088 evidence](../tests/hardware/OT-088-2026-08-19.md). |
| OT-087 | done | Android unsigned release-build foundation | Version code/name `1` / `1.0.0` and an explicit non-debuggable release build type with no signing configuration are accepted. Protocol plus debug/release application tests, both lint variants, debug/instrumentation/release assembly, and bounded packaged inspection pass. The disposable APK matched exact identity/SDK/permission/backup rules, omitted debug/test surfaces, passed DEX checksums, and was explicitly unsigned; it was not installed, signed, uploaded, or distributed. This satisfies exactly the production-variant and stable-version OTAR prerequisites. Six remain, the release gate is `NOT-EVALUATED`, Android remains 60%, and V1 remains exact 43.75%/displayed 44%. See [Decision 0031](../docs/decisions/0031-android-unsigned-release-build-foundation.md) and [OT-087 evidence](../tests/hardware/OT-087-2026-08-19.md). |
| OT-086 | done | Android operational-release admission plan | Decision 0030 accepts the machine-checkable `OTAR0/v0` contract, one canonical plan, a fail-closed validator, and deterministic denial cases. The only current distribution scope is `private-sideload-v1-pilot`; a future Play scope requires a separate current target-API policy recheck and any required toolchain/target-SDK update. At OT-086 acceptance, the exact result was `PLAN-ACCEPTED-EXECUTION-BLOCKED`: no production variant, signer/custody policy, immutable artifact, supported-device matrix, lifecycle/endurance/privacy/support result, installation, distribution, or release pass existed. Android remained 60% and V1 Companion remained exact 43.75%/displayed 44%. See [Decision 0030](../docs/decisions/0030-android-operational-release-admission.md), the [operational-release contract](../docs/platform/ANDROID_OPERATIONAL_RELEASE_ACCEPTANCE_V0.md), and [OT-086 evidence](../tests/hardware/OT-086-2026-08-19.md). |
| OT-085B | done | Physical automatic BLE link-termination acceptance | The exact installed OT-085 image required no new target write. One Android 13 phone found exactly one compatible advertiser, read the fixed 16-byte public value from the READ-only suffix-`0x04` characteristic, made no disconnect request, and accepted the bound GATT's target-driven disconnect inside the frozen timing window. The owner observed `BLE CONNECTED` then `BLE ADVERTISING`, and exactly one compatible service advertiser returned without a stable-identity claim. The hardened test-only runner emits four fixed PASS fields; 139 JVM tests across fifteen suites, lint, debug and instrumentation assembly, and twelve target-admission groups pass. Protected authorization, Ready, operational release, LoRa/GNSS, recovery, support, and field evidence remain open. See [OT-085B evidence](../tests/hardware/OT-085B-2026-08-19.md). |
| OT-085A | done | Physical bounded BLE link/status acceptance | One consumed owner-authorized app-only write installed and verified the exact OT-085 image on `OT-DEV-001`. One Android 13 phone selected the only compatible advertiser, read the exact fixed 16-byte public value from the READ-only suffix-`0x04` characteristic, and passed a phone-driven disconnect plus same-endpoint re-advertising check. The owner observed `BLE CONNECTED` and return to `BLE ADVERTISING`; no identity was retained. The automatic 15-second termination path was not physically exercised, and no protected write, ownership, provisioning, radio/GNSS, Ready, or support claim is permitted. See [OT-085A evidence](../tests/hardware/OT-085A-2026-08-19.md). |
| OT-085 | done | Build-only bounded public BLE link/status | A fixed 16-byte zero-capability `OTB0/v0` value is exposed through one READ-only UUID-suffix-`0x04` characteristic without weakening the protected Protocol Info, Command, or Stream. The runtime owner admits one 15-second connection, requires termination acknowledgement within two seconds, returns through bounded advertising restart, and keeps claims/commands closed. Five public-link groups, fifteen owner groups, the complete 148-executable native matrix, eleven target-admission groups, and two identical pinned builds pass. No device was accessed/flashed and no phone read or OLED transition was observed. See [Decision 0029](../docs/decisions/0029-bounded-read-only-ble-link-status.md) and [OT-085](../tests/hardware/OT-085-2026-08-18.md). |
| OT-084 | done | SECURE_VERSION rollback-floor viability review | Seven pinned ESP-IDF 6.0.2 sources prove ESP32-S3 `SECURE_VERSION` is a permanent 16-step application-firmware anti-rollback field whose native partition model excludes factory/test applications. A strict source-bound evaluator, fixed sanitizer, and six plan groups reject sharing that field with companion authorization because OpenTrail's accepted layout and recovery route depend on the factory application. The complete 147-executable host matrix and eleven target-admission groups pass. External authenticated monotonic hardware remains a future hardware-revision class only; no part/provider/descriptor is selected, no device or target build input changed, and V1 remains 40%. See [Decision 0027](../docs/decisions/0027-reject-esp32s3-secure-version-authorization-floor.md) and [OT-084](../tests/hardware/OT-084-2026-08-18.md). |
| OT-083 | done | Rollback-floor descriptor viability review | Pinned ESP-IDF 6.0.2 source proves the ESP32-S3 USER_DATA candidate occupies a Reed-Solomon-coded eFuse block whose coding unit is writable only once, so it cannot implement the required repeated independent one-bit floor advances. A pure source-bound evaluator, fixed sanitizer, provider-admission correction, six descriptor-plan groups, existing protected-root regressions, eleven target-admission groups, and the complete 146-executable host gate pass. The conditional floor portion of Decision 0021 is superseded; the two HMAC roles remain selected by type, but no floor provider, block, range, capacity, physical authority, or runtime path exists. No target firmware or device state changed. See [Decision 0026](../docs/decisions/0026-reject-esp32s3-user-data-rollback-floor.md) and [OT-083](../tests/hardware/OT-083-2026-08-18.md). |
| OT-082 | done | Build-only protected-root configuration/security metadata source | A Heltec-local one-use source normalizes only the default NVS build configuration and four distinct decoded security-state values. Strict host tests cover configuration permutations, all 16 security combinations, exact four-call ordering, atomic publication, re-entry, reuse, and determinism. Eleven target-admission groups, the complete 145-executable host gate, and two identical pinned target builds pass with exact header/source/object evidence. The source has no runtime path and was not device-executed; it proves no current security state, runtime scheme, configured-key conflict, complete inventory, provider, or authority. See [Decision 0025](../docs/decisions/0025-build-only-protected-root-configuration-security-adapter.md) and [OT-082](../tests/hardware/OT-082-2026-08-18.md). |
| OT-081 | done | Build-only target-side protected-root key-roster adapter | A Heltec-local one-use adapter calls exactly the five OT-080 decoded read-only ESP-IDF APIs across six logical key slots, publishes only a complete coarse roster, and fails closed on invalid purpose, contradiction, re-entry, or reuse. Five strict host groups, ten target-admission groups, the complete 144-executable host gate, and two identical pinned target builds pass. The source is build-compiled but has no runtime call path and was not device-executed. It does not infer provisioning/reservation, cannot produce complete inventory evidence, and grants no read/allocation/provider/provisioning/write/runtime authority. See [Decision 0024](../docs/decisions/0024-build-only-target-side-protected-root-key-roster-adapter.md) and [OT-081](../tests/hardware/OT-081-2026-08-18.md). |
| OT-080 | done | Offline protected-root inventory reader-route admission | Rejected the host Python/eFuse route because it materializes raw key blocks, then accepted `OTPRR0/v0` as a pure offline contract for a future audited target-side ESP-IDF metadata adapter. The route pins five official ESP-IDF 6.0.2 sources, allowlists only five decoded key-purpose/protection/unused APIs, keeps the rollback-floor descriptor and physical read unavailable, and denies every raw-key, raw-block, HMAC, write, burn, protection, deployment, device-read, provisioning, provider, and runtime authority. Six strict C++ route groups, six plan groups, the target admission suite, and the complete host gate pass. At OT-080 acceptance no adapter existed, no hardware was accessed, and V1 completion was unchanged. |
| OT-079 | done | Offline protected-root inventory admission | Accepted `OTPRI0/v0`, a no-complete-reader/no-device plan plus pure supplied-evidence verifier. A complete future private inventory must bind one fresh operation/evidence set, all six key slots and provisioning/protection facts, configured-NVS conflict state, a complete candidate-floor map, cleanup, and the OT-077 disabled security state. Missing, contradictory, stale, mixed, or secret-bearing evidence denies; complete unfavorable evidence remains reviewable without selecting or admitting a provider. At OT-079 acceptance no complete inventory reader/orchestrator was created, no device was accessed, exact allocations remained null, every physical/read/write/eFuse/provisioning/runtime authority remained false, and V1 Companion remained 40%. |
| OT-078 | done | Offline protected-root provider selection | Selected two distinct ESP32-S3 `HMAC_UP` provider roles for `ot_auth` NVS encryption and the private bond-binding PRF, plus a conditional dedicated custom user-eFuse thermometer class for the independent authorization floor. Pure host contracts require provisioned/read-protected/operational/fresh key evidence, exact role and NVS binding, canonical low-order thermometer encoding, one-step advance, exact reread, permanent exhaustion, and fail-closed uncertainty/reconciliation. Physical block IDs, counter field/capacity, inventory, provisioning, runtime injection, and every device/write/eFuse/transition authority remain absent. No hardware was accessed and V1 Companion remains 40%. |
| OT-077 | done | Offline exact recovery-route acceptance | `OTRR0/v0/heltec-v4-ot064-source-restore` freezes a no-stub ESP32-S3 ROM serial route at 115,200 baud with no-reset before/after, one future separately authorized attempt, unconditional exact application restore at `0x010000`, exact source partition table last at `0x008000`, closed-connection independent readback, and bounded boot-success criteria. Future admission requires fresh same-operation/evidence-set read-only proof that secure boot, flash encryption, and secure download are disabled; unknown or mismatch denies. Any incomplete outcome after the first write closes the connection, stays in ROM without reset/boot claim, preserves private artifacts and a minimum private journal before transient cleanup, exposes only sanitized `RECOVERY-UNCERTAIN`, never auto-retries, and requires fresh authority. Strict offline validation denies identity, profile, artifact, order/range/digest, custody, forbidden-operation, execution-surface, or authority drift. The public source table is reproducible; one private application copy is retained and a second independently hashed staged copy remains required. Two distinct protected HMAC roles and an independent monotonic rollback-floor requirement are defined, but no provider is selected/provisioned. No hardware was accessed; every physical authority remains false, the transition remains denied, and V1 Companion remains 40%. |
| OT-076 | done | Exact installed application recovery capture | One owner-authorized, one-use read-only operation captured the exact 470,928-byte OT-064 factory application from `OT-DEV-001` and independently reread it after closing the connection; SHA-256 matches `A7D8E672CF9169F1D1D4E86EEFF80399C47A145E7D64904C207DD5F1B23F359B`. The binary is retained only as a private ignored recovery artifact. The reader/tests/bytecode were deleted, no port/device/private operation identity or detailed output was retained, and no persistent write, erase, software-issued reset, key, eFuse, or transition occurred. Manual RST returned the Trail logo and `BLE ADVERTISING`. OT-077 later accepts the exact route offline; fresh unified evidence, redundant custody, concrete key/floor providers, exact-unit validation, and separate write authority remain required. The transition stays denied and V1 Companion remains 40%. |
| OT-075 | done | Offline protected-storage candidate and recovery gate | Pinned ESP-IDF v6.0.2 deterministically generates the exact 3,072-byte `OTPS0/v0` candidate partition artifact (SHA-256 `F83EDE6D0F206D6032147A2AF0B526700BCB888A6C0CADDF6DD17724E5600E72`) and focused tests reject mismatched source, toolchain, rows, checksum, size, padding, or any physical execution surface. A clean rebuild from the recorded OT-064 source commit did not match the application installed on the unit, so it was rejected and not retained. OT-076 later closed the exact-application prerequisite and OT-077 accepted the exact route offline. Fresh unified evidence, redundant custody, concrete key/floor providers, exact-unit validation, and separate write authority remain required. Active target/runtime/device state is unchanged, the transition stays denied, and V1 Companion remains 40%. |
| OT-074 | done | Physical protected-storage source proof | A bounded OT-DEV-001 read-only operation matched the exact installed 3,072-byte partition table and verified that the complete 1 MiB source region was all `0xFF`, returning only `OTPSTE1/v0` / `SOURCE-PROOF-SATISFIED-ONLY`. No raw bytes, paths, port, device identifier, private operation/evidence identity, or detailed transport output was retained, and the temporary executor and bytecode were deleted with cleanup verified. Manual RST returned the Trail logo and `BLE ADVERTISING`. This satisfies only OT-070's source prerequisite; no partition transition, protected storage, key, rollback-floor, bond, GATT, Ready, LoRa, or GNSS authority was added, and V1 Companion remains 40%. |
| OT-073 | partial | Physical protected-storage source proof | One owner-authorized, one-use OT-DEV-001 read-only attempt used esptool 5.3.1, one no-reset ESP32-S3 ROM connection attempt, no RAM stub, and only the two OT-071 allowlisted ranges. It returned only `OTPSTE0/v0` / `DENY-READ-FAILURE`; the attempt was consumed and not repeated. No raw bytes, paths, port, device identifier, private operation identity, or detailed transport output was retained, and the ephemeral executor files were deleted. Manual RST returned the Trail logo and `BLE ADVERTISING`. Source proof remains absent; no partition transition, protected storage, key, rollback-floor, bond, GATT, Ready, LoRa, or GNSS evidence or authority was added, and V1 Companion remains 40%. |
| OT-072 | done | Canonical V1 Companion release measurement | A distinct six-milestone V1 Companion track reuses accepted shared evidence once, adds a 20%-weight Android milestone at 40% for two of five equal accepted gates, and retains a 0% four-person Companion field gate. Weights total 100; the exact result is 39.75% and displays as 40%. The historical standalone baseline remains exact 31.75%/displayed 32%, and V2 Integrated remains unmeasured. No implementation, package, target, device, or field evidence changed |
| OT-001 | done | Project bootstrap and repository structure | Self-contained directories, agent guide, README, architecture, status, and backlog exist |
| OT-001A | done | OpenTrail GitHub host validation | GitHub Actions builds six C++ verifier/planning/operator CLIs and runs all 123 C++ test executables, 59 independent C# Windows-loader scenarios, 33 simulator-core groups, 23 Windows companion/process groups, 15 private-helper groups, ten native-protocol groups, 13 integrated simulator-WPF groups, the pinned OpenSSL/Espressif cross-tool vector command, and the Python build-only Heltec target admission, MeshCore lease/GNSS/runtime-redaction, Windows USB discovery, privacy-safe field/pilot, crypto-benchmark, offline-map manifest, and publication-safety suites on `main` pushes and pull requests using bounded, least-privilege, commit-pinned Windows 2025/Python 3.13/.NET 8/UCRT64 automation. The matrix includes warning-free WPF loader and simulator builds, fail-closed firmware-bundle admission, board preflight/write admission, portable UI, companion codec/session/semantic/coordinator/authorization/persistence/runtime-owner layers, and target-local self-checks; detailed evidence remains in individual rows and the dated progress log. Local confirmation of the newest matrix exits 0; public confirmation remains a per-push gate. This is host/build evidence, not physical radio, authenticated packet-v1, physical target runtime, physical display/input, installer lifecycle, or measured-radio evidence |
| OT-001B | done | Cross-project public host validation | OpenGauge GitHub Actions separately validates the shared Windows host matrix on `main` pushes and pull requests using bounded, least-privilege, commit-pinned automation. Its current-main warning-free run passes all 41 executables with zero annotations. This is cross-project host evidence, not a physical radio or OpenTrail target build |
| OT-025 | partial | Funding readiness packet | A brand-neutral public packet contains the factual project core, reusable application answers, vendor-neutral hardware template, preliminary milestone budget, qualification/submission checklist, and opportunity register. Every cash, hardware, discount, loan, sponsorship, and service-credit path is on owner-directed hold: no opportunity research for outreach, contact, submission, account activity, acceptance, shipment, or announcement is authorized. Legal applicant/payee, exact hardware freeze, dated quotes, eligibility/terms, private-document handling, and explicit owner clearance remain later gates |
| OT-026 | done | First-release capacity policy | Historical/superseded current scope: Decision 0033/OT-089 preserves this accepted planning evidence but supersedes its four/four-plus/eight current V1 sequence and initial-release repeater claim. The original v0 planned a ceiling of eight active clients in one group plus at most one optional authorized repeater, with evidence progressing from four standalone clients to four-plus-repeater and then eight-plus-repeater on frozen hardware/firmware. Base clients could not depend on repeater/server/internet/phone/laptop/vehicle operation. No phase passed, so this remains historical release-planning evidence rather than a current capacity/support claim |
| OT-027 | done | Base-versus-optional product boundary | One public map defines the self-contained base client and optional repeater, opt-in archive, OpenGauge vehicle, offline-map/large-display, and post-session management roles. Each boundary lists allowed data and fail-independent behavior; no add-on may become a base-operation dependency. Hosting operations, private location/participant data, credentials, and provider specifics remain outside the public repository |
| OT-028 | done | Opt-in breadcrumb archive session boundary | A fixed-memory host session wraps the explicit position scheduler without controlling base radio operation. Exact 56-byte `OTBA/v0` records contain only an opaque nonzero session ID, accepted-record sequence, boot-local capture time, canonical current-position payload, reserves, and CRC-32. Session IDs increase within one object lifetime; sequence advances only on local transport acceptance and retryable pressure retains it. Ten groups plus 100/100 repeats pass. OT-028J separately reserves restart-safe ranges, but no server, remote ACK/durability, stable identity, authorization, cryptography, retention/export/deletion, target binding, real-coordinate trial, or physical result exists |
| OT-028A | done | Bounded breadcrumb archive outbox and durable-ack handoff | A 16-record RAM FIFO validates exact `OTBA/v0`, enforces session/sequence order, never overwrites or evicts on full, and retains records on remote not-ready/rejection/failure. A cooperative uploader removes only the exact head after injected `durable_ack`; post-ACK local mismatch latches closed. Explicit whole-queue discard zeroes occupied entries while retaining non-coordinate order history. Ten groups plus 100/100 repeats pass. This is volatile host evidence—not persistent storage, secure erase, checked-time retry, endpoint/authentication, actual server durability, account/access, retention/export/deletion, target, physical, or recovery evidence |
| OT-028B | done | Checked-time breadcrumb archive retry coordinator | One fixed-memory optional-path owner reads `CheckedMonotonicClock` before any upload, attempts at most one FIFO head per cooperative call, doubles transient not-ready/failure delay to a fixed maximum, allows exact-deadline retry, and resets only after durable ACK plus exact local commit. Empty queue reads no clock; temporary not-ready defers; rollback/source failure, rejection, deadline overflow, or uploader ambiguity retain the queue and latch closed. Ten groups plus 100/100 repeats pass. Target intervals/task/locking, connectivity, authenticated transport, server receipt semantics, persistence, power, UI, and physical evidence remain absent |
| OT-028C | done | Privacy-safe breadcrumb archive presentation | One fixed-memory pure adapter validates copied session/outbox/retry relationships and emits only stopped, active, queued, waiting, full, or failed semantic notices plus a bounded 0-through-16 queue count. Frames contain no coordinates, record bytes, endpoints, credentials, identities, deadlines, or receipt details; offer no actions; and do not present optional failure as a base system fault. Incoherent state fails visibly and impossible count is redacted. Ten groups plus 100/100 repeats pass. Atomic target snapshotting, renderer, physical display, recovery/discard/retention/export/deletion authority, server, and real-coordinate evidence remain absent |
| OT-028D | done | Single-read breadcrumb archive status snapshot | One target-facing source returns the complete session/outbox/retry status tuple through exactly one common-code call. Temporary not-ready returns no frame; failed or unknown state ignores partial output and emits the generic action-free warning with queue zero; revision zero is rejected before source access. The tuple contains no record bytes or coordinates, is 200 bytes on the current 64-bit host, and has a 208-byte compile ceiling. Ten groups plus 100/100 repeats pass. The contract requires but does not implement an ESP-IDF task/lock, target adapter, concurrent copy proof, renderer, physical display, or archive execution authority |
| OT-028E | done | Single-owner breadcrumb archive UI coordinator | One cooperative owner performs exactly one complete snapshot read per valid service call, owns strictly increasing semantic-frame revisions, skips unchanged redraws without consuming a revision, and retains the prior truthful frame across temporary source/display unavailability or display failure. Failed, unknown, and incoherent status remains a redacted action-free warning; changed state after maximum revision is rejected. Ten groups plus 100/100 repeats pass in the complete 96-executable matrix. The owner has no input, archive mutation/upload/storage, or base-radio authority. Target task/lock, renderer, physical display, resource measurement, and on-device recovery remain |
| OT-028F | done | Serialized breadcrumb archive snapshot adapter | One target-shaped source acquires an injected nonblocking lock once, copies the concrete session/outbox/retry statuses while held, releases once, and publishes only after successful release. Contention redacts and defers; lock/unlock failure or unknown state redacts and latches the optional source. Ten groups plus 100/100 repeats pass in the complete 97-executable matrix. No archive mutation or base-radio authority exists. Real ESP-IDF synchronization, shared writer discipline, concurrent stress, target resource measurement, and physical evidence remain |
| OT-028G | done | Private serialized breadcrumb archive runtime owner | One target-shaped owner privately constructs capture, outbox, uploader, retry, and snapshot components. Start/stop/capture/upload/snapshot calls share one injected lock; no direct mutable-owner reference escapes. Contention attempts nothing, component rejection remains separate, and post-operation unlock uncertainty is explicit before the optional path latches. Ten groups plus 100/100 repeats pass in the complete 98-executable matrix. Target-exclusive workflow binding, real ESP-IDF binding, concurrent target stress, network/storage durability, and physical evidence remain |
| OT-028H | done | Revision-bound local breadcrumb archive consent | Canonical archive confirmation frames allow hold-only Start and immediate Stop only on their exact active local revision; archive actions are invalid on every other screen. The controller accepts only resolved local actions, uses checked time for Start, keeps Stop clock-independent, consumes nonzero IDs only inside explicit caller-supplied inclusive lease bounds, never consumes an ID on temporary contention, refuses reuse after uncertain application, and never crosses the final ID. The owner is non-copyable. Eleven groups plus 100/100 repeats pass in the complete 109-executable matrix. No radio/server/automatic start input exists. Renderer/physical input, full application shell, selected-target binding, ESP-IDF composition, and on-device evidence remain |
| OT-028I | done | Complete local breadcrumb archive workflow coordinator | One cooperative non-copyable owner keeps the serialized runtime reference, private local consent, fresh snapshot-backed controls, Start/Stop confirmation, Cancel, action application, and post-action refresh in one revision sequence. Unknown/incoherent state offers only Stop; Start remains hold-only; Stop is immediate. Exact-parent-revision re-entry retains the cursor inside explicit durable lease bounds. Snapshot/runtime contention defers before unsafe action, and failed post-Start refresh or revision exhaustion attempts privacy-safe Stop and latches. Fourteen groups plus 100/100 repeats pass in the complete 109-executable matrix. Complete application shell, renderer/physical input, selected-target bootstrap binding, ESP-IDF binding/concurrent stress, and on-device evidence remain |
| OT-028J | done | Restart-safe breadcrumb archive session lease reservation | A two-slot exact 64-byte `OTBL/v1` commit-last store reserves one inclusive nonoverlapping session-ID range before returning its first value. Restart abandons unused IDs; an applied-but-reported-failed final commit causes the entire uncertain range to be skipped. The record has no participant/device/group/location/endpoint/account identity and lives in an isolated fifth `breadcrumb_archive_state` / `ot_archive` domain. Nine store groups, five real key/value-composition groups, affected consent/workflow/bootstrap/navigation/page suites, and 100/100 focused repeats pass in the complete 109-executable matrix. Secure blank-state entropy, authenticated integrity/rollback resistance, recovery UX, ESP-IDF/NVS and selected-target boot composition, physical interruption, and on-device durability remain |
| OT-028K | done | Durable archive lease-to-workflow bootstrap | A fixed-memory, non-copyable one-boot owner explicitly commits and reads back one `OTBL/v1` range before constructing the local archive workflow. Dormant service/entry touches no storage/runtime/input/display; repeated initialization reuses the one boot lease; invalid or uncertain allocation latches without retry, reset, workflow construction, or base-radio authority. Applied-but-reported-failed commit recovery skips the uncertain range, and a one-ID composition refuses a second Start without crossing its bound. Eight groups plus 100/100 repeats pass in the complete 109-executable matrix. Exact ESP-IDF/NVS and secure-seed binding, recovery UX, complete application shell, renderer/physical input, concurrent stress, reset/brownout/endurance, and on-device evidence remain |
| OT-028L | done | Exact-revision archive parent/workflow handoff | A non-copyable local router accepts only resolved `open_archive_controls` matching the exact active parent revision. First entry derives the next workflow revision and commits the durable lease before controls; Cancel returns a minimum newer parent revision; later exact active parent entry reuses the same boot workflow/lease without another allocation. Invalid/stale/already-active/exhausted input rejects before storage, and lease failure latches before runtime/display. Eight groups plus 100/100 repeats pass in the complete 109-executable matrix. This is navigation only and cannot Start capture. Complete application-shell routing, exact target binding, renderer/physical input, concurrency/recovery UX, and on-device evidence remain |
| OT-028M | done | Optional archive parent page and restoration | A non-copyable nested page explicitly presents one semantic `status` frame with Archive controls and Back before any archive I/O. Exact Open enters the durable navigation/workflow; nested Cancel restores this parent at the returned newer revision; Back exits to the broader future shell. Initial/restoration display-not-ready states retry without duplicate navigation or lease allocation. Invalid summary/revision and input/display/lease failures remain inside the optional page before base-radio authority. Nine groups plus 100/100 repeats pass in the complete 109-executable matrix. This is not the full home/message/quick-status/critical/position shell, renderer, physical input, target task/backend, or device evidence |
| OT-029 | done | Generic quick-status payload catalog | Exact 12-byte `OTQ0/v0` fixes four scenario-neutral meanings: I'm OK, Need assistance, Anyone online?, and Available to help. The payload contains only magic/version/length, one semantic value, a zero reserve, and CRC-32; participant/device/group/location/time/message/ACK/text/key/routing data are absent. Ten groups plus 100/100 repeats pass in the complete 109-executable matrix. CRC is not authentication, Need assistance is not guaranteed rescue, and packet-v1 type/priority/replay/expiry/ACK, outbound admission, renderer, target/radio, and physical delivery remain |
| OT-029A | done | Revision-safe generic quick-status menu | Two canonical four-action pages preserve Back while exposing all four fixed statuses within the portable UI limit. Exact Next/Previous transitions own newer revisions; display deferral retries without another input poll; stale/invalid input selects nothing. A valid choice returns only typed local selection plus a newer parent floor—never queued/sent/delivered evidence. Ten menu groups and the thirteen-group local-interface suite pass 100/100 repeats in the complete 109-executable matrix. Broader shell, authenticated outbound admission/outcome UX, renderer/target, and physical controls remain |
| OT-029B | done | Quick-status parent page and restored selection handoff | One narrow status page exposes Quick status plus Back, enters the two-page chooser at the exact next revision, restores itself at the returned newer revision, and withholds a typed choice until restoration succeeds. Deferral polls no second input and surfaces nothing early; nested/parent Back return no choice. Ten groups plus 100/100 repeats pass in the complete 109-executable matrix. This is not the broader shell or outbound path; no queue/radio/identity/storage/GPS/archive/server/alert authority or sent/delivered claim exists |
| OT-030 | partial | Dual virtual-LCD simulator | OT-030A established the isolated two-window host shell, OT-030B replaced its invented LCD workflow with the shared C++ portable shell/render plan and added passive USB-candidate discovery plus an explicit generation-bound companion bridge, and OT-030C completed the bounded shared message/inbox/compose/critical-ACK host surface. Real two-device USB use, authenticated OpenTrail packet/application transport, radio/peer-delivery proof, target firmware, physical-display/input retest, packaging, clean-machine, and supported-hardware evidence remain. The simulator is not a phone/laptop field dependency or a substitute for the four-unit pilot |
| OT-030A | done | Isolated laptop simulator shell | One .NET 8 WPF process opens separate Client A and Client B virtual-LCD windows over independently bounded state and two allowlisted in-memory simulated companions. Per-client limits are 32 outgoing commands, 128 message entries, and 32 alert entries; each peer inbox is capped at 64. Exact label allowlisting, exclusive opaque endpoints, nonblocking polling, clock-rollback fault, stale state, reconnect isolation, stale-inbox drain, exception-contained publication, failure-retained drafts, serialized window commands, explicit post-await UI dispatch, independent close, and close-time in-flight-service draining pass 17 core plus 10 WPF groups, 100/100 core repeats, and 100 serialized Connect/Reconnect races. Deterministic 100%/125%/150%/200% bitmap checks pass, the 100% home render was visually accepted, and the complete local host gate exits 0. UI copy distinguishes local queue/bridge/peer-simulator evidence and explicitly denies real-radio/physical-LCD proof. No USB/MeshCore/device I/O, physical input/display, full monitor/theme/AT, package, installer, clean-machine, signing, or support evidence exists |
| OT-030B | done | Shared portable UI and bounded companion bridge | The Windows simulator now renders only the shared C++ `PortableUiShell` and fixed 466 x 466 circular render plan through a versioned native line host; the generic WPF Home/Messages/Compose/Alerts state machine is retired. Two-phase offer/present/commit, revision/generation checks, exact action-slot mapping, 64-logical-pixel targets, recovery/fault arbitration, close-time service draining, and local-loopback A-to-B quick/critical behavior pass the accepted 32-core/23-Windows/15-helper/11-WPF focused gate. Passive VID/PID discovery never opens or queries a device. USB requires an explicit available selection, exact open-time runtime recheck, one client per private endpoint, private remembered assignment with Forget, bounded process I/O, and deterministic teardown; the SenseCAP repeater role is excluded. The live USB application admits only fixed quick-status and critical-alert requests; acknowledgement, arbitrary chat, archive, and position requests fail closed. The unauthenticated helper can decode an inbound correlated `OTS0:A` observation, which can advance only an already-outbound matching alert. No hardware or serial port was accessed for this acceptance, and it proves no OpenTrail packet, radio delivery, authenticated peer, target firmware, physical LCD, or supported hardware |
| OT-030C | done | Shared bounded message, compose, and critical-ACK UI | Shared C++ and WPF integration adds a 12-message, 96-byte printable-ASCII presentation boundary in a shell-owned sidecar for the exact offered frame, preserving the existing 24-byte `UiFrame` ABI and legacy result-object memory budgets. It includes explicit C++-owned truncated/unavailable copies; two-row newest-first Inbox/Outbox pages; exact read-on-presented-detail state; eight fixed C++ compose templates over four pages; bridge-local delivery evidence; and hold-only acknowledgement of an exact active inbound critical alert. Protocol v2 is newline-delimited, 4096-byte command/8192-byte response bounded, uppercase-hex, exact-field, generation/revision/request/session/sequence correlated, and rejects v1, partial EOF, NUL, oversized input, malformed exceptional text, stale input, replayed evidence, and contradictory completion. The warning-free focused gate passes strict C++ plus 33 Core, 23 Windows bridge, 15 private helper, ten native-protocol, and 11 integrated WPF groups, including exact-once request handling and two native LCD sessions crossing one fixed-template local-loopback message. The representative Home, Compose, post-send, and inbound-detail visual set passed review, and the complete expanded 112-executable host/publication-safety gate exits 0. Remote publication remains pending. Template chat and inbound-critical acknowledgement are local-loopback only. Live USB acknowledgement requests fail closed; an inbound observed `OTS0:A` may correlate only an already-outbound matching critical alert. None of this is authenticated-peer, radio-delivery, operator-response, or support evidence |
| OT-031 | done | Shared Android-companion and standalone-touchscreen direction | Decision 0007 keeps a future affordable Android companion using a separately approved mesh device and the original self-contained touchscreen client over shared versioned behavior in this repository. Platform/hardware adapters and evidence remain separate; the simulator is a reference rather than a runtime dependency. Android lifecycle/permissions/accessibility/signing/distribution and device approval remain open, iPhone/store direction is undecided, and the current four-self-contained-unit V1 is unchanged |
| OT-032 | done | Provisional Limited Underground Trail product-family naming | Decision 0008 records `Limited Underground` as parent; `Limited Underground Trail` as Android application and family; Essential as the screenless phone-required LoRa companion; Gold as the one-touchscreen client; Platinum as the two-display client; Trail Repeater as optional repeater; and `Limited Underground Firmware Loader` as shared desktop tooling. The loader remains visibly Preview/inspection-only until real write and recovery acceptance passes. All names are working and provisional pending professional clearance, no `®` is used, and repository/folder/namespace names, `OT-*` IDs, protocol/GATT/schema/crypto/compatibility/board identifiers, and device IDs remain stable. This decision changes no V1 evidence or support claim |
| OT-033 | done | One-controller BLE companion protocol foundation | Decision 0009 and the BLE Companion GATT v0 contract keep the device authoritative for security, queues, delivery, location, and history while one phone renders state and submits intent. Exact 16-byte `OTB0/v0` capability records and maximum 148-byte `OTC0/v0` fragments are brand-neutral, fixed-capacity, versioned, and strict. A host guard requires an opaque controller binding plus encrypted-link, authenticated-bond, and application-authorization evidence; it enforces one active controller, exact session binding, monotonic non-wrapping session/request IDs, no-reapply duplicate classification, stale/direction/fragment rejection, and redacted status across 15 warning-free groups and 100/100 repeats. The complete 113-executable host/publication-safety gate passes. No BLE stack, pairing/OOB workflow, Android project, semantic payload, request fingerprint/result cache, reassembly, target binding, radio/GNSS/persistence integration, or physical evidence exists. No device was accessed or written; V1 is unchanged |
| OT-034 | partial | Build-only Heltec V4 ESP32-S3 target candidate | A separate `firmware/targets/heltec_v4_bench` scaffold pins ESP-IDF v6.0.2 and `esp32s3`, emits one fixed startup line plus a recurring boot-local USB Serial/JTAG heartbeat, and has an exact machine-readable candidate contract. The application does not initialize, access, or bind radio, BLE, Wi-Fi, GNSS, storage, identity, secrets, GPIO, display, power, or complete portable-client composition. Three host admission groups enforce candidate/support/write boundaries and prohibit device-write tooling. The native helper and size analysis pass: 141,965 application bytes, 86% of the application partition free, exact BIN/ELF/map/bootloader/partition/sdkconfig hashes, and an 8.05-second incremental rerun reproducing them. No device was discovered, opened, or flashed. Only the application-owned dynamic elapsed-millisecond value has been source-reviewed; framework boot/runtime logs remain unreviewed. The generic 2 MB/DIO/80 MHz image header is not the observed 16 MB/2 MB-PSRAM Heltec profile. Exact received-board authority, profile rebuild, sacrificial-first ROM recovery, board bindings, physical runtime, radio operation, and all support claims remain open; V1 is 30% |
| OT-035 | done | Fixed BLE companion semantic payload codecs | Exact fixed-capacity `OTX0/v0` snapshot-request, `OTN0/v0` typed status, `OTA0/v0` action, and `OTR0/v0` result records occupy 8, 32, 20, and 20 bytes and fit one v0 fragment. A strict dispatcher binds them to snapshot-request, snapshot, action-request, and action-result `OTC0` kinds and rejects events because v0 defines none. The snapshot contains only a nonzero device revision, closed radio/GNSS/power/position states, queued-action count, and optional exact device-owned critical-alert ID. Actions admit only four canonical quick-status IDs, acknowledgement of that exact alert ID, and explicit position-sharing Start/Stop. Results distinguish locally admitted, device-queued, and rejected without sent/delivered claims; stale-alert and queue-full reasons are kind-constrained. Thirteen focused warning-free groups, 100/100 repeats, and the complete 114-executable host/publication-safety gate pass. The semantic layer contains no strings, coordinates, group secrets, `OTS0`, `UiFrame`, event/history payload, request fingerprint/result cache, BLE stack, Android binding, target binding, or device evidence. No hardware was accessed or written; V1 remains 30% |
| OT-036 | done | Buildable native Android client foundation | A pure Kotlin module strictly mirrors the brand-neutral `OTB0/v0` and `OTC0/v0` codec bounds and consumes the shared C++ golden vectors. A native Jetpack Compose shell uses stable technical package `io.github.nbjelanovic.otclient`, shows the current working product name, and exposes explicit Disconnected, Selecting, Connecting, Connected, and Failed states over a deterministic fake transport with two local choices and exactly one active fake connection. Six protocol tests and four application-state tests pass with warning-as-error lint and debug APK assembly; independent artifact inspection confirms min SDK 26, target SDK 35, the expected package/label, and no Bluetooth, nearby-device, location, internet, storage, or management permission. The activity controller is not lifecycle-safe, free-form adapter errors are fake-only, and no BLE adapter, GATT/security session, semantic device binding, install, release signing, distribution, or physical-device evidence exists. No hardware was accessed; V1 remains 30% |
| OT-037 | done | Companion codecs linked into the build-only ESP32-S3 candidate | The `heltec_v4_bench` target links the accepted companion framing and semantic codec sources. A fixed boot-path self-check compares exact 16-byte `OTB0`, 20-byte `OTA0`, and 40-byte `OTC0` plus `OTA0` vectors, decodes them, and requires exact semantic dispatch before the startup/heartbeat path; fixed failure suspends. Static admission passes 3/3 and 100/100 repeats. ESP-IDF v6.0.2 builds a hash-stable 145,657-byte generic image, 3,692 bytes above OT-034, with both companion objects in the link map. Evidence remains `BUILD-LINKED-NOT-RUN`, `NOT-FLASHED`, and `UNREVIEWED-RUNTIME`; no device was accessed. Generic 2 MB/DIO/80 MHz configuration, exact-board authority, recovery, BLE/GATT/session runtime, radio, GNSS, storage, physical execution, and support remain open. OT-034 stays `partial`; V1 remains 30% |
| OT-038 | done | Android semantic parity and typed fake workflow | Pure Kotlin mirrors the exact fixed `OTX0/v0`, `OTN0/v0`, `OTA0/v0`, and `OTR0/v0` records against nine shared C++ golden rows and enforces exact single-fragment `OTC0` kind binding. The fake-only Compose surface presents typed test status and submits all four quick statuses, exact pending-alert acknowledgement, and explicit position-sharing Start/Stop through correlated bounded monotonic test envelopes. Queue/revision/session/exchange exhaustion, stale alerts, invalid actions, and connection-transition failures reject without mutation. Six envelope, ten semantic, and eleven app-state tests, clean lint, debug APK assembly, and independent artifact inspection pass. All outcome copy says fake/test; no Bluetooth permission/adapter, lifecycle-safe owner, device/emulator access, install, signing, distribution, or radio evidence exists. V1 remains 30% |
| OT-039 | done | Duplicate-safe companion request coordinator | A fixed-memory target-neutral C++ owner combines the accepted session guard, semantic dispatch, injected snapshot/action authority, exact response encoding, and one exact request/response cache. Pure preparation plus atomic commit ensures output bytes are valid before an action can apply; commit errors guarantee no mutation. Byte-identical duplicates replay without authority recall, changed-byte conflicts reject, and capacity, aliasing, stale/session/exhaustion, authority failure, terminal retry, and close/reopen paths fail closed. Sixteen strict groups plus 100/100 repeats pass in the complete 115-executable host matrix. Authority adapters, BLE/GATT runtime, persistent history/cache, radio/GNSS/storage binding, delivery evidence, and physical operation remain absent. V1 remains 30% |
| OT-040 | done | Companion coordinator linked into the build-only target | The generic ESP32-S3 candidate now links the accepted request coordinator and gates startup/heartbeat on a deterministic fixed-authority boot self-check. Exact action/result and snapshot/status envelopes, response correlation, one prepare/commit application, and byte-identical duplicate replay without a second authority call pass strict native checks, 100/100 repeats, static admission, and the complete 116-executable host matrix. ESP-IDF v6.0.2 reproducibly builds a 148,949-byte generic image with all companion/self-check objects retained. Evidence remains `BUILD-LINKED-NOT-RUN`, `NOT-FLASHED`, `UNREVIEWED-RUNTIME`, generic 2 MB, and fake-authority only; no device was opened by the target path and no GATT/radio/GNSS/storage/delivery/runtime evidence exists. OT-034 stays `partial`; V1 remains 30% |
| OT-041 | done | Lifecycle-safe unwired Android BLE runtime boundary | A pure Kotlin owner fixes the v0 GATT identifiers and coordinates one scan/GATT/reconnect generation through injected Bluetooth/timer facades. Encrypted/authenticated bond plus application authorization precedes MTU, Protocol Info, indication-only Stream subscription, and authoritative snapshot opening. Peer/global fragment bounds, exact session/action correlation, bounded phase/result timeouts, owner-thread callbacks, stale generation, observer re-entrancy, reconnect exhaustion, and stop/destroy release fail closed across 17 BLE tests; six envelope, ten semantic, and eleven controller tests also pass with clean lint and APK assembly. The visible UI remains fake-only, the manifest remains BLE/location/network/storage-permission-free, and no concrete Android Bluetooth facade, UI wiring, device/emulator/ADB/BLE access, ESP-IDF GATT peer, install, signing, or field evidence exists. V1 remains 30% |
| OT-042 | done | Dormant NimBLE GATT service linked into the target | The generic ESP32-S3 candidate links the exact v0 service and three characteristic definitions under one-connection Secure Connections-only NimBLE peripheral/GATT-server configuration with authenticated/encrypted access and 16-byte minimum key size. Registration requires injected application authorization and coordinator authority; the application does not register/start NimBLE/controller state or advertise. Commands are denied before mutation until exact registered CCCD handles and per-connection indication subscription/disconnect ownership exist. Static admission passes 3/3 and 100/100; two pinned builds reproduce a 155,061-byte generic image and exact hashes. Evidence remains generic 2 MB, `BUILD-LINKED-NOT-RUN`, `NOT-FLASHED`, controller `NOT-STARTED`, advertising `NOT-IMPLEMENTED`, authorization `NOT-INJECTED`, and no device access. V1 remains 30% |
| OT-043 | done | Unwired Android 12+ BluetoothGatt facade | A concrete facade behind OT-041 filters the exact service, validates the complete profile, enables indication-only Stream, bounds scans/results/opaque tokens, supports API 31/32 and API 33+ GATT shapes, serializes callbacks on the main thread, and rechecks Scan/Connect permission on every active state- or data-bearing queued callback; disconnected cleanup remains best-effort. Six pure policy tests cover preflight/API choice, exact operation plan and token bounds, per-operation permission policy, queued scan admission, profile validation, and one-operation/terminal-close behavior; 17 runtime tests separately cover lifecycle and stale generations. Concrete platform calls are compile/lint/source-audited with contained `SecurityException`, not executed against Android hardware. The exact Android gate passes 50 tests, lint, and APK inspection. The manifest declares Scan with `neverForLocation` and Connect only, but the shipped activity remains fake-only with no Nearby Devices UX/facade wiring. No device/emulator/ADB/live BLE/install/signing/field evidence exists. V1 remains 30% |
| OT-044 | done | Response-safe one-connection GATT session lifecycle | A fixed-memory target-neutral owner binds exact registered Command/Stream/CCCD handles, one connection, ATT MTU, encrypted/authenticated/application-authorized evidence, indication subscription, coordinator session, and one outstanding delivery token. The sink reserves the complete response before coordinator mutation. Congestion and guarded callback re-entry reject without mutation/state advance; wrong/stale completion preserves the exact pending response; unsubscribe, security loss, submit failure, negative exact completion, or timeout contain and block until exact disconnect. Fifteen strict groups and 100/100 repeats pass; target boot self-check/static admission pass 100/100; two ESP-IDF v6.0.2 builds reproduce a 157,957-byte generic image. The lifecycle remains build-linked/self-checked only: controller not started, no advertising, authorization not injected, real NimBLE Command denied, no device access/flash. OT-034 remains partial; V1 remains 30% |
| OT-045 | done | Explicit Android Bluetooth mode and lifecycle UI wiring | The app now explicitly chooses Local test or Bluetooth-device mode with no fallback. Bluetooth mode owns Android 12+ Nearby Devices permission request/settings recovery, scan/select/connect/disconnect, typed public errors, and accepted actions through one lifecycle-safe controller/runtime/facade composition. Deferred observer-triggered stop/destroy, mode switches during scan/connect/Ready, late permission callbacks, revocation, off-owner calls, and exactly-once final cleanup fail closed. The combined gate passes 59 JVM tests and lint; the inspected 9,595,057-byte debug APK declares Scan with `neverForLocation`, Connect, and the generated same-app permission only. Production application authorization remains deny-all, so no live Ready state or phone/device/emulator/ADB/BLE/install/signing/field evidence exists. V1 remains 30% |
| OT-046 | done | One-phone device authorization authority | A fixed-memory target-neutral authority accepts only a trusted opaque 128-bit bond token that is stable across reboot and never an address, public/client identifier, client value, or key. Encrypted/authenticated claims bind exact boot, monotonic session, and private controller challenges; one owner/controller and explicit 30-second physical claim/revoke/replace/reset windows reject replay, rollback, invalid actions, second controllers, and stale sessions. Injected persistence owns atomic compare/commit/exact-readback plus a rollback-resistant generation floor; commit failure/uncertainty/conflict latches closed, while load not-ready/failure remains fail-closed and retryable before restore. Sixteen strict groups, 100/100 repeats, and the complete 118-executable host matrix pass. This is host-only/not build-linked; no bond store, physical input, persistence backend, target, BLE runtime, or device evidence exists. V1 remains 30% |
| OT-047 | done | Device-authoritative Android phone authorization UX | Bluetooth mode exposes authorize-this-phone and replace-lost-phone with a 30-second physical-device instruction. Only bounded device-issued Pending/Accepted/Denied/Replaced events matching exact token, purpose, and controller generation advance state; timeout or malformed/lost results remain unknown and require reconnect/resync. Tokens are rejected before buffering and never displayed/persisted. Lifecycle/permission/mode/callback/queue/timer/cleanup races fail closed. The gate passes 66 JVM tests and lint; the 9,611,441-byte APK has SHA-256 `3EB3986BD17F3DFC918936CF8978E44A36089E38D9CDCD877336BB9A16024C44`. Production claim client remains disabled; no bond, phone/device/emulator/ADB/BLE/install/signing/field evidence exists. V1 remains 30% |
| OT-048 | done | Fixed companion authorization wire and response tracker | Exact fixed `OTL0/v0` Start, `OTP0/v0` Pending, and `OTF0/v0` terminal payloads occupy 8, 24, and 28 bytes under dedicated `OTC0` kinds `0x03`, `0x84`, and `0x85`. Purpose is authorize or replace; Accepted/Replaced require matching purpose and no denial reason; Denied requires one closed reason including explicit unknown/unsupported. One nonzero device-issued opaque 128-bit correlation is privately boot-bound to exact provisional session/exchange/purpose and is never identity/address/secret/display/log/persistence data. A fixed C++ tracker requires encrypted/authenticated and explicitly negotiated provisional evidence, pending-before-terminal, one terminal, exact context, and owner-called exact close as sole connection-generation release; timeout remains a local unknown-authority state, not the wire Unknown denial. Fourteen strict groups, ten shared vectors, 100/100 repeats, and the complete 119-executable host matrix pass. Because three touched shared sources were already target-linked, two pinned ESP-IDF v6.0.2 rebuilds reproduced the 157,957-byte generic image and 158,080-byte BIN with exact kind recognition and normal-path rejection; the new authorization wire/tracker source was absent from the link map. At OT-048 acceptance `OTB0/v0` had no claim capability and GATT required application authorization before negotiation, so no provisional GATT or enabled workflow existed. V1 remained 30% |
| OT-049 | done | Android authorization-wire codec and tracker parity | Pure Kotlin mirrors the frozen three payloads, dedicated `OTC0` kinds, ten shared vectors, closed enums/coherence, and a bounded-state provisional response tracker including owner-called exact-close-only generation release. The production claim client remains disabled and no MainActivity, BLE runtime, or GATT transport wiring was added. The gate passes 77 JVM tests across eight suites (protocol 6 + 10 + 10; application 6 + 17 + 11 + 1 + 16) and lint; the 9,627,825-byte debug APK has SHA-256 `967FCD7A032ECED63789378F5B3C0F6AC86D06CE9CF3B6B16205E7C49B8093A3`. This is codec/tracker evidence only: no negotiated live capability, provisional session, device authority, emulator/phone/peripheral/ADB/install/signing/field evidence, or production authorization exists. V1 remains 30% |
| OT-050 | done | Restricted device provisional authorization lifecycle | A separate exact 20-byte `OTB0/v0.1` record adds claim capability `0x10` and a nonzero device provisional nonce; claim-capable payload capacity is exactly 28-128, normal MTU is 151, and claim admission requires MTU at least 51 plus exact registered Protocol Info/Command/Stream/CCCD handles, encrypted/authenticated-bond evidence, and current indication subscription. One fixed-memory owner reserves before Pending, confirms Pending before later physical/authority resolution, reserves before terminal authority mutation, and promotes only after exact Accepted/Replaced terminal confirmation. Failure/timing/security/subscription/disconnect/stale paths tombstone without invented denial or delivery. Twenty strict groups and 100/100 repeats, the complete 120-executable host matrix, target self-check 100/100, static admission 3/3, and two pinned builds pass. The 165,472-byte BIN has SHA-256 `E2ACF6672925D2FF298BD58E7C7BCBA564D46F1B7A6853D67865CE62F09D12B9`. At OT-050 acceptance this was `BUILD-LINKED-NOT-RUN`: live NimBLE remained v0.0/AUTHOR-denied, controller/service/advertising were not started, and nothing was flashed. V1 remains 30% |
| OT-051 | done | Android provisional authorization orchestration | Kotlin mirrors exact `OTB0/v0.1`, including 27-reject/28-accept payload coherence, and adds a runtime-backed claim client that can enforce encrypted/authenticated evidence, Protocol Info read, advertised MTU request, exact Stream indications, one Claim Start, correlated Pending/terminal, exact lease cleanup, and explicit post-promotion Snapshot Request when injected. Denial, timeout, permission loss, disconnect, malformed/stale input, and lifecycle release close without auto-retry; transport timeout stays local uncertainty. The gate passes 90 JVM tests across ten suites (protocol 6 + 10 + 10 + 3; application 7 + 9 + 17 + 11 + 1 + 16), lint with no issues, and debug assembly. The 9,644,209-byte APK has SHA-256 `28ED3014ACE420F8C531625211D26BD3FB9D522F1349BACA0878F94726534D8A`. At OT-051 acceptance `MainActivity` still injected the disabled client, target live provisional events remained unavailable, and there was no emulator/phone/device/ADB/install/signing/live-authorization evidence. V1 remains 30% |
| OT-052 | done | Real NimBLE provisional callback adapter | A fixed-memory serialized adapter binds exact registered Protocol Info/Command/Stream handles and an independently discovered Stream CCCD, current device-side encryption/authentication/bond/16-byte-key/MTU evidence, private trusted binding, monotonic connection generation, and immutable indication tuple. Protected v0.1 Protocol Info plus claim-only Command routing is available before application authorization; response capacity precedes mutation and exact Accepted/Replaced confirmation alone promotes. Same-handle stale completions fail through submission-era correlation plus pinned NimBLE teardown order. Ten strict groups at 100/100, target self-check 100/100, all 121 native entries, static 3/3, pinned-order admission, and two identical builds pass. The 170,432-byte BIN has SHA-256 `22CAE43F7AEA9D980602C41E1ACEB49CA1174315EE87598D15E6717A27A1E4D4`. The service is not registered, controller is not started, advertising is absent, and nothing was flashed. V1 remains 30% |
| OT-053 | done | Android protected-read production composition | Explicit Bluetooth mode now injects the real provisional claim composition with no fake fallback. Bonded status is only a prerequisite; successful protected exact v0.1 Protocol Info access is device-enforced security-path evidence. The path requests advertised MTU 151, enables exact Stream indications, performs Claim Start/Pending/terminal, and sends explicit Snapshot Request only after Accepted/Replaced. The gate passes 101 JVM tests across ten suites (protocol 29; application 8 + 15 + 17 + 11 + 1 + 20), lint clean, and debug assembly. The 9,644,209-byte APK has SHA-256 `BE385FEB8966210C4C09027388C3F560745F6A075B9CBB1ABF25DC0893C0033C`. Because the target service remains dormant, there is no phone/device pairing, authorization, Ready, install, signing, or live BLE evidence. V1 remains 30% |
| OT-054 | done | Protected authorization persistence prerequisite | Added the exact 32-byte `OAP0/v0` owner/tombstone record, protected-store compare/commit/readback contract, independent rollback-floor requirement, and private bond-reference plus separate device-secret PRF boundary. CRC is corruption-only; public address/ID/name, peer value, and raw key material remain outside the record and PRF input. Seventeen strict groups at 100/100, target reboot self-check 100/100, all 122 native entries, static 3/3, and two identical pinned builds pass. The 175,824-byte BIN has SHA-256 `D39430096B7BEDD0F69D9ECCDE2424EDCD635C0BEA904EB2E4FCA3EEED307080`. Target preflight deliberately denies because protected NVS initialization and key usability are unverified and private bond storage, separate PRF key, atomic protected record/floor backend, and independent rollback floor are unavailable. Production code does not open NVS or access eFuse/HMAC state; nothing was flashed. V1 remains 30% |
| OT-055 | done | Android connected-device foreground-service owner | One explicit visible Bluetooth action starts a non-exported `connectedDevice` foreground service that calls foreground disclosure before constructing and solely owning the real BLE facade/runtime/authorization/GATT/timer graph. It is `START_NOT_STICKY`, has no boot receiver/background auto-start, keeps Local test separate, and never retries a claim automatically. Activity stop/rotation/unbind release only bounded observation; explicit mode exit stops the service. The manifest adds only base/connected-device FGS and notifications to Scan/Connect plus AndroidX's generated same-app permission—no network/location/storage. The gate passes 124 JVM tests across twelve suites (protocol 29; application 95), clean lint, manifest inspection, and debug assembly. The 9,660,781-byte APK has SHA-256 `33174B72792E2AFC0D03AB52DFAC6613BAE48618BF268C3197D7E04105897722`. No Android OS/device/install or live BLE evidence exists, and the target remains dormant. V1 remains 30% |
| OT-056 | done | Bounded NimBLE runtime owner and boot path | A fixed-memory one-connection owner serializes real NimBLE callbacks through an eight-entry queue, starts only after deterministic self-checks and exact denied-storage preflight, registers the protected service, advertises only stable service metadata, cleans disconnect before bounded tokenized re-advertising, and contains queue/startup/host/reset/retry/stop failures. OT-054 storage/private-bond admission remains denied, so configured SC/MITM/bonding is not usable-bond proof; all claims/normal commands remain closed and every connection is immediately terminated. Thirteen strict groups at 100/100, target self-check 100/100, all 123 native entries, static 3/3, pinned teardown/stop ordering, full Test-Host, and two identical builds pass. The 433,104-byte BIN has SHA-256 `8A25508B50B29FE2A09CF3390AE53473BBA0BF04F60AE9A6366B930D516FCE2A`. This is `CODED-BUILD-LINKED-NOT-RUN`, `NOT-FLASHED`, no-device evidence. V1 remains 30% |
| OT-057 | done | Android Group / Location presentation | A renderer-neutral bounded model exposes group/location cards without promoting display data into identity or authority. Real Bluetooth mode reports coordinates unavailable because no accepted device coordinate feed exists; Local mode uses separately labeled deterministic synthetic cards. No phone GPS, map, tiles, network, location permission, or storage permission was added. The gate passes 134 JVM tests across thirteen suites (protocol 29; application 105), clean lint, manifest inspection, and debug assembly. The 9,677,165-byte APK has SHA-256 `697D73A6E48F1850A2756FB0886A8201C653804FB5A2B9628DD26790C8EC65B1`. No phone/device/install/live-location proof exists. V1 remains 30% |
| OT-058 | done | Bounded simulator native-session pump | Reworked the integrated WPF/native session pump to coalesce bounded notifications while retaining exact request order and the existing five-second timeout. Focused gates pass 33 Core, 23 Windows bridge, 15 private helper, ten native-protocol, and 13 integrated WPF groups; full Test-Host exits 0. This is local simulator evidence only, not physical LCD, Android, BLE-device, radio, or field evidence. V1 remains 30% |
| OT-059 | done | Exact Heltec memory and recovery-layout build profile | OT-DEV-001 evidence now bounds an ESP-IDF v6.0.2 build profile selecting 16 MiB flash, QIO/80 MHz, embedded 2 MiB quad PSRAM at 80 MHz, boot initialization/memory test, and explicit capability allocation. The exact layout contains otadata, a 5,177,344-byte factory slot, two 5,242,880-byte OTA slots, and a 1 MiB application-owned state row ending at 16 MiB. Four target-only admission groups and two reproducible pinned builds pass; the 437,552-byte BIN has SHA-256 `F0E81310C62CA0C17CA2531AF9B0D5BD5E6E115E1649F84C97514F72D51D6A3A`. No updater/OTA/storage/recovery authority, physical profile proof, selected unit, recovery rehearsal, write authorization, flash, port, or runtime evidence exists. OT-034 remains `partial`; V1 remains 30% |
| OT-060 | done | Android foreground screen retention and physical install | `MainActivity` uses the Activity-window `FLAG_KEEP_SCREEN_ON` immediately after superclass creation, with no wake lock, permission, brightness override, lock-screen behavior, or service coupling. The Android-only gate passes 135 JVM tests across thirteen suites, lint with `No issues found.`, manifest inspection, and debug assembly. The 9,677,165-byte APK has SHA-256 `9CE206EEEAE2B13FC5C1092CEF41C226607FD3A9905A5797D4EBE31F3DC7F01C`. One owner-authorized physical Android 16/API 36 handset installed and launched that exact APK. With the original USB stay-awake setting restored and a 30-second timeout, Trail remained awake, interactive, and at normal active brightness for an untouched 40-second interval; backgrounding released focus and reopening succeeded. The exact APK also reached visibly fake Local test mode without a Bluetooth prompt or real BLE service. This is one debug-install and foreground-display result—not BLE/LoRa, authorization, Ready, release, endurance, or field evidence. V1 remains 30% |
| OT-061 | done | First physical OpenTrail Heltec target and BLE advertisement | Only `OT-DEV-001` was selected; `OT-DEV-002` remained disconnected and untouched. Manual ESP32-S3 ROM entry/exit returned to the unchanged public MeshCore runtime before one owner-authorized full-chip erase and one write of the four frozen OpenTrail regions. Exact input rehash, the single write, and post-write `verify-flash` passed. One manual reset reached deterministic boot self-checks, NimBLE runtime, and at least two five-second USB heartbeat records without failure. The exact accepted 9,677,165-byte Android APK (SHA-256 `9CE206EEEAE2B13FC5C1092CEF41C226607FD3A9905A5797D4EBE31F3DC7F01C`) then reported exactly one compatible OpenTrail service candidate on one physical Android 13/API 33 phone; nothing was selected, connected, paired, or identified. This is experimental target/runtime/BLE-advertisement evidence only. Protected storage, GATT exchange, authorization, Ready, LoRa, GNSS, display, GPIO, recovery-after-loss, support, and field proof remain open. No additional write, recovery, or unit-2 authority remains. The historical phone-independent evidence calculation advances to 31%; current V1 Companion and V2 Integrated scores remain unmeasured. |
| OT-062 | done | Exact Trail artwork and physical Android visual acceptance | The owner-supplied 1774 x 887 Limited Underground Trail PNG is packaged byte-for-byte and rendered fitted above the existing accessible brand text on the real Compose entry surface, with no timed splash, artificial delay, navigation, permission, service, or Bluetooth change. The isolated gate passes 136 JVM tests across thirteen suites, clean warning-as-error lint, manifest inspection, and debug assembly. The 12,236,702-byte APK has SHA-256 `0E3A9C91E4AB68F0D6C45FB1D5A613CED7EE33154155AB2D0E76CE453F52918E`; its packaged 2,559,044-byte PNG matches source SHA-256 `A3024504BA261ADDAFD2A85F49F6BCE630D1E9AB994EEA348D5842A6D2AB7422`. That APK replaced the prior debug build in place on the physical Android 13/API 33 phone, cold-launched successfully, received explicit owner visual approval, and passed Home/background/reopen. No screenshot or device identifier was retained. This is Android entry-artwork evidence only—not system splash, clean install, broad accessibility, secure GATT/Ready, Heltec OLED, touchscreen, release, endurance, or field proof. V1 remains 31%; current V1 Companion and V2 Integrated scores remain unmeasured. |
| OT-063 | done | Read-only protected-storage target admission probe | A typed target-linked probe observes only coarse configuration, named NVS-partition presence, selected HMAC_UP purpose, read protection, and a private operational self-test. It contains no NVS initialization/open/write, key generation, eFuse programming, pairing, bonding, identifier output, or GATT-admission change. Six strict host groups, deterministic boot self-check 100/100, five target-only static groups, and two reproducible ESP-IDF v6.0.2 builds pass. The 440,240-byte BIN has SHA-256 `0D064045D44D7F4D1120D164912CEAE9E1103ECE159E226B1CEEE2B11489B650`. At OT-063 acceptance, current configuration short-circuited at `nvs_encryption_not_configured` before target reads; that build was not flashed and OT-DEV-001 still ran OT-061. Protected NVS initialization, private bond storage, distinct binding-PRF key, atomic record/floor storage, independent rollback floor, secure GATT, authorization, and Ready remained open. Historical V1 remained 31%; current V1 Companion and V2 Integrated remained unmeasured. |
| OT-064 | done | Physical Trail OLED startup and BLE status | A target-local display owner, fail-contained SSD1315-compatible Heltec V4 adapter, and deterministic 128 x 64 one-bit Limited Underground Trail bitmap pass three focused host groups, seven final target-only admission groups, and two byte-identical pinned ESP-IDF v6.0.2 builds. Only `OT-DEV-001` received one owner-authorized factory-app write at `0x010000`; no full erase or other partition write occurred. The 470,928-byte app has SHA-256 `A7D8E672CF9169F1D1D4E86EEFF80399C47A145E7D64904C207DD5F1B23F359B`; exact read-only verification passed before reset. The owner observed the recognizable Trail logo followed by `BLE ADVERTISING`; boot self-check PASS, four heartbeats in 16 seconds, and no failure/panic marker were observed. Android found exactly one compatible service candidate without selection, connection, pairing, or identifier retention. This proves only the selected unit's startup/status OLED path—not exact controller silicon/revision, protected storage, GATT/Ready, LoRa, GNSS, interactive UI/input, support, or field behavior. Both write authorizations are consumed; unit 2 remains untouched. Heltec target completion advances to 25%; exact V1 is 31.75% and displays as 32%. |
| OT-065 | done | Reversible protected authorization-store foundation | A concrete backend-neutral coordinator composes exact two-slot authorization media with a separately injected monotonic generation floor. It accepts only one record at the fresh floor, prepares and exactly verifies the inactive slot before compare-advancing trust, and rereads both authorities before publication. Twelve strict host groups cover restore, rotation, reboot, stale/corrupt/conflicting/prepared-ahead state, write and floor ambiguity, and final mismatch. Decision 0010 and inactive `OTPS0/v0` Heltec candidate plans select no keys or rollback provider, change no active partition/sdkconfig/runtime/device state, and grant no physical authority. Protected target encryption, private bond storage, distinct PRF key, physical rollback floor, migration, recovery, GATT authorization, and Ready remain open; V1 remains 32%. |
| OT-066 | done | Trusted phone GATT authority composition | One target-neutral production composition pins an opaque private bond reference to an exact connection generation, derives a private owner token through the existing PRF boundary, mints private boot/session/controller values, and maps the durable one-phone authority into the existing GATT claim seam. Eight strict groups prove exact caching, changed-reference/stale-generation denial, re-pair separation, physical-gated first claim, reconnect without owner rewrite, wrong-phone denial, explicit replacement, lease release, reentry containment, and persistence uncertainty. The Heltec target remains denied and unchanged; real protected bond storage, key provisioning, physical control, pairing, GATT exchange, authorization, and Ready remain open. V1 remains 32%. |
| OT-067 | done | Protected authorization KV slot media | One target-neutral adapter binds the two exact `OAP0/v0` record slots to `ot_auth` / `ot_owner` / `oap_slot_a|b`, accepts only exact 32-byte values, treats missing keys as absent, requires a durable commit, and contains invalid slots, malformed values, backend failures, post-write ambiguity, and reentry. Ten strict groups plus the unchanged 12-group protected-store regression prove rotation, reboot restore, rollback refusal, prepared-ahead refusal, and ambiguous-commit containment. The backend remains injected; no active target partition/config/runtime/device, NVS initialization, key, rollback authority, bond, pairing, GATT authorization, or Ready state changes. V1 remains 32%. |
| OT-068 | done | Inactive Heltec authorization NVS backend | One target-local ESP-IDF NVS adapter implements the exact OT-067 get/set/commit boundary for an already-opened handle. Eight strict groups prove unopened-handle refusal, exact binding/size checks, missing and exact reads, closed native-error mapping, exact writes, explicit commit, and uncertain post-mutation failure; the ten-group media regression and nine target-admission groups pass. Two pinned builds reproduce a 470,928-byte BIN with SHA-256 `9F5AFB320A015E3BFFD866A9EE31F76198739521FA7519845ACDA12B9B52BAE5`. The adapter is build-compiled but not runtime-injected; the active layout has no `ot_auth`, NVS encryption remains disabled, denied authorities and installed firmware remain unchanged, and no key/floor/bond/pairing/GATT/Ready/device authority is added. V1 remains 32%. |
| OT-069 | done | Inactive Heltec authorization NVS context | One target-local owner consumes only an exact existing encrypted-NVS configuration and exposes OT-068 only after the exact candidate partition, secure initialization, and namespace open succeed. Ten strict lifecycle groups, a disabled-configuration zero-I/O check, nine target-admission groups, and two reproducible pinned builds pass. The 470,928-byte BIN has SHA-256 `9D4EBCD8BB68183798BF47267252A1B2A94A114FACD16E8CF975AEBE43314EEF`. The owner is build-compiled but not runtime-injected; active layout/config/runtime/device state and denied authorities remain unchanged. No key/floor/bond/pairing/GATT/Ready/device authority is added. V1 remains 32%. |
| OT-070 | done | Protected-storage partition transition admission | A pure target-neutral guard admits only an exact `OTHP0/v0` to `OTPS0/v0` partition-table-only proposal with fresh installed-layout readback, verified blank source or separately verified migration, exact recovery evidence, no runtime/key/eFuse/other-flash request, and one exact operation-scoped authority. All evidence generations and artifact digests are bound to the same nonzero operation/evidence-set identity. Thirteen strict C++ groups pass across 100 repeats; five exact-manifest groups, the existing 9/9 Heltec admission, and the complete host gate pass. The design-only manifest remains denied, retains zero execution authority, and changes no active target/build/runtime/device state. No transition, key, floor, bond, pairing, GATT, or Ready evidence is added. V1 remains 32%. |
| OT-071 | done | Read-only protected-storage transition evidence tooling | A streaming offline verifier accepts only the exact 3,072-byte installed partition table and complete 1 MiB all-`0xFF` source region, bound to nonzero operation/evidence-set identities, and emits only a fixed sanitized outcome. A separate Heltec read plan remains denied, selects no unit or port, contains no command or executable hardware reader, and grants no authority. Eight verifier groups, six transition-manifest groups, and the existing 9/9 target admission pass. No hardware read or active target/build/runtime/device change occurred; source proof remains absent and cannot itself authorize the transition. V1 remains 32%. |
| OT-002 | done | Initial architecture documentation | Layers, boundaries, failure modes, roles, and architecture gates documented |
| OT-003 | partial | Hardware abstraction contracts | Radio, GPS, logging, persistent-storage, secure-randomness, checked monotonic-clock, power-state, and local display/input contracts have deterministic fakes/tests. Portable-client structural review is host-tested under OT-003E, and OT-003F proves the first checked-time outbound service composition. Concrete ESP-IDF/board bindings, full boot/inbound/UI/persistence runtime, target build, and on-device evidence remain |
| OT-003A | partial | Hardware and regulatory inventory | Before OT-061, both companions were runtime-confirmed as Heltec V4 OLED with MeshCore USB Companion `v1.16.0-07a3ca9`; `OT-DEV-001` now runs the experimental OpenTrail target while untouched `OT-DEV-002` retains MeshCore; owner purchase evidence identifies the Meshnology V4 GPS two-unit bundle (ASIN `B0FS1WQWKF`). They are assembled bench clients, not the board-level first complete-client build. A redacted USB check proves both firmware builds detected GNSS, accepted/verified enablement, and emit a GPS telemetry field; privacy-safe current fix/satellite evidence remains absent. The repeater is runtime-confirmed as Seeed SenseCap Solar, and owner purchase evidence identifies P1-Pro (ASIN `B0FMDHBWX8`; current Seeed MeshCore SKU `100023690`); this packaged-repeater candidate progressed from active/no-fix to a live fix, with later checks at 4, 7, and 8 satellites. The arrived owner-reported Wio Tracker L1 Pro is only `experimented`: public USB model `Seeed Wio Tracker L1`, owner-flashed USB Companion `v1.17.0-727fc05`, four stable read-only runtime/profile cycles, GNSS detected/inactive, and a non-transmitting Heltec configuration comparison. Its exact label/SKU/revision and pre-write state remain unverified. The dated fail-closed US field gate still requires exact labels/FCC grants, firmware mode, antennas/gain/cable loss, and configuration review. `OT-DEV-001` has ROM-level MCU/memory evidence; `OT-DEV-002` does not. Exact received revisions, RF variants, installed antennas, physical GNSS modules/wiring, pinouts/power, fix accuracy/loss behavior, and grant coverage remain unresolved |
| OT-003B | done | Checked monotonic-clock boundary | A production-facing guard accepts one boot-local 64-bit millisecond source, keeps UTC separate, permits equal ticks and temporary not-ready recovery, and latches rollback or source failure without consuming later samples. Fixed saturating status counters and a bounded 16-sample FIFO fake cover zero/equal/forward/full-range time, continuity, rollback, source failure, new-boot composition, and capacity/order across eight groups plus 100 repeats. The complete 30-executable matrix passes locally. ESP-IDF timer/task binding, native-wrap handling, deep-sleep/brownout policy, resolution/latency/drift, long-run continuity, and physical failure injection remain target gates |
| OT-003C | done | Fail-visible power-state boundary | One atomic adapter observation keeps source readiness, external power, battery presence, orthogonal charge state, optional normalized percentage/voltage, and monotonic age separate. Composition must inject distinct low/critical percentage and nonzero freshness policy; the common component never estimates percentage from voltage. Eleven groups plus 100 repeats cover exact bands, charging, external-only, missing, fault, invalid, future/stale, and bounded FIFO behavior in the complete 31-executable local matrix. Board adapter, charger/shutdown control, hardware thresholds, endurance, rendered UX, and physical power evidence remain target gates |
| OT-003D | done | Revision-bound local display/input boundary | A fixed semantic frame carries screen role, attention/notice, radio/position/power indicators, optional peer count, unread count, and up to four unique action slots without pixels, coordinates, GPIOs, free text, or identity-bearing content. Only an atomically presented increasing revision becomes active; normalized input must match it exactly. Canonical critical confirmation requires confirm/cancel order plus hold; OT-028H adds a separate canonical archive confirmation, OT-028I adds snapshot-derived archive controls, and OT-029A adds two exact quick-status pages. Thirteen base groups plus the archive consent/workflow and quick-status menu suites cover capabilities, frame validation, display failure/retry, revisions, stale/disabled/invalid input, confirmation/system-fault rules, and bounded fakes. OT-019A adds coarse recovery notices; OT-012B adds position privacy actions; OT-012F owns their UI sequence. Target renderers/input adapters, localization/accessibility/readability, resource/power performance, distracted-driving policy, and physical evidence remain |
| OT-003E | done | Portable-client composition preflight and whole-contract review | Ten non-owning target endpoints cover radio, GPS, log sink, 64-byte multi-domain storage, separate 704-byte replay storage, entropy, monotonic clock, power, display, and input. Pure shared power/display validators plus explicit MTU/action/hold policy aggregate every structural issue while querying only radio MTU and performing no mutable adapter I/O. The whole-contract audit found and corrected the separate replay-storage obligation. Eight groups pass in the complete 33-executable matrix. Concrete adapters, ESP-IDF target/task/build, protected storage, and physical evidence remain OT-003 gates |
| OT-003F | done | Checked-time outbound service coordinator | One fixed-memory cooperative cycle reads `CheckedMonotonicClock` once, skips GPS while sharing is stopped, and orders active-sharing location, position scheduling, priority handoff, delivery, and opaque-radio service with the same successful time. Not-ready invokes no downstream work; rollback/source failure stops sharing and latches closed; position/handoff failure does not block existing messaging. Ten groups plus 100 repeats include same-cycle exact packet delivery and pressure recovery in the 58-executable matrix. OT-012D adds fail-visible position presentation, OT-012E owns checked action-time commands, and OT-012F owns cooperative UI sequencing. ESP-IDF tasks/synchronization, inbound/boot/persistence/power/log composition, concrete adapters, authentication, and physical behavior remain |
| OT-020 | partial | Wio Tracker L1 Pro compatibility | The arrived owner-reported Pro unit is `experimented` for USB/runtime/configuration evidence only. Windows exposed public model `Seeed Wio Tracker L1`, USB family `2886:1667`; four fixed read-only cycles returned USB Companion `v1.17.0-727fc05`, a stable 910.525 MHz/BW 62.5 kHz/SF7/CR5/22 dBm profile, increasing uptime, and zero errors/traffic. GNSS was detected but inactive with no GPS telemetry. A non-transmitting Heltec comparison matched private channel/default-scope state only in memory, confirmed distinct identities without emitting them, and found clocks within one second. Shipping/pre-write state was not preserved because the owner had already flashed/configured it. C# and Python loaders now recognize the family; the 59-group C# suite, three built-in live refreshes, and three exact-roster source-free external UI Automation cycles pass with one Heltec, one SenseCAP, and one Wio runtime-identified and zero ready. Exact label/SKU/revision, over-air interoperability, GNSS fix/loss, antenna/RF/regulatory, power/endurance, BLE, DFU/recovery, and clean-machine evidence remain. This is not compatible, validated, or supported hardware evidence. OT-020A and OT-020B complete only host-side lifecycle/recovery and Windows-authority prerequisites; neither activates GNSS nor closes any live GNSS, compatibility, power, regulatory, or firmware-recovery gate |
| OT-020A | done | Host-only Wio GNSS lifecycle/recovery evidence contract | A deterministic coordinator accepts only one exactly bound Wio target with the required firmware/role, location-safe policy, GPS read back off, zero pending work, and no existing recovery journal. It journals before enable, judges acknowledgement by readback, exposes telemetry presence only, restores and verifies GPS off, revalidates the target binding, uses expected-record comparisons for journal replacement/deletion, and requires settled counters with the same session-scoped continuity token, zero pending work, and no transmission. Existing journals force a recovery-only path that can never enable GPS and retains authority on uncertainty. All 15 focused synthetic injected-adapter scenario groups pass; the complete `tools/Test-Host.ps1` gate exits 0 with publication safety, all host matrices, and existing read-only loader acceptance. No live adapter/device I/O occurred. OT-020 remains `partial`, the unit remains `experimented`, shipping/pre-write state remains unavailable, and the entire live GNSS phase remains open |
| OT-020B | done | Host-only Wio GNSS Windows authority boundary | One Windows-only authority supplies OT-020A with a DPAPI CurrentUser-protected 32-byte HMAC key, domain-separated target and session-scoped continuity tokens, a nonblocking per-target named mutex, and two reduced counter observations across a five-second quiet interval. The continuity token is not a device boot identity; transport generation and plausible uptime remain independent adapter obligations. Busy, abandoned, raced, release/close-uncertain, private-error, pending, traffic, generation, uptime, and timing cases fail closed. The exact prior-window maximum is 180 seconds inclusive; 180.001 seconds fails. All 15 authority groups and the separate 15 OT-020A lifecycle groups pass; the hardened full host gate exits 0 with publication safety. OT-020B has no discovery, serial, PnP, BLE, MeshCore, live-device, or mutable device-command surface. The aggregate gate's pre-existing read-only three-device loader precheck is separate and is not OT-020B hardware evidence. OT-020 remains `partial`, the Wio remains `experimented`, and the entire live GNSS phase remains open |
| OT-015 | done | Diagnostics/logging foundation | Fixed-capacity ERROR/WARN/INFO/DEBUG/TRACE logger demonstrates compile/runtime filtering, monotonic timestamps, component tags, redaction, truncation/sanitization, test sink, and counted backpressure in seven host scenarios. A production-facing RAM ring retains the newest 32 canonical records, assigns boot-local sequences, snapshots oldest-first without partial output, preserves order across clear, and counts rollover/rejection across eight groups plus 100 repeats. The `OTRD0/v0` recovery adapter adds one versioned 32-bit, generation-free redacted event with exact fixed text, coherence validation, severity mapping, filtering, sink-failure evidence, and real ring capture across eight groups plus 100 repeats. OT-019B and OT-012I add strict offline host decoding; one unified CLI dispatches their exact canonical prefixes and passes four smoke paths without log retrieval or execution authority. Target task binding/concurrency, persistent audit/export, packaging, and physical capture remain |

## Transport and protocol

| ID | Status | Task | Acceptance evidence |
| --- | --- | --- | --- |
| OT-004 | done | LoRa transport abstraction | Fixed-capacity opaque-frame contract covers MTU, errors, metadata validity, cooperative state, queues/counters, and a deterministic two-node fake without protocol coupling; eight host scenarios pass |
| OT-005 | partial | Node identity and group model | Identity/name/alias/membership boundaries and threat model are documented; eight host lifecycle/collision scenarios pass. A dated candidate review fixes libsodium-first target benchmarking, mbedTLS/PSA and Monocypher comparisons, Noise-C reference-only status, separate Ed25519/X25519 purposes, and conditional Noise XK invitation direction without making a selection. A rollback-safe counter lease, key/value target composition, exact fail-closed 96-bit nonce packing, and canonical purpose-separated traffic-key context are host-tested under OT-005A/G/D/E. Exact target benchmark and KDF, entropy adapter, handshake/alias implementation, administrator recovery, protected rollback-resistant persistence, packet-v1 AEAD/AAD, and physical join/revoke/reset evidence remain |
| OT-005A | done | Rollback-safe outbound counter leasing | A fixed 64-byte `OTCN` record and separate two-slot persistence domain commit a nonoverlapping 64-bit range before any counter is returned. The fixed-memory allocator reserves before first/renewed use, wastes unused ranges on restart, binds state to a 128-bit domain plus group epoch, omits ordinary reset, and fails closed on mismatch, corruption, conflict, future version, exhaustion, I/O, and five mutation boundaries. Ten host groups plus the OT-005G key/value composition pass locally. Domain derivation, protected target storage/rollback, AEAD composition, and physical power-cut/endurance remain OT-005/target gates |
| OT-005G | done | Key/value-backed outbound counter composition | The real `OTCN` lease store now runs through the multi-domain key/value adapter using only exact `ot_state` / `ot_counter` / `slot_a|b` bindings. Fresh adapters prove restart rotation and non-overlap; failed-unapplied prepared commits permit retry only after confirmed absence, while applied-then-failed marker commits return no range and force the next boot to skip it. Wrong-sized values fail before mutation. Five groups, 100/100 focused repeats, and the complete 95-executable host matrix pass. ESP-IDF binding, protected integrity/rollback, rekey/reset authority, locking, physical interruption/endurance, packet-v1, and on-device evidence remain |
| OT-005D | done | Lease-bound AEAD nonce composition | A fixed 12-byte result requires matching nonzero 128-bit lease/key domains and a nonzero rollback-safe counter, then packs the crypto-adapter-supplied 32-bit prefix plus the 64-bit counter in network byte order. Mismatch and invalid input return typed failure with zero output across seven host groups. This performs no KDF, key derivation, counter allocation, AEAD, packet-v1, or library selection; exact cryptographic domain/key/prefix derivation and target evidence remain OT-005 gates |
| OT-005E | done | Canonical traffic-key derivation context | A fixed 52-byte `OTKD/v1` public context binds a nonzero group ID, group epoch, full 32-byte authoritative sender fingerprint, and separate group-AEAD-key/nonce-prefix/counter-domain purposes in network byte order. Eight host groups cover exact bytes, purpose separation, context changes, and zero/unknown refusal. It performs no KDF or secret handling; exact target library, salt/input key material, output lengths, vectors, wiping, and epoch-secret rotation remain OT-005 gates |
| OT-005F | done | Protected-header requirements reconciliation | Review against the accepted immutable one-repeater policy found that the former 36-byte budget could not carry both its required authenticated 64-bit destination alias and fragment metadata. The corrected 44-byte profile explicitly charges destination, has no mutable TTL, raises base overhead to 60 bytes, and leaves 39 signed-group plaintext bytes at the 163-byte example MTU. Ten updated budget groups pass. Final offsets/types/flags, fragment/reassembly policy, destination privacy, signature coverage, AEAD, and packet-v1 remain open |
| OT-005B | done | Reproducible cryptographic benchmark evidence boundary | Strict `OTCB0/v0` plans require the fixed three-candidate order, exact ready target/toolchain/sdkconfig/dependency/radio fields, 100 cold and warm repetitions, and eight canonical security/lifecycle gates. Results bind to a canonical plan hash and record eight operation timing sets, build warnings, resources, watchdogs, radio cost, hashes, raw-evidence retention, and every gate. Eight groups reject false readiness, overwrite, mismatch, private text, incomplete measurements, and measured failures; they pass locally and on public `main`. The public target plan remains `draft_blocked`; no on-device benchmark or library selection exists |
| OT-005C | done | Fail-closed secure-randomness source boundary | An algorithm-neutral interface exposes typed not-ready/ready/failed entropy state, bounds requests to 1-64 bytes, and requires full output or no buffer change. A deterministic 512-byte scripted fake is isolated under test support and covers sequential single-use bytes, invalid/oversized requests, exhaustion, injected failure/retry, readiness transitions, persistent failure, and the maximum boundary across eight groups plus 100 repeats. The complete 29-executable host matrix passes locally. No ESP-IDF entropy/DRBG adapter, target readiness proof, production key generation, physical cold-start/brownout/RF-concurrency evidence, or cryptographic selection is claimed |
| OT-006 | done | Experimental packet envelope v0 | A 22-byte v0 envelope documents MTU-derived budget, version/type/flags/length/ephemeral IDs, CRC-16, rejection behavior, a standard CRC vector, and six passing codec scenario groups; it is explicitly non-production and unauthenticated |
| OT-006A | done | Protected packet candidate budget | A fixed-memory model charges authenticated header, AEAD tag, source authentication, optional forwarding wrapper, and theoretical LoRa airtime to every fragment. After destination-alias reconciliation, the base 44+16 profile leaves 103 plaintext bytes at a 163-byte example MTU; adding a 64-byte Ed25519 signed-group candidate leaves 39 and makes a 16-byte position 140 bytes/461,312 us at the bench PHY. Ten groups cover base/signed/wrapper profiles, the 64-byte alert case, 163/255 MTUs, single/three/empty payloads, exact boundary, limits, and invalid PHY locally. This is sizing evidence, not packet-v1 offsets, crypto, fragmentation policy, target MTU, or physical airtime |
| OT-006B | done | Critical-alert protected-radio feasibility | The 64-byte `OGA0` and `OGK0` semantic frames each require two 39-byte signed-group candidate fragments at the 163-byte example MTU: 312 transmitted bytes and 1,025,024 us theoretical source airtime before retries/repeater copies. Alert plus ACK is four source fragments; one repeater doubles aggregate transmission airtime. The documented gate keeps fragmentation blocked pending source-signature scope, nonce/replay/reassembly/ACK/retry policy, bounded resource behavior, target measurement, and regulatory review |
| OT-006C | done | Verified protected-fragment reassembly policy | A fixed-memory host boundary accepts only future crypto-adapter-produced verified fragments, binds group/epoch/sender/message/count, supports four concurrent sessions and up to 16x103 plaintext bytes, reorders canonically, treats exact duplicates idempotently, clears byte/count conflicts, rejects full capacity without eviction, and expires at the exact timeout. Ten groups include the 39+25-byte alert shape. Cryptographic verification, unforgeable target composition, final packet/signature/replay/retry policy, ESP32 resources, power loss, and field behavior remain open |
| OT-007A | done | Two-node transport characterization | USB preflight and raw-RX authentication checks passed. A temporary private-channel sample delivered 5/5 numbered messages each direction with 0 loss, 0 duplicates, 233.3-247.2 ms latency, 11.25-12.25 dB SNR, exact +5 TX/+5 RX counters per node, zero errors, and empty queues. The temporary channel was erased and verified empty. RSSI remained invalid and airtime remained whole-second resolution; these are recorded measurement limitations, not omitted evidence. |
| OT-007 | done | Two-node message proof of concept | Two host integration scenarios pass; C++-encoded v0 frames then delivered 3/3 each direction through a temporary private MeshCore adapter with no loss/duplicates/errors, exact counter deltas, verified decode/CRC, and verified channel cleanup |
| OT-008 | done | Acknowledgement and duplicate handling | Six message-class policies, fixed-capacity delivery state, confirmation/retry/expiry/error behavior, reboot-restored duplicate window, and lost-ACK integration are host-tested. Authenticated ACK wire encoding and hardware timing remain later protocol/field gates |
| OT-008A | done | Canonical duplicate-checkpoint serialization | A fixed 672-byte `OTD0` record serializes up to 32 replay keys and remaining lifetimes with explicit little-endian fields, canonical zero padding, strict version/capacity/key/lifetime checks, duplicate-key rejection, CRC-32, and atomic output. Seven codec groups, the full 23-executable host matrix, and 100 codec/window repeats pass. Atomic durable storage, wear/privacy lifecycle, authentication, secure rollback, and physical power-loss evidence remain later gates |
| OT-008B | done | Two-slot duplicate-checkpoint host store | A fixed 704-byte `ODS0` envelope composes `OTD0` with automatic 64-bit generations, two-slot rotation/repair, readback byte/decode verification, visible single-slot recovery, and fail-closed I/O, invalid-only, conflict, and exhaustion behavior. Nine store groups, the full 24-executable host matrix, and 100 store/codec/window repeats pass. ESP32/NVS binding, physical atomicity/endurance, privacy lifecycle, authenticated integrity, trusted rollback protection, and physical power-loss evidence remain later gates |
| OT-008C | done | Context-bound duplicate checkpoint store | Fixed-size `ODS0/v1` reuses the original 704-byte slots while embedding a nonzero 64-bit group-context ID and 32-bit group epoch. Every inner key must match the outer epoch; zero, wrong-context/epoch, and legacy unbound v0 media fail closed without restore or overwrite. The coordinator also validates its store binding and returns typed bound-media/legacy service outcomes. Ten store groups, nine coordinator groups, the full 28-executable matrix, and 100 focused repeats of both suites pass locally; the exact published matrix passes in run `31374678550`. Protected target namespace, authenticated integrity, anti-rollback, authorized reset/migration, physical power-cut, and endurance remain |
| OT-008D | done | NVS-ready duplicate checkpoint key/value adapter | A backend-neutral adapter binds the two exact 704-byte context-bound `ODS0/v1` slots to `ot_state` / `ot_replay` / `ods_dup_a|b`, rejects invalid arguments and wrong-sized values, requires explicit backend commit after write/erase, treats missing-key erase as idempotent, and keeps failed or applied-then-failed commits visible for restart reconciliation. Nine groups, 100/100 focused repeats, and the complete 86-executable host matrix pass. Protected ESP-IDF namespace access, target locking, authenticated integrity, rollback protection, authorized reset/migration, physical interruption, endurance, and on-device evidence remain |
| OT-009 | partial | Controlled repeater proof of concept | Eight three-node host scenarios prove bounded role/permission forwarding, exact TTL decrement, origin/reflection duplicate suppression, group isolation, queue/rate congestion limits, and broadcast/unicast behavior. A SenseCAP repeater retransmitted exactly two private flood samples. Explicit one-hop direct routes then delivered in both directions; disabling repeat caused the same route to fail with +1 direct RX/+0 direct TX and no destination message, proving the logical repeater path. A non-secret temporary-channel lease has four host recovery groups plus real stopped-session cleanup. A 300-minute alternating close-bench run delivered 300/300 with zero loss/duplicates/errors, 229.8-312.1 ms latency, exact +300 repeater flood RX/TX, repeat preserved, and verified channel/journal cleanup. A post-soak packet-v0 regression then delivered 6/6 with valid codec/CRC and verified cleanup. Authenticated routing fields, direct SX1262/OpenTrail binding, and field evidence remain |
| OT-009B | done | Immutable zero/one-repeater architecture and host-policy boundary | Decision 0004 limits the first release to direct clients plus at most one authorized repeater. A new algorithm-neutral policy requires source auth/authorization, verified context/epoch, immutable permission, and monotonic process time before replay observation, then applies duplicate, exact-byte queue, rate, and exact-expiry controls without rewriting a TTL. Nine groups and the full 27-executable matrix pass locally and on public `main`. Named-source broadcast still needs cryptography beyond common group AEAD access; protected replay persistence, target binding, reboot/power-loss, and field behavior remain |
| OT-009C | done | Reboot-safe single-repeater replay coordination | The host coordinator requires exact protected group/epoch namespace evidence, initializes an authorized empty store, restores and repairs a known-degraded two-slot `ODS0` checkpoint before operation, and persists every queued/congestion replay observation before allowing queue release. Failed/uncertain saves and unreadable media disable forwarding. Nine groups and the full 28-executable matrix pass locally and on public `main` in run `31372816356`. The ordering prevents reboot amplification but can lose a volatile queued frame after save and before transmit. Protected target binding, authenticated integrity/rollback, physical power-cut/wear, durable-outbox policy, and field evidence remain |
| OT-010 | partial | Priority/emergency messaging | Nine host scenarios plus delivery integration prove class-derived priority, reserved urgent capacity, strict preemption, rate windows, expiry, FIFO, and explicit failure/preemption events. OT-010A adds loss-aware priority-to-delivery transfer without changing the unauthenticated wire boundary. Authenticated wire priority, measured mixed traffic, physical evidence, and rendered failure UX remain |
| OT-010A | done | Loss-aware priority-to-delivery handoff | A fixed-memory single-owner coordinator peeks the strict-priority/FIFO head, asks `DeliveryController` to accept the exact frame, and commits the source entry only after acceptance. Delivery capacity, duplicate ID, and MTU rejection retain the source; remaining queue lifetime caps delivery expiry; impossible commit mismatch latches closed. Ten groups plus 100 repeats cover retention, ordering, exact expiry, unsafe time spans, and the complete scheduler-produced position-packet path through a fake-radio peer in the 58-executable matrix. OT-003F services it in one checked-time outbound cycle. Authentication, concrete target concurrency, direct-radio binding, physical delivery, and rendered status remain |
| OT-021 | done | Baseline group-load estimator | A fixed-memory host model combines exact LoRa airtime with member count, periodic position/status traffic, alert bursts, bounded source attempts, and explicit repeater copies. Six groups cover the four-client standalone, four-plus-repeater, and eight-plus-repeater phases, retry scaling, zero traffic, and fail-closed validation. The current one-hour profile reports 1.2727%, 2.5455%, and 5.0911% scheduled demand; collision, delivery, range, protocol-overhead, power, and regulatory evidence remain physical gates |
| OT-022 | done | Privacy-safe field-test evidence boundary | `OTFL0` converts ignored raw MeshCore soak output into atomic aggregate public JSON under neutral role labels, rejects transport ports/serials/addresses/keys/secrets/PINs/channel names/coordinates plus incoherent counts, and refuses implicit overwrite. Four host groups pass. A new two-client-plus-repeater burst delivered 30/30 at a two-second interval with zero loss/duplicates/new errors, exact +30 repeater flood RX/TX, repeat preserved, 2/2 cleanup, and a validated public summary. This is close-bench logging evidence, not four/eight-client field capacity |
| OT-023 | partial | Four-person standalone pilot v0 | `OTFP0/v0` fixes four identical clients, zero infrastructure dependency during the session, three one-hour environment classes, 300 message origins, 900 peer-delivery opportunities, privacy-safe evidence, and provisional fail gates. Two host groups reject incorrect math and false readiness. The plan remains `draft_blocked` until one exact client model/firmware is frozen across four units with battery, enclosure, GNSS, display, local input, and USB recovery; no live four-person session has run |
| OT-024 | partial | Deterministic four-person pilot verdict | Strict `OTPR0/v0` aggregate results produce pass/fail/ineligible/invalid verdicts against one ready `OTFP0/v0` plan. Fail-closed template generation refuses blocked plans, unlisted scenarios, and overwrite while preloading frozen public configuration and leaving evidence claims false; all nine groups pass publicly. Live four-person evidence remains |

## Location, groups, and persistence

| ID | Status | Task | Acceptance evidence |
| --- | --- | --- | --- |
| OT-011 | done | GPS abstraction | Fixed-unit provider contract, optional-field validity, no-fix, validation, exact stale boundary, no-UTC boot, refresh recovery, and monotonic-time rejection pass nine deterministic host scenarios |
| OT-012 | done | Position broadcast format | Fixed 16-byte current/stale/unknown payload, conservative age/accuracy, canonical rejection, 38-byte packet integration, and theoretical airtime/cadence budget pass eight codec, one transport-integration, and four airtime scenario groups. OT-012A supplies host scheduling mechanics, OT-012B semantic local privacy control, OT-012C experimental packet-v0/background-priority admission, OT-010A loss-aware delivery handoff, OT-003F one checked-time outbound fake-radio cycle, OT-012D fail-visible runtime-aware presentation, OT-012E checked action-time command authority, OT-012F cooperative UI/revision ownership, OT-012G external semantic refresh before input, OT-012H privacy-safe UI diagnostics, and OT-012I strict offline operator decoding. Authentication, physical/rendered privacy UX, authenticated packet/priority composition, selected cadence, direct-radio hardware airtime, contention, and regulatory evidence remain later gates |
| OT-012A | done | Start/stop position broadcast scheduler | A fixed-memory scheduler starts only by explicit command, stops immediately, submits only current validated 16-byte positions with the actual attempt time, coalesces delayed cadence from actual accepted work, and uses a separate retry delay for missing fixes or typed sink pressure/failure. Repeated start while active is idempotent; monotonic rollback and deadline exhaustion latch off. Ten groups plus 100 repeats cover policy, exact boundaries, suppression, pressure, failures, stop/restart, time faults, malformed input, and bounded fakes in the complete 58-executable matrix. OT-003F supplies one checked service timestamp and avoids GPS reads while stopped. Final cadence, authenticated packet/priority composition, direct radio/GPS bindings, physical behavior, and regulatory evidence remain |
| OT-012B | done | Local position-sharing privacy control | Fixed stopped/active/waiting/deferred/failed notices and start/stop actions pass through the checked revision-bound UI. Start only arms scheduling without sink access; stop is immediate; stale, unrelated, unknown, and repeated actions cannot cause unintended mutation. Recoverable conditions retain stop while terminal policy/time faults expose no execution action. Ten groups plus 100 repeats cover button/touch shapes, checked resolution, failures, stale revisions, idempotence, and typed rejection in the complete 58-executable matrix. OT-012D adds runtime-aware latched-fault precedence, OT-012E removes target-supplied Start time, and OT-012F owns cooperative revisions/actions. Exact renderer/text, target synchronization, direct radio/GPS composition, physical behavior, and reboot/default preference policy remain |
| OT-012C | done | Experimental position packet and priority admission | A fixed-memory sink revalidates only canonical current payloads, obtains injected ephemeral packet-v0 metadata, encodes the exact 38-byte frame, and admits it only as background position traffic using the scheduler's actual attempt time. Metadata/rate pressure maps to not-ready, reserve/full pressure maps to full, and malformed/noncurrent payloads, invalid/exhausted metadata, duplicate IDs, or invalid queue state fail closed. Ten groups plus 100 repeats cover exact policy, round-trip, scheduler composition, pressure, priority, expiry, and failures in the complete 58-executable matrix. OT-010A carries admitted traffic into delivery without source loss, and OT-003F services the complete fake-radio path with one checked time. Packet v0 remains unauthenticated and prohibited for real coordinates; identity/counter lifecycle, authenticated composition, direct-radio binding, field behavior, and regulation remain |
| OT-012D | done | Fail-visible outbound position safety | The target-facing presentation validates scheduler plus outbound-runtime status. Coherent rollback/source fault overrides stopped sharing with a critical no-action frame; unknown/incoherent copied state fails closed; stale healthy Start and direct Start route through live coordinator authority; Stop remains safe/idempotent. Ten groups plus 100 repeats use real coordinator faults and checked UI revisions in the complete 58-executable matrix. OT-012E closes action-time checked-clock composition and OT-012F owns cooperative revisions/actions; renderer, exact target synchronization, reboot behavior, and physical input remain |
| OT-012E | done | Checked action-time position commands | `OutboundServiceCoordinator` owns target-facing Start/Stop. Start reads one checked action-time sample internally, defers without scheduler access on not-ready, and stops/latches on source failure or rollback; a latched retry consumes no source sample. Stop is immediate and clock-independent. The semantic adapter accepts no caller timestamp and maps retry, clock fault, and scheduler rejection separately. Ten groups plus 100 repeats and 100 updated safety repeats pass in the complete 58-executable matrix. OT-012F supplies cooperative UI ownership; exact target synchronization, concrete clock binding, renderer/retry UX, reboot policy, diagnostics, and physical input remain |
| OT-012F | done | Single-owner position-sharing UI coordinator | One cooperative service owns initial/current revision publication, one checked input poll, live coordinator Start/Stop, and mandatory post-action refresh. Temporary clock deferral retains the truthful Start frame; stale/failed input cannot mutate position; initial display failure retries its unused revision; post-action display failure and revision exhaustion stop sharing and latch further input closed. Ten groups plus 100 repeats pass in the complete 58-executable matrix. This is not an ESP-IDF task/lock, renderer, physical input, or concurrency result; exact target serialization, retry UX, reboot policy, diagnostics, and hardware evidence remain |
| OT-012G | done | Observed position-state refresh before input | The UI coordinator retains its last committed semantic frame and compares current live outbound/scheduler presentation before polling input. GPS wait/recovery, sink deferral, and permanent clock fault publish a higher revision first; queued older input remains unread until refresh and is then stale. Internal timestamps/counters do not churn revisions. Failed observed publication stops sharing and latches input closed. Ten groups plus 100 repeats pass in the complete 58-executable matrix. Exact ESP-IDF serialization/atomicity, renderer behavior, physical input/display timing, diagnostics, and hardware evidence remain |
| OT-012H | done | Privacy-safe position UI diagnostic event | Canonical `OTPD0/v0` validates one coordinator result and encodes a fixed 32-bit event with coarse event/outcome, displayed position notice, normalized reason, and presentation/change/containment flags. The logger writes exactly `OTPD0=XXXXXXXX`; idle is suppressed; filtering and sink rejection remain distinct. Revisions, timestamps, counters, location/content/identity, addresses, credentials, and free text are structurally absent. Ten groups plus 100 repeats pass in the complete 58-executable matrix. OT-012I supplies strict host decoding. Target log binding, retention/export/clear/persistence policy, and physical service evidence remain |
| OT-012I | done | Strict offline position UI diagnostic decoder | A checked parser accepts only the exact uppercase `OTPD0=XXXXXXXX` record, preserves unsupported-version versus invalid-word/message errors, reruns all word/coherence validation, and exposes stable names for every v0 category. The CLI prints deterministic fixed-field output, never echoes rejected input, and performs no network, file, device, retention, or export work. Ten groups, canonical/invalid CLI smoke tests, and 100 repeats pass in the complete 58-executable matrix. Packaged tooling, target log retrieval, consent/retention/export policy, and physical service evidence remain |
| OT-013 | done | Group membership/joining | Algorithm-neutral, fixed-capacity lifecycle and operator UX specify administrator-gated single-use invitations, four authentication obligations, separate promotion, epoch-advancing revoke/rekey, revoked-identity exclusion, last-admin protection, and reset/recovery boundaries; twelve host scenario groups pass. Exact cryptography, persistence, rendered UX, and physical multi-device evidence remain OT-005/OT-014/field gates |
| OT-014 | done | Persistent configuration | Two fixed 64-byte slots provide version/schema checks, CRC-32, commit-last recovery, generation selection/conflict/exhaustion, v1-to-v2 migration, safe defaults, structural secret-domain separation, no-op/rate/alternating-slot wear controls, and five power-loss boundaries across twelve host scenario groups. A backend-neutral target adapter now maps the four domains to isolated `ot_config`, `ot_secret`, `ot_proto`, and `ot_counter` namespaces while preserving erase-before-program, bit-clearing, partial RAM writes, full-blob sync/commit, and uncertainty latching across twelve groups plus 100 repeats in the complete 95-executable matrix. The secret namespace is structural separation only. CRC is not authentication; ESP-IDF binding, protected secret storage, secure rollback, and physical endurance remain later gates |
| OT-014A | done | NVS-ready multi-domain persistent storage adapter | A backend-neutral adapter binds five exact 64-byte two-slot domains to separate `ot_config`, `ot_secret`, `ot_proto`, `ot_counter`, and `ot_archive` namespaces. It maps missing keys to erased `0xFF`, requires erase before partial bit-clearing writes, holds unsynced changes in RAM, writes/commits one complete blob per sync, and latches failed or uncertain mutation until erase/restart. Real configuration rotation, applied-then-failed final-commit restart recovery, OT-005G counter composition, OT-017AD ACK-session composition, and OT-028J archive-lease composition pass. Twelve adapter groups, five counter groups, six ACK-session groups, five archive-lease groups, 100/100 focused repeats each, and the complete 109-executable host matrix pass. The secret, counter, protocol, and archive namespaces are not protected stores; ESP-IDF binding, access control, authentication, rollback protection, locking, physical interruption, endurance, and on-device evidence remain |

## Maps and integration

| ID | Status | Task | Acceptance evidence |
| --- | --- | --- | --- |
| OT-016 | partial | Offline map architecture research | A dated source/attribution and target gate now prohibits public OSM tile-server prefetch, requires explicit offline/redistribution rights and visible attribution, compares MBTiles 1.3, PMTiles v3, and an indexed raster reference, and fixes off-device preparation plus immutable staged/read-only activation, prior-good recovery, and mapless degradation. OT-016A supplies a strict host manifest/exact-byte verifier, OT-016B the activation/fallback policy, OT-016C the restart checkpoint, OT-016D the abstract recoverable store, OT-016E persist-before-exposure boot ordering, OT-016F verified runtime transitions, OT-016G safe replacement-candidate ordering, OT-016H the clean first-baseline path, OT-016I authorized selector-only service reseed, OT-016J the NVS-ready key/value adapter boundary, OT-016K the single-use local-service authorization handoff, OT-016L the protected trusted-generation source boundary, OT-016M the protected-source boot composition, OT-016N the protected-source runtime transition composition, OT-016O the protected-source candidate replacement composition, OT-016P the protected-source first-baseline composition, OT-016Q the protected-source authorized service-reseed composition, OT-016R the reset/replacement lifecycle policy, OT-016S the protected-domain authorization handoff, OT-016T the canonical trust-domain lifecycle record, OT-016U the recoverable trust-domain record store, OT-016AB the non-erasable trust-domain key/value adapter, OT-016V the permit-consuming trust-domain provisioner, OT-016W the recoverable stable trust-domain activation coordinator, OT-016X the read-only active trust-domain boot coordinator, OT-016Y the domain-aware candidate-entry coordinator, OT-016Z the restart-safe domain-aware trial-boot coordinator, and OT-016AA the domain-aware runtime-transition coordinator. The exact-target matrix still must cover JPEG/RGB565 raster rendering, storage/RAM/frame/power timing, corruption, interruption, removal, and circular-screen usability. No provider/data package, selected container/renderer/storage/transfer path, signature policy, received display, ESP-IDF backend, or on-device result is claimed |
| OT-016A | done | Offline map package manifest and host verification | Strict `OTMP0/v0` binds canonical source/licence/offline-rights/attribution, experimental container/encoding/scheme, Web Mercator coverage/zooms, exact length/tile count/SHA-256, and reader/firmware/storage/scratch requirements. It permits only MBTiles/JPEG/TMS, PMTiles/JPEG/XYZ, and indexed-raster JPEG-or-RGB565/XYZ candidates; public OSM tile endpoints, unlicensed offline/redistributable use, extra fields, incompatible layouts, bad bounds, insufficient storage, mutation, and truncation fail closed. Seven host groups include CLI path redaction. This is read-only off-device preflight, not source authentication, package approval, target activation, rendering, or compatibility evidence |
| OT-016B | done | Fail-safe offline map activation policy | A bounded C++ guard starts mapless on missing/unreadable/ambiguous/invalid boot selection, admits only fully evidenced alternate-slot candidates, leaves the active package unchanged through staging, begins trial only after exact selector-commit evidence, retains prior-good until bounded complete reads pass, and otherwise requires exact verified fallback or visible mapless degradation. Candidate, active, prior, and fallback-media loss fail closed across ten groups and 100/100 repeats. The interface contains no paths, geographic content, identity, secrets, free text, or communications authority. Package authentication, durable selector storage, filesystem/renderer integration, target composition, and physical evidence remain |
| OT-016C | done | Restart-safe map selector checkpoint | Fixed 64-byte `OTM0/v0` encodes canonical stable/trial/fallback state, abstract active/prior slots and generations, health/deadline/boot bounds, record generation, zero reserved bytes, a commit-last marker, and CRC. Atomic guard restore revalidates exact package evidence, clears volatile health/time, increments bounded trial boots, preserves prior recovery, and requires fallback at the boot limit. Ten groups and 100/100 repeats reject corruption, uncommitted/future/coherence/policy/evidence mismatch, missing prior, invalid arguments, and unsafe export. OT-016D supplies the abstract two-slot store; CRC is not authentication/anti-rollback, and protected generation, physical storage, filesystem/renderer integration, target composition, and physical interruption evidence remain |
| OT-016D | done | Recoverable abstract map selector store | `OTM0` byte 59 is a commit-last marker covered by CRC. A two-slot store writes the complete uncommitted record, commits one byte, verifies exact readback, rotates away from prior-good state, selects only a unique newer generation, repairs known invalid/uncommitted peers, and rejects unreadable slots, equal-generation conflict, exhaustion, and below-floor restore. Read-only exact live-checkpoint verification plus exact post-erase empty verification raises coverage to fourteen groups; OT-016E/F/I own boot/runtime/service ordering. No physical storage/atomicity/endurance/power-loss evidence, protected trusted floor, package authentication, target integration, or renderer exists |
| OT-016E | done | Persist-before-exposure map selector boot | A typed coordinator restores only into a private guard, publishes stable/fallback state without rewriting, and requires resumed-trial increments plus boot-limit fallback transitions to pass commit-last save and exact readback before release. Empty state becomes explicitly mapless; failed writes, uncertain commits, bad readback, unreadable/conflicting/stale media, package mismatch, invalid policy, and dirty live ownership never expose a private candidate. Ten groups and 100/100 repeats pass. No physical backend, trusted-floor ownership, target task, package access/rendering, power interruption, or on-device result exists |
| OT-016F | done | Verified runtime map transition persistence | A coordinator requires exact persisted/live/policy/current-generation agreement, applies trial read/time, fallback completion, and prior cleanup privately, avoids writes for volatile-only/rejected outcomes, and publishes persistent changes only after commit-last save/readback. Invalid fallback evidence verified-clears only the two selector records; failed clearing and storage/generation uncertainty stay mapless and reconciliation-required. Thirteen groups plus 100/100 repeats pass. No physical backend/erase guarantee, protected floor, package deletion, target task, renderer, interruption, or on-device result exists |
| OT-016G | done | Safe replacement-map candidate ordering | From one stable active baseline, a typed coordinator verifies exact live/policy/generation/floor agreement, stages and marks the externally evidenced alternate-slot package only on a private guard, rechecks the exact preflight generation, then publishes trial state only after commit-last save and exact readback. Invalid evidence leaves the current map active; save/generation uncertainty fails mapless. Eleven groups and all four affected suites pass 100/100 repeats. OT-016H separately owns first use; exclusive target ownership, physical package/storage adapters, authentication, renderer, interruption, and on-device evidence remain open |
| OT-016H | done | Restart-safe first map baseline | A first-use coordinator accepts only a clean `no_selector` mapless guard, exact policy, fully evidenced package, two readable empty selector slots, and zero trusted history. It constructs stable state privately and uses `save_if_empty` to commit/read back canonical selector record generation 1 before exposure. Existing, dirty, unreadable, conflicted, previously trusted, or concurrently changed state is never overwritten and cannot be reset/reseeded through this API. Ten groups and all six affected suites pass 100/100 repeats. OT-016I separately owns service reseed; explicit provisioning authority, locking, physical package/storage adapters, authentication, renderer, interruption, and on-device evidence remain open |
| OT-016I | done | Authorized map selector service reseed | A service-only coordinator requires a fresh exact-operation permit from OT-016K, an already-mapless owner, exact policy, valid package evidence, and exclusive selector-store ownership. It advances beyond observable/trusted generation history, verified-clears only the two selector records, rechecks emptiness through `save_if_empty`, and publishes a stable baseline only after commit-last exact readback. Twelve coordinator groups and fourteen store groups cover permit absence/binding/replay, clean-first-use routing, active-owner refusal, dirty/conflicted state, partial/dishonest erase, exhaustion, persistence uncertainty, restart restore, and a post-clear selector race. Protected trust, physical package/storage behavior, concrete service UX/audit, target locking, interruption, renderer, and on-device evidence remain open |
| OT-016J | done | NVS-ready map selector key/value adapter boundary | A backend-neutral adapter fixes `ot_state` / `ot_maps` / `otm_sel_a|b`, exact 64-byte blobs, durable backend commit after prepared write or key erase, full-blob byte-59 marker rewrite, idempotent missing-key erase, and strict native error containment. Ten groups cover binding/arguments, exact/missing/wrong-sized reads, write/commit failure, malformed/already-committed refusal, erase, and real store save/rotation/verified-clear composition. With OT-016K, all ten map suites pass 100/100 repeats and the complete 68-executable matrix passes. The accompanying official-source ESP-IDF plan prohibits broad erase and radio reseed, defines local service authority handoff and physical interruption evidence, and makes no ESP-IDF backend, partition/security configuration, target lock/task, durability, or on-device claim |
| OT-016K | done | Single-use local-service map reseed authorization | An injected verifier must atomically consume a nonzero opaque handle and return an exact-bound, short-lived grant for selector reseed over local USB or independently authenticated local wireless. The common authorizer checks boot, scope, transport, complete policy/package/trusted-floor binding, all five service confirmations, a committed local-confirmation revision, and a hard five-minute lifetime ceiling before minting a non-copyable permit. The coordinator burns the permit before store access, rechecks boot and checked use time, and rejects mismatch, expiry, or replay. Ten authorization groups, twelve reseed groups, all ten map suites at 100/100 repeats, and the complete 68-executable matrix pass. No credential verifier, challenge protocol, protected backend replay store, audit, target UI, ESP-IDF composition, or physical authentication result exists |
| OT-016L | done | Protected map selector trusted-generation source boundary | A boot-local non-copyable enforcer reads an injected protected-generation backend, refuses rollback/conflict/nonincreasing requests before mutation, requires an atomic expected-value compare-and-advance, and accepts success only after exact readback. Every advance error or post-write ambiguity latches all later source I/O closed until fresh-boot reconciliation; zero remains the explicit never-trusted baseline and no ordinary reset API exists. Ten groups pass under strict C++17 warnings-as-errors. Lower coordinators retain scalar generations for isolated tests, while protected boot, transition, candidate, baseline, and reseed wrappers now derive them; no protected backend, target composition, reset/replacement authority, physical durability, hardware counter, or anti-rollback claim exists |
| OT-016M | done | Protected-generation map selector boot composition | A boot-only coordinator obtains the trusted floor from OT-016L before selector access, runs restore and boot-created persistence against a private guard, and rechecks or atomically advances trust with exact readback before publication. Nonzero trusted history with empty selector media, selector rollback, failed trust reads, uncertain advance, final conflict, and dirty live ownership cannot expose a map; valid store-ahead state may advance trust only after complete selector validation. Ten groups, 100/100 focused repeats, and the complete 70-executable host matrix pass. Concrete protected storage/counter, reset/replacement authority, target locking, physical durability, and on-device evidence remain open |
| OT-016N | done | Protected-generation map selector runtime transition composition | A runtime wrapper obtains both the current selector generation and rollback floor from OT-016L before selector access, runs trial, deadline, fallback, and prior-cleanup operations against a private guard, then requires an exact protected recheck or selector-save-before-trust-advance ordering before publication. Initial trust failure blocks storage; zero/stale/mismatched state and final trust change contain map exposure; uncertain advance requires fresh-boot reconciliation; invalid-fallback selector clear retains protected history for authorized service rather than becoming first use. Eleven groups, 100/100 focused repeats, all thirteen map suites, and the complete 71-executable host matrix pass. Concrete protected storage/counter, reset/replacement authority, target locking, physical durability, and on-device evidence remain open |
| OT-016O | done | Protected-generation map selector candidate replacement composition | A replacement wrapper obtains both selector generation values from OT-016L before selector access, runs candidate staging and exact-generation commit-last save against a private guard, advances and exactly reads back protected history only after selector persistence, and publishes trial state only after exact agreement. Rejected candidates recheck trust before retaining the active map; initial trust failure blocks storage; zero/stale/mismatched state, save failure, post-save conflict, and uncertain advance contain map exposure. Eleven groups, 100/100 focused repeats, all fourteen map suites, and the complete 72-executable host matrix pass. Concrete protected storage/counter, reset/replacement authority, target/package-slot locking, physical durability, and on-device evidence remain open |
| OT-016P | done | Protected-generation map selector first-baseline composition | A first-use wrapper requires an exact clean no-selector mapless owner, inspects OT-016L before selector access, permits only zero protected history, saves canonical selector generation 1 on a private guard, advances and reads back protected history from 0 to 1, and publishes the first active map only after exact agreement. Retryable initial trust failure preserves clean first use; nonzero history blocks storage; invalid package/policy, existing or dirty selector, save failure, post-save conflict, and uncertain advance cannot expose the baseline. Eleven groups, 100/100 focused repeats, all fifteen map suites, and the complete 73-executable host matrix pass. Concrete protected storage/counter, reset/replacement authority, provisioning/package-slot locking, physical durability, and on-device evidence remain open |
| OT-016Q | done | Protected-generation map selector service-reseed composition | An authorized recovery wrapper inspects OT-016L before selector access, derives the lower reseed floor internally, and requires the single-use OT-016K permit to match that exact protected value. Selector-only verified clear and replacement save remain private; protected history advances only after exact selector readback; the recovered map publishes only after exact protected readback. Initial source failure preserves storage and permit, every lower-layer failure prevents trust advance, and post-save conflict or uncertainty remains ambiguous-mapless for fresh-boot reconciliation. Twelve groups, 100/100 focused repeats, all sixteen map suites, and the complete 74-executable host matrix pass. Concrete credential and protected-generation backends, reset/replacement authority, target locking, physical durability, and on-device evidence remain open |
| OT-016R | done | Map selector reset and replacement lifecycle policy | A pure fixed-shape classifier keeps ordinary factory reset outside both map persistence domains, routes selector reseed only through OT-016K/Q while protected history is intact, blocks temporary source failure without selector access, and refuses to treat missing/replaced same-device history as first use. Same-device source replacement requires a future independent external recovery decision; independently established blank replacement hardware routes only to fresh-domain commissioning, while retained-selector import is rejected. Ten groups, all seventeen map suites, 100/100 focused repeats, and the complete 75-executable host matrix pass. No physical continuity detector, external recovery authority, new-domain provisioner, credential verifier, target lock/executor, ESP-IDF composition, or physical result exists |
| OT-016S | done | Protected map-domain replacement and commissioning authorization | An injected target backend must atomically consume one opaque handle and return an exact-bound, short-lived grant for either same-device domain replacement or blank new-device commissioning. Common code derives the scope from OT-016R, requires local USB, a nonzero proposed 128-bit domain, coherent empty or quarantined selector evidence, six confirmations, a committed local revision, exact boot/binding/time echo, and move-only permit ownership. Temporary source failure, retained state on new hardware, invalid routes/media, wireless/radio transport, mismatch, expiry, and replay mint no permit. Ten groups, all eighteen map suites, 100/100 focused repeats, and the complete 76-executable host matrix pass. The permit has no consumer: device continuity, entropy, concrete credentials/audit/replay, domain/retirement records, provision/reset execution, target locking, ESP-IDF composition, and physical evidence remain open |
| OT-016T | done | Canonical map trust-domain lifecycle record | Fixed 80-byte `OTMD/v0` preserves `OTM0/v0` unchanged while binding a nonzero current domain, explicit retired domain, quarantined selector floor, accepted selector generation, domain epoch, record generation, commit-last marker, and CRC. Fresh commissioning permits only epoch-1 pending-baseline/active state with no retired domain; same-device replacement requires distinct current/retired domains, pending-reseed or active state, epoch at least 2, and active selector generation strictly above the retired floor. OT-016S now requires the retired domain for replacement and forbids one at commissioning. Ten codec groups plus the tightened authorization suite, all nineteen map suites at 100/100, and the complete 77-executable host matrix pass. No recoverable domain-record store, authenticated/rollback-resistant backend, continuity/entropy proof, permit consumer, provisioner, target lock, physical durability, or on-device evidence exists |
| OT-016U | done | Recoverable map trust-domain lifecycle store | A separate two-slot store for exact 80-byte `OTMD/v0` records selects only the newest unique valid generation, accepts generation 1 only as fresh pending commissioning on readable empty media, and thereafter enforces exact next-generation maintenance, activation, monotonic selector-acceptance, or linked replacement transitions. It preserves the prior committed record across twelve interrupted prepared-write boundaries, commits byte 75 last, verifies exact readback, repairs known degraded peers, and refuses unreadable/invalid-only/conflicted/exhausted/backward/floor-lowering/immediate-domain-reuse state. Ten groups, all twenty-seven map suites at 100/100, and the complete 95-executable matrix pass. The store has no erase/reset or provisioning authority; the target storage adapter is OT-016AB; no ESP-IDF backend, rollback anchor, permit consumer, target lock, physical durability, or on-device evidence exists |
| OT-016V | done | Authorized map trust-domain provisioner | The sole common-code permit consumer burns exact binding/boot/time authority before any I/O, requires exclusive stopped/mapless ownership, and preflights the domain store, selector media, and protected source before mutation. It commits and verifies the exact pending `OTMD/v0` record before verified selector clear and before establishing an independently uninitialized protected source at generation zero. Matching pending state resumes only under a new exact permit; applied-then-failed source calls can be reconciled without reapply, while every post-pending failure remains mapless and reconciliation-required. Thirteen groups, all twenty-one map suites at 100/100, and the complete 79-executable host matrix pass. Success is prepared, not active: protected target adapters, concrete credentials/continuity/entropy, baseline/reseed activation composition, target locking, physical durability, and on-device evidence remain |
| OT-016W | done | Recoverable stable map trust-domain activation | The coordinator resumes exact pending or active stable-baseline state, persists selector generation 1 or exactly retired-floor plus 1, atomically advances and verifies the exact protected domain/generation, commits and verifies active `OTMD/v0`, performs final selector/source checks, and only then publishes the live map. Selector-save, protected-advance, applied-then-failed, uncertain domain-commit, and already-active restarts reconcile without lowering or rebinding durable history. Fourteen groups, all twenty-two map suites at 100/100, and the complete 80-executable host matrix pass. Active-domain boot, candidate/trial/fallback/cleanup synchronization, protected target adapters, target locking, physical durability, and on-device evidence remain |
| OT-016X | done | Read-only active map trust-domain boot | The boot coordinator accepts only a clean stopped owner and exact stable active state. It verifies the active `OTMD/v0` record and protected domain/generation before selector access, restores the exact accepted `OTM0/v0` privately, then rereads the domain, selector, and protected source before publication. It performs zero writes, commits, erases, repairs, or protected mutations; disagreement stays mapless. Thirteen groups, all twenty-three map suites at 100/100, and the complete 81-executable host matrix pass. Candidate/trial/fallback/cleanup synchronization, protected target adapters, physical restart, and on-device evidence remain |
| OT-016Y | done | Domain-aware map candidate entry | From an exact stable active baseline, the coordinator persists private trial selector generation `N+1`, rechecks the active domain, atomically advances/readbacks the exact protected domain, reverifies the selector, advances/readbacks `OTMD/v0` accepted generation, then rereads all three owners before trial publication. Rejected candidates preserve the active map only after exact final rechecks; post-selector failure stays mapless without rollback. Thirteen groups, all twenty-seven map suites at 100/100, and the complete 95-executable host matrix pass. Domain-aware trial boot/recovery is OT-016Z and runtime transitions are OT-016AA; protected target adapters, physical interruption, and on-device evidence remain |
| OT-016Z | done | Restart-safe domain-aware map trial boot | The coordinator accepts only exact active-domain relationships `D=S=G` or selector `G=D+1` with protected generation on either side, restores trial/fallback state privately, persists a resumed boot or boot-limit transition before advancing the exact protected generation and then the active `OTMD/v0` accepted generation, and publishes only after final three-owner agreement. Unrelated gaps and every uncertain post-selector result stay mapless for reconciliation; no rollback or rebind exists. Fourteen groups, all twenty-seven map suites at 100/100, and the complete 95-executable host matrix pass. Healthy promotion, fallback completion, and previous-package cleanup are now OT-016AA; protected target adapters, physical interruption, and on-device evidence remain |
| OT-016AA | done | Domain-aware map runtime transitions | Healthy trial reads and rejected operations require exact unchanged domain, selector, and protected-source rechecks. Promotion, deadline/failure fallback, valid fallback completion, and previous-package cleanup commit selector generation `N+1`, advance/read back the exact protected domain, then advance/read back `OTMD/v0` accepted history before final three-owner rechecks and publication. Interrupted promotion and uncertain domain commit recover through the expanded OT-016Z active-checkpoint boot path; invalid fallback evidence retains protected/domain history for service. Eleven groups, all twenty-seven map suites at 100/100, and the complete 95-executable host matrix pass. Protected target adapters, target locking, physical package operations, interruption/wear, rendering, and on-device evidence remain |
| OT-016AB | done | Non-erasable map trust-domain key/value adapter | A backend-neutral adapter binds the two exact 80-byte `OTMD/v0` records to `ot_state` / `ot_map_domain` / `otmd_a|b`, requires zero-marker prepared writes, commits each backend mutation, and implements byte-75 marker commit through exact read/verify/full-blob rewrite. It exposes no erase or reset API and leaves failed marker commit uncertain for restart inspection. Five groups, all twenty-seven map suites at 100/100, and the complete 95-executable host matrix pass. ESP-IDF binding, protected authentication/rollback, target locking, physical interruption, endurance, and on-device evidence remain |
| OT-017 | done | OpenGauge alert interface specification | Mirrored specifications and three normative fixtures define an explicit 64-byte frame for seven alert types, canonical units, assert/clear lifecycle IDs, optional UTC/value fields, and CRC-32. Independent OpenGauge exporter and OpenTrail ingress codecs round-trip the same bytes. Nine ingress scenario groups enforce semantic, trust/authorization, stale/future, duplicate/conflict, fixed-rate/emergency-reserve, monotonic-time, and fail-closed producer-capacity policy; four producer groups reject invalid exports. Physical transport authentication and field delivery remain later integration gates |
| OT-017A | done | Critical-alert acknowledgement interface | Independent OpenTrail and OpenGauge codecs encode/decode an explicit 64-byte `OGK0` accepted/rejected ACK containing consumer/producer/event/condition/lifecycle, boot session, sequence, observed age, canonical reserved bytes, and CRC. Three mirrored normative vectors and four scenario groups per repo pass full host matrices plus 100 repeats. Transport authentication/authorization, replay window/persistence, delivery-controller/outbox composition, and physical end-to-end ACK delivery remain |
| OT-017B | done | Final-ingress-to-ACK responder | A bounded responder converts only final alert-ingress decisions into `OGK0`: accepted and identical duplicate become accepted/none; authenticated unauthorized/stale/conflict/rate decisions map to canonical rejection reasons; malformed, unauthenticated, producer-mismatched, and clock-rollback input is suppressed. It adds monotonic elapsed age and advances boot-session sequence only after successful encoding. Eight host groups plus 100 repeats cover lifecycle, correlation/decode, duplicate recovery, negative mapping, suppression, inconsistent state, age/clock, and sequence wrap. OT-017C supplies boot-session allocation; authenticated response transport, per-session sequence persistence, full round-trip composition, and physical delivery remain |
| OT-017C | done | Durable ACK responder boot-session allocator | A fixed 64-byte `OTAS` record stores exact consumer ID, authorization epoch, nonzero boot session, generation, canonical reserved bytes, CRC, and a commit-last marker in a separate protocol-state domain. Two-slot allocation increments the session before returning it, alternates slots, verifies readback, and requires explicit reset/reseed for identity/epoch change. Ten host groups plus 100 repeats cover first use/responder composition, increment/rotation, explicit rebind, corruption, equal conflict/future version, exhaustion, reads/requests, all five power-loss mutation boundaries, uncommitted/malformed state, and reset failure; OT-017AD adds exact key/value composition. Per-session sequence, protected target storage, secure rollback, OpenGauge rebind, authenticated transport, and physical power-loss evidence remain |
| OT-017AD | done | Key/value-backed ACK boot-session composition | The real `OTAS` allocator now runs through the multi-domain key/value adapter using only exact `ot_state` / `ot_proto` / `slot_a|b` bindings. Fresh adapters prove session increment and slot rotation; failed-unapplied prepared commits retry only after confirmed absence, applied-then-failed marker commits return no session and force the next boot to skip it, explicit reset durably erases both keys before different-identity reseed, and wrong-sized values fail before mutation. Six groups, 100/100 focused repeats, and the complete 95-executable host matrix pass. ESP-IDF binding, authenticated integrity, trusted rollback, reset/reseed UI, target locking, physical interruption/endurance, OpenGauge rebind, and on-device evidence remain |
| OT-017D | done | Host-mediated physical alert/ACK round trip | Two role-reversed close-bench cycles carried 2/2 exact 64-byte `OGA0` alerts and 2/2 responder-produced correlated 64-byte `OGK0` ACKs over temporary MeshCore channel text. Every correlation field passed; loss, duplicates, and new errors were zero; total round trips were 1009.6-1014.0 ms; the SenseCAP recorded exact aggregate +4 flood RX/+4 flood TX and stayed repeat-on; 4/4 endpoint cleanup checks passed with no journal remaining. The host supplied authenticated/authorized context, so on-device authenticated transport, direct SX1262 binding, forced-route evidence for this payload, failure injection, outbox completion, and field behavior remain later gates |
| OT-017E | done | Cross-project physical ACK completion composition | Two strengthened role-reversed cycles delivered exact `OGA0`/`OGK0` frames with zero loss/duplicates/errors and 1005.3-1014.2 ms round trips. Each returned ACK passed the real OpenGauge peer authorization, session, replay, and exact correlation ingress and completed its reconstructed in-flight outbox; final queued/in-flight counts were zero. SenseCAP aggregate was exact +4 flood RX/TX and 4/4 cleanup passed. Persistent live target state, protected keys, direct transport, negative/failure/restart/revoke cases, and field evidence remain |
| OT-017F | done | Physical stale-rejection policy | Two role-reversed cycles returned exact correlated rejected/stale `OGK0` frames with zero loss/duplicates/errors and 1013.1-1014.7 ms round trips. OpenGauge processed both through authorization/session/replay/correlation but reported `outbox_completed=false`, zero delivery acknowledgements, no retry release, and explicit terminal failure. SenseCAP aggregate was exact +4 flood RX/TX and 4/4 cleanup passed. Retryable rejection and persistent failure/restart/revoke/field cases remain |
| OT-017G | done | Physical retryable rate-limit rejection | Two role-reversed cycles returned exact correlated rejected/rate-limited `OGK0` frames with zero loss/duplicates/errors and 1000.8-1008.3 ms round trips. OpenGauge processed both with zero acknowledgements/completions, released exactly one queued retry, left zero in flight, and did not record terminal failure. SenseCAP aggregate was exact +4 flood RX/TX and cleanup passed 4/4. Persistent backoff/retry completion and failure/restart/revoke/field cases remain |
| OT-017H | done | Physical rate-limit retry to accepted completion | Two role-reversed four-leg sequences returned rate-limit rejection, enforced not-ready at 25 ms and byte-identical retry readiness at 26 ms, retransmitted the same alert, and completed only after an accepted next-sequence ACK. Both finished with one acknowledgement, one recorded remote retry, zero queued/in-flight/terminal failures, zero application duplicates/new radio errors, exact aggregate +8 SenseCAP flood RX/TX, and 4/4 cleanup. State was reconstructed after both responses; durable live/restart/field cases remain |
| OT-017I | done | Live OpenGauge state across physical retry lifecycle | A single OpenGauge process started with one real in-flight entry before each first radio send and retained authorization/replay/outbox state across rate-limit rejection, exact backoff, same-frame physical retry, and final accepted ACK in both endpoint roles. Both finished with one acknowledgement/retry, zero queued/in-flight/terminal failures, zero application duplicates/new radio errors, exact aggregate +8 SenseCAP flood RX/TX, and 4/4 cleanup. Durable restart/power-loss and on-device/field cases remain |
| OT-017J | done | Cross-project outbox restart reconstruction | OpenGauge's canonical 640-byte `OOC0` checkpoint is now wired into boot-only atomic live-outbox import/export. Five integration groups reconstruct exact queued retry readiness, in-flight ACK timeout, maximum lifetime, frames/state/attempts against a new monotonic origin and fail closed on prepared, corrupt, configuration-mismatched, nonempty, or unrepresentable state. Its full 29-executable matrix and 100 focused repeats pass. Coordinated ACK/authorization generation, durable storage, power-loss, authenticated integrity, rollback, and physical/on-device restart remain |
| OT-017K | done | Canonical cross-project outbox policy identity | OpenGauge now derives the nonzero versioned `OCF0` compatibility fingerprint internally from all four outbox timers, maximum attempts, and emergency reserve; callers cannot label incompatible retry/lifetime policy as matching. A thirteenth checkpoint group, the full 29-executable matrix, and 100 focused repeats pass. Coordinated durable storage and physical/on-device restart remain |
| OT-017L | done | Coordinated cross-project recovery generation | OpenGauge's fixed 960-byte `OCR0` envelope binds the exact ACK replay/authorization checkpoint and exact outbox checkpoint to one nonzero generation with canonical padding, nested validation, outer CRC, and atomic decode. Four groups, the full 30-executable matrix, and 100 repeats pass. Live dual import, recoverable durable storage, power-loss, authentication, rollback, and physical/on-device restart remain |
| OT-017M | done | Preflighted live cross-project recovery | OpenGauge now exports both state owners into one `OCR0` generation and validates both exact boot imports on private component copies before committing either live state under exclusive ownership. Five groups, the full 31-executable matrix, and 100 repeats restore exact retry readiness and replay state while proving invalid generation, stopped/prepared state, policy/authorization mismatch, and corruption cause no partial live mutation. Two-slot storage and physical/on-device restart remain |
| OT-017N | done | Recoverable cross-project recovery store | OpenGauge's two-slot host store writes exact coordinated `OCR0` generations with full readback verification, restores the newest unique valid slot, preserves the prior good generation across partial/corrupt writes, exposes degraded I/O, and fails closed on conflicts or policy/authorization mismatch. Eight groups, the full 32-executable matrix, and 100 repeats pass. Target NVS, physical power-cut/wear, authenticated integrity, rollback, and on-device restart remain |
| OT-017O | done | Store-owned recovery generation allocation | OpenGauge's normal recovery save now allocates highest-valid plus one, starts at 1 on empty media, rotates slots monotonically, rejects unreadable/conflicted baselines, and reports 64-bit exhaustion before export/write. Ten store groups, the full 32-executable matrix, and 100 repeats pass. Factory-reset authority, target NVS, physical power-cut/wear, authenticated integrity, rollback, and on-device restart remain |
| OT-017P | done | Uncertain recovery commit reconciliation | OpenGauge marks every storage write error commit-uncertain with intended slot/generation. Sixteen interrupted-overwrite lengths spanning `OCR0` layout boundaries recover the prior good generation; a complete write followed by I/O failure reconciles as committed on boot. Twelve store groups, the full 32-executable matrix, and 100 repeats pass. Target-backend atomicity, physical power-cut/wear, authenticated integrity, rollback, and on-device restart remain |
| OT-017Q | done | Cross-project peer-authorization restart boundary | OpenGauge's fixed 256-byte `OPA0` record preserves up to eight active/revoked logical peers, role permissions, channel, opaque key handles, and authorization epochs. Export refuses pending approvals; boot-only import is canonical, policy-bound, and atomic. Eight groups, the full 33-executable matrix, and 100 repeats pass. Coordinated `OPA0`/`OCR0` restore, protected target storage, authenticated integrity, rollback, and physical power-loss evidence remain |
| OT-017R | done | Cross-project recoverable peer-authorization store | OpenGauge's fixed 288-byte `OPS0` envelope and two-slot host store wrap exact `OPA0`, allocate monotonic generations, verify exact readback, preserve the prior good generation across ten interrupted-write boundaries, reconcile a full write followed by I/O error at boot, expose degraded reads, and fail closed on conflict/exhaustion. Ten groups, the full 34-executable matrix, and 100 repeats pass. Coordinated authorization/replay/outbox restore, protected target storage, authenticated integrity, rollback, and physical power-loss evidence remain |
| OT-017S | done | Cross-project atomic alert-system recovery | OpenGauge's fixed 1280-byte `ORS0` record binds exact `OPA0` peer authorization and exact `OCR0` ACK/outbox state to one generation. Dependency-correct private registry/outbox candidates and a temporary ACK ingress preflight epoch, policy, pointer, replay, and retry dependencies before any live owner changes. Six groups, the full 35-executable matrix, and 100 repeats pass. Recoverable `ORS0` storage, target binding, authenticated integrity, rollback, physical power-loss, and on-device boot evidence remain |
| OT-017T | done | Cross-project recoverable alert-system store | OpenGauge's two-slot host store writes exact 1280-byte `ORS0` generations, owns normal monotonic allocation, preserves the newest good slot across eleven interrupted-write boundaries, requires exact readback/decode, reconciles a full write followed by I/O error at boot, exposes degraded reads, and refuses conflict/exhaustion. Eight groups, the full 36-executable matrix, and 100 repeats pass. ESP-IDF protected storage, authenticated integrity, rollback/reset authority, physical power-loss/wear, and on-device boot evidence remain |
| OT-017U | done | Cross-project trusted generation boundary | OpenGauge `restore_at_or_above` refuses a newest valid `ORS0` below a caller-supplied trusted minimum before any live owner import; `save_next_after` advances beyond both last-trusted and valid local generations, with pre-write exhaustion. Ten focused groups, the unchanged 36-executable matrix, and 100 repeats pass. A hardware-backed trusted source, authenticated integrity, reset/replacement authority, and physical evidence remain |
| OT-017V | partial | Cross-project target recovery adapter | OpenGauge's target plan defines exact two-slot `ORS0` semantics, protected opaque-handle resolution, a separate trusted-generation source, boot/save ordering, coordinated reset/replacement, and physical interruption/endurance evidence. OT-017AE now records its exact backend-neutral key/value byte binding plus real boot/save restart composition. No exact ESP-IDF target/backend, protected key store, monotonic trust source, physical interruption, or on-device boot result exists yet |
| OT-017W | done | Cross-project protected key-handle preflight | Direct and stored OpenGauge `ORS0` restore validate active peers through an injected opaque-handle boundary before outbox/ACK preflight or any live mutation; revoked peers are skipped. Unavailable, purpose-mismatch, and backend failures retain typed peer-specific evidence and leave all live owners unchanged. Eight system groups, eleven store groups, the unchanged 36-executable matrix, and 100 repeats each pass. A concrete protected-key backend and on-device evidence remain |
| OT-017X | done | Cross-project typed recovery boot | OpenGauge's host coordinator combines provisioning, trusted-generation state, non-mutating slot inspection, protected-key `ORS0` restore, and exact trusted-floor catch-up. It distinguishes first boot, restored, degraded, safe-mode, and service-required outcomes and keeps transport disabled until operational. Nine groups, the complete 37-executable matrix, and 100 repeats pass. Target boot composition, protected backends, physical interruption, and operator recovery remain |
| OT-017Y | done | Cross-project verified recovery save | OpenGauge normal persistence now requires exact local/trusted generation agreement, verifies the next `ORS0` before advancing trust, and verifies exact trust readback before reporting committed. Missing, rollback, conflict, local-ahead, uncertain commit, and trust-update failures remain typed. Eight groups, the complete 38-executable matrix, and 100 repeats pass. Target storage/trust, physical interruption, endurance, provisioning/reset, and operator recovery remain |
| OT-017Z | done | Cross-project unreadable-slot fail-close | OpenGauge distinguishes known empty/invalid peer media from unreadable media that may conceal a newer committed generation. Unreadable state returns service-required, does not advance trust, and cannot enable transport even when the older visible generation imports privately. Ten boot groups, the complete 38-executable matrix, and 100 repeats pass. Physical backend diagnosis and operator recovery remain |
| OT-017AA | done | Cross-project known-degraded repair | OpenGauge repairs only a current operational degraded result with matching active/trusted generation and one valid plus one known empty/invalid slot. It commits the next `ORS0`, advances/readbacks trust, and verifies both slots valid; healthy, unreadable, service, stale, and uncertain cases fail closed. Five groups, the complete 39-executable matrix, and 100 repeats pass. Physical durability, scheduling, audit, and unreadable-media service remain |
| OT-017AB | done | Cross-project redacted recovery status | OpenGauge maps boot, save, and repair results into one fixed-shape operator record with coarse state/reason/action, slot health, generations, protected-key failure class, and transport/repair flags. Peer IDs and key handles are structurally absent; unknown or inconsistent input fails closed. Seven groups, the complete 40-executable matrix, and 100 repeats pass locally. Target logging/rendering, audit retention, and physical service UX remain |
| OT-017AC | done | Cross-project versioned recovery diagnostic event | OpenGauge records one atomic 32-bit word containing only redacted recovery operation, state/reason/action, slot health, key-failure class, and transport/attention/repair/redaction flags. Magic/version/coherence validation fails closed; generations and identity-bearing fields are omitted. Eight groups, the complete 43-executable matrix, and 100 repeats pass publicly. Target logger binding, persistent retention/export, and physical service capture remain |
| OT-017AE | done | Cross-project `ORS0` key/value boot/save composition | OpenGauge binds the two exact 1280-byte recovery slots to `og_state` / `og_recovery` / `ors0_a|b`, requires commit after write/present-key erase, and composes real boot/save coordinators through restarted adapter/store instances. Normal one/two-slot restart, verified save, applied-uncertain trust catch-up, and unapplied-uncertain prior-generation recovery pass thirteen groups, 100/100 repeats, and the public 43-executable matrix. No OpenTrail ESP-IDF binding, protected key/trust source, physical interruption, or on-device result is claimed |
| OT-018 | planned | Display/UI feasibility spike | Candidate hardware renders representative map/peer/alert screen with measured RAM, frame time, boot time, and input behavior |
| OT-019 | partial | Update/recovery architecture | A v0 boundary fixes signed/versioned hardware-bound bundles, inactive-slot write/readback, persisted trial selection, bounded health confirmation, automatic rollback, trusted version floors, USB-first delivery, and layered physical/ROM recovery. A pure lifecycle guard passes eight host groups. The canonical 64-byte `OTU0/v0` checkpoint adds exact policy/hardware/version binding, corruption detection, atomic restore, and reboot-safe trial/rollback intent across eight groups plus 100 repeats while discarding boot-local evidence. Its abstract two-slot store owns generation allocation, preserves prior-good state across partial/corrupt writes, verifies readback, repairs known degradation, fails closed on unreadable/conflicted state, rejects a newest generation below a caller-supplied trusted minimum, allocates beyond local/trusted state, and supports non-mutating inspection across 20 groups plus 100 repeats. A typed boot coordinator privately restores and validates the observed image, durably records trial/rollback transitions, exactly advances trust, and exposes live state only after successful ordering across 15 groups plus 100 repeats. A verified normal-save coordinator requires exact local/trusted agreement, verifies checkpoint-before-trust ordering, and routes uncertain/local-ahead state to reboot reconciliation across 10 groups plus 100 repeats. A lifecycle-transition coordinator applies health, tick, confirmation, and rollback to a private guard copy, publishes durable state only after the verified save commits, and stops the original guard on persistence failure across 10 groups plus 100 repeats. A fixed redacted status boundary validates boot/save/transition coherence and emits only coarse operator state/reason/action, generation evidence, and recovery flags across eight groups plus 100 repeats; unknown or contradictory input blocks normal operation as service-required. `OTRD0/v0` then writes one canonical generation-free 32-bit outcome through the existing logger with independent encode/decode coherence, filtering, and sink-failure checks across eight groups plus 100 repeats. A bounded production-facing RAM ring retains and decodes real `OTRD0` events without adding persistence claims. OT-019B adds strict offline category decoding without file, device, network, log-retrieval, or recovery execution authority. A backend-neutral key/value adapter fixes exact `ot_state` / `ot_update` / `otu_chk_a|b` names, 64-byte values, explicit durable commit after write/erase, idempotent missing-key erase, and applied-then-failed restart discovery across nine groups plus 100 repeats. Target task binding/concurrency, retention/rendering, scheduling/reboot, exact ESP-IDF target and partition/security configuration, signer/key custody, authenticated target storage, hardware-backed trusted generation, cleanup/reset authority, updater adapters, and physical interruption/recovery evidence remain |
| OT-019A | done | Fail-visible semantic recovery presentation | Valid `OTRD0/v0` outcomes map to fixed status/system-fault UI frames. Trial, rejected-transition, and cleanup notices allow acknowledgement only; blocked rollback, safe-mode, service, and reconciliation states expose no execution action. Invalid words with a valid revision produce a generic critical service frame, while revision zero is unpresentable. Nine groups plus 100 repeats exercise generated/fallback frames through the real checked local-interface boundary in the complete 58-executable matrix. Exact renderer, target scheduling/revision ownership, physical recovery execution, and operator workflow remain |
| OT-019B | done | Strict offline update-recovery diagnostic decoder | A checked parser accepts only the exact uppercase `OTRD0=XXXXXXXX` record, preserves unsupported-version versus invalid-word/message errors, reruns all binary/coherence validation, and exposes stable names for every v0 operation, state, reason, and action. The CLI prints deterministic fixed-field output, never echoes rejected input, performs no file/device/network/log/export work, and has no recovery execution authority. Ten groups, canonical/invalid CLI smoke tests, and 100 repeats pass in the complete 58-executable matrix. Packaged tooling, target log retrieval, accessible rendering, authorization/retention/deletion policy, and physical service evidence remain |
| OT-019C | done | NVS-ready update checkpoint key/value adapter | A backend-neutral adapter binds the two exact 64-byte `OTU0/v0` slots to `ot_state` / `ot_update` / `otu_chk_a|b`, rejects invalid arguments and wrong-sized values before acceptance, requires explicit backend commit after write/erase, treats missing-key erase as idempotent, and keeps failed or applied-then-failed commits visible for restart reconciliation. Nine groups, 100/100 focused repeats, and the complete 85-executable host matrix pass. ESP-IDF binding, partition/security configuration, target locking, hardware-backed trust, physical interruption, endurance, and on-device evidence remain |
| OT-019D | done | Fail-closed firmware-install board preflight | One pure loader-facing policy separates inspectability from flash permission and accumulates every blocker. Exact low-level processor, flash, required PSRAM, hardware profile, revision, target role, bootloader schema, and image-size evidence must agree; runtime model names do not authorize a write. Bench client, complete client, and packaged repeater images cannot cross roles. Clean install requires explicit destructive confirmation, while recovery also requires separate physical authorization. Thirteen preflight groups pass. Privacy-safe Windows discovery plus runtime adapters add eight groups and a dated 2026-08-12 live three-device snapshot: two Heltec V4 OLED MeshCore companions and one Seeed SenseCAP Solar MeshCore repeater were identified with raw replies/pairing/identity/local ports omitted and Flash still blocked. Installed runtime role remains non-authoritative for the unresolved OpenTrail target role. No low-level probe, approved received-unit profile, signed-bundle verifier, writer, Windows UI/package, or physical flash/recovery evidence exists |
| OT-019E | done | Fail-closed signed firmware-bundle admission | One pure pre-device policy requires externally evidenced complete/canonical manifest parsing, manifest digest, signature, trusted exact signer ID, exact hardware/processor/role/revision binding, minimum bootloader schema, non-rollback release generation, full image read, exact bounded length, and verified nonzero image digest. Twelve groups reject each missing/mismatched condition independently. Success permits only the separate OT-019D board preflight; it never authorizes erase/write. OT-019I supplies bounded candidate-container/canonical-manifest parsing and image SHA-256 evidence, and OT-019J supplies host RSA-PSS verification plus an empty production trust boundary. Approved signer custody/public keys, protected revocation/generation state, signed release bundles, admission composition, writer, and physical update evidence remain absent |
| OT-019F | done | Final firmware-write admission composition | One pure coordinator requires OT-019E bundle admission, OT-019D board/install preflight, and exact equality across their hardware profile, processor, target role, revision range, minimum bootloader schema, maximum image size, and signed/candidate image length. Eight groups prove either gate or any binding mismatch blocks `ready_to_write`, while destructive/recovery authorization remains intact. It performs no I/O. Exclusive one-use result ownership/invalidation, concrete parser/probe/policy owners, writer/readback, boot/rollback/recovery, Windows UI, and physical evidence remain |
| OT-019G | done | Privacy-safe Windows loader inspection view | One strict presentation adapter converts only reduced USB/runtime evidence into fixed screen copy, familiar allowlisted board/role labels, firmware, five human-readable blockers, and action state. Four groups cover three-device presentation, sensitive-field omission, generic runtime failure, and fail-closed schema/unexpected-permission handling. The current live bench view reports three found/inspected and zero ready: one Heltec companion, one SenseCAP repeater, and one Wio companion; its document-level firmware/write/recovery actions remain disabled. OT-019H provides the WPF consumer and OT-019I a separate local candidate-bundle inspector; rendered/accessibility evidence, signature trust, writer, physical update, and recovery remain |
| OT-019H | partial | Read-only Windows loader desktop shell | A real .NET 8 WPF application invokes the detailed privacy-safe development pipeline when available and otherwise uses a source-free Windows USB/runtime path, independently validates either reduced document, and binds candidate cards. Refresh/F5, explicit single-device selection, bounded local candidate-bundle selection, and exact authoritative selected-device matching are active; every device-write action remains disabled. One replaceable identity boundary composes the attorney-review-pending local display `Limited Underground Firmware Loader — Preview`; `Inspection only` and attorney-review-pending status remain visible until real write and recovery acceptance passes, and presentation JSON remains non-authoritative for branding, visible blocker copy omits the repository name, and safeguards reject standalone/prohibited LU structures, TLU/LUT/LUTrail, and `®` without changing namespaces, schemas, or `OT-*` records. The built-in path bypasses the laptop's access-denied CIM/PnP query through SetupAPI, privately allowlists exact USB families, and holds COM names only for fixed MeshCore identity commands. Companion inspection permits only app-start/device-info and skips private BLE-PIN bytes; repeater inspection permits only `board`, `ver`, and `get role`. It uses no Python, MeshCLI, shell, network, arbitrary command, line-toggle/reset, or mutation surface. Three consecutive live C# refreshes report one Heltec V4 OLED companion, one SenseCAP Solar repeater, and one Wio Tracker L1 companion, all three runtime-identified and zero ready to flash. Candidate-only hardware panels separate current runtime evidence, vendor-published family baselines, and deliberate maintenance steps; none is authoritative, no reset occurs, and exact hardware profile/revision/processor/bootloader/product-role gates remain blocked. The owner-requested presentation uses square gray Windows 95-style surfaces, navy headings, classic typography, and an application-owned beveled button template that fixes the observed near-white-on-white disabled labels. The application builds warning-free and 59 C# scenario groups fail closed on identity-policy violations, malformed counts, any ready-to-flash candidate, global Flash authority, local-port disclosure, stale device/selection/bundle snapshots, missing or mismatched authoritative device fields, excessive helper output, unsafe blocker copy, excessive packaged candidates, missing development files, USB-family lookalikes, malformed frames, unrecognized runtime responses, malformed/mismatched bundle candidates, untrusted signers, signature/manifest tampering, wrong-size trust keys, hardware-profile authority, fixed-vector drift, missing DPI declaration, incomplete theme resources, or stale assertive error content after failed-refresh recovery. A hidden error peer clears text, Automation ID, help, and live setting; only an actual current visible failure is assertive. Device summaries omit internal references, disabled actions explain blockers, dynamic status values are bound, keyboard focus is visible, actual device-item peers expose validated summaries and blocker help, and summary/safety/error/bundle/selection peers expose their current visible messages; deterministic desktop/minimum rendered layout is accepted under OT-019R, three controlled production-window refresh/selection cycles pass under OT-019S, explicit per-monitor configuration plus 125%/150%/200% scaled rendering pass under OT-019T, and system-aware contrast mapping plus deterministic production rendering passes under OT-019U, Tab focus traversal under OT-019V, automation-peer semantics under OT-019W, routed wide/wrapped device navigation under OT-019X, in-window theme-transition state preservation under OT-019Y, the hardened package refresh under OT-019Z, external source-free UI Automation under OT-019AA, live-region delivery under OT-019AB, focus-safe card-origin F5 under OT-019AC, shown wide/minimum/wide resize preservation under OT-019AD, functional in-process ScrollItem reachability under OT-019AE, selected/unselected reverse focus traversal under OT-019AF, and production plus packaged heading semantics under OT-019AG; professional clearance and real assistive-technology acceptance remain open. Only the newest active refresh may publish; refresh clears selection and stale bundle results; selection changes invalidate prior bundle state; close invalidates all authorities; and failed/cancelled development inspection triggers best-effort termination of its owned process tree. Validation uses a unique verified system-temporary root with project-specific intermediate/output trees and exact `finally` cleanup. A repeatable PowerShell 5.1/7-compatible builder RID-restores and publishes a self-contained `win-x64` inspection-only ZIP; its manifest hashes every payload, permits USB-family/runtime-identity and bounded local bundle structure/image-digest/signature inspection, declares that no production signer is configured, rejects signature admission and authoritative hardware-profile status, and excludes source, firmware, writer/recovery tools, keys, debug symbols, and filenames exposing repository engineering names. A separate verifier matches every payload hash, launches a fresh source-free extraction, and optionally accepts its external UI Automation selection/refresh/live-event/heading workflow. The replacement package now passes that workflow against the exact one-Heltec/one-SenseCAP/one-Wio public roster. OT-019AI adds an independent source-free 900 x 620 probe plus three consecutive hardened integrated passes covering exact package/roster verification, three Refresh cycles, real `ScrollItem`, no-error states, privacy, and zero-residue cleanup after a verifier-only timing race was corrected with a complete settled-state wait. Injected packaged failure/recovery remains a separate gate. No production signer/trust state, authoritative received-board profile/low-level probe, admission composition, writer, erase/reset/DFU/recovery path, or mutation authority exists. Physical key injection, focus appearance across live themes, Narrator, real monitor transitions, live Windows contrast-theme switching, visible physical-input refresh, attorney clearance, installer lifecycle, clean-machine operation, code signing, public distribution, signed releases, target boot/rollback/recovery, and physical update evidence remain |
| OT-019I | done | Bounded Windows firmware-bundle candidate inspector | The Windows utility selects only `.fwbundle` files and opens them read-only without displaying or retaining the path. The adapter caps the ZIP at 20 MiB, requires exactly `manifest.json`, `image.bin`, and `manifest.sig`, enforces a 4 KiB canonical UTF-8 manifest with fixed property order/types/bounds, caps the image at 16 MiB, streams the complete image through SHA-256, and requires its exact signed length/digest. The initial seven groups accepted one structural ESP32-S3 complete-client candidate and rejected wrong extension, extra entries, noncanonical bytes, digest mismatch, all-zero signature, and image-length mismatch. OT-019J freezes the signature field at 384 bytes and adds cryptographic verification; OT-019Q consumes the manifest fields for exact selected-device matching. Admission, write, erase, reset, reboot, DFU, rollback, and recovery remain absent |
| OT-019J | done | RSA-PSS firmware-bundle verifier and trust boundary | The Windows inspector requires `rsa_pss_3072_sha256`, signs the exact canonical manifest, derives a 16-character lookup ID from SHA-256 of public SubjectPublicKeyInfo, and verifies with the complete pinned RSA-3072/exponent-65537 public key. Its immutable catalog accepts at most three public keys, contains no private material, and is deliberately empty in the package. Five initial groups prove trusted ephemeral verification and fail-closed empty/unrelated catalogs, signature or signed-manifest tampering, and wrong-size keys. OT-019K adds the fixed vector and raises the suite to 44. Production key custody, approved pinned keys, protected revocation/generation state, signed releases, admission composition, exact-board authority, writer, and physical recovery remain absent |
| OT-019K | done | Fixed public cross-tool signature vector | One public-only fixture binds the exact canonical manifest, RSA-3072 public SubjectPublicKeyInfo, 384-byte signature, signer ID, manifest digest, deterministic fixture-image digest, and 32-byte PSS salt. No private key is stored. The real loader, OpenSSL 3.5.6, Espressif `espsecure` 5.3.1 Secure Boot v2, and `cryptography` 50.0.0 verify the same bytes; OpenSSL/Espressif reject manifest tampering. Pinned requirements and the command are wired into host validation, with public Actions confirmation pending the next push. Independent security review, production custody/pinning/revocation, target Secure Boot configuration, exact-board admission, write, and recovery remain absent |
| OT-019L | done | Candidate-only connected hardware profile guidance | Both inspection producers now add one strict profile-evidence object for each card. Exact allowlisted USB/runtime evidence, a vendor-published family baseline, and the deliberate maintenance/received-revision step are separate fields. The current Heltec runtime resolves only to the WiFi LoRa 32 V4 family candidate, the SenseCAP runtime only to the Solar Node family candidate, and the Wio runtime only to the Wio Tracker L1 family candidate. All three remain `Runtime candidate only`, require a deliberate future maintenance or DFU/bootloader session, and keep `authoritative_for_flash=false`; an unidentified USB device receives no restart offer. Three live refreshes remain three inspected and zero ready without reset, write, identity, pairing, or coordinate capture. The updated Python checks and 59-group warning-free Windows suite pass. Exact received revisions, low-level processor/memory/bootloader evidence, RF variant, approved product roles, and every writer/recovery gate remain open; the current package externally passes the same public Wio roster without granting authority |
| OT-019M | done | Fail-closed maintenance-attempt policy and visible recovery warning | A pure Windows-loader policy permits at most one future supervised low-level attempt per session, requires explicit operator disruption confirmation before that attempt, blocks retry after the attempt, and requires verified normal runtime recovery following failure. Both inspection producers expose the one-attempt limit and visible recovery caution; unknown hardware has zero attempt authority. Four Python groups and all 45 warning-free Windows loader groups pass. No action consumes the policy: the app still has no reset, line-toggle, esptool, DFU, erase, write, reboot, recovery adapter, or authoritative board profile, and V1 progress is unchanged |
| OT-019N | done | Device-snapshot-bound local bundle inspection | One independent revision authority requires a current validated device snapshot before local bundle inspection, permits only one active result, and invalidates that result immediately when device refresh begins or the window closes. The UI clears prior bundle details on refresh, disables selection until a new snapshot publishes, and cannot display an old async result after connected-device state changes. The warning-free Windows suite passes 46 groups. This is stale-state prevention only; OT-019Q adds the separate exact-device matcher, while release admission, trusted production signer, writer, and every device mutation remain absent |
| OT-019O | done | Heltec maintenance-failure manual recovery observation | One supervised no-stub read-only ESP32 `chip-id` attempt on assembled client `OT-DEV-002` failed at host serial configuration before ROM connection; no stub, read, erase, write, eFuse, or firmware action occurred, and no retry was made. Normal MeshCore runtime temporarily stopped answering. After USB reconnection/re-enumeration, the unit returned with the same firmware, role, repeat setting, and radio configuration plus zero errors/queue/receive errors, and the complete bench view again showed three runtime-identified and zero ready. This proves manual recovery for one unit on this host only; automatic ROM entry, exact low-level identity, authoritative profile, and supported update/recovery remain unresolved |
| OT-019P | done | Explicit snapshot-bound device selection | The Windows 95-style connected-card surface is a keyboard-accessible single-selection list with a visible selected border and live status that says selection is not Flash permission. A pure authority accepts only reduced generic candidate ordinals from the current validated snapshot, stores no transport/hardware/identity data, rejects stale/unknown candidates, clears on refresh/close, and invalidates prior bundle state when selection changes. Bundle inspection requires both the current snapshot and one selection; every Flash control remains disabled. The warning-free Windows suite passes 47 groups. OT-019Q consumes that current selection without adding admission, writer, or physical-update authority |
| OT-019Q | done | Exact selected-device firmware-bundle match | One pure matcher accepts only a separately authoritative received-unit profile and compares its hardware-profile ID, processor, product role, received board revision, bootloader schema, and maximum image bytes with the inspected manifest. Runtime model/role, USB family, vendor baseline, profile-candidate text, and selection alone never create authority; all three live cards therefore remain unmatched. Each exact mismatch fails closed, while a complete match still grants neither release admission nor Flash permission. The warning-free Windows suite passes 48 groups. Authoritative received-unit profiles, production signer/revocation/generation state, admission ownership, writer/readback, boot/rollback, and physical update remain absent |
| OT-019R | done | Deterministic Windows loader rendered-layout acceptance | An opt-in STA renderer composes the real production `MainWindow` with a validated privacy-safe three-device document and captures 1600×900 desktop, 900×620 minimum, and scrolled-minimum views without opening a transport. Pixel review exposed and fixed a transparent root that hid summary copy and an unconstrained list that clipped the third card. Desktop now shows all three cards; minimum width wraps two-plus-one and vertical scrolling reaches the third while fixed bundle/safe-mode footers and disabled Flash labels remain readable. The complete 111-executable host matrix, Python/publication-safety checks, cross-tool vector, 48 loader groups, and a fresh verified 464-file local package pass. This is rendered layout/resize evidence only; keyboard/Narrator, high-DPI/system-theme, repeated live refresh, installer/clean-machine, writer, and physical update acceptance remain open |
| OT-019S | done | Production-window repeated refresh/selection state acceptance | An internal dependency seam preserves the public WPF constructor while letting host acceptance supply a controlled validated read-only inspection function to the real `MainWindow`. Three cycles each republish three cards, clear stale selection, disable bundle selection until one current card is chosen, restore Refresh, preserve zero-ready status, then bind bounded bundle inspection to the new selection. The run exposed and fixed a stale post-inspection footer. An opt-in follow-up runs the packaged Windows USB/runtime adapter through the production window: three consecutive live reads each republished all three bench candidates, kept zero ready, cleared selection, disabled bundle selection, and restored Refresh. Visible mouse/keyboard input, Narrator, installer/clean-machine, writer, and physical update acceptance remain open |
| OT-019T | done | Explicit per-monitor DPI configuration and scaled-render acceptance | The production WPF executable embeds one manifest declaring `PerMonitorV2` plus the legacy `true/pm` fallback. The real minimum-size window renders deterministically at 125%, 150%, and 200% pixel density with exact dimensions/DPI, nonblank production pixels, an intact scroll viewport, and measurable Refresh and firmware-selection controls. Pixel review accepts the selected-device copy, visible device cards, bundle blocker, safe-mode boundary, and disabled Flash label at each profile. The warning-free suite passes 50 groups, the live precheck remains three runtime-identified/zero ready, and a fresh verified 464-file package passes. This is scaled-render evidence only; real monitor transitions, alternate system/high-contrast themes, keyboard/Narrator, clean-machine, writer, and physical update acceptance remain open |
| OT-019U | done | System-aware contrast palette and deterministic theme acceptance | Production controls consume semantic dynamic brushes. Classic mode preserves the approved gray/navy Windows 95 palette; high-contrast mode maps paired window/text, control/text, highlight/highlight-text, and disabled-text values from current WPF `SystemColors`, and accessibility/color/visual-style/window preference changes reapply them on the UI dispatcher. A deterministic black/white/yellow production render requires at least 7:1 body/disabled contrast and retains three cards/selection/scroll access. A focused follow-up found the classic disabled pairing at 3.79:1 and darkened the brush to `#404040`, above 5.5:1. A stricter shown-window pass then found local button foregrounds could override the shared disabled trigger. All production buttons now inherit that template, and a six-state classic/high-contrast matrix enumerates every disabled card/footer/bundle/Refresh instance in ready-unselected, ready-selected, and held-refresh states, measuring opaque resolved foregrounds against each actual rendered `ButtonBorder` surface at >=4.5:1 classic and >=7:1 deterministic high contrast. This is resolved WPF control/template evidence, not final pixel antialiasing, live activation of every Windows theme, keyboard/Narrator, clean-machine, writer, or physical-update evidence |
| OT-019V | done | Production-window Tab focus traversal | A second real production `MainWindow` publishes and selects one validated card, then exercises WPF focus traversal. The first run exposed noninteractive blocker `ItemsControl` content in the Tab sequence. Blocker content is now nonfocusable and the connected-device list is one Tab group. The accepted order is Refresh, current device-list selection, enabled bounded bundle action, then back to Refresh; disabled card and footer Flash actions are skipped. The warning-free suite passes 52 groups. Physical key injection, arrow-key selection, focus appearance across live themes, Narrator announcements, and assistive-technology review remain open |
| OT-019W | done | Production-window UI Automation semantics | The actual production peers expose Window, Button/Invoke, List/single-optional-Selection, and ListItem/SelectionItem contracts. The first run found device items announcing the CLR model type without blocker help and mutable regions exposing fixed labels instead of current status. Accessible summary/help now live on each selectable item; summary, safety, selection, bundle, and error peers expose current visible messages; disabled Flash remains unavailable with a name and blocker help. UI Automation selection reaches only the bounded current-device workflow and internal candidate ordinals remain absent from item name/help. The warning-free suite passes 53 groups. Narrator speech/timing/verbosity, Braille, other assistive technologies, and physical desktop input remain open |
| OT-019X | done | Wide and wrapped device keyboard navigation | Routed WPF key acceptance on the shown production window covers native Right/Left across the wide three-card row and native Down/Up across the 900×620 two-plus-one wrap. The first wrapped run found native End could remain at item zero because the horizontal wrap panel resolved a row edge rather than the final item. The list now handles only Home/End, selecting, scrolling to, and focusing the exact first/last current card while status and bounded bundle availability follow selection; native spatial arrows and the accepted Tab cycle remain intact. Routed F5 reaches the same bounded production command, republishes three cards, clears selection, disables bundle selection, and restores Refresh. The suite remains 53 groups with expanded keyboard-path assertions. Physical key injection, keyboard hardware/layout variants, Narrator, and other assistive technologies remain open |
| OT-019Y | done | In-window theme-transition state preservation | The shown 900×620 production window applies classic, deterministic high contrast, then classic again through the same dynamic-resource path without recreation. The selected and keyboard-focused last wrapped card, vertical scroll offset, actual UI Automation item name, bounded bundle availability, disabled Flash action, and zero-ready summary remain unchanged. Each device container now uses the explicit accessible focus visual. Pixel review found the initial deterministic focus color duplicated the yellow selection outline; focus is now white while selection remains yellow. The first opt-in live-refresh rerun also found an eager application focus-style lookup prevented its supported pre-application fixture window from loading; dynamic resolution now keeps both construction paths valid. Fixed classic/high-contrast minimum renders were reviewed and the suite passes 54 groups. This is in-window WPF state/render evidence, not actual Windows theme switching, OS notification timing, every system/custom theme, physical input, or Narrator acceptance |
| OT-019Z | done | Accessibility-hardened utility package refresh | The post-acceptance source was RID-restored and published as a new self-contained `win-x64` engineering ZIP. The builder and a separate clean verifier each extracted the exact archive, matched all 464 payload lengths and SHA-256 hashes, enforced the inspection-only capability and forbidden-payload boundaries, and launched the source-free executable. The current retained local archive is 72,103,538 bytes with SHA-256 `133A4E133A78D0CE789873B6E43226EAA455B59EDFF81D8DCF4369C172DED2C5`; Windows PowerShell 5.1 also passed three external UI Automation selection/Refresh/live-region/heading cycles against the exact public roster `Heltec V4 OLED`, `SenseCAP Solar`, and `Wio Tracker L1`. A non-remediating Defender scan inspected the exact ZIP with exclusions ignored and found no threats. It is Git-ignored and not a public release. Installer install/upgrade/uninstall, clean-machine operation, code signing, distribution, production trust, writer/recovery, and physical update acceptance remain open |
| OT-019AA | done | Source-free external UI Automation and refresh acceptance | Stable privacy-safe IDs expose the exact packaged Window, Refresh, dynamic summary/selection/bundle regions, device list, bounded bundle action, and disabled Flash action to a native out-of-process Windows UI Automation client. From a unique extraction, all three device peers expose ListItem/SelectionItem/ScrollItem contracts, public names and blocker help, disabled per-card Flash, and distinct 1/3, 2/3, and 3/3 positions without candidate, COM, or MAC-like identifiers. Three external cycles select a current item, verify only bounded bundle inspection becomes enabled, invoke read-only Refresh, and require empty selection, the same privacy-safe public-name multiset across all three republished items, zero ready, bundle selection disabled, and every Flash action disabled. Owned process/temp cleanup passes. Physical keyboard/mouse, Narrator speech/timing, Braille/other AT, installer, and clean-machine evidence remain open |
| OT-019AB | done | Source-free live-region event delivery | A compiled Windows UI Automation client subscribes to the exact packaged window's LiveRegionChanged subtree without a PowerShell callback runspace. Three cycles require current bundle and selection events, then selection-cleared, bundle-waiting, inspection-busy, bundle-reset, final zero-ready, and selection-prompt events in order; names equal settled properties, allow only the three public region IDs, reject private identifiers/callback errors/stale events, and tolerate only adjacent exact duplicates. The exact handler is removed before owned process/temp cleanup. Windows PowerShell 5.1 and the 464-file source-free package pass. This proves native event delivery, not Narrator wording/timing/verbosity, Braille, or another AT's UX |
| OT-019AC | done | Focus-safe F5 from a device card | Routed F5 now starts from the focused last card in the wrapped production layout. Refresh destroys that item container, so the window records whether focus was on Refresh or the device list and returns focus to enabled Refresh when the current refresh completes. Success restores three unselected cards, disabled bundle/Flash state, and next-Tab access to the new list; an acceptance-only failure restores focused Refresh with an empty list, visible unavailable error, and all actions blocked. Initial-load focus is not stolen. Physical key injection, layout/hardware variants, and Narrator remain open |
| OT-019AD | done | Shown wide/minimum/wide resize transition | A shown production window transitions from its effective wide size to the 900×620 minimum and back with geometry matched to realized host capacity; window width and viewport change materially and restore. On the local 1920×1080 display, exact 1120×760 → 900×620 → 1120×760 produces one row → two-plus-one wrap → one row. The stricter unassisted run found minimum reflow could leave the focused third card below the viewport and drop focus; resize now defers until layout settles, verifies the captured selection remains current, brings that container into view, and restores focus. With no post-resize test-side scrolling, the same selected model/index/container, public item name, visible status, three list-item peers, enabled bounded bundle action, disabled Flash action, viewport intersection, and finite/in-range scroll survive. Deferred updates also preserve a focus move to Refresh or a nonselected card instead of reclaiming it. The suite passes 55 groups. Physical resize input, real monitor/DPI transitions, Narrator, and clean-machine operation remain open |
| OT-019AE | done | Functional UI Automation ScrollItem reachability | In a shown 900×620 production window, the third wrapped device card begins wholly outside the outer content viewport and its actual peer reports offscreen. Invoking that peer's real `ScrollItem` provider moves the outer vertical offset into range, reveals the card, and changes the peer to onscreen without selecting a device, moving focus from Refresh, changing public name/help, or enabling bundle selection or the footer Flash action; the zero-ready summary remains unchanged. The warning-free suite passes 56 groups. This is deterministic in-process production-peer invocation, not packaged out-of-process scrolling, Narrator/Braille/other AT UX, physical wheel/touch/mouse input, virtualization or larger candidate sets, installer, or clean-machine evidence |
| OT-019AF | done | Selected and unselected reverse focus traversal | A fresh shown 900×620 window exercises WPF Previous traversal in both conditional states. With card three selected, focus moves Refresh → enabled bundle action → the exact selected card → Refresh without changing selection/model/status. With no selection, focus moves Refresh → device list → Refresh, leaves selection empty, and keeps the bundle action disabled. Both paths skip all three disabled card Flash controls and the disabled footer Flash action. The suite passes 57 groups. This is deterministic WPF focus traversal, not physical Shift+Tab/key injection, keyboard-layout, Narrator, or clean-machine evidence |
| OT-019AG | done | Production and packaged UI Automation heading hierarchy | The visible production structure now exposes one public level 1 utility-title heading, level 2 current inspection/bundle/safe-mode headings, and one level 3 public display-name heading per connected device. Exact order, level, Text control type, dynamic selection/refresh names, and absence of candidate/COM-like copy pass in-process. A freshly extracted source-free package passes the same hierarchy before selection, after selection, and after each of three external Refresh cycles under Windows PowerShell 5.1. The suite passes 58 groups. This does not prove Narrator/Braille heading commands, spoken order/verbosity, other AT UX, installer, or clean-machine operation |
| OT-019AH | done | Failed-refresh accessibility recovery | A controlled production-window failure exposes one current visible assertive error peer with nonempty public text, Automation ID, and help. The following successful refresh must hide that peer, clear its text/ID/help, set its live setting Off, remove stale error content from the recovered automation tree, republish the expected one-Heltec/one-SenseCAP/one-Wio roster, clear selection, keep every Flash action disabled, and report all three runtime-identified with zero ready. The first run found the collapsed peer retained stale assertive content; production state clearing fixed it. The warning-free suite passes 59 groups and three built-in live refreshes; the replacement package passes its normal exact-roster external UI Automation workflow. Packaged failure injection, Narrator/Braille behavior, installer, and clean-machine acceptance remain open |
| OT-019AI | done | Source-free minimum-window ScrollItem and recovered-tree acceptance | An independent source-free probe passed against the exact retained package and public Heltec/SenseCAP/Wio roster at 96 DPI. UI Automation resized the real window to 900 x 620, then the Wio card's actual `ScrollItem` changed it from offscreen at x=425/y=1085 to onscreen at x=425/y=254 without changing its 342 x 635 size. Refresh focus, empty selection, disabled bundle/Flash authority, enabled Refresh, and the exact three-found/three-runtime/zero-ready summary held; `inspection-error` was absent before/after, and cleanup left zero owned processes/temp directories. The first integrated attempt exposed a verifier-only combined-assertion timing race, not an established product defect; a complete settled-state wait fixed it. Three consecutive hardened runs—one two-repeat batch plus one reviewer-hardened run—then each passed the exact 464-file package, duplicate-sensitive exact roster, three Refresh cycles, minimum-window `ScrollItem`, initial/selected/post-refresh no-error checks, privacy, manifest/hash/launch, and zero-residue cleanup. The full host suite and Windows PowerShell 5.1 parse/publication-safety checks pass. No package change, physical input, Narrator, clean-machine, writer, recovery, compatibility, or V1-progress claim is made |

Current naming note: Decision 0008 supersedes the earlier `Limited Underground
Trail Device Utility` working display recorded in OT-019H. Current source uses
`Limited Underground Firmware Loader — Preview` and visibly retains
`Inspection only` plus attorney-review-pending status. The historical packaged
artifact evidence remains evidence for the exact older package and is not
silently relabeled.

## Recommended sequence

Complete the remaining OT-003A physical/regulatory inventory and OT-005
cryptographic gates. Continue OT-020 with over-air, GNSS, label/revision,
antenna/RF/regulatory, power, BLE, and recovery evidence; preserve the explicit
gap that this unit's shipping/pre-write state was not recorded. OT-009's remaining hardware work is a
direct SX1262/OpenTrail binding and later field measurements; the
software-forced MeshCore path is proven. OT-017's physical serial/wireless
adapter, key lifecycle, replay protection, and field failure UX remain
integration gates rather than semantic-schema work. Security and regulatory
constraints must remain inputs before any public packet v1 is declared.
