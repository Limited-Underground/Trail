# OpenTrail Engineering Backlog

Statuses: `done` means the documented acceptance criteria are evidenced; `partial` means bounded evidence exists but acceptance is incomplete; `planned` means no implementation claim.

## Foundation

| ID | Status | Task | Acceptance evidence |
| --- | --- | --- | --- |
| OT-001 | done | Project bootstrap and repository structure | Self-contained directories, agent guide, README, architecture, status, and backlog exist |
| OT-001A | done | OpenTrail GitHub host validation | GitHub Actions builds three verifier/planning CLIs and runs all 45 C++ test executables plus the Python MeshCore lease, privacy-safe field/pilot, and crypto-benchmark evidence suites on `main` pushes and pull requests using bounded, least-privilege, commit-pinned Windows 2025/Python 3.13/UCRT64 automation. The current matrix includes the eight-group bounded production RAM ring, eight-group versioned update-recovery diagnostic adapter, eight-group redacted update-recovery status boundary, ten-group update-recovery transition coordinator, ten-group update-recovery save coordinator, fifteen-group update-recovery boot coordinator, twenty-group update checkpoint store, ten-group protected-reassembly, eight-group traffic-key-context, seven-group AEAD-nonce, eight-group portable-client composition, twelve-group local-interface, eleven-group power-state, eight-group secure-randomness, and eight-group monotonic-clock boundaries in addition to the ten-group outbound-counter, nine-group pilot-result/template, eight-group crypto-benchmark, ten-group protected-packet-budget, nine-group immutable-repeater, and nine-group reboot-safe repeater-replay suites. This is host/build evidence, not physical radio, target firmware/bindings, cryptography, rendered UI, or measured-radio evidence |
| OT-001B | done | Cross-project public host validation | OpenGauge GitHub Actions separately validates the shared Windows host matrix on `main` pushes and pull requests using bounded, least-privilege, commit-pinned automation. Its current-main warning-free run passes all 41 executables with zero annotations. This is cross-project host evidence, not a physical radio or OpenTrail target build |
| OT-025 | partial | Funding readiness packet | A brand-neutral public packet contains the factual project core, reusable application answers, vendor-neutral hardware template, preliminary milestone budget, qualification/submission checklist, and opportunity register. Every cash, hardware, discount, loan, sponsorship, and service-credit path is on owner-directed hold: no opportunity research for outreach, contact, submission, account activity, acceptance, shipment, or announcement is authorized. Legal applicant/payee, exact hardware freeze, dated quotes, eligibility/terms, private-document handling, and explicit owner clearance remain later gates |
| OT-002 | done | Initial architecture documentation | Layers, boundaries, failure modes, roles, and architecture gates documented |
| OT-003 | partial | Hardware abstraction contracts | Radio, GPS, logging, persistent-storage, secure-randomness, checked monotonic-clock, power-state, and local display/input contracts have deterministic fakes/tests. Portable-client target composition and the whole-contract review are host-tested under OT-003E. Concrete ESP-IDF/board bindings, target build, and on-device composition evidence remain |
| OT-003A | partial | Hardware and regulatory inventory | Both companions are runtime-confirmed as Heltec V4 OLED with MeshCore USB Companion `v1.16.0-07a3ca9`; the repeater is runtime-confirmed as Seeed SenseCap Solar. Official-source reconciliation identifies likely Heltec WiFi LoRa 32 V4 and SenseCAP Solar Node family characteristics without promoting them to exact-unit facts. The dated fail-closed US field gate requires exact labels/FCC grants, firmware mode, antennas/gain/cable loss, and configuration review. `OT-DEV-001` has ROM-level MCU/memory evidence; `OT-DEV-002` does not. Exact SKUs/revisions, RF variants, installed antennas, pinouts/power details, and grant coverage remain unresolved |
| OT-003B | done | Checked monotonic-clock boundary | A production-facing guard accepts one boot-local 64-bit millisecond source, keeps UTC separate, permits equal ticks and temporary not-ready recovery, and latches rollback or source failure without consuming later samples. Fixed saturating status counters and a bounded 16-sample FIFO fake cover zero/equal/forward/full-range time, continuity, rollback, source failure, new-boot composition, and capacity/order across eight groups plus 100 repeats. The complete 30-executable matrix passes locally. ESP-IDF timer/task binding, native-wrap handling, deep-sleep/brownout policy, resolution/latency/drift, long-run continuity, and physical failure injection remain target gates |
| OT-003C | done | Fail-visible power-state boundary | One atomic adapter observation keeps source readiness, external power, battery presence, orthogonal charge state, optional normalized percentage/voltage, and monotonic age separate. Composition must inject distinct low/critical percentage and nonzero freshness policy; the common component never estimates percentage from voltage. Eleven groups plus 100 repeats cover exact bands, charging, external-only, missing, fault, invalid, future/stale, and bounded FIFO behavior in the complete 31-executable local matrix. Board adapter, charger/shutdown control, hardware thresholds, endurance, rendered UX, and physical power evidence remain target gates |
| OT-003D | done | Revision-bound local display/input boundary | A fixed semantic frame carries screen role, attention/notice, radio/position/power indicators, optional peer count, unread count, and up to four unique action slots without pixels, coordinates, GPIOs, free text, or identity-bearing content. Only an atomically presented increasing revision becomes active; normalized input must match it exactly. Canonical critical confirmation requires confirm/cancel order plus hold. Twelve groups plus 100 repeats cover capabilities, frame validation, display failure/retry, revisions, stale/disabled/invalid input, critical/system-fault rules, and bounded fakes in the complete 32-executable matrix. Target renderers/input adapters, localization/accessibility/readability, resource/power performance, distracted-driving policy, and physical evidence remain |
| OT-003E | done | Portable-client composition preflight and whole-contract review | Ten non-owning target endpoints cover radio, GPS, log sink, 64-byte multi-domain storage, separate 704-byte replay storage, entropy, monotonic clock, power, display, and input. Pure shared power/display validators plus explicit MTU/action/hold policy aggregate every structural issue while querying only radio MTU and performing no mutable adapter I/O. The whole-contract audit found and corrected the separate replay-storage obligation. Eight groups pass in the complete 33-executable matrix. Concrete adapters, ESP-IDF target/task/build, protected storage, and physical evidence remain OT-003 gates |
| OT-020 | planned | Wio Tracker L1 Pro compatibility | A non-destructive arrival procedure and read-only Windows preflight tool cover exact label/SKU/revision, shipping firmware preservation, USB/UF2 recovery enumeration, redacted BLE settings, GNSS current/stale/loss behavior, bounded Heltec interoperability, cleanup, and recovery. No unit has arrived or been tested |
| OT-015 | done | Diagnostics/logging foundation | Fixed-capacity ERROR/WARN/INFO/DEBUG/TRACE logger demonstrates compile/runtime filtering, monotonic timestamps, component tags, redaction, truncation/sanitization, test sink, and counted backpressure in seven host scenarios. A production-facing RAM ring retains the newest 32 canonical records, assigns boot-local sequences, snapshots oldest-first without partial output, preserves order across clear, and counts rollover/rejection across eight groups plus 100 repeats. The `OTRD0/v0` recovery adapter adds one versioned 32-bit, generation-free redacted event with exact fixed text, coherence validation, severity mapping, filtering, sink-failure evidence, and real ring capture across eight groups plus 100 repeats. Target task binding/concurrency, persistent audit/export, and physical capture remain |

## Transport and protocol

| ID | Status | Task | Acceptance evidence |
| --- | --- | --- | --- |
| OT-004 | done | LoRa transport abstraction | Fixed-capacity opaque-frame contract covers MTU, errors, metadata validity, cooperative state, queues/counters, and a deterministic two-node fake without protocol coupling; eight host scenarios pass |
| OT-005 | partial | Node identity and group model | Identity/name/alias/membership boundaries and threat model are documented; eight host lifecycle/collision scenarios pass. A dated candidate review fixes libsodium-first target benchmarking, mbedTLS/PSA and Monocypher comparisons, Noise-C reference-only status, separate Ed25519/X25519 purposes, and conditional Noise XK invitation direction without making a selection. A rollback-safe counter lease, exact fail-closed 96-bit nonce packing, and canonical purpose-separated traffic-key context are host-tested under OT-005A/D/E. Exact target benchmark and KDF, entropy adapter, handshake/alias implementation, administrator recovery, protected rollback-resistant persistence, packet-v1 AEAD/AAD, and physical join/revoke/reset evidence remain |
| OT-005A | done | Rollback-safe outbound counter leasing | A fixed 64-byte `OTCN` record and separate two-slot persistence domain commit a nonoverlapping 64-bit range before any counter is returned. The fixed-memory allocator reserves before first/renewed use, wastes unused ranges on restart, binds state to a 128-bit domain plus group epoch, omits ordinary reset, and fails closed on mismatch, corruption, conflict, future version, exhaustion, I/O, and five mutation boundaries. Ten host groups pass locally and on public `main`. Domain derivation, protected target storage/rollback, AEAD composition, and physical power-cut/endurance remain OT-005/target gates |
| OT-005D | done | Lease-bound AEAD nonce composition | A fixed 12-byte result requires matching nonzero 128-bit lease/key domains and a nonzero rollback-safe counter, then packs the crypto-adapter-supplied 32-bit prefix plus the 64-bit counter in network byte order. Mismatch and invalid input return typed failure with zero output across seven host groups. This performs no KDF, key derivation, counter allocation, AEAD, packet-v1, or library selection; exact cryptographic domain/key/prefix derivation and target evidence remain OT-005 gates |
| OT-005E | done | Canonical traffic-key derivation context | A fixed 52-byte `OTKD/v1` public context binds a nonzero group ID, group epoch, full 32-byte authoritative sender fingerprint, and separate group-AEAD-key/nonce-prefix/counter-domain purposes in network byte order. Eight host groups cover exact bytes, purpose separation, context changes, and zero/unknown refusal. It performs no KDF or secret handling; exact target library, salt/input key material, output lengths, vectors, wiping, and epoch-secret rotation remain OT-005 gates |
| OT-005F | done | Protected-header requirements reconciliation | Review against the accepted immutable one-repeater policy found that the former 36-byte budget could not carry both its required authenticated 64-bit destination alias and fragment metadata. The corrected 44-byte profile explicitly charges destination, has no mutable TTL, raises base overhead to 60 bytes, and leaves 39 signed-group plaintext bytes at the 163-byte example MTU. Ten updated budget groups pass. Final offsets/types/flags, fragment/reassembly policy, destination privacy, signature coverage, AEAD, and packet-v1 remain open |
| OT-005B | done | Reproducible cryptographic benchmark evidence boundary | Strict `OTCB0/v0` plans require the fixed three-candidate order, exact ready target/toolchain/sdkconfig/dependency/radio fields, 100 cold and warm repetitions, and eight canonical security/lifecycle gates. Results bind to a canonical plan hash and record eight operation timing sets, build warnings, resources, watchdogs, radio cost, hashes, raw-evidence retention, and every gate. Eight groups reject false readiness, overwrite, mismatch, private text, incomplete measurements, and measured failures; they pass locally and on public `main`. The public target plan remains `draft_blocked`; no on-device benchmark or library selection exists |
| OT-005C | done | Fail-closed secure-randomness source boundary | An algorithm-neutral interface exposes typed not-ready/ready/failed entropy state, bounds requests to 1-64 bytes, and requires full output or no buffer change. A deterministic 512-byte scripted fake is isolated under test support and covers sequential single-use bytes, invalid/oversized requests, exhaustion, injected failure/retry, readiness transitions, persistent failure, and the maximum boundary across eight groups plus 100 repeats. The complete 29-executable host matrix passes locally. No ESP-IDF entropy/DRBG adapter, target readiness proof, production key generation, physical cold-start/brownout/RF-concurrency evidence, or cryptographic selection is claimed |
| OT-006 | done | Experimental packet envelope v0 | A 22-byte v0 envelope documents MTU-derived budget, version/type/flags/length/ephemeral IDs, CRC-16, rejection behavior, a standard CRC vector, and six passing codec scenario groups; it is explicitly non-production and unauthenticated |
| OT-006A | done | Protected packet candidate budget | A fixed-memory model charges authenticated header, AEAD tag, source authentication, optional forwarding wrapper, and theoretical LoRa airtime to every fragment. After destination-alias reconciliation, the base 44+16 profile leaves 103 plaintext bytes at a 163-byte example MTU; adding a 64-byte Ed25519 signed-group candidate leaves 39 and makes a 16-byte position 140 bytes/461,312 us at the bench PHY. Ten groups cover base/signed/wrapper profiles, the 64-byte alert case, 163/255 MTUs, single/three/empty payloads, exact boundary, limits, and invalid PHY locally. This is sizing evidence, not packet-v1 offsets, crypto, fragmentation policy, target MTU, or physical airtime |
| OT-006B | done | Critical-alert protected-radio feasibility | The 64-byte `OGA0` and `OGK0` semantic frames each require two 39-byte signed-group candidate fragments at the 163-byte example MTU: 312 transmitted bytes and 1,025,024 us theoretical source airtime before retries/repeater copies. Alert plus ACK is four source fragments; one repeater doubles aggregate transmission airtime. The documented gate keeps fragmentation blocked pending source-signature scope, nonce/replay/reassembly/ACK/retry policy, bounded resource behavior, target measurement, and regulatory review |
| OT-006C | done | Verified protected-fragment reassembly policy | A fixed-memory host boundary accepts only future crypto-adapter-produced verified fragments, binds group/epoch/sender/message/count, supports four concurrent sessions and up to 16x103 plaintext bytes, reorders canonically, treats exact duplicates idempotently, clears byte/count conflicts, rejects full capacity without eviction, and expires at the exact timeout. Ten groups include the 39+25-byte alert shape. Cryptographic verification, unforgeable target composition, final packet/signature/replay/retry policy, ESP32 resources, power loss, and field behavior remain open |
| OT-007A | done | Two-node transport characterization | USB preflight and raw-RX authentication checks passed. A temporary private-channel sample delivered 5/5 numbered messages each direction with 0 loss, 0 duplicates, 233.3-247.2 ms latency, 11.25-12.25 dB SNR, exact +5 TX/+5 RX counters per node, zero errors, and empty queues. The temporary channel was erased and verified empty. RSSI remained invalid and airtime remained whole-second resolution; these are recorded measurement limitations, not omitted evidence. |
| OT-007 | done | Two-node message proof of concept | Two host integration scenarios pass; C++-encoded v0 frames then delivered 3/3 each direction through a temporary private MeshCore adapter with no loss/duplicates/errors, exact counter deltas, verified decode/CRC, and verified channel cleanup |
| OT-008 | done | Acknowledgement and duplicate handling | Six message-class policies, fixed-capacity delivery state, confirmation/retry/expiry/error behavior, reboot-restored duplicate window, and lost-ACK integration are host-tested. Authenticated ACK wire encoding and hardware timing remain later protocol/field gates |
| OT-008A | done | Canonical duplicate-checkpoint serialization | A fixed 672-byte `OTD0` record serializes up to 32 replay keys and remaining lifetimes with explicit little-endian fields, canonical zero padding, strict version/capacity/key/lifetime checks, duplicate-key rejection, CRC-32, and atomic output. Seven codec groups, the full 23-executable host matrix, and 100 codec/window repeats pass. Atomic durable storage, wear/privacy lifecycle, authentication, secure rollback, and physical power-loss evidence remain later gates |
| OT-008B | done | Two-slot duplicate-checkpoint host store | A fixed 704-byte `ODS0` envelope composes `OTD0` with automatic 64-bit generations, two-slot rotation/repair, readback byte/decode verification, visible single-slot recovery, and fail-closed I/O, invalid-only, conflict, and exhaustion behavior. Nine store groups, the full 24-executable host matrix, and 100 store/codec/window repeats pass. ESP32/NVS binding, physical atomicity/endurance, privacy lifecycle, authenticated integrity, trusted rollback protection, and physical power-loss evidence remain later gates |
| OT-008C | done | Context-bound duplicate checkpoint store | Fixed-size `ODS0/v1` reuses the original 704-byte slots while embedding a nonzero 64-bit group-context ID and 32-bit group epoch. Every inner key must match the outer epoch; zero, wrong-context/epoch, and legacy unbound v0 media fail closed without restore or overwrite. The coordinator also validates its store binding and returns typed bound-media/legacy service outcomes. Ten store groups, nine coordinator groups, the full 28-executable matrix, and 100 focused repeats of both suites pass locally; the exact published matrix passes in run `31374678550`. Protected target namespace, authenticated integrity, anti-rollback, authorized reset/migration, physical power-cut, and endurance remain |
| OT-009 | partial | Controlled repeater proof of concept | Eight three-node host scenarios prove bounded role/permission forwarding, exact TTL decrement, origin/reflection duplicate suppression, group isolation, queue/rate congestion limits, and broadcast/unicast behavior. A SenseCAP repeater retransmitted exactly two private flood samples. Explicit one-hop direct routes then delivered in both directions; disabling repeat caused the same route to fail with +1 direct RX/+0 direct TX and no destination message, proving the logical repeater path. A non-secret temporary-channel lease has four host recovery groups plus real stopped-session cleanup. A 300-minute alternating close-bench run delivered 300/300 with zero loss/duplicates/errors, 229.8-312.1 ms latency, exact +300 repeater flood RX/TX, repeat preserved, and verified channel/journal cleanup. A post-soak packet-v0 regression then delivered 6/6 with valid codec/CRC and verified cleanup. Authenticated routing fields, direct SX1262/OpenTrail binding, and field evidence remain |
| OT-009B | done | Immutable zero/one-repeater architecture and host-policy boundary | Decision 0004 limits the first release to direct clients plus at most one authorized repeater. A new algorithm-neutral policy requires source auth/authorization, verified context/epoch, immutable permission, and monotonic process time before replay observation, then applies duplicate, exact-byte queue, rate, and exact-expiry controls without rewriting a TTL. Nine groups and the full 27-executable matrix pass locally and on public `main`. Named-source broadcast still needs cryptography beyond common group AEAD access; protected replay persistence, target binding, reboot/power-loss, and field behavior remain |
| OT-009C | done | Reboot-safe single-repeater replay coordination | The host coordinator requires exact protected group/epoch namespace evidence, initializes an authorized empty store, restores and repairs a known-degraded two-slot `ODS0` checkpoint before operation, and persists every queued/congestion replay observation before allowing queue release. Failed/uncertain saves and unreadable media disable forwarding. Nine groups and the full 28-executable matrix pass locally and on public `main` in run `31372816356`. The ordering prevents reboot amplification but can lose a volatile queued frame after save and before transmit. Protected target binding, authenticated integrity/rollback, physical power-cut/wear, durable-outbox policy, and field evidence remain |
| OT-010 | partial | Priority/emergency messaging | Nine host scenarios plus delivery integration prove class-derived priority, reserved urgent capacity, strict preemption, rate windows, expiry, FIFO, and explicit failure/preemption events. Authenticated wire priority, measured mixed traffic, physical evidence, and rendered failure UX remain |
| OT-021 | done | Baseline group-load estimator | A fixed-memory host model combines exact LoRa airtime with member count, periodic position/status traffic, alert bursts, bounded source attempts, and explicit repeater copies. Six groups cover the four-client standalone, four-plus-repeater, and eight-plus-repeater phases, retry scaling, zero traffic, and fail-closed validation. The current one-hour profile reports 1.2727%, 2.5455%, and 5.0911% scheduled demand; collision, delivery, range, protocol-overhead, power, and regulatory evidence remain physical gates |
| OT-022 | done | Privacy-safe field-test evidence boundary | `OTFL0` converts ignored raw MeshCore soak output into atomic aggregate public JSON under neutral role labels, rejects transport ports/serials/addresses/keys/secrets/PINs/channel names/coordinates plus incoherent counts, and refuses implicit overwrite. Four host groups pass. A new two-client-plus-repeater burst delivered 30/30 at a two-second interval with zero loss/duplicates/new errors, exact +30 repeater flood RX/TX, repeat preserved, 2/2 cleanup, and a validated public summary. This is close-bench logging evidence, not four/eight-client field capacity |
| OT-023 | partial | Four-person standalone pilot v0 | `OTFP0/v0` fixes four identical clients, zero infrastructure dependency during the session, three one-hour environment classes, 300 message origins, 900 peer-delivery opportunities, privacy-safe evidence, and provisional fail gates. Two host groups reject incorrect math and false readiness. The plan remains `draft_blocked` until one exact client model/firmware is frozen across four units with battery, enclosure, GNSS, display, local input, and USB recovery; no live four-person session has run |
| OT-024 | partial | Deterministic four-person pilot verdict | Strict `OTPR0/v0` aggregate results produce pass/fail/ineligible/invalid verdicts against one ready `OTFP0/v0` plan. Fail-closed template generation refuses blocked plans, unlisted scenarios, and overwrite while preloading frozen public configuration and leaving evidence claims false; all nine groups pass publicly. Live four-person evidence remains |

## Location, groups, and persistence

| ID | Status | Task | Acceptance evidence |
| --- | --- | --- | --- |
| OT-011 | done | GPS abstraction | Fixed-unit provider contract, optional-field validity, no-fix, validation, exact stale boundary, no-UTC boot, refresh recovery, and monotonic-time rejection pass nine deterministic host scenarios |
| OT-012 | done | Position broadcast format | Fixed 16-byte current/stale/unknown payload, conservative age/accuracy, canonical rejection, 38-byte packet integration, and theoretical airtime/cadence budget pass eight codec, one transport-integration, and four airtime scenario groups. Authentication/privacy UX, scheduler, direct-radio hardware airtime, contention, and regulatory evidence remain later gates |
| OT-013 | done | Group membership/joining | Algorithm-neutral, fixed-capacity lifecycle and operator UX specify administrator-gated single-use invitations, four authentication obligations, separate promotion, epoch-advancing revoke/rekey, revoked-identity exclusion, last-admin protection, and reset/recovery boundaries; twelve host scenario groups pass. Exact cryptography, persistence, rendered UX, and physical multi-device evidence remain OT-005/OT-014/field gates |
| OT-014 | done | Persistent configuration | Two fixed 64-byte slots provide version/schema checks, CRC-32, commit-last recovery, generation selection/conflict/exhaustion, v1-to-v2 migration, safe defaults, structural secret-domain separation, no-op/rate/alternating-slot wear controls, and five power-loss boundaries across twelve host scenario groups. CRC is not authentication; ESP32 binding, secret storage, secure rollback, and physical endurance remain later gates |

## Maps and integration

| ID | Status | Task | Acceptance evidence |
| --- | --- | --- | --- |
| OT-016 | planned | Offline map architecture research | Licensed sources, attribution, format, renderer, storage/RAM, transfer, corruption, and update prototypes compared |
| OT-017 | done | OpenGauge alert interface specification | Mirrored specifications and three normative fixtures define an explicit 64-byte frame for seven alert types, canonical units, assert/clear lifecycle IDs, optional UTC/value fields, and CRC-32. Independent OpenGauge exporter and OpenTrail ingress codecs round-trip the same bytes. Nine ingress scenario groups enforce semantic, trust/authorization, stale/future, duplicate/conflict, fixed-rate/emergency-reserve, monotonic-time, and fail-closed producer-capacity policy; four producer groups reject invalid exports. Physical transport authentication and field delivery remain later integration gates |
| OT-017A | done | Critical-alert acknowledgement interface | Independent OpenTrail and OpenGauge codecs encode/decode an explicit 64-byte `OGK0` accepted/rejected ACK containing consumer/producer/event/condition/lifecycle, boot session, sequence, observed age, canonical reserved bytes, and CRC. Three mirrored normative vectors and four scenario groups per repo pass full host matrices plus 100 repeats. Transport authentication/authorization, replay window/persistence, delivery-controller/outbox composition, and physical end-to-end ACK delivery remain |
| OT-017B | done | Final-ingress-to-ACK responder | A bounded responder converts only final alert-ingress decisions into `OGK0`: accepted and identical duplicate become accepted/none; authenticated unauthorized/stale/conflict/rate decisions map to canonical rejection reasons; malformed, unauthenticated, producer-mismatched, and clock-rollback input is suppressed. It adds monotonic elapsed age and advances boot-session sequence only after successful encoding. Eight host groups plus 100 repeats cover lifecycle, correlation/decode, duplicate recovery, negative mapping, suppression, inconsistent state, age/clock, and sequence wrap. OT-017C supplies boot-session allocation; authenticated response transport, per-session sequence persistence, full round-trip composition, and physical delivery remain |
| OT-017C | done | Durable ACK responder boot-session allocator | A fixed 64-byte `OTAS` record stores exact consumer ID, authorization epoch, nonzero boot session, generation, canonical reserved bytes, CRC, and a commit-last marker in a separate protocol-state domain. Two-slot allocation increments the session before returning it, alternates slots, verifies readback, and requires explicit reset/reseed for identity/epoch change. Ten host groups plus 100 repeats cover first use/responder composition, increment/rotation, explicit rebind, corruption, equal conflict/future version, exhaustion, reads/requests, all five power-loss mutation boundaries, uncommitted/malformed state, and reset failure; affected responder/configuration suites repeat 100 times. Per-session sequence, target storage, secure rollback, OpenGauge rebind, authenticated transport, and physical power-loss evidence remain |
| OT-017D | done | Host-mediated physical alert/ACK round trip | Two role-reversed close-bench cycles carried 2/2 exact 64-byte `OGA0` alerts and 2/2 responder-produced correlated 64-byte `OGK0` ACKs over temporary MeshCore channel text. Every correlation field passed; loss, duplicates, and new errors were zero; total round trips were 1009.6-1014.0 ms; the SenseCAP recorded exact aggregate +4 flood RX/+4 flood TX and stayed repeat-on; 4/4 endpoint cleanup checks passed with no journal remaining. The host supplied authenticated/authorized context, so on-device authenticated transport, direct SX1262 binding, forced-route evidence for this payload, failure injection, outbox completion, and field behavior remain later gates |
| OT-017E | done | Cross-project physical ACK completion composition | Two strengthened role-reversed cycles delivered exact `OGA0`/`OGK0` frames with zero loss/duplicates/errors and 1005.3-1014.2 ms round trips. Each returned ACK passed the real OpenGauge peer authorization, session, replay, and exact correlation ingress and completed its reconstructed in-flight outbox; final queued/in-flight counts were zero. SenseCAP aggregate was exact +4 flood RX/TX and 4/4 cleanup passed. Persistent live target state, protected keys, direct transport, negative/failure/restart/revoke cases, and field evidence remain |
| OT-017F | done | Physical stale-rejection policy | Two role-reversed cycles returned exact correlated rejected/stale `OGK0` frames with zero loss/duplicates/errors and 1013.1-1014.7 ms round trips. OpenGauge processed both through authorization/session/replay/correlation but reported `outbox_completed=false`, zero delivery acknowledgements, no retry release, and explicit terminal failure. SenseCAP aggregate was exact +4 flood RX/TX and 4/4 cleanup passed. Retryable rejection and persistent failure/restart/revoke/field cases remain |
| OT-017G | done | Physical retryable rate-limit rejection | Two role-reversed cycles returned exact correlated rejected/rate-limited `OGK0` frames with zero loss/duplicates/errors and 1000.8-1008.3 ms round trips. OpenGauge processed both with zero acknowledgements/completions, released exactly one queued retry, left zero in flight, and did not record terminal failure. SenseCAP aggregate was exact +4 flood RX/TX and cleanup passed 4/4. Persistent backoff/retry completion and failure/restart/revoke/field cases remain |
| OT-017H | done | Physical rate-limit retry to accepted completion | Two role-reversed four-leg sequences returned rate-limit rejection, enforced not-ready at 25 ms and byte-identical retry readiness at 26 ms, retransmitted the same alert, and completed only after an accepted next-sequence ACK. Both finished with one acknowledgement, one recorded remote retry, zero queued/in-flight/terminal failures, zero application duplicates/new radio errors, exact aggregate +8 SenseCAP flood RX/TX, and 4/4 cleanup. State was reconstructed after both responses; durable live/restart/field cases remain |
| OT-017I | done | Live OpenGauge state across physical retry lifecycle | A single OpenGauge process started with one real in-flight entry before each first radio send and retained authorization/replay/outbox state across rate-limit rejection, exact backoff, same-frame physical retry, and final accepted ACK in both endpoint roles. Both finished with one acknowledgement/retry, zero queued/in-flight/terminal failures, zero application duplicates/new radio errors, exact aggregate +8 SenseCAP flood RX/TX, and 4/4 cleanup. Durable restart/power-loss and on-device/field cases remain |
| OT-017J | done | Cross-project outbox restart reconstruction | OpenGauge's canonical 640-byte `OOC0` checkpoint is now wired into boot-only atomic live-outbox import/export. Five integration groups reconstruct exact queued retry readiness, in-flight ACK timeout, maximum lifetime, frames/state/attempts against a new monotonic origin and fail closed on prepared, corrupt, configuration-mismatched, nonempty, or unrepresentable state. Its full 29-executable matrix and 100 focused repeats pass. Coordinated ACK/authorization generation, durable storage, power-loss, authenticated integrity, rollback, and physical/on-device restart remain |
| OT-017K | done | Canonical cross-project outbox policy identity | OpenGauge now derives the nonzero versioned `OCF0` compatibility fingerprint internally from all four outbox timers, maximum attempts, and emergency reserve; callers cannot label incompatible retry/lifetime policy as matching. A thirteenth checkpoint group, the full 29-executable matrix, and 100 focused repeats pass. Coordinated durable storage and physical/on-device restart remain |
| OT-017L | done | Coordinated cross-project recovery generation | OpenGauge's fixed 960-byte `OCR0` envelope binds the exact ACK replay/authorization checkpoint and exact outbox checkpoint to one nonzero generation with canonical padding, nested validation, outer CRC, and atomic decode. Four groups, the full 30-executable matrix, and 100 repeats pass. Live dual import, recoverable durable storage, power-loss, authentication, rollback, and physical/on-device restart remain |
| OT-017M | done | Preflighted live cross-project recovery | OpenGauge now exports both state owners into one `OCR0` generation and validates both exact boot imports on private component copies before committing either live state under exclusive ownership. Five groups, the full 31-executable matrix, and 100 repeats restore exact retry readiness and replay state while proving invalid generation, stopped/prepared state, policy/authorization mismatch, and corruption cause no partial live mutation. Two-slot storage and physical/on-device restart remain |
| OT-017N | done | Recoverable cross-project recovery store | OpenGauge's two-slot host store writes exact coordinated `OCR0` generations with full readback verification, restores the newest unique valid slot, preserves the prior good generation across partial/corrupt writes, exposes degraded I/O, and fails closed on conflicts or policy/authorization mismatch. Eight groups, the full 32-executable matrix, and 100 repeats pass. Target NVS, physical power-cut/wear, authenticated integrity, rollback, and on-device restart remain |
| OT-017O | done | Store-owned recovery generation allocation | OpenGauge's normal recovery save now allocates highest-valid plus one, starts at 1 on empty media, rotates slots monotonically, rejects unreadable/conflicted baselines, and reports 64-bit exhaustion before export/write. Ten store groups, the full 32-executable matrix, and 100 repeats pass. Factory-reset authority, target NVS, physical power-cut/wear, authenticated integrity, rollback, and on-device restart remain |
| OT-017P | done | Uncertain recovery commit reconciliation | OpenGauge marks every storage write error commit-uncertain with intended slot/generation. Sixteen interrupted-overwrite lengths spanning `OCR0` layout boundaries recover the prior good generation; a complete write followed by I/O failure reconciles as committed on boot. Twelve store groups, the full 32-executable matrix, and 100 repeats pass. Target-backend atomicity, physical power-cut/wear, authenticated integrity, rollback, and on-device restart remain |
| OT-017Q | done | Cross-project peer-authorization restart boundary | OpenGauge's fixed 256-byte `OPA0` record preserves up to eight active/revoked logical peers, role permissions, channel, opaque key handles, and authorization epochs. Export refuses pending approvals; boot-only import is canonical, policy-bound, and atomic. Eight groups, the full 33-executable matrix, and 100 repeats pass. Coordinated `OPA0`/`OCR0` restore, protected target storage, authenticated integrity, rollback, and physical power-loss evidence remain |
| OT-017R | done | Cross-project recoverable peer-authorization store | OpenGauge's fixed 288-byte `OPS0` envelope and two-slot host store wrap exact `OPA0`, allocate monotonic generations, verify exact readback, preserve the prior good generation across ten interrupted-write boundaries, reconcile a full write followed by I/O error at boot, expose degraded reads, and fail closed on conflict/exhaustion. Ten groups, the full 34-executable matrix, and 100 repeats pass. Coordinated authorization/replay/outbox restore, protected target storage, authenticated integrity, rollback, and physical power-loss evidence remain |
| OT-017S | done | Cross-project atomic alert-system recovery | OpenGauge's fixed 1280-byte `ORS0` record binds exact `OPA0` peer authorization and exact `OCR0` ACK/outbox state to one generation. Dependency-correct private registry/outbox candidates and a temporary ACK ingress preflight epoch, policy, pointer, replay, and retry dependencies before any live owner changes. Six groups, the full 35-executable matrix, and 100 repeats pass. Recoverable `ORS0` storage, target binding, authenticated integrity, rollback, physical power-loss, and on-device boot evidence remain |
| OT-017T | done | Cross-project recoverable alert-system store | OpenGauge's two-slot host store writes exact 1280-byte `ORS0` generations, owns normal monotonic allocation, preserves the newest good slot across eleven interrupted-write boundaries, requires exact readback/decode, reconciles a full write followed by I/O error at boot, exposes degraded reads, and refuses conflict/exhaustion. Eight groups, the full 36-executable matrix, and 100 repeats pass. ESP-IDF protected storage, authenticated integrity, rollback/reset authority, physical power-loss/wear, and on-device boot evidence remain |
| OT-017U | done | Cross-project trusted generation boundary | OpenGauge `restore_at_or_above` refuses a newest valid `ORS0` below a caller-supplied trusted minimum before any live owner import; `save_next_after` advances beyond both last-trusted and valid local generations, with pre-write exhaustion. Ten focused groups, the unchanged 36-executable matrix, and 100 repeats pass. A hardware-backed trusted source, authenticated integrity, reset/replacement authority, and physical evidence remain |
| OT-017V | partial | Cross-project target recovery adapter | OpenGauge's target plan defines exact two-slot `ORS0` semantics, protected opaque-handle resolution, a separate trusted-generation source, boot/save ordering, coordinated reset/replacement, and physical interruption/endurance evidence. No exact ESP-IDF target/backend, protected key store, trust source, adapter implementation, power-cut result, or on-device boot composition exists yet |
| OT-017W | done | Cross-project protected key-handle preflight | Direct and stored OpenGauge `ORS0` restore validate active peers through an injected opaque-handle boundary before outbox/ACK preflight or any live mutation; revoked peers are skipped. Unavailable, purpose-mismatch, and backend failures retain typed peer-specific evidence and leave all live owners unchanged. Eight system groups, eleven store groups, the unchanged 36-executable matrix, and 100 repeats each pass. A concrete protected-key backend and on-device evidence remain |
| OT-017X | done | Cross-project typed recovery boot | OpenGauge's host coordinator combines provisioning, trusted-generation state, non-mutating slot inspection, protected-key `ORS0` restore, and exact trusted-floor catch-up. It distinguishes first boot, restored, degraded, safe-mode, and service-required outcomes and keeps transport disabled until operational. Nine groups, the complete 37-executable matrix, and 100 repeats pass. Target boot composition, protected backends, physical interruption, and operator recovery remain |
| OT-017Y | done | Cross-project verified recovery save | OpenGauge normal persistence now requires exact local/trusted generation agreement, verifies the next `ORS0` before advancing trust, and verifies exact trust readback before reporting committed. Missing, rollback, conflict, local-ahead, uncertain commit, and trust-update failures remain typed. Eight groups, the complete 38-executable matrix, and 100 repeats pass. Target storage/trust, physical interruption, endurance, provisioning/reset, and operator recovery remain |
| OT-017Z | done | Cross-project unreadable-slot fail-close | OpenGauge distinguishes known empty/invalid peer media from unreadable media that may conceal a newer committed generation. Unreadable state returns service-required, does not advance trust, and cannot enable transport even when the older visible generation imports privately. Ten boot groups, the complete 38-executable matrix, and 100 repeats pass. Physical backend diagnosis and operator recovery remain |
| OT-017AA | done | Cross-project known-degraded repair | OpenGauge repairs only a current operational degraded result with matching active/trusted generation and one valid plus one known empty/invalid slot. It commits the next `ORS0`, advances/readbacks trust, and verifies both slots valid; healthy, unreadable, service, stale, and uncertain cases fail closed. Five groups, the complete 39-executable matrix, and 100 repeats pass. Physical durability, scheduling, audit, and unreadable-media service remain |
| OT-017AB | done | Cross-project redacted recovery status | OpenGauge maps boot, save, and repair results into one fixed-shape operator record with coarse state/reason/action, slot health, generations, protected-key failure class, and transport/repair flags. Peer IDs and key handles are structurally absent; unknown or inconsistent input fails closed. Seven groups, the complete 40-executable matrix, and 100 repeats pass locally. Target logging/rendering, audit retention, and physical service UX remain |
| OT-017AC | done | Cross-project versioned recovery diagnostic event | OpenGauge records one atomic 32-bit word containing only redacted recovery operation, state/reason/action, slot health, key-failure class, and transport/attention/repair/redaction flags. Magic/version/coherence validation fails closed; generations and identity-bearing fields are omitted. Eight groups, the complete 41-executable matrix, and 100 repeats pass locally. Target logger binding, persistent retention/export, and physical service capture remain |
| OT-018 | planned | Display/UI feasibility spike | Candidate hardware renders representative map/peer/alert screen with measured RAM, frame time, boot time, and input behavior |
| OT-019 | partial | Update/recovery architecture | A v0 boundary fixes signed/versioned hardware-bound bundles, inactive-slot write/readback, persisted trial selection, bounded health confirmation, automatic rollback, trusted version floors, USB-first delivery, and layered physical/ROM recovery. A pure lifecycle guard passes eight host groups. The canonical 64-byte `OTU0/v0` checkpoint adds exact policy/hardware/version binding, corruption detection, atomic restore, and reboot-safe trial/rollback intent across eight groups plus 100 repeats while discarding boot-local evidence. Its abstract two-slot store owns generation allocation, preserves prior-good state across partial/corrupt writes, verifies readback, repairs known degradation, fails closed on unreadable/conflicted state, rejects a newest generation below a caller-supplied trusted minimum, allocates beyond local/trusted state, and supports non-mutating inspection across 20 groups plus 100 repeats. A typed boot coordinator privately restores and validates the observed image, durably records trial/rollback transitions, exactly advances trust, and exposes live state only after successful ordering across 15 groups plus 100 repeats. A verified normal-save coordinator requires exact local/trusted agreement, verifies checkpoint-before-trust ordering, and routes uncertain/local-ahead state to reboot reconciliation across 10 groups plus 100 repeats. A lifecycle-transition coordinator applies health, tick, confirmation, and rollback to a private guard copy, publishes durable state only after the verified save commits, and stops the original guard on persistence failure across 10 groups plus 100 repeats. A fixed redacted status boundary validates boot/save/transition coherence and emits only coarse operator state/reason/action, generation evidence, and recovery flags across eight groups plus 100 repeats; unknown or contradictory input blocks normal operation as service-required. `OTRD0/v0` then writes one canonical generation-free 32-bit outcome through the existing logger with independent encode/decode coherence, filtering, and sink-failure checks across eight groups plus 100 repeats. A bounded production-facing RAM ring retains and decodes real `OTRD0` events without adding persistence claims. Target task binding/concurrency, retention/rendering, scheduling/reboot, exact ESP-IDF target, partitions, signer/key custody, authenticated target storage, hardware-backed trusted generation, cleanup/reset authority, updater adapters, and physical interruption/recovery evidence remain |

## Recommended sequence

Complete the remaining OT-003A physical/regulatory inventory and OT-005
cryptographic gates. Run OT-020 only after the Wio Tracker arrives, preserving
its shipping firmware before any write. OT-009's remaining hardware work is a
direct SX1262/OpenTrail binding and later field measurements; the
software-forced MeshCore path is proven. OT-017's physical serial/wireless
adapter, key lifecycle, replay protection, and field failure UX remain
integration gates rather than semantic-schema work. Security and regulatory
constraints must remain inputs before any public packet v1 is declared.
