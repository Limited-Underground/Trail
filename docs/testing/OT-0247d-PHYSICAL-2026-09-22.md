# OT-0247d corrected two-node trial

2026-09-22. One owner-authorized trial of the [prepared candidate](OT-0247d-HOST-PREPARATION-2026-09-21.md).
Result: **failed full acceptance; seven of eight status deliveries**.
No automatic retry or firmware correction was performed in this task.

## Setup and source boundary

Two existing Heltec WiFi LoRa32 V4.2 / ESP32-S3 / 16 MiB bench units, roles A/B.
The owner confirmed USB connection, normal Trail screens and attached antennas
before execution, and matching codes plus both BOOT/user button actions during
the trial. Phones were not used. Exact identities remain private and were
verified by the maintained transport before operations.

Canonical root `C:/lu/OpenTrail`; active checkout `.private/ot177-publication`,
HEAD `c31f7196f4f947e632630e6fde04570a21edcf14` with accepted dirty correction.
Candidate size532640, SHA-256
`4d82424d30d0b35d99eed82eb14609c344984682e9a07413e42183e7063bdff7`.
All1016 retained matrix source pins and controller compatibility checks passed
before execution. Private bundle `.private/ot0247d-preparation` binds source,
runtime, controller, grant, request and captures; attempt
`e4c1bb7bdcc2992915aa6f1e7e29299a` is consumed.

Evaluation radio:915MHz/BW125kHz/SF7/CR4:5/configured2dBm, frame limit158bytes,
max16 transmissions per role, unchanged60-second invitation. Bench distance,
obstacles and antenna performance were not measured; no range claim follows.

## Observed operation

Three handshake transfers and both local confirmations completed. Both target
traces record activation. Four activation transfers preceded the status loop.
The source sequence and saved command results establish four A-to-B statuses
and three B-to-A statuses. B failed during its sender-side RFPOLL for the fourth
B status. This is not a failed initial handshake or first activation transfer.

The first recorded host failure is `status_B`, B/RFPOLL, `target_refused`.
B's original rejecting sample records fault3/layer3/reason7: now61054ms,
deadline61037ms, a17ms overrun. A records fault3/layer1/reason7: now191597ms,
deadline191587ms, a10ms overrun. Both are original invitation-expiry samples;
do not subtract clocks between nodes. Both target diagnostics and traces were
captured on this attempt.

A reports8 attempted/completed transmissions; B reports6/6. Final successful
RFSTAT receive/error/stopped counters were not obtained. Seven delivered
statuses do not establish complete loss/duplicate statistics or RF latency.
Protocol cleanup is `protocol_unverified_handles_closed`, not a passing CLOSE.
Host command timing covers134 commands through failed cleanup. The host failure
event is at61.210639seconds from its own timeline origin; target invitation
origins differ. The accepted model's58.509924seconds includes cleanup and must
not be directly subtracted from the60-second target invitation.

Both activations occurred about35.9seconds into their own invitation windows.
Review-to-confirmation intervals were8.381seconds for A and11.164seconds for B,
including interaction and scheduling. Those are observations, not user error
or proof of pure reaction time. A practical test budget must include this phase.
Detailed timing analysis remains private beside the trial data. The complete
command-count reconciliation against the76-command model is:40 extra confirmation
STATUS polls (44 versus4),14 guarded sender-rearm polls after RFREADY0 (versus0),
8 startup commands outside the model, and4 absent final-transfer/cleanup commands:
76+40+14+8-4=134. Every completed physical transmission required guarded rearm;
the model's immediate-completion schedule avoided that path. The measured
comparison-to-both-confirmed host interval was10.620977seconds; the model already
allows10seconds plus two0.6second holds. User response is not the demonstrated
budget defect. The real extra polling/rearm work must enter the reproduction.

## Restoration and closeout

Original application spans733184bytes at65536 and full NVS12288bytes at53248
were restored sequentially, independently read back, and both devices reset.
Protected ranges were verified. Controller and child exited0; custody closed;
one candidate attempt, zero restore-only retries. The independent read-only
closure audit checked10 original capture hashes/lengths,6 protected descriptors,
2 original application prefixes,2 fresh evaluation namespaces and ordered
restoration/reset events. It verifies the pinned operator's readback evidence,
not a second independent hardware operation.

The owner confirmed both final normal Trail screens after controller closure.
This is a separate visual gate from restored bytes and reset events.

## Next boundary

Keep the failed attempt and source-bound timing. Before any further trial,
reproduce the actual confirmation/polling and command-cost sequence host-side,
compare it with the accepted timing model, and identify a bounded correction
that retains fresh authorization, the unchanged deadline and restoration.
Further implementation and another physical attempt require their own reviewed
scope; this result grants neither. Physical acceptance, phone integration, V1
completion and public website capability remain unchanged.
