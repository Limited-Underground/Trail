# OT-0247d corrected candidate physical trial

OBSERVED 2026-09-23. Approved revision 2, one candidate attempt. Canonical root C:/lu/OpenTrail; active checkout C:/lu/OpenTrail/.private/ot177-publication. Branch codex/ot236-libsodium-capture at c31f7196f4f947e632630e6fde04570a21edcf14, with existing uncommitted work preserved.

## Setup and boundary

Two existing Heltec V4.2 bench units, USB-connected, antennas attached, owner present. Cases remained closed and batteries remained connected. Phones were not used. The owner confirmed matching comparison codes and one approximately one-second BOOT hold/release on each device. No codes or private identifiers were recorded.

Candidate: build/ot0247g-enrolled-a2/heltec_v4_enrolled_eval.bin, 533360 bytes, SHA256 0779d6d3622966efc8b93ff5ac39ad2eeb9988ffe544d68e17291b0830f16bd8. Reused the accepted OT-0247g source-bound matrix, builds and reproducibility; independent preflight rechecked 1018 source pins, 13 manifest entries, runtime binding and candidate. Fresh controller prepare passed. No source correction or rebuild during this trial.

Exact reviewed proposal/controller/request bindings are retained in .private/ot0247d-g-preparation. The controller generated a fresh one-use grant; the previous failed trial and consumed grant remain preserved. Candidate writes were sequential, following fresh identity checks and capture of both originals/protected ranges.

Evaluation radio profile: 915 MHz, BW125 kHz, SF7, CR4:5, 2 dBm; frame maximum158 bytes, maximum16 transmissions per role, unchanged60-second invitation window. This is a nearby two-node evaluation, not range or production RF acceptance.

## Physical exchange

PASS: three handshake transfers, matching local confirmation on both nodes, four activation transfers and eight status transfers (four per direction). Journal outcome status_exchanged requires successful bridge operation, idle handles and exact final counter verification.

| Role | TX attempts | TX completed | RX frames | RX errors | Radio stopped |
|---|---:|---:|---:|---:|---|
| A | 8 | 8 | 7 | 0 | yes |
| B | 7 | 7 | 8 | 0 | yes |

Both diagnostic snapshots and original target traces report fault0; milestone mask63 and one button press/release per role. All expected transfers were accounted for, with no additional receive frames in the terminal counters. This single run does not establish a statistical loss/duplicate rate or field endurance.

Private evidence: .private/ot0247d-g-preparation/trial/diagnostics.jsonl; enrolled-confirmation journal and originals for attempt8b3698abf19b9fccc4d681f35347430b. The final host timing/result files are present. Independent closure audit passed all10 original hashes and baseline checks, both restored/reset event sequences, closed journal/idle bridge, no active locks, and exactly one candidate grant with no recovery grant. All104 timing events validate; all100 timed commands succeeded. The timeline records three RFSEND, four RFCONTROL, eight RFSTATUS and15 guarded RFFINISH completions.

Host bridge cleanup completed at55.073405 seconds from its timeline origin. Handshake commands consumed8.877170s, confirmation polling5.435976s, activation including TRAFFIC12.918788s, and eight status transfers24.646573s. These are bounded host phase measurements, not synchronized one-way RF latency. Comparison was announced at11.616447s with A49.715s/B49.459s of review time remaining. No loss or extra receive frames appeared in this one exchange; no independent RF sniffer or range/endurance test was performed.

## Restoration and acceptance boundary

PASS: both full original applications (733184 bytes at65536) and NVS (12288 bytes at53248) independently restored/read back, protected spans verified, both devices reset. Journal closed, child/controller exit0, custody lock removed. One candidate attempt, zero restore-only retries. Owner confirmed both normal Trail screens after closure. No V1 percentage or public website capability change is made from an unaccepted trial. The next dependent implementation is OT-0238b after owner acceptance of this task. No Git mutation, publication, production deployment or battery/case access occurred.

Validation commands: isolated pinned controller prepare and execute with reviewed operator-binding SHA256; independent baseline/closure audit; python tools/check_repository_docs.py and python tests/host/repository_docs_tests.py. Exact controller invocation and approved hashes remain in the prepared bundle.
