# RadioLib BUSY regression excerpts

The three `.cpp` excerpts are exact RadioLib 7.7.1 method bodies from the
accepted managed dependency. Their full upstream source and method hashes are
recorded in `manifest.json`; upstream terms are retained in `license.txt`.
The controlled HAL/peripheral scaffolding is test code. It does not claim to
simulate hardware SPI, RF transmission or physical BUSY state.
