# OT-145 one-attempt authority for the corrected runtime

**HOST-ONLY AUTHORITY ACCEPTED — no device, endpoint, reset, flash, benchmark,
radio, or phone operation occurred. The authority was not executed.**

The owner explicitly approved the next bounded task. OT-145 uses that approval
only to create and accept the authority for the unchanged OT-144 executable-
bundle preparation. It does not execute the authority or grant continuation.

## Accepted immutable authority

- Schema: `OT144MOAA0`
- ID: `OT-144-OT005-MONOCYPHER-ONE-ATTEMPT-AUTHORITY-V0`
- Status: `authorized_one_attempt_not_executed`
- Size: 3,775 bytes
- Raw SHA-256:
  `29c84ae8de494be2ac08e35cdeba5c4409f9381bc4f457a29cef64fb391afe31`
- Canonical SHA-256:
  `b6d2f291c9da683e772786aab15b17165cd0f73345f8345598d0954913098920`
- Bound preparation raw SHA-256:
  `9736e6c68647da594e31b8c32e076fd40c2fccedd1bbd1d740b5cc0443667455`
- Bound preparation canonical SHA-256:
  `f8bc22259c42d7ff82904e97da46bfa3a6af58529bc2f619330be36dc6b430a0`

The authority binds the corrected 149,824-byte application BIN with SHA-256
`8e345d41e869cf781e3d6eb3b2269f26882b7c1d9f43b856ff5795c6cc56a034`
and the exact 473,152-byte Trail restoration BIN with SHA-256
`0c40aeb6c95ade9940aa21065cbc73a72dcd82e96ade9d13126693147feb5741`.
It also binds the OT-144 adapter SHA-256
`19eb64c29deda5af9e4d523a8bc1630ae2f9350cba4043e35ac7691146b09b71`
and the unchanged accepted preparation.

## One-attempt boundary

- Exactly two distinct private endpoints are required.
- Both installed Trail applications must pass exact readback and reset before
  journal creation or any application write.
- Only the benchmark application may be written, at offset `0x10000`.
- Benchmark readback must pass before capture.
- Radio and selection remain prohibited.
- The fixed private OT-144 journal consumes the attempt before the first
  benchmark write; success or abort is terminal and the authority is not
  reusable.
- Every touched node must restore, read back, and hard-reset exactly to Trail.
  Recovery-only mode remains retryable until restoration succeeds.
- Public records retain no ports, device identifiers, filesystem paths, raw
  capture, run nonces, or raw exception text.

This is a workspace-local operational guarantee, not a global ledger,
hardware-enforced monotonic counter, signed consumption proof, or copy-proof
mechanism. Copying the public repository or authority, deleting private state,
or executing from another workspace is not authorized.

The historical OT-143 authority remains unconsumed but non-executable because
its accepted layers bind conflicting application images. It is immutable,
non-reusable, and grants no inherited attempt.

## Validation

- OT-144 executable-bundle/authority focused tests: 10/10 passed.
- Direct validation of the canonical authority against the exact benchmark,
  restoration image, preparation, and adapter: passed.
- Complete local Windows host matrix: passed with exit 0.
- Remote Host validation: pending final gate.

No private OT-144 journal, execution receipt, or recovery receipt exists at
this checkpoint.

## Boundary and next gate

No Monocypher frame, timing, primitive, resource, or benchmark result is
admitted. No radio, candidate selection, suite selection, Phase 2 completion,
support, compatibility, regulatory, production, secure-LoRa, end-to-end, or
score claim is added. The latest admitted benchmark result remains OT-122. V1
remains exact 43.75%, displayed 44%; the historical baseline remains exact
31.75%, displayed 32%.

The next permitted hardware step is only the single OT-145-authorized two-node
application-only attempt from this workspace, with exact Trail restoration of
every touched node on success or abort.

No public website status update is required because OT-145 changes no public
capability, milestone completion, score, field-test readiness, support,
release, or physical-acceptance claim.
