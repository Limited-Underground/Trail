# OT-157 reset-aware Noise XK executable/restoration bundle acceptance

**HOST-ONLY PREPARATION — no device, phone,
firmware build, flash, readback, reset, serial port, radio transmission,
cryptographic measurement, or physical observation occurred. No authority was
created or accepted.**

## Accepted boundary

The accepted bundle freezes the unchanged OT-153 firmware and exact Trail
restoration inputs with the accepted OT-156 reset-aware runner/runtime and new
OT-157 coordinator and adapter bindings. It does not reuse the consumed OT-153
private state or the consumed OT-154 authority.

The successor coordinator keeps the frozen restoration-safe state machine but
uses fresh `OT157NXJ0` / `OT157NXCR0` schemas and OT-157-only private filenames.
One inherited hard-coded OT-153 filename validator was found by the focused
executable test and replaced with an exact OT-157 validator. A clean-checkout
validation then exposed an accidental dependency on this workstation's ignored
`.private` directory. The corrected validator creates only that exact private
root when absent, validates the live inherited paths, and the focused fixture
now begins from an absent temporary private root. A follow-up adversarial review
also closes every filesystem-error path across resolution, creation,
reparse/symlink inspection, and identity checks before authority validation or
device I/O, without exposing private exception text.

The concrete adapter uses the reconnectable backend and refuses before backend
construction or device I/O while the separate OT-158 authority tool and record
are absent.

## Exact executable bindings

| Role | Bytes | SHA-256 |
| --- | ---: | --- |
| Canonical preparation | 6,113 | `889bb6440f71864f29e815be926fbcc6c04d2de442be3cf38b6a2eceb365ba2a` |
| Bundle validator | 16,815 | `84a5c7c7b52413fda8c47fcf6e92a049d3eb9861443617dcdeed8e6d6a7f65a5` |
| OT-156 runner | 11,099 | `81d0a329d34c20e76362b9a3f07221b77b77f02189dd06660119a02ac1700244` |
| OT-156 runtime | 6,588 | `bcdb1a772971aa665be699c2699a2b69625c4e8bd2352abd21811be0a4295dd8` |
| OT-157 coordinator | 6,986 | `a17d4615a50d2e373ba7fab98b6bed160e6b24e291aac2ec83f0fa19d1479db1` |
| OT-157 adapter | 3,935 | `ec744f7430627600de1491d5729d8b5874ac38977faa786df90a4d39ce2d3de8` |

The preparation independently revalidates the exact OT-153 record (7,496 bytes,
raw SHA-256 `84d7ba8c601f59115fe5d7c74275dc8ccba807a10f87a14ee5afbbe40b3a184b`),
its reproduced A/B build tuple, the 296,640-byte benchmark application, and the
500,944-byte Trail restoration application with SHA-256
`f2a58414f82eed585ba90bf7671b06c8ebfaa82e8b23f6ac2093de95143b4e0e`.

## Preserved behavior

- Both nodes must pass exact installed-Trail readback and reset before a future
  authorized one-use journal boundary.
- Only the application at `0x10000` may be written.
- Both benchmark writes/readbacks/resets precede the runner.
- Both restart acknowledgements precede both fresh opens; stale handles and
  queued receipts cannot cross the reset boundary.
- Both reopened postboot contracts precede every radio verb.
- Every touched node is restored, read back, and reset independently. Recovery
  is restore-only and never loads or writes benchmark bytes or opens radio.
- The exact successful contract remains 14 transmissions, 736 radio-payload
  bytes, and 1,447,424 microseconds theoretical airtime.

## Validation state

- OT-157 bundle suite: 10/10 passed.
- OT-157 coordinator suite: 7/7 passed.
- OT-157 adapter suite: 6/6 passed.
- OT-156 runtime/runner suites: 17/17 passed.
- Frozen OT-153 chain: 75/75 passed.
- Authoritative raw-byte audit passed with every registered OT-157 byte exact.
- Complete Windows Host matrix passed locally with exit code 0.

No physical OT-155 root cause, hardware attempt, radio result, selection,
Phase 2/3 completion, readiness, support, production, regulatory, or score
claim is made. OT-158 remains a separate authority decision.
