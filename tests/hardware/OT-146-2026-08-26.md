# OT-146 OT-145-authorized Monocypher execution evidence

**PASS — the single authority is consumed; both anonymous nodes completed the
bounded comparison and were restored exactly to Trail.**

## Accepted binding and preflight

OT-146 used only the accepted OT-145 `OT144MOAA0` authority and unchanged
OT-144 executable preparation:

- corrected Monocypher comparison application: 149,824 bytes, SHA-256
  `8e345d41e869cf781e3d6eb3b2269f26882b7c1d9f43b856ff5795c6cc56a034`;
- Trail restoration application: 473,152 bytes, SHA-256
  `0c40aeb6c95ade9940aa21065cbc73a72dcd82e96ade9d13126693147feb5741`;
- application-only offset `0x10000`; and
- authority raw SHA-256
  `29c84ae8de494be2ac08e35cdeba5c4409f9381bc4f457a29cef64fb391afe31`.

The first combined non-consuming preflight failed closed before journal
creation or firmware write. Separate read-only diagnosis proved both installed
applications matched the exact Trail image and both hard resets worked; the
same unchanged preflight then passed. The owner visually confirmed both
displays were on before execution. No attempt was consumed until the fixed
private OT-144 journal was created by the authorized executor.

## Single execution outcome

The authority was executed exactly once. Both anonymous nodes passed benchmark
application readback and strict validation of all 1,014 result frames. Each
node completed all five admitted Monocypher comparison operations with 500
data-cache-conditioned and 500 warm samples total, exactly 100 of each per
operation.

| Operation | Node A cold/warm median | Node B cold/warm median |
| --- | ---: | ---: |
| Ed25519 sign | 9,277 / 9,277 us | 9,277 / 9,277 us |
| Ed25519 verify | 26,766 / 26,767 us | 26,767 / 26,767 us |
| X25519 | 18,858 / 18,858 us | 18,858 / 18,858 us |
| ChaCha20-Poly1305 encrypt | 110 / 110 us | 110 / 110 us |
| ChaCha20-Poly1305 decrypt | 123 / 123 us | 123 / 123 us |

Both nodes reported 349,392 starting and 349,372 minimum-free internal 8-bit
heap bytes, 20 peak dynamic RAM bytes, an 8,192-byte stack allocation, 5,048
stack high-water free bytes, 3,144 maximum stack-used bytes, and zero watchdog
resets. The public receipt retains exact min/median/p95/max summaries for every
operation and anonymous node.

## Restoration and privacy

Both benchmark-touched nodes restored, read back, and hard-reset exactly to the
Trail application. The terminal receipt records
`two_node_monocypher_passed_and_restored`, `restoration_complete=true`, and no
failure. Recovery was not required. The owner visually confirmed both displays
were back on after execution.

Radio was not used. Public records contain no endpoint, device identifier,
filesystem path, raw capture, run nonce, or backend error text. The private
terminal receipt is 5,357 bytes with SHA-256
`beb9947d04c8ca3557fb1beb3efa4982bfd23f94968292db7e680f14da2369fc`;
the private journal is 1,173 bytes with SHA-256
`da9c0503253249900984ffd46b45edcaeeee08808ee96208d750bf7551404e48`.

## Validation and boundary

- The terminal receipt and journal independently pass a privacy-safe structural
  audit.
- Both anonymous nodes record benchmark readback, capture validation, exact
  restore readback, and hard reset as true.
- The focused public-record test passes 7/7, the deterministic raw-byte audit
  passes all 137 authoritative inputs, and the complete Windows host matrix
  passes locally. A green remote Host validation run on the exact published
  commit remains the final CI gate.
- The 6,567-byte canonical public receipt has SHA-256
  `9a5ea09fa8cdf465f5c83b6a0eb69fb80579806b0635ac63083e1234e4f91464`;
  focused public-record validation passes 7/7.

The OT-145 authority is consumed and non-reusable. This admits the bounded
Monocypher five-of-eight comparison result, but does not itself complete Phase
2 or select a candidate, suite, library, handshake/KDF, or packet-v1 wire
format. Monocypher remains structurally nonselectable because SHA-256,
HKDF-SHA256, and Noise XK remain unavailable. No radio, support, compatibility,
regulatory, production, secure-LoRa, end-to-end, field-readiness, or score claim
is added. V1 remains exact 43.75%, displayed 44%; the historical baseline
remains exact 31.75%, displayed 32%.

Next independently admit the completed Phase 2 evidence and explicitly select
the suite/library, handshake/KDF, and packet-v1 wire format before
implementation. Public website synchronization is required for the newly
demonstrated bounded two-node capability without changing the percentage or
field-test-readiness claim.
