# OT-147 two-device live battery/GNSS footer install and acceptance

**PASS — both anonymous Heltec V4 test nodes now run the exact OT-147
application and visibly render live `BAT:` and `GPS:` footer values.**

## Accepted build and authority

- ESP-IDF v6.0.2 at pinned commit
  `7101770dc6db2667b3c477cc31365dd1acd6db4e`.
- Reproducible application version `ot147-live-status-v0`, 500,944 bytes,
  SHA-256
  `f2a58414f82eed585ba90bf7671b06c8ebfaa82e8b23f6ac2093de95143b4e0e`.
- Application-only offset `0x10000`; 5,177,344-byte factory slot.
- Accepted authority raw SHA-256
  `a367ce3a13a89241aa6bd55c2ffec4adb17c15262e6c640c1608b2e43b8a43c5`.
- Exactly one two-node attempt, consumed before the first write and not
  reusable.

## Execution result

Both anonymous nodes were read before the authority was consumed or any write
began. Each installed application was exactly 473,152 bytes with SHA-256
`0c40aeb6c95ade9940aa21065cbc73a72dcd82e96ade9d13126693147feb5741`.

Each node then received one application-only write at `0x10000`. Independent
500,944-byte readbacks matched the exact OT-147 application SHA-256, and both
nodes hard-reset successfully. No bootloader, partition table, OTA data, NVS,
eFuse, radio, or phone operation occurred.

The owner visually confirmed that both displays show live `BAT:` and `GPS:`
values after startup. This physically accepts the live footer binding and
populated rendering on both current test nodes; fail-closed rendering remains
host-tested rather than physically induced here.

## Validation

- Battery host tests: passed.
- Heltec V4 GNSS host tests: 7 groups passed.
- Heltec startup-display host tests: 8 groups passed.
- Heltec target admission: 12 groups passed.
- Accepted-authority validation: 7/7 passed.
- Terminal-receipt validation: 7/7 passed.
- Authoritative raw-byte policy: 142 inputs passed.
- Complete Windows host matrix: passed locally.
- GitHub Host validation for implementation/authority commit
  `0e87ab3b9873372130939df2d86780a63d1fd265`: run
  `33013630057`, completed successfully.

The canonical 2,151-byte terminal receipt has SHA-256
`165893b72537b04da210e1dbec268b7681f98c2a8aa0ba6bcf0906c3a28897cd`.

## Claim boundary

Battery percentage is an approximate voltage-derived estimate, not a
calibrated fuel-gauge result. The owner's expectation that long-connected
batteries should be full remains a calibration/charger-behavior check; exact
accuracy is not claimed here.

Visible GPS satellite values prove the bounded satellite-count source and
display path, not position, fix quality, accuracy, stale/loss behavior, or
outdoor performance. The LoRa activity arrow remains unbound. This result does
not prove endurance, supported hardware, production readiness, secure LoRa,
end-to-end phone operation, field readiness, regulatory acceptance, or any
score increase. V1 remains exact 43.75%, displayed 44%.

Public records contain only anonymous node labels and no endpoint, device
identifier, filesystem path, MAC address, raw capture, BLE secret, or private
receipt path. The website description should be corrected in the next approved
batched status deployment; no separate percentage change is warranted.
