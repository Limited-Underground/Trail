# OT-127 one-time Monocypher second corrective-retry authority

**AUTHORIZED, NOT YET EXECUTED — one bounded two-node application-only attempt.**

OT-127 binds a fresh non-reusable authority to the exact privacy-safe OT-126 abort, immutable OT-123 preparation, exact benchmark and Trail images, and a new successor runner. The authority validates with raw SHA-256 `d043fc7dc700ce2c43914fe079b4a594b04e730252d8cb490f02097d9472448b` and canonical SHA-256 `126135ca4c07670e17a0c4fee36399d04295461d193ac4f6e02ecca86360724e`. The runner SHA-256 is `ff81188b1f211aaf504192d3827147b2f572b29e895e45eeeee1bc505ffb5438`.

The successor fixes the two defects exposed by OT-126:

- the capture deadline is fixed at 180 seconds and is not a command-line option;
- each fresh handle keeps a 10-second first-frame grace, the overall deadline never restarts, and no reopen occurs after a frame;
- both installed Trail applications are verified and both devices are hard-reset before journal creation, including on preflight failure;
- all application-only restore, readback, reset, privacy, no-radio, and nonselection boundaries remain fail closed.

The strict authority tests pass 8/8 and the successor runner tests pass 34/34. The authority validator reports one authorized attempt, fixed 180-second capture, 10-second first-frame grace, no benchmark result yet, and non-reusability. Both USB endpoints and both visible Trail displays passed the physical preflight before this authority was prepared.

No hardware write occurs in this checkpoint. No benchmark result, radio use, candidate selection, suite selection, support, compatibility, regulatory, production, secure-LoRa, end-to-end, or score claim is added. V1 remains exact 43.75%, displayed 44%.
