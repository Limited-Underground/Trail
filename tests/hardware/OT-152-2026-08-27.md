# OT-152 Noise XK radio-cost measurement preparation

**HOST-ONLY PREPARATION — no device, phone, firmware build, flash, radio,
cryptographic operation, or physical measurement occurred.**

## Accepted inputs

OT-152 binds the accepted OT-116 benchmark plan, OT-114 US915 direct-radio
evidence/admission, OT-117 benchmark-only libsodium Noise XK adapter admission,
OT-122 local execution receipt, and OT-148 Phase 2 reconciliation by exact raw
and canonical SHA-256. It also binds the exact adapter header/source bytes.

The accepted `OTNXK0/v0` handshake consists of three raw benchmark messages:
48 bytes initiator-to-responder, 48 bytes responder-to-initiator, and 64 bytes
initiator-to-responder. Each fits the admitted 255-byte direct-LoRa ceiling, so
the prepared mapping is 160 payload bytes in three single-frame transmissions.
It adds no wrapper bytes, OTA1 ACK frames, Packet V1 selection, or production
wire-format claim.

## Theoretical reference and retry boundary

The frozen OT-114 US915 profile yields 97,536, 97,536, and 118,016
microseconds of theoretical airtime, totaling 313,088 microseconds per
successful handshake. These numbers reproduce the accepted LoRa formula but
are not physically measured airtime.

Future execution must reverse roles and run one baseline plus one bounded-retry
scenario per role. In the retry scenario, message 2 is intentionally withheld
after message 1 once. The initiator must reach the fixed 2,196 ms deadline,
abort and wipe temporary secrets, reject stale attempt frames, create fresh
attempt identity, and restart the complete handshake once. The second attempt
must complete with split-key cross-match. Per role, the bounded-retry scenario
therefore prepares 208 payload bytes in four transmissions and 410,624
microseconds of theoretical airtime. Across both role directions and both
scenarios, the prepared total is 736 payload bytes in 14 transmissions and
1,447,424 microseconds of theoretical airtime.

The deadlines preserve OT-113's accepted conservative policy: exact outbound
airtime plus exact expected-response airtime plus 500 ms responder-turnaround
and 1,500 ms scheduling margin. The resulting message-2 and message-3 limits
are 2,196 ms and 2,216 ms.

## Validation

The strict validator pins every parent and adapter identity, recalculates the
LoRa airtime and deadlines, rejects duplicate keys, non-finite numbers, private
paths/identifiers, contract drift, expanded retry, Packet V1 selection,
hardware/radio authority, Phase 2 completion, candidate selection, and record
digest drift. Focused validation passes 20/20.

The complete Windows host matrix and deterministic raw-byte checkout audit are
required publication gates. The public preparation record contains no raw
handshake payload, key, endpoint, device identifier, coordinate, or private
trace.

## Boundary and next gate

No Noise XK radio result is admitted. `handshake_total_wire_bytes`,
`fragments`, physical `measured_airtime_us`, and `bounded_retry_result` remain
unresolved until a later two-node execution produces the strict aggregate
receipt in both directions.

OT-153 must first freeze the exact executable runner, firmware, and Trail
restoration binding, then separately accept fresh explicit non-reusable
two-node radio authority. No hardware action is authorized by OT-152. V1
remains exact 43.75%, displayed 44%. This internal preparation changes no
public status, so no website update is required.
