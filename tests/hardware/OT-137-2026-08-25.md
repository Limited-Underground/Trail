# OT-137 OT-136-authorized one-attempt execution evidence

**ONE ATTEMPT CONSUMED BY FAIL-CLOSED ABORT — exact Trail restoration is
complete; no benchmark result, radio evidence, or score credit is admitted.**

## Accepted binding and preflight

OT-137 executes only the exact OT-136 binding accepted by Decision 0075:

- the immutable OT-135 byte-bounded START/READY runner and unchanged strict
  1,014-frame parser/schema;
- the fresh OT-136 restoration-safe coordinator/private state and concrete
  endpoint-bound adapter;
- the exact 187,680-byte Monocypher benchmark application and exact 473,152-byte
  Trail restoration application; and
- the workspace-local, non-reusable authority for exactly one two-node
  application-only attempt.

The non-consuming preflight validated both installed Trail applications and
reset both nodes. The owner then confirmed that both displays went out and
returned to the Trail logo.

## Single execution outcome

The authority was validated and executed exactly once. Node A passed benchmark
application readback. Capture then failed closed as `capture_failed` /
`preamble_invalid` before any result frame was accepted.

Bounded privacy-safe diagnostics:

- `lifecycle=stable_continuous`
- `reset_attempts=1`
- `lifecycle_polls=4`
- `stable_presence_polls=3`
- `open_attempts=1`
- `start_write_attempts=1`
- `read_calls=2`
- `empty_reads=0`
- `bytes_observed=1024`
- `preamble_lines_ignored=11`
- `complete_lines=11`
- `frame_lines_buffered=0`

The two reads observed 1,024 bytes. Eleven complete opaque pre-`READY` records
were counted while accepted complete-record bytes remained within the 512-byte
allowance. The remaining partial pre-`READY` data was not an exact `READY`
prefix, and `preamble_bytes + len(partial)` exceeded 512, so the runner failed
closed. This does not claim that 1,024 preamble bytes were accepted. No record
contents, endpoint, path, device identifier, backend text, or raw capture is
published.

Node A restored, read back, and reset exactly to Trail. Node B was never
benchmark-written and remained on Trail. The public receipt records
`restoration_complete=true`. The owner visually confirmed that both devices
reset again and displayed the Trail logo after the abort. Recovery was not
required.

## Validation

- OT-135 byte-bounded successor runner: 14/14 passed.
- OT-136 restoration-safe coordinator: 11/11 passed.
- OT-136 executable-bundle/authority validator: 10/10 passed.
- OT-136 hardware adapter: 14/14 passed.
- OT-137 sanitized abort record: 7/7 passed.
- Deterministic raw-byte checkout audit: 82/82 passed.
- Complete host validation, privacy/publication-safety validation, loader
  builds/tests, and simulator validation pass.
- Public abort receipt: 2,888 bytes, SHA-256
  `1f6a75e2941045eb3585161769bb2a3ae544b1192d04594dc9d9bec53d77212c`.
- Private execution receipt SHA-256 anchor:
  `ce7fea428a86a2a5293c201edf123d6b94edab9bef5a050918ce3a43adf0e1be`.
- Private journal SHA-256 anchor:
  `9d1f37e035ed6e901a521fcf904e1f8e0a1781a614dc2e8d737223e76ab6699b`.

## Boundary and next gate

The OT-136 authority is consumed and non-reusable. No Monocypher benchmark
result, timing, primitive result, resource result, radio evidence, candidate
selection, Phase 2 completion, physical support, regulatory acceptance,
production readiness, end-to-end proof, or score credit is admitted. The latest
admitted benchmark result remains OT-122.

No further Monocypher hardware attempt is authorized. Next investigate and
host-test the bounded pre-`READY` partial-data boundary and live boot/control
compatibility without weakening the cumulative 512-byte/time bounds, exact
`READY`, frame-before-`READY` rejection, post-`READY` strictness, privacy-safe
diagnostics, or real 1,014-frame parser. Any later device access requires a new
immutable executable binding and fresh explicit non-reusable one-attempt
authority.

V1 remains exact 43.75%, displayed 44%; the historical baseline remains exact
31.75%, displayed 32%. Public website synchronization is required because the
canonical latest increment, evidence, and next gate changed; no percentage or
physical-acceptance claim changed.
