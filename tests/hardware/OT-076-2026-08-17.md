# OT-076 — Exact installed application recovery capture

Status: **done** for the bounded read-only recovery-artifact gate.

## Accepted result

One owner-authorized, one-use read-only operation captured the exact factory
application currently installed on `OT-DEV-001`. The operation returned only
`OTPRBE1/v0` / `RECOVERY-APPLICATION-CAPTURED-ONLY` after the serial
connection was closed and the staged artifact was independently reread.

| Field | Accepted evidence |
| --- | --- |
| Flash range | offset `0x010000`, 470,928 bytes |
| SHA-256 | `A7D8E672CF9169F1D1D4E86EEFF80399C47A145E7D64904C207DD5F1B23F359B` |
| Retention | exact application retained only as a private ignored recovery artifact |


No transient port, device identifier, private operation/evidence identity,
exception text, detailed transport output, or local artifact path is retained
in this record. The one-use reader, its offline tests, and bytecode were deleted
after the attempt; cleanup was verified. The exact binary remains outside Git.

## Authority boundary

This closes only the missing exact installed-application artifact prerequisite.
It does not accept or exercise a recovery route, make the recovery bundle
complete, bind fresh transition-operation evidence, authorize a partition or
application write, initialize protected storage, provision a key/eFuse or
rollback floor, or prove bond persistence, GATT authorization, Ready, LoRa, or
GNSS. Every physical write and partition-transition authority remains false.

After manual RST, the owner observed the Trail logo followed by
`BLE ADVERTISING`. This is a bounded startup/status observation, not heartbeat,
Android discovery, connection, pairing, authorization, or Ready evidence.
