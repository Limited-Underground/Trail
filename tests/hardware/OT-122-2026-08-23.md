# OT-122 bounded libsodium Noise XK and runtime-resource execution — 2026-08-23

## Result

**PASS — bounded libsodium continuation only.** Two independently admitted anonymous experimental nodes each passed 8/8 operations with 800 data-cache-conditioned and 800 warm samples, exactly 100 per operation. The eighth operation performed a complete benchmark-only Noise XK handshake. Benchmark write/readback, capture validation, exact Trail restoration, restore readback, and reset passed on both nodes.

Before the first write, a private read-only ROM-loader preflight verified that both application slots matched the exact pinned 473,152-byte Trail restore image. Temporary readbacks and raw tool output were deleted and are not public evidence. The run used application-only writes at offset `0x10000` and the proven 115,200-baud ROM-loader path.

## Public artifacts

| Artifact | Size | SHA-256 |
|---|---:|---|
| `OT121LPER1` continuation receipt | — | `2b023c640bfbec8ad6eb5d1d63d65e8f1ad75dcfe593566aba6b3d468355a178` |
| Exact benchmark image | 293,120 bytes | `665dd55e5e21c3af7639ee2d1e0765b52250b3f3243692bb46baabe80ec1d973` |
| Accepted sdkconfig | — | `b4fb46a1d2fa27953a9e9f02cd87da8be60c09d7f5e3ef00905839f7f38f2f9f` |
| Exact restored Trail image | 473,152 bytes | `0c40aeb6c95ade9940aa21065cbc73a72dcd82e96ade9d13126693147feb5741` |

## Measurements

| Measurement | Node A | Node B |
|---|---:|---:|
| Noise XK cold median | 116,256 us | 116,256 us |
| Noise XK warm median | 116,255 us | 116,256 us |
| Internal heap start | 334,504 bytes | 334,504 bytes |
| Internal heap local minimum | 334,504 bytes | 334,504 bytes |
| Peak dynamic heap use | 0 bytes | 0 bytes |
| Worker stack allocation | 8,192 bytes | 8,192 bytes |
| Stack high-water free margin | 3,880 bytes | 3,880 bytes |
| Maximum stack use | 4,312 bytes | 4,312 bytes |
| Watchdog resets | 0 | 0 |

The identical zero peak-heap result is valid: no internal 8-bit heap decrease occurred inside the monitored benchmark window. Static benchmark state is accounted separately from dynamic heap. The linked image report records 53,373 bytes of `.data` plus `.bss`; an independently matched no-candidate build is still required before admitting the exact linked-flash delta and final static-RAM comparison.

## Boundary

The receipt remains `phase_two_complete=false` and `radio_used=false`. This result does not measure the other candidates or radio, perform cross-candidate admission, select a library, suite, handshake, KDF, or packet format, implement secure LoRa, declare hardware support or compatibility, establish regulatory or production readiness, prove two-phone end-to-end behavior, or change project scores. No device identifiers, filesystem paths, raw captures, serial ports, or private pairing material are retained.