# OT-133 immutable successor binding and one-attempt execution evidence

**ONE ATTEMPT CONSUMED BY FAIL-CLOSED ABORT — exact Trail restoration is
complete; no benchmark result, radio evidence, or score credit is admitted.**

## Accepted binding

OT-133 leaves OT-129 through OT-132 immutable and creates a true successor
execution boundary:

- `tools/ot133_monocypher_coordinator.py` uses fresh private journal and receipt
  state and binds the OT-132 capture runner;
- `tools/ot133_monocypher_hardware_adapter.py` provides the single endpoint-
  bound application write/readback/reset/presence/serial surface;
- `tools/ot133_monocypher_execution_authority.py` validates the new coordinator,
  adapter, OT-132 runner, unchanged strict parser/schema, exact benchmark image,
  and exact Trail restoration image;
- the authority permits exactly one two-node application-only attempt and is
  consumed on success or abort.

The non-consuming preflight validated both installed Trail applications and
reset both nodes. The owner then confirmed both displays showed the Trail logo.

## Single execution outcome

The authority was validated and executed exactly once. Node A passed benchmark
application readback. Capture then failed closed as `capture_failed` /
`preamble_invalid` before any result frame was accepted.

Bounded privacy-safe diagnostics:

- `lifecycle=stable_continuous`
- `reset_attempts=1`
- `lifecycle_polls=4`
- `stable_presence_polls=3`
- `open_attempts=1`
- `start_write_attempts=1`
- `read_calls=1`
- `empty_reads=0`
- `bytes_observed=512`
- `preamble_lines_ignored=9`
- `complete_lines=9`
- `frame_lines_buffered=0`

The exact boundary is nine complete pre-`READY` records in the first 512-byte
read versus the frozen eight-record allowance. No record contents, endpoint,
path, device identifier, backend text, or raw capture is published.

Node A restored, read back, and reset exactly to Trail. Node B was never
benchmark-written and remained on Trail. The public receipt records
`restoration_complete=true`. The owner visually confirmed both displays showed
the Trail logo after the abort.

## Validation

- OT-132 successor runner: 14/14 passed.
- OT-133 restoration-safe coordinator: 11/11 passed.
- OT-133 executable-bundle/authority validator: 10/10 passed.
- OT-133 hardware adapter: 14/14 passed.
- OT-133 sanitized abort record: 7/7 passed.
- Complete host validation, privacy/publication-safety validation, loader
  builds/tests, and simulator validation pass.
- Public abort receipt SHA-256:
  `5a57675d1d367968fa1af20c97ab0a7ca4eb005a5b3f1fcf77c52fc552af6d04`
- Private execution receipt SHA-256 anchor:
  `f20f2db317b1d6b566524e697ff92dd2a307eceec196b9dd221d1d9ab68e8b69`
- Private journal SHA-256 anchor:
  `01277e5d71ea133f1ebcf9db48484bae641b5126bd9b5838fe21ea699d69c2b6`

## Boundary and next gate

The OT-133 authority is consumed and non-reusable. No Monocypher benchmark
result, timing, resource result, radio evidence, candidate selection, Phase 2
completion, physical support, regulatory acceptance, production readiness,
end-to-end proof, or score credit is admitted. The latest admitted benchmark
result remains OT-122.

No further Monocypher hardware attempt is authorized. Next investigate and
host-test the nine-record boundary while preserving the 512-byte/time bounds,
exact `READY`, frame-before-`READY` rejection, post-`READY` strictness,
privacy-safe diagnostics, and the real 1,014-frame parser. Any later device
access requires a new immutable executable binding and fresh explicit
non-reusable one-attempt authority.

V1 remains exact 43.75%, displayed 44%.
