# OT-077 — Offline exact recovery-route acceptance

Date: 2026-08-18
Result: **PASS for offline route contract; physical execution denied**

## Accepted evidence

- `OTRR0/v0/heltec-v4-ot064-source-restore` binds one ESP32-S3 ROM serial
  recovery route to the exact 16 MiB Heltec bench profile.
- The route restores only the exact privately retained OT-064 application at
  `0x010000`, followed by the exact accepted `OTHP0/v0` partition-table binary
  at `0x008000`.
- The application write is unconditional. The source-table binary must be
  reproduced from public `partitions.csv` with the pinned ESP-IDF v6.0.2
  partition utility before its full size and digest are admitted.
- Offline validation rejects mismatched target, profile, route, toolchain,
  region order, offset, length, digest, forbidden operation, mutable execution
  surface, authority, or accepted-result state.
- The future physical route requires fresh nonzero operation/evidence binding,
  a complete all-`0xFF` pre-authorization source-region proof, closed-connection
  independent readback of both restored regions, and a separate bounded manual
  reset/startup observation requiring boot self-check PASS, Trail logo,
  `BLE ADVERTISING`, and at least two expected heartbeat records within 12
  seconds.
- Two distinct protected HMAC key roles and an independent monotonic rollback-
  floor contract are now defined, but no provider or irreversible resource is
  selected or provisioned.
- The private application currently has one proved retained copy. A future
  physical operation requires a separately staged second copy and independent
  pre-access digest checks; redundant custody is not yet accepted.

- Future physical admission requires one fresh read-only observation, bound to
  the same nonzero operation/evidence identities, proving secure boot, flash
  encryption, and secure download mode are all disabled. Unknown or mismatch
  denies before the first write.
- Any outcome after the first write invocation that is not completely verified
  closes the connection, keeps the unit in ROM without reset or boot-success
  claim, and records `RECOVERY-UNCERTAIN`. Private artifacts and a minimum
  private journal are preserved before transient cleanup; public evidence is
  sanitized, automatic retry is denied, and retry needs fresh authorization.

## Authority boundary

No device or port was accessed. No command, writer, executable hardware path,
flash, erase, reset, recovery, key, eFuse, partition transition, or runtime
activation occurred. The route is accepted only as an offline contract; its
partition-table, application, and post-restore boot results remain physically
unproved. Every physical authority remains false.

The recovery bundle remains incomplete because fresh unified operation
evidence, concrete key/floor providers, exact-unit recovery validation,
redundant private application custody, and separate physical-write authority
are absent. V1 Companion remains exact 39.75% and displays as 40%; the
historical baseline remains exact 31.75% and displays as 32%.
