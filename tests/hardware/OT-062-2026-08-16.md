# OT-062 Android Trail artwork and physical visual acceptance

Date: 2026-08-16

## Scope

This increment adds the owner-supplied Limited Underground Trail artwork to the
real Android Compose entry surface. It does not add a timed splash Activity,
startup delay, navigation state, permission, service behavior, or Bluetooth
behavior. Existing accessible `Limited Underground` and `Trail` text remains in
the composition; the artwork is decorative to accessibility services.

The owner separately requested a future fixed-logo/content-scroll treatment.
That visual change is not part of OT-062.

## Exact source and artwork

| File | Bytes | SHA-256 |
| --- | ---: | --- |
| `android/app/src/main/kotlin/io/github/nbjelanovic/otclient/MainActivity.kt` | 39,083 | `F955A0D4E965329CFAB67C386C9867ABC79CDEA775680FA6F080C7885233276D` |
| `android/app/src/test/kotlin/io/github/nbjelanovic/otclient/ConnectedDeviceManifestPolicyTest.kt` | 5,698 | `ADC3215B18A7D4A0508E7E1DF0A2393E6139FA8AABC0930AC255737D77F4EB17` |
| `android/app/src/main/res/drawable-nodpi/limited_underground_trail.png` | 2,559,044 | `A3024504BA261ADDAFD2A85F49F6BCE630D1E9AB994EEA348D5842A6D2AB7422` |

The approved PNG is 1774 x 887 pixels. The APK contains exactly one packaged
`limited_underground_trail.png`; its unpacked bytes and SHA-256 match the source
asset exactly.

## Android gate

The isolated Android foundation gate completed successfully on the exact source:

- thirteen XML suites and 136 JVM tests: protocol 29 plus application 107;
- zero failures, errors, or skips;
- warning-as-error lint: `No issues found.`;
- debug APK assembly and manifest inspection passed;
- the exact APK is 12,236,702 bytes with SHA-256
  `0E3A9C91E4AB68F0D6C45FB1D5A613CED7EE33154155AB2D0E76CE453F52918E`;
- package `io.github.nbjelanovic.otclient`, min SDK 26, target SDK 35, optional
  BLE, and the previously accepted permission surface are unchanged;
- the exact artwork/no-delay policy requires the resource, accessible brand
  text, fitted rendering, and absence of sleep, delayed-post, or splash APIs.

The retained build and test receipts contain no phone identifier, address,
serial, COM port, or other private device data.

## Physical observation

The exact APK replaced the prior debug build in place on the already authorized
physical Android 13/API 33 phone. A cold launch completed successfully. The
owner viewed the rendered screen and explicitly accepted the artwork and
framing. A coordinate-free Home/background action followed by reopening the
same Activity also succeeded.

No phone model, address, serial, screenshot, notification content, account,
coordinate, or other device identifier was retained. No Bluetooth scan,
selection, connection, pairing, permission change, or Heltec action was part of
this physical observation.

## Boundaries

This is exact Android artwork packaging and one physical visual acceptance. It
is not an Android system-splash implementation, clean-install result, broad
accessibility acceptance, release signing, store packaging, endurance, secure
GATT, authorization, Ready, Heltec OLED rendering, future touchscreen
rendering, or field proof. No simulator or emulator was used.

OT-061 target/runtime evidence is unchanged. The historical phone-independent
V1 calculation remains 31%; current V1 Companion and V2 Integrated scores
remain unmeasured. No public website status change is required for this visual-
only Android increment.
