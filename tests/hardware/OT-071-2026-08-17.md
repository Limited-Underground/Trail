# OT-071 read-only protected-storage transition evidence tooling

Date: 2026-08-17

## Accepted result

OpenTrail now has a streaming offline verifier and a denied, unit-bounded
physical-read plan for the exact OT-070 source proof. The verifier checks the
accepted 3,072-byte installed partition-table image and the complete 1 MiB
all-`0xFF` source region without loading the 1 MiB input as one retained result.
It emits only `OTPSTE0/v0` and one fixed outcome category.

The separate read plan binds the two future read-only regions and privacy
requirements but remains denied. It contains no command or executable hardware
reader. A future reader must be one-use and privately bound to the exact unit,
operation, evidence set, and port before separate owner authorization.

## Validation

- Eight focused verifier groups pass.
- Six protected-storage transition manifest groups pass.
- The existing nine-group Heltec target admission passes.
- Diff and privacy checks pass.

## Physical boundary

No device, port, or hardware was accessed for OT-071. The read plan remains
`DESIGN-ONLY-READ-AUTHORITY-ABSENT`, selects no unit, contains no command, and
has zero executed attempts. No raw read artifact or device-specific identifier
was retained. A physical read remains a later separately authorized action.

Even a future `SOURCE-PROOF-SATISFIED-ONLY` result will not authorize the
partition transition. Exact recovery evidence, protected key roles, an
independent rollback-floor decision, and a separate physical-write authority
will remain open.

Historical V1 remains exactly 31.75% and displays as 32%.
