# OT-075 — Offline protected-storage candidate and recovery gate

Date: 2026-08-17
Result: **PASS for offline candidate artifact; recovery incomplete; transition denied**

## Accepted evidence

- Pinned ESP-IDF v6.0.2 generated the exact `OTPS0/v0` candidate partition
  table offline.
- The generated binary is exactly 3,072 bytes with SHA-256
  `F83EDE6D0F206D6032147A2AF0B526700BCB888A6C0CADDF6DD17724E5600E72`.
- Decoding confirms unchanged boot/OTA rows, encrypted 64 KiB `ot_auth` at
  `0xF00000`, 960 KiB `ot_state` at `0xF10000`, and an exact 16 MiB end.
- The entry-record MD5 is `2D318DA9C2F0566688277C3BA110A8E9`;
  remaining table bytes are erased padding.
- Focused tests reject the wrong toolchain, source identity, row, checksum,
  length, padding, and any physical execution surface.

## Recovery result

The exact installed OT-064 application is 470,928 bytes with SHA-256
`A7D8E672CF9169F1D1D4E86EEFF80399C47A145E7D64904C207DD5F1B23F359B`.
No matching artifact was available. A clean offline rebuild of recorded source
commit `a327104ac67a3f5918a8b0191c96dceb05b5399b` under ESP-IDF v6.0.2
completed, but its SHA-256 was
`2B76593D5B797299DBB1A163C0152E0D463828C0B671887DA4AFAE11AFFE1D37`.
That nonmatching build was not accepted and its temporary worktree was removed.

## Authority and privacy boundary

No device or port was accessed. No flash, erase, reset, recovery, key, eFuse,
partition transition, or runtime activation occurred. Generated artifacts are
offline and ignored. The recovery bundle and transition remain denied. A later
operation must use the exact installed application recovery artifact, an
accepted recovery route, fresh same-operation evidence, and separate physical
write authorization.

V1 Companion remains exact 39.75% and displays as 40%; no milestone completion
changed.
