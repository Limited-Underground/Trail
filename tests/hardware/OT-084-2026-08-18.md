# OT-084 — ESP32-S3 SECURE_VERSION rollback-floor viability review

- Date: 2026-08-18
- Result: PASS — SECURE_VERSION rejected for authorization; no provider selected
- Hardware run: NOT RUN
- Device accessed: No
- Device/eFuse read or write: No
- Runtime or target build changed: No

## Accepted evidence

- Seven pinned ESP-IDF 6.0.2 sources prove the exact 16-bit field, irreversible
  popcount behavior, firmware anti-rollback role, OTA-only native partition
  model, and factory-layout incompatibility.
- The pure OT-084 evaluator and fixed sanitizer pass strict C++17 host tests,
  including every source-pin, field fact, architecture acceptance, authority,
  and disclosure mutation plus 100 deterministic repeats.
- Six SECURE_VERSION plan groups and the existing protected-provider,
  descriptor, inventory, and target-admission regressions pass.
- The complete 147-executable C++ host matrix and native CLI smoke checks pass.
  OT-084 changes no target build input.

## Boundary

Both reviewed on-chip floor candidates are rejected. External authenticated
monotonic hardware is only a future hardware-revision option: no part is
selected or present, and no compatibility, board change, device operation, or
authority is claimed. Provider, descriptor, inventory, provisioning,
transition, protected runtime, GATT authorization, and Ready remain closed.

V1 Companion remains exactly 39.75% and displays as 40%. The next gate is the
owner product-direction decision recorded by Decision 0027.
