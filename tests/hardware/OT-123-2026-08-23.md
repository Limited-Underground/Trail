# OT-123 Monocypher comparison benchmark preparation — 2026-08-23

## Result

**PASS — computer-only preparation; execution and matched control remain pending.**

The exact Monocypher 4.0.3 five-of-eight comparison target now has a closed
1,014-frame contract, strict parser, fail-closed two-node application-only
runner, two reproducible zero-warning builds, and a retained official ESP-IDF
JSON2 candidate size report. Its mandatory primitive gate pins the official
RFC 8032 Ed25519 and RFC 8439 ChaCha20-Poly1305 known-answer vectors and rejects
an all-zero low-order X25519 result. Before creating its fixed journal or making
any write, the runner reads back and hash-verifies the exact installed Trail
application on both nodes. No phone or device was accessed.

## Build evidence

Both fresh builds used ESP-IDF `v6.0.2` at commit
`7101770dc6db2667b3c477cc31365dd1acd6db4e`, ESP32-S3, the exact accepted
106,913-byte sdkconfig SHA-256
`4260688e6323cfda7a50912b4cc9c77a7b6f5133b6970b543bf0ce822ffd023f`,
`PROJECT_VER=ot107-config-v0`, no ccache, disabled component-manager networking,
and zero compiler warnings.

The two runs reproduced:

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| application BIN | 186,640 | `5e075fb791a658546fca714fc60de095ecbf14f7c443f414d3ac8642965a3b64` |
| application ELF | 3,842,940 | `51b02f506304837a11a9570e2ab9c43097d37c06ee1d74a8727bf1c106e3b420` |
| linker map | 3,733,060 | `830246c5eb5ad85a790df3b9dbc7a447896209faabd3cff38ed81508a4478353` |
| generated sdkconfig | 106,913 | `4260688e6323cfda7a50912b4cc9c77a7b6f5133b6970b543bf0ce822ffd023f` |

The private raw build-log SHA-256 values are
e4ce0eb66f255baf6b6d11cf78e678c3ca06969b720c4b0133c4b6216b3d3318
and 8d5038981e1f50b203adf810a100c641b07dd57952cf976e7a488e64e944336a.
Both path-independent normalized receipts have SHA-256
83b5a21d7f9fa0b00cbca6c6fa3934b676a8bf6fdd77ee693bc8944f9f8327d9,
and both official JSON2 reports have SHA-256
56bcf252c5392326ba07376881c960d76058750d933812da581caee337b02ac4.

## Resource-accounting boundary

The retained JSON2 report records candidate absolute `total_size=186516`,
`.data=12037`, `.bss=38920`, TLS `.tdata/.tbss=16`, and complete static RAM `50973` bytes. These values are
not presented as candidate deltas. `OTMRAC0/v0` requires a structurally matched
no-candidate control built twice under the same harness, instrumentation,
sdkconfig, partition layout, flags, project version, and pinned toolchain. That
control has not yet been implemented or built, so linked-flash and static-RAM
deltas remain null. The frozen formula includes DIRAM .data, .bss, and .noinit
plus TLS .tdata/.tbss and requires a signed-delta-capable successor validator
before admitting zero or negative results.

## Validation

Focused validation passed 67 checks/groups:

- 10 shared candidate-harness tests;
- 9 OT-121 frame-regression groups;
- 5 Monocypher harness tests;
- 9 Monocypher frame groups;
- 26 hardware-free runner tests; and
- 8 preparation/accounting integrity tests.

The complete `tools/Test-Host.ps1` repository gate also passed, including
publication-safety, Windows loader, and simulator validation.

The build and parser paths initialize no radio, expose no device identity, and
retain no port, hardware identifier, filesystem path, raw capture, key, or
secret in public evidence.

## Claim limits

This checkpoint prepares the exact comparison image and recovery-safe execution
path only. It does not execute a benchmark, access hardware, use radio, build
the matched control, admit a resource delta, complete Phase 2, select a
candidate or suite, prove support/compatibility/regulatory/production status,
implement secure LoRa, or change the V1 score.
