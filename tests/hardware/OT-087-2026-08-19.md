# OT-087 — Android unsigned release-build foundation evidence

- Date: 2026-08-19
- Result: PASS — unsigned release-build foundation accepted; execution blocked
- Planning outcome: `PLAN-ACCEPTED-EXECUTION-BLOCKED`
- Release gate: `NOT-EVALUATED`
- Hardware, phone, Bluetooth, or installation run: NOT RUN
- Signing key, certificate, or external account accessed: No
- Package signed, installed, uploaded, or distributed: No
- Android foundation gate: PASS — Gradle `BUILD SUCCESSFUL`

## Accepted evidence

- Decision 0031 freezes version code `1` / version name `1.0.0`, retains a
  `-dev` debug suffix, and configures an explicit non-debuggable,
  non-JNI-debuggable release build type with no signing configuration.
- The accepted Gradle run completed 137 actionable tasks: 129 executed and
  eight restored from cache. Protocol ran 29 tests across four suites; the
  debug application ran 110 tests across eleven suites; the release
  application ran 109 tests across ten suites. All 248 test executions passed
  with zero failures, errors, or skips. Debug and release warning-as-error
  lint, debug assembly, debug instrumentation assembly, and unsigned release
  assembly also passed.
- The bounded packaged-artifact inspector accepted an 8,475,824-byte local APK
  with SHA-256
  `392DA0CB606326418C53A86C3446BDE09537EA280345447046651599A7DF1AB9`.
  That size and digest identify only this disposable engineering output; they
  are not reproducibility, signing, distribution, or coherent release-
  candidate evidence.
- Packaged inspection found application ID
  `io.github.nbjelanovic.otclient`, version code/name `1` / `1.0.0`, minimum
  SDK 26, target SDK 35, no effective debuggable or test-only state, no
  instrumentation, and `allowBackup=false`.
- The packaged manifest used exactly six permissions: the five authored
  Bluetooth/foreground-service/notification permissions plus the separately
  admitted package-scoped AndroidX generated permission. No location, network,
  storage, boot, install-package, or wake-lock permission was present.
- The packaged full-backup rules exclude root, file, database, shared-
  preference, and external domains at path `.`. The data-extraction rules apply
  the same complete exclusions to both cloud backup and device transfer.
- Both packaged DEX files passed checksum verification. Neither DEX nor the
  merged manifest contained `PublicLinkAutomaticTerminationPolicy` or
  `PublicLinkProbeInstrumentation`. No v1 signature surface was present, and
  the pinned Android build tool returned the exact unsigned result
  `DOES NOT VERIFY` / missing `META-INF/MANIFEST.MF`.
- Seventeen deterministic OTAR scenario groups pass. The updated canonical
  plan has SHA-256
  `0bf0cc944cc1bff7eaa8988c6fd1511271a644e740304c444ec7777b9e589568`.
  It satisfies only the explicit release-variant and stable-version
  prerequisites.
- Six blockers remain: physical acceptance matrix, privacy/data-safety,
  release identity, rollback policy, signer/custody, and support policy. The
  plan still grants no execution authority and the release gate remains
  `NOT-EVALUATED`.
- Temporary DEX inspection files were deleted immediately. The disposable APK
  and task-local build cache are removed after the complete repository and
  publication validation gates finish; no raw dump or private path is retained
  in public evidence.

## Boundary

OT-087 proves only a production-shaped unsigned Android build foundation and
two satisfied plan prerequisites. It does not produce an accepted, immutable,
signed, installable, distributable, supported, reproducible, production-ready,
or operationally accepted release candidate.

No phone, Heltec, Bluetooth, signing material, certificate, account, store, or
distribution surface was accessed. Release-identity clearance, signer custody,
privacy/data-safety, rollback, support, the physical API/device matrix, every
lifecycle/usability/endurance case, protected authorization/`Ready`, and the
four-pair field proof remain open.

Android remains 60%. V1 Companion remains exact 43.75% and displays as 44%.
