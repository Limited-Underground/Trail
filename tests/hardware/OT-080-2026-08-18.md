# OT-080 — protected-root inventory reader route offline evidence

- Date: 2026-08-18
- Result: PASS — offline target-side route only
- Route: `OTPRR0/v0/esp32s3-esp-idf-6.0.2-metadata-adapter`
- Reader present: No
- Reader executed: No
- Hardware execution: NOT RUN
- Device accessed: No
- Runtime changed: No
- Irreversible action: No

## Accepted evidence

Source review established that the host Python `EspEfuses` route materializes all eFuse blocks, including KEY0 through KEY5 raw bytes, in host memory and can emit raw values. OT-080 records that route as rejected evidence rather than admitting it as a reader.

The accepted offline route:

- pins the accepted ESP-IDF 6.0.2 target-side API sources by full SHA-256;
- pins the reviewed Python/espefuse sources only as evidence for rejecting that host route;
- allowlists five decoded read-only key-purpose and protection APIs;
- forbids host full-summary, JSON, `raw_value`, raw block, raw key, raw bitmap, HMAC self-test, and every write/burn/protection surface;
- defines one possible future no-reset detecting connection and attempt but authorizes neither;
- limits output to normalized private six-slot, NVS-protection, rollback-floor, and security categories;
- requires transport close on every future exit without reset, retry, or mutation;
- leaves the rollback-floor descriptor and exact allocation null and unavailable for device reads; and
- leaves every physical, read, reset, write, provider, provisioning, and runtime authority false.

## Offline validation

`tests/host/protected_root_inventory_reader_plan_tests.py` validates the route identity, accepted and rejected source evidence, exact API allowlist and forbidden surfaces, absence of a reader and execution authority, bounded future connection policy, normalized metadata/privacy boundary, fixed sanitizer, cleanup behavior, and unchanged score assertion.

Validation completed with:

- six strict C++ route groups and 100 deterministic acceptance repetitions;
- six target-side route-plan groups;
- the existing six protected-root inventory-plan groups;
- all nine Heltec target-admission groups; and
- the complete 143-executable C++ host matrix plus publication-safety gates.

This PASS is limited to offline route coherence. It is not reader implementation evidence and is not device evidence.

## Unaccepted work

- an executable target-side adapter or reader;
- owner authorization for implementation or execution;
- any command, attempt, connection, unit, port, operation, or output-path binding;
- any security, key, eFuse, or device metadata read;
- a rollback-floor device-read descriptor;
- exact eFuse allocation or provider admission;
- any write, burn, protection change, provisioning, or runtime integration; and
- any project score increase.

## Public result

`OT-080 OFFLINE TARGET-SIDE READER ROUTE PASS — EXECUTION UNAUTHORIZED`
