# OT-131 executable Monocypher attempt abort

**ABORT RECORDED — the one-attempt authority is consumed, every benchmark-touched node is restored to exact Trail, and no Monocypher result is admitted.**

OT-131 first supplied the missing executable boundary around the unchanged OT-130 coordinator: one backend instance owns application write/readback, reset, endpoint-presence, and serial-open operations; a non-writing preflight verifies and resets both exact Trail applications; and execution requires the owner's separate visual acknowledgement. The replacement preparation and authority bind that adapter and supersede the unexecuted OT-130 authority without inheriting an attempt.

The single authorized application-only attempt then reached Node A benchmark readback, but capture failed closed with `capture_failed` / `preamble_invalid`. Its bounded diagnostics report lifecycle `stable_continuous`, one reset, one open, one START write, 512 bytes observed, one complete line, and zero buffered frame lines. Those counters classify the failure boundary; they do not disclose the preamble or prove a physical root cause.

Node A did not admit a capture and was restored, readback-verified, and reset to exact Trail. Node B was never benchmark-flashed and remained on Trail. The private receipt reports `restoration_complete=true`, and the owner visually confirmed both Trail logos after the abort.

The private journal and receipt remain unchanged and unpublished. The public abort receipt contains only anonymous A/B roles, the closed failure category and bounded counters, restoration state, and false claims. It contains no port, device identifier, filesystem path, raw capture, preamble content, backend text, private artifact, or run nonce.

## Public evidence

- Sanitized abort receipt: `tests/benchmarks/crypto/OT-131-OT005-MONOCYPHER-EXECUTION-ABORT-RECEIPT-V0.json`
- Result: `monocypher_execution_aborted`
- Failure: `capture_failed` / `preamble_invalid`
- Benchmark-touched nodes restored: 1/1
- Restoration complete: yes
- Owner-observed Trail logos after abort: 2/2
- Monocypher benchmark results admitted: 0
- Radio used: no

The OT-131 authority is consumed by the abort and is not reusable. Phase 2 remains incomplete; no candidate or suite is selected, and no support, compatibility, regulatory, production, secure-LoRa, end-to-end, or score claim changes. V1 remains exact 43.75%, displayed 44%.
