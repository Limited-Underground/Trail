# OT-136 immutable OT-135 successor binding and one-attempt authority

**HOST-ONLY EXECUTABLE BINDING ACCEPTED — no device access, flash, benchmark,
radio, or phone operation occurred. The authority was not executed.**

OT-136 leaves OT-129 through OT-135 byte-for-byte unchanged and creates a new
executable boundary around the OT-135 byte-bounded preamble runner. The bundle
binds a fresh coordinator/private-state namespace, one concrete endpoint-bound
adapter, the unchanged strict parser/schema, the exact benchmark application,
and the exact Trail restoration application.

## Bound execution and restoration behavior

- Exactly two distinct endpoints are required.
- Both installed Trail applications must pass exact readback and reset before a
  private journal or application write is allowed.
- Writes are application-only at offset `0x10000`; benchmark readback must pass
  before capture.
- The exact OT-135 runner preserves the 512-byte cumulative complete-record
  pre-READY budget, fixed deadlines, exact READY transition, strict frame
  boundaries, privacy-safe counters, and unchanged real 1,014-frame parser.
- Every touched node must restore, read back, and hard-reset exactly to Trail.
  Recovery-only mode does not require the benchmark artifact and remains
  retryable until restoration succeeds.
- Public results retain only anonymous roles, closed failure codes, and bounded
  counters. Ports, device identifiers, filesystem paths, raw capture, and raw
  exception text remain private.

## Workspace-local one-attempt boundary

The fresh authority grants exactly one two-node application-only attempt in
this workspace and is consumed on success or abort. The fixed private OT-136
journal is created before the first benchmark write, and its existence blocks a
second execution through the accepted coordinator and adapter.

This is a workspace-local operational guarantee. It is not a global ledger,
hardware-enforced monotonic counter, signed consumption proof, or protection
against copying the public repository or authority, deleting private state, or
running from another workspace. None of those actions is authorized by this
record. The authority is accepted here but remains unexecuted.

## Final immutable identities

- Preparation: schema `OT136MEBP0`, ID
  `OT-136-OT005-MONOCYPHER-EXECUTABLE-BUNDLE-PREPARATION-V0`, status
  `executable_bundle_prepared_authority_pending`, 4,725 bytes, raw SHA-256
  `bc968110fce49e45352d7dfd884a9f8fb0d4a3a8a2ce4fa510087f97570e113c`,
  canonical SHA-256
  `391dbb41215129ff6a55f3feb1338728ad0c7abce6b8ffeaca4adf7d4a1a1640`.
- Authority: schema `OT136MOAA0`, ID
  `OT-136-OT005-MONOCYPHER-ONE-ATTEMPT-AUTHORITY-V0`, status
  `authorized_one_attempt_not_executed`, 4,142 bytes, raw SHA-256
  `a890b570e088ecb941b3cd8a8915ba2950d99224963efaccf1b83c64e79296ad`,
  canonical SHA-256
  `dab18d4b4d5b7bee6ee2f8da4c15e8c7377f6bb57222b0a6eeb2dc4e3ab13e21`.
- Coordinator: 25,904 bytes, SHA-256
  `3d340194f98b9d99d7833510d19b142e660ee2999d2d4e20000ca13d7f380867`.
- Hardware adapter: 12,848 bytes, SHA-256
  `73a3cfd606ae2249c1920b27fa94daccecde20154f8b484de03fdd003c13aba8`.
- Execution-authority validator: 22,790 bytes, SHA-256
  `47bdeb66fa1cf25abb7e09cf89bb21b8dce2f88759ca3ab4fd2fa767f8c57eb9`.

## Validation

- OT-136 restoration-safe coordinator: 11/11 passed.
- OT-136 executable-bundle/authority validator: 10/10 passed.
- OT-136 concrete hardware adapter: 14/14 passed without device access.
- OT-135 and frozen OT-132 adversarial runners: 14/14 each passed.
- Frozen OT-133 coordinator, authority, adapter, and abort suites:
  11/11, 10/10, 14/14, and 7/7 passed.
- Deterministic raw-byte checkout audit: 81/81 passed.
- Authority validation confirms `attempt_count=1`, `reusable=false`, and the
  benchmark remains unexecuted.

## Boundary and next gate

No hardware was accessed and no firmware was flashed. No Monocypher frame,
timing, primitive, resource, or benchmark result is admitted. No radio,
candidate selection, suite selection, Phase 2 completion, support,
compatibility, regulatory, production, secure-LoRa, end-to-end, or score claim
is added. The latest admitted benchmark result remains OT-122. V1 remains exact
43.75%, displayed 44%; the historical baseline remains exact 31.75%, displayed
32%.

The next permitted hardware step is only the single OT-136-authorized two-node
application-only attempt from this workspace, with exact Trail restoration of
every touched node on success or abort.

No public website status update is required because OT-136 changes no public
capability, milestone completion, score, or physical acceptance claim.
