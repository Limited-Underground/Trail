# OT-094 — Host-only OT-005 candidate-readiness contract

Date: 2026-08-20

Result: `CANDIDATE-READINESS-CONTRACT-FROZEN-HOST-ONLY; OTCB0-EXECUTION-BLOCKED`

## Accepted artifact

OT-094 accepts the strict machine-readable
[`OTCBR0/v0` ledger](../benchmarks/crypto/OT-094-OT005-CANDIDATE-READINESS-V0.json)
with readiness ID `OT-094-OT005-CANDIDATE-READINESS-V0`, status
`readiness_blocked`, and canonical SHA-256
`705b30693196e2f46d8bda7c17acb1e04d7b9092c4a3817286c14d189001b9d3`.

The ledger binds the unchanged historical `OTCB0/v0` plan SHA-256
`49792b585286823ffa9b7589704d57e8393b3dbf3d514917ffd7b5970301edb7`
and accepted `OTCBL0/v0` baseline SHA-256
`16ffe83af7e3c1f00b5d123eae30e3ac4a0ea2dea0cb08bcc60b990d3e881733`.
Neither artifact was rewritten.

## Current blocker result

All six ordered readiness requirements remain blocked:

1. `exact_received_target_profile_unresolved`
2. `final_candidate_build_configuration_unresolved`
3. `espressif_libsodium_source_lock_absent`
4. `esp_idf_mbedtls_psa_dependency_lock_and_api_config_unresolved`
5. `monocypher_source_lock_absent`
6. `direct_radio_mtu_phy_region_unresolved`

The target's exact received revision and RF variant remain null and the target
is unsupported. The accepted OT-093 sdkconfig remains a pre-selection baseline,
not the final common or candidate-overlay configuration. Espressif libsodium and
Monocypher have no project-owned source/dependency lock. The installed ESP-IDF
mbedTLS 4.1.0 source observation remains an unlocked comparison observation,
not API/config eligibility. No OpenTrail direct-radio region, MTU, or full PHY
profile is frozen.

## Legacy plan hardening

A legacy `OTCB0/v0` plan that merely declares itself `ready` is structural input
only. Without an independently accepted, fully resolved readiness artifact it
cannot create a result template or yield a passing evaluation. The accepted-ready
trust-anchor set is empty. The canonical plan remains `draft_blocked`,
`readiness_verified=false`, and `execution_authorized=false`.

## Validation

- `PASS: 10 crypto benchmark evidence scenario groups`
- `PASS: 13 OTCBR0 candidate-readiness scenario groups`
- `PASS: 13 OTCBL0 build-lock scenario groups`
- `PASS: 12 Heltec V4 bench target scenario groups`
- `PASS: 16 V1/V1.5 scope-admission scenario groups`
- Publication-safety scanner and its host scenarios: pass
- Complete `tools/Test-Host.ps1`: exit `0`

The readiness validator enforces exact fields, types, ordering, immutable
bindings, bounded structure, duplicate-key rejection, privacy, cross-field
coherence, sanitized failures, and no execution-capable imports or APIs.

## Authority and privacy

No dependency acquisition, candidate import, result-template creation,
benchmark build, benchmark execution, hardware/device access, radio
transmission, key/entropy operation, suite selection, packet-v1 selection,
implementation, physical evidence, or score credit occurred. Public evidence
contains no local path, device identifier, address, key, secret, PIN, packet
content, or private trace.

## Progress and next gate

Android remains 60%. V1 Companion remains exact 43.75% and displayed 44%.
The historical standalone baseline remains exact 31.75% and displayed 32%.
V1.5 and V2 remain unmeasured.

Next, close all six readiness requirements under separate authority. Then
accept a new immutable executable benchmark plan, separately authorize and run
the exact target comparison, and make a later explicit suite/wire decision.
