# OT-132 host-only Monocypher opaque-preamble correction

**HOST-ONLY CORRECTION ACCEPTED — no device access, flash, benchmark, radio,
phone operation, or execution authority occurred.**

OT-132 adds a successor capture runner for the bounded OT-131
`capture_failed` / `preamble_invalid` boundary. It does not alter OT-129 or any
OT-130/OT-131 preparation, authority, adapter, receipt, or evidence.

## Corrected boundary

- Complete pre-`READY` records may contain arbitrary bytes and are ignored only
  within the unchanged maximum of eight records and 512 total bytes.
- Only exact complete `OTCBXCTL1 READY\n` transitions to capture. Embedded,
  prefixed, suffixed, case-changed, or CRLF lookalikes remain opaque chatter.
- When the chatter budget is exactly full, only an in-progress byte-for-byte
  prefix of exact `READY` may finish; the first mismatching byte fails closed.
- A result frame before `READY` remains `frame_before_ready`. Duplicate `READY`
  remains `ready_invalid`. Any opaque record after `READY` remains
  `frame_malformed`.
- Partial bytes, fixed deadlines, one reset/open lifecycle, exact frame count,
  the unchanged 1,014-frame parser, and the privacy-safe counter-only diagnostic
  schema remain strict.

All opaque test records are fabricated fixtures. They are not recovered device
bytes and do not establish or imply the physical content or root cause of the
OT-131 preamble.

## Validation

- OT-132 adversarial transport tests: 14/14 passed.
- Frozen OT-129 transport: 11/11 passed.
- Frozen OT-129 firmware contract: passed.
- Frozen OT-130 coordinator: 11/11 passed.
- Frozen OT-130 bundle/authority: 11/11 passed.
- Frozen OT-131 execution authority: 10/10 passed.
- Frozen OT-131 hardware adapter: 14/14 passed.
- Frozen OT-131 abort record: 6/6 passed.
- Unchanged real OT-123 frame parser: 9/9 groups passed.
- OT-132 runner SHA-256:
  `d131286969d82f0ddef8b3051b6d64588042a4d49fafa2aca1d164de617f6a3d`
- OT-132 tests SHA-256:
  `ebb0999f1fd6254247ec592c116093df39ef3187907366a651042cbaaebdeee4`

## Boundary

This is not an executable hardware bundle or authority. A later task must bind
the exact successor runner, unchanged strict parser, restoration-safe
coordinator, benchmark application, and exact Trail restoration image, then
require fresh explicit non-reusable one-attempt authority before either device
is accessed.

No Monocypher benchmark result, frame, timing, resource result, radio evidence,
candidate selection, Phase 2 completion, physical evidence, support claim, or
score is admitted. OT-131 remains consumed. V1 remains exact 43.75%, displayed
44%.
