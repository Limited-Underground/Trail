# OT-081 — Build-only target-side protected-root key-roster adapter

Date: 2026-08-18
Result: PASS — build and synthetic-host evidence only
Hardware run: NOT RUN
Device accessed: No
Reader executed: No

## Accepted result

The Heltec target now contains a target-local one-use adapter for the exact five
decoded read-only ESP-IDF APIs admitted by OT-080. It evaluates all six logical
key slots, retains no raw bytes, and publishes a roster only after all calls and
invariants succeed. Invalid purpose, contradictory unused evidence, re-entry,
and repeat use fail closed with an empty result.

The source distinguishes `proven_unused` from provisioning. A false unused
result does not become a provisioned-key claim, and reservation is not inferred.
The output is therefore coarse roster evidence only and cannot satisfy the
complete OT-079 inventory contract.

## Exact source evidence

| File | Bytes | SHA-256 |
| --- | ---: | --- |
| `main/companion_protected_root_key_roster_adapter.hpp` | 1,844 | `8C96CA3C3F3012BC9441BD278748343674380403651C38175382877200758C94` |
| `main/companion_protected_root_key_roster_adapter.cpp` | 4,107 | `4DDD22907FF53ADB9DB44058892820DF19CCA975816D70CBDB8A168AE09A6EFC` |
| `tests/host/companion_protected_root_key_roster_adapter_tests.cpp` | 9,821 | `EF7308F8C22803F0EBB9EC49E3C22F151AE508C5A7F34C4E5F8BE2834D768749` |
| `tests/host/ot081_esp_idf/esp_efuse.h` | 878 | `3457344827E9D2A4D1E1169012E3FB87837D2D51C7D71D1DED97683FC17C16F2` |

The build helper independently rehashed the five reviewed ESP-IDF 6.0.2 source
files frozen by OT-080 before compiling.

## Validation evidence

- strict C++17 warning-as-error adapter test: PASS, five groups;
- re-entry mutation at every one of the 30 admitted calls: PASS;
- invalid purpose stops before any later slot metadata call: PASS;
- Heltec target admission: PASS, 10/10 groups;
- complete host validation: PASS, 144 executables;
- pinned ESP-IDF 6.0.2 target build: PASS twice;
- compiled adapter object present exactly once;
- both builds produced identical artifacts:
  - BIN: 470,928 bytes, SHA-256
    `04CA23950DA585A7D87F6A92C4BA2EAF51B7604EC9A5FFE50421BEEC77C0927D`;
  - ELF: SHA-256
    `6F060AF199DAEBFA254B13BACCAB42ED329999E0CEEA4479CBF0163A761D979F`;
  - map: SHA-256
    `1B1A31F23213C0025B410768B8BC26AC033317161E872E364F5DF459BA6D00DF`;
- final build evidence: SHA-256
  `AB13F2108F8A5CE48F8A8E17816D81475463E1219B91E16FEF9F4BDA5C0444F6`.

The build receipt says `BUILD-COMPILED-NOT-RUNTIME-INJECTED` and
`NOT-AUTHORIZED-NOT-RUN`. It binds the exact adapter header and source hashes
plus the compiled-object name, 28,380-byte length, and SHA-256
`0682CE5D0DC65DB8B6075168B950277D9C4491DA069365E4850A841F92566A9F`.

## Limits preserved

No target startup or runtime source includes or constructs the adapter. No
device connection, reset, eFuse read, raw dump, key material access, HMAC,
write/burn, protection change, provisioning, field selection, protected-storage
initialization, GATT authorization, or Ready operation occurred or was
authorized. A coarse key-roster leaf is present, but the complete inventory
reader/orchestrator remains absent and all operation attempts/connections remain zero.

Configured-NVS conflict, full security state, provisioning/reservation facts,
and rollback-floor facts are still unavailable. The next safe increment is to
define equally bounded decoded sources for the remaining non-floor inventory
facts; an exact non-secret rollback-floor descriptor still requires a separate
decision before any floor-read design.

This build-only result does not change milestone completion. V1 Companion
remains exact 39.75%, displayed as 40%.
