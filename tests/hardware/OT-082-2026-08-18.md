# OT-082 — build-only protected-root configuration/security adapter

- Date: 2026-08-18
- Result: BUILD/HOST PASS; DEVICE NOT ACCESSED
- Target: `heltec_v4_bench`
- Hardware run: No
- Device, eFuse, NVS, or security-state read: No
- Runtime injected or executed: No

## Accepted evidence

OT-082 adds a target-local, one-use, all-or-none source for the default build's
normalized NVS protection configuration and four decoded security-state values.
The strict focused C++ test passed, the Heltec static admission gate passed 11
groups, and the pinned ESP-IDF v6.0.2 target build compiled the source exactly
once. The build receipt binds the exact header, source, and object identities.

The built application remained 470,928 bytes. The accepted build proves only
compilation and the bounded source surface. It does not report any current
device value and does not establish that NVS is protected on a unit.

The complete 145-executable C++ host matrix passed. Two consecutive pinned
target builds produced the identical 470,928-byte application SHA-256
`B2AEDC3D744C861BD0380F04740B14430355B7EAAF3A72893828D83A50B97042`.
The second receipt independently retained source SHA-256
`879E1D6155C0D9F5460D9B57377DED518B9DCB5D6573ABF8DA6BFE463BD5852C`
and object SHA-256
`4327554E55FE0F5E7BE7B8B6D12B159B9F045ED57D64A1C243A53D3D7E6433D5`.

## Authority and remaining gates

No complete inventory reader/orchestrator, command, transport, device access,
metadata read, reset, provisioning, write, provider admission, or runtime
activation was authorized or performed. Raw fields, blocks, blobs, keys,
bitmaps, identifiers, and paths remain unavailable.

Configured-NVS conflict composition, runtime-override proof, provisioning and
reservation facts, and an exact non-secret rollback-floor descriptor remain
open. V1 completion remains exact 39.75% and displays as 40%.

Publication safety, canonical score arithmetic, remote commit, and website
deployment remain separate publication gates.
