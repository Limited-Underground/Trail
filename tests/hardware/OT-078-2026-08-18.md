# OT-078 — Offline protected-root provider selection

Date: 2026-08-18
Result: **PASS — provider types selected offline; physical admission closed**

## Accepted evidence

- The two protected-key roles select distinct ESP32-S3 `HMAC_UP` eFuse block
  providers. No block identifier is selected or published.
- A pure pair evaluator denies absent, out-of-range, equal, unprovisioned,
  wrongly purposed, unprotected, untested, stale, mixed-evidence, reentrant,
  or incorrectly configured observations.
- The rollback floor conditionally selects a dedicated custom ESP32-S3
  user-eFuse thermometer provider class. Its exact block, bit range, and
  capacity remain unknown.
- A pure floor evaluator accepts only a contiguous low-order set-bit prefix,
  one-step advance, and exact post-advance reread. It classifies definite
  pre-burn failure separately from post-burn uncertainty, denies reentry and
  exhaustion, and preserves fail-closed reboot reconciliation.
- The exact manifest contains no command, port, attempt, physical identity,
  key material, or authority.

## Not proved or authorized

No eFuse inventory was read. No key or counter field was selected, generated,
provisioned, read-protected, burned, or tested on hardware. Protected NVS,
private bond authority, rollback resistance on the target, GATT authorization,
Ready, partition transition, application write, recovery rehearsal, and
production anti-tamper remain unproved and unauthorized.

The evidence-weighted V1 Companion result remains exact 39.75%, displayed as
40%; provider-selection code and offline contracts do not complete a physical
milestone.
