# OT-083 — ESP32-S3 rollback-floor descriptor viability review

- Date: 2026-08-18
- Result: PASS — reviewed custom USER_DATA thermometer rejected; no provider selected
- Hardware run: NOT RUN
- Device accessed: No
- Device/eFuse read or write: No
- Runtime or target build changed: No

## Accepted evidence

- Pinned ESP-IDF 6.0.2 sources prove ESP32-S3 USER_DATA is in an
  Reed-Solomon-coded eFuse block whose coding unit cannot be written again once
  nonempty.
- That write behavior is incompatible with the required repeated independent
  one-bit authorization-floor advances.
- The pure OT-083 evaluator and fixed sanitizer pass strict C++17 host tests,
  including exact source-pin mutation, descriptor-fact mutation, authority and
  disclosure denial, malformed-enum containment, and 100 deterministic repeats.
- The existing rollback-floor provider test now proves that complete factual
  fields and authority cannot revive the rejected provider class.
- Six descriptor-plan groups, five protected-provider-plan groups, six
  protected-root-inventory-plan groups, and eleven Heltec target-admission
  groups pass.
- The complete 146-executable host gate passes.

## Boundary

The two protected HMAC roles remain selected by type only. The rollback-floor
provider, block, first bit, and capacity are null; selection, physical
admission, provisioning, device access, eFuse read/write, and runtime authority
remain false. No target source, target build input, device byte, or installed
firmware changed, so no target rebuild or hardware action was required.

V1 Companion remains exactly 39.75% and displays as 40%. The next gate is an
architecture decision between a separate review of the limited
`SECURE_VERSION` primitive and external monotonic hardware. Inventory
composition remains blocked until a viable independent floor exists.
