# OT-144 corrected runtime/preparation binding

**HOST-ONLY FAIL-CLOSED CORRECTION ACCEPTED — no device access, flash, reset,
benchmark, radio, or phone operation occurred. No execution is authorized.**

OT-144 records and corrects a cross-layer mismatch discovered before hardware
execution. It preserves Decision 0080 and every OT-143 artifact as immutable
history and creates a fresh runtime/preparation boundary for the corrected
OT-142 application.

## Discovery

- Accepted OT-143 authority raw SHA-256:
  `235e1227ef8ebed75a510335b017f9c0539d0508426a11414546591002f1bce0`.
- The authority is unconsumed: no fixed private OT-143 journal, execution
  receipt, or recovery receipt exists.
- The OT-143 preparation and authority pin the 149,824-byte
  `ot142_monocypher_corrected_bench.bin` with SHA-256
  `8e345d41e869cf781e3d6eb3b2269f26882b7c1d9f43b856ff5795c6cc56a034`.
- The OT-143 coordinator instead pins the 149,920-byte
  `ot139_monocypher_quiet_bench.bin` with SHA-256
  `29eee8c7294064d772770e2b4591c352eb0a9068b63f5a1fc62d89481ec5f204`.
- Because the accepted adapter invokes that coordinator, the authority cannot
  execute its own corrected image. The mismatch was found host-only before any
  endpoint discovery or device I/O.

The authority is therefore historical and unconsumed, but non-executable. It
is not marked consumed, replaced in place, weakened, or repurposed.

## Corrected OT-144 boundary

The fresh OT-144 coordinator, adapter, authority validator, and preparation
bind the exact corrected target consistently:

- benchmark name: `ot142_monocypher_corrected_bench.bin`;
- benchmark bytes: 149,824;
- benchmark SHA-256:
  `8e345d41e869cf781e3d6eb3b2269f26882b7c1d9f43b856ff5795c6cc56a034`;
- application-only write offset: `0x10000`;
- exact Trail restoration image: 473,152 bytes, SHA-256
  `0c40aeb6c95ade9940aa21065cbc73a72dcd82e96ade9d13126693147feb5741`.

The accepted two-node preflight, fixed private-state separation, benchmark
readback, OT-135 START/READY and 512-byte preamble boundaries, strict
1,014-frame parser/schema, privacy-safe terminal records, and exact
restore/readback/reset behavior remain unchanged. Real-file cross-layer tests
must reject a stale coordinator image pin or any other runtime/preparation
disagreement before hardware access.

## Immutable identities

- Preparation: schema `OT144MEBP0`, ID
  `OT-144-OT005-MONOCYPHER-EXECUTABLE-BUNDLE-PREPARATION-V0`, status
  `executable_bundle_prepared_authority_pending`, `6,298` bytes, raw
  SHA-256 `9736e6c68647da594e31b8c32e076fd40c2fccedd1bbd1d740b5cc0443667455`.
- Coordinator: `25,912` bytes, SHA-256
  `2e8955c7a425c33208d8b6e944fdef52259cd146274bec44c543e1f4b7af69be`.
- Hardware adapter: `12,854` bytes, SHA-256
  `19eb64c29deda5af9e4d523a8bc1630ae2f9350cba4043e35ac7691146b09b71`.
- Execution-authority validator: `26,519` bytes,
  SHA-256 `d0598b529438d0b37671d65fd6c17d97791d2ab564806b378203e2834ecd1a52`.

## Validation

- OT-144 coordinator: `11/11 passed`.
- OT-144 hardware adapter: `14/14 passed`.
- OT-144 authority/preparation validator:
  `10/10 passed`.
- OT-144 real-file cross-layer binding consistency:
  `4/4 passed`.
- Deterministic raw-byte audit: `130/130 passed`.
- Complete Windows host matrix: `passed`.

## Boundary and next gate

OT-144 creates no authority JSON and authorizes no attempt. No Monocypher frame,
timing, primitive, resource, or benchmark result is admitted. No radio,
candidate selection, suite selection, Phase 2 completion, support,
compatibility, regulatory, production, secure-LoRa, end-to-end, or score claim
is added. The latest admitted benchmark result remains OT-122. V1 remains exact
43.75%, displayed 44%; the historical baseline remains exact 31.75%, displayed
32%.

Before any device access, the owner must separately approve creation and
acceptance of a fresh OT-144 non-reusable authority for exactly one two-node
application-only attempt. Every touched node must restore exactly to Trail on
success or abort.

No public website status update is required because OT-144 changes no public
capability, milestone completion, score, field-test readiness, support, release,
or physical-acceptance claim.

