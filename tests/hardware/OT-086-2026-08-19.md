# OT-086 — Android operational-release admission evidence

- Date: 2026-08-19
- Result: PASS — plan accepted; execution blocked
- Planning outcome: `PLAN-ACCEPTED-EXECUTION-BLOCKED`
- Release gate: `NOT-EVALUATED`
- Hardware or phone run: NOT RUN
- Signing key or external account accessed: No
- Android foundation gate: PASS — Gradle `BUILD SUCCESSFUL`
- Production/release application built, installed, removed, signed, or
  distributed: No

## Accepted evidence

- Decision 0030 and the `OTAR0/v0` contract freeze one machine-checkable
  operational-release evidence boundary. At OT-086 acceptance in commit
  `236cbadc5f6bcc31cdc54919fcd08f837e4ffe46`, the then-checked-in plan had ID
  `OT-086-ANDROID-OPERATIONAL-RELEASE-V0` and canonical SHA-256
  `30c163a1378acf57391e6fd3fadcf82ad3b29ed3ca205f20ad93ed80c0503f74`.
- The only admitted distribution scope is
  `private-sideload-v1-pilot`: controlled private installation on four V1
  pilot phones that must each be approved and frozen before execution. It does
  not constitute public/store distribution, package, installation, supported-
  device, or release evidence.
- A future Google Play scope is outside this plan. It requires a separate
  current target-API policy recheck and any required Android toolchain and
  target-SDK update before admission.
- The pure validator reports eight exact public blockers: physical acceptance
  matrix, privacy/data-safety approval, production variant, release identity,
  release version, rollback policy, signer/custody, and support policy.
- Sixteen deterministic scenario groups pass. They cover the canonical
  blocked result, digest determinism, exact schema shape, baseline/version
  coherence including the first-supported-release no-upgrade-credit boundary,
  frozen private-sideload scope, signing/custody shape, exact blocker
  derivation, non-authoritative synthetic readiness, authored/generated
  permission and platform/policy drift, the Android source-baseline drift
  regression, absence of OT-085 test-only components, privacy and execution-
  authority denial, prohibited private fields/values, bounded duplicate-key/
  size/path-sanitized UTF-8 input loading without private-value echo,
  deterministic CLI failure, and absence of execution/output-creation
  surfaces.
- The complete Android foundation gate passed on 2026-08-19 with Gradle
  `BUILD SUCCESSFUL`: 81 actionable tasks, 78 executed and 3 from cache. The
  gate included `testDebugUnitTest`, `lintDebug`, `assembleDebug`, and
  `assembleDebugAndroidTest`. These are development-foundation checks and
  debug/test assemblies; they are not a production variant, immutable release
  artifact, release signature, installation, distribution, or operational-
  release pass.
- The validator can emit only a plan result. It grants no signing, store,
  account, device, installation, hardware, protected-characteristic, ownership,
  provisioning, `Ready`, LoRa, GNSS, or field authority.

## Boundary

OT-086 proves that a later Android operational-release run is bounded and fails
closed. It does not supply a production variant or candidate, signer/custody
policy, immutable artifact, supported-device matrix, lifecycle/endurance/
privacy/accessibility/support result, rollback/removal result, or coherent
cleanup evidence set.

`OTAR0/v0` is the first supported release plan. Its
`first-release-not-applicable` in-place-upgrade status earns no credit; the
accepted future candidate must instead pass uninstall/reinstall, promised data
removal, and downgrade rejection/no-downgrade handling. A real in-place
upgrade is mandatory for the next supported release.

Android remains 60%. V1 Companion remains exact 43.75% and displays as 44%.
The next Android release-track gate is to satisfy the named prerequisites and
execute the accepted `private-sideload-v1-pilot` plan as one coherent
candidate evidence set under separate operation-specific authorization.
Protected one-phone authorization/`Ready` and the four-pair Companion field
proof remain separate mandatory V1 gates.
