# OT-037 companion-codec target-link evidence

Date: 2026-08-14

Result: build and link passed; **BUILD-LINKED-NOT-RUN**; **NOT-FLASHED**

## Scope

This acceptance linked the host-accepted companion framing and semantic codec
sources into the generic `firmware/targets/heltec_v4_bench` ESP-IDF v6.0.2
candidate. It did not discover, open, select, erase, write, reset, reboot, or
otherwise access a physical device.

The boot path now compares exact fixed 16-byte `OTB0`, 20-byte `OTA0`, and
40-byte `OTC0` plus `OTA0` vectors, decodes them, and requires the semantic
dispatcher to accept the exact action-request binding. It emits only fixed
self-check PASS/FAIL text. Failure suspends before the startup line or recurring
heartbeat. This self-check is compiled but has not run on hardware.

## Accepted build result

- Static target admission passed 3/3 and 100/100 repeats.
- `tools/Build-HeltecV4BenchTarget.ps1` exited 0 under ESP-IDF v6.0.2.
- ESP-IDF size analysis reported 145,657 application bytes, an increase of
  3,692 bytes over the OT-034 baseline.
- The link map contains the companion protocol and semantic codec objects.
- An incremental rebuild completed in 16.91 seconds and reproduced every
  artifact hash.
- The ignored evidence record remained `NOT-FLASHED`,
  `BUILD-LINKED-NOT-RUN`, and `UNREVIEWED-RUNTIME`.

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `opentrail_heltec_v4_bench.bin` | 145,776 | `5484C640A4F5A5AF7EAEE00C2F5E3B3D1223111017AB599A97D6FCC28BDE3C11` |
| `opentrail_heltec_v4_bench.elf` | 3,471,348 | `F11FADB39DEEF6F4714A8DD66052803AF64138FAC15B0B165A993B9D8555D6E0` |
| `opentrail_heltec_v4_bench.map` | 3,437,706 | `D5B10A0D613552B25C6849D8F4177AC2BE70EBAB67ED247770859BD208102893` |
| `bootloader.bin` | 21,088 | `564FC8660031072583C3A834656DA6BE858532EFBC863B5BC9587FACBD98D641` |
| `partition-table.bin` | 3,072 | `7F00B6C042A89B15B0CAC534F82ED988CAF29278FF5700B0C511EB1B5BB7C820` |
| generated `sdkconfig` | 90,375 | `472C086ED47588370F20E663EF413B9A0192A43FC9BD9C31E43D188749152E41` |

## Evidence boundary and next gate

The image still uses the generic 2 MB flash, DIO, 80 MHz profile rather than an
authoritative configuration for the observed 16 MB flash and 2 MB PSRAM bench
candidates. No BLE stack, GATT service, controller session owner, radio, GNSS,
application storage, display, power, or complete device runtime is present.

Before any physical write, exact-board configuration and manual ROM recovery
must pass the existing owner-gated procedure on a sacrificial-first unit. This
record is build, link, size, and reproducibility evidence only.
