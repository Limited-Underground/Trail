# OT-079 — protected-root inventory offline evidence

- Date: 2026-08-18
- Result: PASS — offline plan and supplied-evidence verifier only
- Hardware execution: NOT RUN
- Device accessed: No
- Runtime changed: No
- Irreversible action: No

## Accepted evidence

The OT-079 protected-root inventory plan, pure supplied-evidence verifier, and Decision 0022 form a coherent, fail-closed contract for a possible future read-only inventory.

The accepted plan:

- keeps every device, connection, reset, bootloader, ROM-command, metadata-read, key-read, eFuse-read, raw-dump, write, burn, protection-change, provider-admission, provisioning, and runtime-integration authority false;
- leaves the exact eFuse block, first bit, and capacity absent;
- keeps the conditional provider unselected, unadmitted, unprovisioned, inactive, and unavailable to runtime code;
- requires fresh same-operation evidence and denies missing, unknown, stale, cross-operation, or mismatched facts;
- treats the OT-077 security state as an observation requirement, not permission to inspect or modify a unit;
- separates private exact-unit evidence from the fixed sanitized public result;
- accepts complete unfavorable inventory for review without selecting or admitting a provider; and
- does not claim hardware, factory-state, allocation, provisioning, or runtime validation.

## Validation boundary

This PASS records offline plan and deterministic verifier coherence only. No command was issued to a device, no connection or port was inspected, no reset or bootloader action occurred, and no security, key, or eFuse metadata was read.

The strict focused C++ verifier test passed, including 100 deterministic
evaluations. Six inventory-plan groups, five protected-provider-plan groups,
nine Heltec target-admission groups, and the complete 142-executable C++ host
matrix plus publication-safety gate passed with exit code 0.

The following remain unaccepted:

- owner authorization for a read-only inventory;
- any exact-unit or factory-state observation;
- any eFuse block, field, bit range, or capacity;
- provider admission;
- protected-key provisioning;
- any eFuse write, burn, or protection change;
- runtime integration; and
- any project score increase.

## Public result

`OTPRI0/v0/OFFLINE-PLAN-VERIFIER-PASS — DEVICE INVENTORY NOT AUTHORIZED OR RUN`

No raw inventory, exact device identifier, private connection binding, or operation journal exists from this work.
