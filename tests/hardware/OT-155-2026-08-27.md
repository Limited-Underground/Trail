# OT-155 OT-154-authorized Noise XK radio execution abort

**Date:** 2026-08-27

**Scope:** One exact two-node application-only attempt under the non-reusable
OT-154 authority

**Result:** Fail-closed abort, exact Trail restoration complete, no admitted
radio result

## Authority and immutable inputs

- Authority: `OT-154-OT005-LIBSODIUM-NOISE-XK-RADIO-ONE-ATTEMPT-AUTHORITY-V0.json`
  - raw SHA-256: `d792bd6814d03a196f74fdda5a675386e74b02e7e65335892a57f51a7dc5beb0`
  - consumed on success or abort: `true`
  - reusable: `false`
- Benchmark application:
  - name: `ot153_noise_xk_radio_cost.bin`
  - bytes: `296640`
  - SHA-256: `ed2eef319d5bca22d1d89a0be61e63463ada1a8fb3277238cdf95cf93093cd3c`
- Trail restoration application:
  - name: `opentrail_heltec_v4_bench.bin`
  - bytes: `500944`
  - SHA-256: `f2a58414f82eed585ba90bf7671b06c8ebfaa82e8b23f6ac2093de95143b4e0e`
- Application offset: `0x10000`
- Bound runner SHA-256:
  `8b20512bf25f06247bb59defa092b8db82fde753484a3936d9e4aee2fba808be`
- Bound coordinator SHA-256:
  `6635c73c6952b322ec1d72043a80f637b2cc04b70c1763f578ea4f38559aeaf3`
- Bound hardware adapter SHA-256:
  `d84aa9a1c0556f6421141a25336a3e87dab054cc971722b3f8058fe0a254f94b`

## Preflight and execution observations

Before the irreversible consumption boundary, both anonymous nodes passed
installed-Trail application readback and hard reset. After consumption:

1. Node A passed benchmark write, application readback, and hard reset.
2. Node B passed benchmark write, application readback, and hard reset.
3. The coordinator entered the bound radio path.
4. The attempt terminated as `radio_run_failed`.
5. `radio_run_invoked` is `true` and `radio_result_validated` is `false`.

The frozen coordinator does not retain a safe exact substage within this failure
category. No exact endpoint-open, restart, exchange, result-validation, host,
USB, firmware, or physical-radio root cause is claimed by this record.

## Exact restoration

The unconditional restoration boundary ran for both benchmark-touched nodes.
Each node passed:

- exact Trail application write;
- exact Trail application readback; and
- hard reset after restoration.

`restoration_complete` is `true`, `recovery_required` is `false`, and no recovery
receipt exists. The retained coordinator evidence is sufficient to prove exact
application restoration without publishing endpoint or device identifiers.

## Private evidence digests

Only byte counts and SHA-256 digests are admitted publicly:

- private execution journal: `1274` bytes, SHA-256
  `8e71fc9bc3b73e8825281f4e58b7da23daba9cc020dc737bf90986b38ddad757`
- private execution receipt: `1693` bytes, SHA-256
  `be5c9a4b9b92979d37cb168a91ea46100b301d2b8ee3afb3f3dcaa507e6c56af`

The public receipt contains anonymous roles only. It contains no endpoint, port,
device identifier, filesystem path, raw serial, backend text, run nonce, key, or
private state.

## Admission boundary

This evidence admits only a consumed, exactly restored abort. It admits no
validated radio result, frame or transmission count, timing, loss/duplicate/
corruption observation, candidate selection, suite or wire selection, Phase 2
completion, field readiness, support, production, regulatory acceptance, or
score credit. The OT-154 authority is consumed and cannot authorize a retry.

The next permitted step is a separate host-only diagnosis/correction that adds
bounded reset-aware transport behavior and safe stage evidence. Any later device
attempt requires a newly frozen executable bundle and fresh non-reusable
one-attempt authority.

See [Decision 0091](../../docs/decisions/0091-record-ot155-noise-xk-radio-execution-abort.md)
and the [sanitized abort receipt](../benchmarks/crypto/OT-155-OT005-LIBSODIUM-NOISE-XK-RADIO-EXECUTION-ABORT-RECEIPT-V0.json).
