# OT-121 bounded libsodium local-primitives execution — 2026-08-23

## Result

**PASS — bounded local-primitives slice only.** Under the one-time Phase 2 authority recorded by Decision 0059, two independently admitted anonymous experimental nodes each passed 7/7 libsodium local-primitive operations. Each node recorded 700 data-cache-conditioned (cold-labeled) and 700 warm samples across those operations, exactly 100 per operation, after two prerequisite gates passed. The captures validated, and benchmark write/readback, exact restoration, restore readback, and reset completed on both nodes.

The admitted sdkconfig remained unchanged. A diagnosed overflow of the 3,584-byte main-task stack was corrected by running the benchmark in a core-pinned 8 KiB FreeRTOS worker task, preserving that accepted sdkconfig.

## Public artifacts

| Artifact | Size | SHA-256 |
|---|---:|---|
| `OT121LPER1` public receipt | — | `731824641945c9930e2afc401ea583154dedf6051fcb1ede0f3b165fb8c1eb7f` |
| Exact benchmark image | 288,448 bytes | `405a1ac4b508d591aa70e4ba2c76c1ffcd6e90cdee1489802182573b530def31` |
| Accepted sdkconfig | — | `b4fb46a1d2fa27953a9e9f02cd87da8be60c09d7f5e3ef00905839f7f38f2f9f` |
| Exact restored image | 473,152 bytes | `0c40aeb6c95ade9940aa21065cbc73a72dcd82e96ade9d13126693147feb5741` |

The firmware build succeeded. It emitted three upstream Kconfig `NOTE` diagnostics and zero compiler warnings. Focused validation passed: firmware harness 10/10, frame validator 8/8, and runner tests 21/21.

The public receipt records `benchmark_readback_verified=true`, `capture_validated=true`, `restore_readback_verified=true`, and `restore_reset_completed=true` for both anonymous nodes. Its privacy flags are false for device identifiers, filesystem paths, raw capture, and serial ports.

## Boundary

This evidence contains no Noise XK or radio execution, no cross-candidate comparison, and no complete Phase 2 result. It makes no candidate, library, suite, handshake, KDF, or wire selection; no score change; and no support, compatibility, regulatory, production, secure-LoRa, or two-pair end-to-end claim. Exact restoration describes this run only and is not general recovery or supported-target acceptance. Linked-flash, static-RAM, peak-dynamic-RAM, max-stack, and watchdog results were not supplied.

The next gate is to complete the remaining Phase 2 candidate, Noise, and radio measurements under the existing bounded execution session, followed by separate Phase 3 admission and explicit suite/wire selection.
