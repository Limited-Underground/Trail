# OT-153 Noise XK executable and restoration bundle

**HOST-ONLY PREPARATION — no device, phone, firmware flash, radio
transmission, cryptographic execution, or physical measurement occurred.**

## Accepted bundle

OT-153 binds the exact OT-152 preparation to one deterministic future
execution path. The bundle includes the reproducible Heltec V4.2 ESP32-S3
firmware target, strict Noise XK radio runner, simultaneous two-node
restoration-safe coordinator, concrete endpoint-bound adapter, and exact Trail
restoration image. It pins ESP-IDF v6.0.2 at commit
`7101770dc6db2667b3c477cc31365dd1acd6db4e`, RadioLib 7.7.1, libsodium
1.0.22, the accepted `OTNXK0/v0` adapter, and the existing RadioLib HAL.

Two fresh initially absent cache-disabled builds reproduce the same four-role
artifact tuple: bootloader BIN, partition-table BIN, application BIN, and
application ELF. The accepted tuple is `bootloader.bin 22,480 bytes / 96e83ebe4434cd6c9049a59f396b4f8bd06c159b40259da573bdb701c571eca5; partition-table.bin 3,072 bytes / 7f00b6c042a89b15b0cac534f82ed988caf29278ff5700b0c511eb1b5bb7c820; application BIN 296,640 bytes / ed2eef319d5bca22d1d89a0be61e63463ada1a8fb3277238cdf95cf93093cd3c; application ELF 4,734,928 bytes / 197151373b03149e9fbcf6eea4247a04eb87aaec4b0c30d2965f051c11a4424d`.
Only the future application BIN may be written at `0x10000`, and only after a
separate authority. Bootloader, partition table, NVS, and other flash regions
remain unwritable under this preparation.

The exact restore image is `opentrail_heltec_v4_bench.bin`, 500,944 bytes,
SHA-256 `f2a58414f82eed585ba90bf7671b06c8ebfaa82e8b23f6ac2093de95143b4e0e`,
at application offset `0x10000`. Both installed Trail applications must pass
preflight readback and reset before a journal or first benchmark write. Every
benchmark-touched node must be independently restored, read back, and reset on
success, failure, or exception.

## Frozen future execution

The runtime preserves the OT-152 contract exactly: 48/48/64-byte raw Noise XK
messages, 160 bytes and three transmissions per successful handshake, role
reversal, and one message-2-withheld whole-handshake restart per direction.
Across both directions the prepared maximum is 736 radio-payload bytes, 14
transmissions, and 1,447,424 microseconds theoretical airtime. The response
deadlines remain 2,196 ms for message 2 and 2,216 ms for message 3.

The runner/coordinator boundary requires fresh session and attempt identity,
temporary-secret wipe before restart, stale-attempt rejection without adding a
radio frame, split-key cross-match, strict frame/counter accounting, and
privacy-safe public aggregation. Raw payloads, keys, identifiers, endpoints,
absolute paths, and private traces are excluded from the public record.

## Validation

- Canonical preparation raw SHA-256: `84d7ba8c601f59115fe5d7c74275dc8ccba807a10f87a14ee5afbbe40b3a184b`
- Canonical preparation SHA-256: `06de67211443c1432336a0e1d16f4d62be58d870e93cc4b70c2d494c199bcfd9`
- Focused validation: `75/75 passed (firmware 19, runner 21, bundle 12, coordinator 12, adapter 11)`
- Complete Windows Host validation matrix: `passed locally on 2026-08-27`
- Test-gate stabilization: the existing simulator restart regression now applies
  its 150 ms forced timeout only to the intended first request; the restarted
  request uses the production 10-second budget. Production helper code is
  unchanged; focused simulator validation passed core 33, Windows bridge 23,
  helper 15, native protocol 10, and UI 13/13.

## Boundary and next gate

OT-153 creates no one-attempt authority and performs no hardware or radio work.
OT-154 must separately accept fresh, explicit owner approval in a non-reusable
two-node radio authority before either connected node may be touched. Success or
abort will consume that authority; OT-153 alone cannot authorize preflight,
flash, reset, serial control, radio transmission, or recovery.

No candidate, library, suite, handshake/KDF, or Packet V1 selection is made.
Phase 2 remains incomplete, no readiness or score claim changes, and V1 remains
exact 43.75%, displayed 44%. No public website status changed, so no website
update is required.
