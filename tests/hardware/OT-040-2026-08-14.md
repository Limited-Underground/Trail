# OT-040 companion-coordinator target-link evidence

Date: 2026-08-14

Result: build and link passed; **BUILD-LINKED-NOT-RUN**; **NOT-FLASHED**;
**UNREVIEWED-RUNTIME**

## Scope

This acceptance linked the host-accepted fixed-memory companion request
coordinator into the generic `firmware/targets/heltec_v4_bench` ESP-IDF v6.0.2
candidate. The boot path uses fixed fake snapshot/action authorities to verify
exact `OTA0`/`OTR0` and `OTX0`/`OTN0` records inside `OTC0`, response
correlation, one prepare/commit application, and byte-identical duplicate replay
without a second authority call.

The combined companion boot self-check emits only fixed PASS/FAIL text. Failure
suspends before the startup line or recurring heartbeat. The self-check is
compiled but has not run on hardware. It is not a real device-authority,
Bluetooth, radio, delivery, storage, or application-runtime result.

## Accepted build result

- Strict native C++17 compilation with `-Wall -Wextra -Wpedantic -Werror`
  passed. Each execution completed 100 deterministic self-check repetitions,
  and 100/100 process repeats passed.
- Static target admission passed 3/3 and 100/100 repeats.
- The complete host matrix passed all 116 enabled C++ test entries.
- `tools/Build-HeltecV4BenchTarget.ps1` exited 0 under ESP-IDF v6.0.2.
- ESP-IDF size analysis reported 148,949 application bytes, an increase of
  3,292 bytes over the OT-037 result.
- The link map retains the companion protocol, semantic codec, request
  coordinator, and target-local boot-self-check objects.
- An incremental rebuild reproduced every recorded artifact hash.
- The ignored build record remained `NOT-FLASHED`,
  `BUILD-LINKED-NOT-RUN`, and `UNREVIEWED-RUNTIME`.

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `opentrail_heltec_v4_bench.bin` | 149,072 | `878E514665127A4B2550A0212521173C6F50F2B60AE1110A127D04A5F12D9505` |
| `opentrail_heltec_v4_bench.elf` | 3,537,692 | `70724918FA4099560E3589F45187659C5C4B94BEA522BC495F93145DA5A7E813` |
| `opentrail_heltec_v4_bench.map` | 3,458,193 | `529ED1F5BC54174DC54F7A015FC44267FBC8ABFBD4308AE33BD673E0C011FACA` |
| `bootloader.bin` | 21,088 | `564FC8660031072583C3A834656DA6BE858532EFBC863B5BC9587FACBD98D641` |
| `partition-table.bin` | 3,072 | `7F00B6C042A89B15B0CAC534F82ED988CAF29278FF5700B0C511EB1B5BB7C820` |
| generated `sdkconfig` | 90,375 | `472C086ED47588370F20E663EF413B9A0192A43FC9BD9C31E43D188749152E41` |

## Hardware-access boundary

The OT-040 target code, build helper, static admission, and focused self-check
did not discover, open, select, erase, write, reset, reboot, or otherwise access
a physical device. The broader pre-existing Windows-loader portion of the full
host matrix did perform its established privacy-safe, read-only enumeration of
the three connected USB candidates; it did not write or flash them. That
incidental loader evidence is not OT-040 target-runtime evidence.

## Remaining gate

The image still uses the generic 2 MB flash, DIO, 80 MHz profile rather than an
authoritative configuration for the observed 16 MB flash and 2 MB PSRAM bench
candidates. No NimBLE stack, GATT service, controller session owner, real
authority adapter, radio, GNSS, application storage, display, power, or complete
device runtime is present.

Before any physical write, exact-board configuration and manual ROM recovery
must pass the existing owner-gated procedure on a sacrificial-first unit. This
record is build, link, size, reproducibility, and host-self-check evidence only.
